---
phase: quick/260823-gu4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/not-found.tsx
  - src/app/login/page.tsx
  - src/app/signup/page.tsx
  - src/app/suspended/page.tsx
  - src/app/onboarding/plan/page.tsx
  - src/app/onboarding/plan/plan-picker.tsx
  - src/app/onboarding/create-store/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/plan/page.tsx
  - src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx
autonomous: false
requirements: [QUICK-UI-RETROFIT]

must_haves:
  truths:
    - "A future planner reading .planning/design-references/ finds the merchant-platform design source registered, with its token values, and cannot confuse it with the zinc/DTC storefront flagship reference"
    - "Primary actions (Continue, Create store, Sign up) render blue-600, not zinc-900"
    - "Page chrome renders on the slate surface scale, not pure white/zinc"
    - "Headings on auth, onboarding, dashboard, suspended and not-found render in Outfit; body copy, labels and buttons still render in Plus Jakarta Sans"
    - "The gold accent is available as a first-class token without hijacking shadcn's neutral --accent hover slot"
    - "Zero behavioral regressions: lint, typecheck, production build and the full 250-test suite all pass unchanged"
  artifacts:
    - path: ".planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md"
      provides: "Canonical merchant-platform design source registration for Phases 3-6"
      min_lines: 60
      contains: "njeirheinard21-ai/Einort-Commerce-Front-End"
    - path: "src/app/globals.css"
      provides: "Blue/gold/slate oklch token set, radius 0.75rem, gold accent token"
      contains: "--gold-accent"
    - path: "src/app/layout.tsx"
      provides: "Outfit display font loaded via next/font/google as --font-heading"
      contains: "Outfit"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css @theme inline"
      via: "next/font variable --font-heading consumed by Tailwind font-heading utility"
      pattern: "--font-heading"
    - from: "src/components/ui/card.tsx CardTitle"
      to: "--font-heading"
      via: "existing font-heading class — inherits Outfit with no edit"
      pattern: "font-heading"
---

<objective>
Retrofit the merchant platform's design tokens and display typography to the user's
actual dedicated merchant-platform design (blue / gold / slate + Outfit headings),
replacing the zinc-monochrome palette that Phase 1's UI-SPEC borrowed from a
*different* surface's visual reference (the DTC/editorial storefront flagship).
Also register that design source in `.planning/design-references/` so Phases 3-6
cannot miss it again.

Purpose: Everything shipped in Phases 1-2 currently wears a palette sourced from the
wrong reference. Fixing it now — while the surface area is 10 pages of centered cards —
costs one plan. Fixing it after Phase 3-6 build out the dashboard costs a re-skin
of the whole product.

Output: One design-reference doc, a re-tokenized `globals.css`, Outfit wired as the
display face, `font-heading` applied to heading-role elements only, and a green gate.

**This is a re-skin, not a re-architecture.** No page structure, layout, routing,
component logic, or test behavior changes. Locked user decisions:
- Keep separate `/login` and `/signup` routes (NOT the reference's combined
  split-screen mode-toggle page).
- Keep the existing centered-card layouts for onboarding/plan, dashboard,
  dashboard/plan, suspended (NOT the reference's sidebar+header app shell — that is
  legitimately Phase 3+ scope, once there is real dashboard content to shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/app/globals.css
@src/app/layout.tsx

The design reference repo is already cloned for inspection at:
`C:\Users\LFDSER~1\AppData\Local\Temp\claude\D--Maxs-Claude\c8adcf0c-818b-4784-8940-c6dc1a2f145c\scratchpad\Einort-Commerce-Front-End`
(source: private GitHub repo `https://github.com/njeirheinard21-ai/Einort-Commerce-Front-End`)

You do **not** need to re-read it to execute this plan — every value you need is in
`<confirmed_reference_tokens>` below, already converted to oklch. Open it only if you
want to sanity-check a specific component's look while writing the doc in Task 1.
It is a Google-AI-Studio-generated Vite/React prototype. It is a **visual/token
reference only — do not port its code.**
</context>

<confirmed_reference_tokens>
<!-- Verified by direct inspection of the reference repo's src/index.css. -->
<!-- Hex → oklch conversions computed via sRGB→Oklab; use the oklch column, -->
<!-- since globals.css is uniformly oklch and must not mix formats. -->

Brand (blue, standard Tailwind blue ramp), reference `--color-brand-*`:

| Token | Hex | oklch |
|-------|-----|-------|
| brand-300 | `#93c5fd` | `oklch(0.809 0.096 251.8)` |
| brand-400 | `#60a5fa` | `oklch(0.714 0.143 254.6)` |
| brand-500 | `#3b82f6` | `oklch(0.623 0.188 259.8)` |
| brand-600 | `#2563eb` | `oklch(0.546 0.215 262.9)` |

Surface (slate, standard Tailwind slate ramp), reference `--surface-*`:

| Token | Hex | oklch |
|-------|-----|-------|
| slate-50  | `#f8fafc` | `oklch(0.984 0.003 247.9)` |
| slate-100 | `#f1f5f9` | `oklch(0.968 0.007 247.9)` |
| slate-200 | `#e2e8f0` | `oklch(0.929 0.013 255.5)` |
| slate-300 | `#cbd5e1` | `oklch(0.869 0.020 252.9)` |
| slate-400 | `#94a3b8` | `oklch(0.711 0.035 256.8)` |
| slate-500 | `#64748b` | `oklch(0.554 0.041 257.4)` |
| slate-600 | `#475569` | `oklch(0.446 0.037 257.3)` |
| slate-800 | `#1e293b` | `oklch(0.279 0.037 260.0)` |
| slate-900 | `#0f172a` | `oklch(0.208 0.040 265.8)` |
| slate-950 | `#020617` | `oklch(0.129 0.041 264.7)` |

Accent (gold), reference `--color-accent-*`:

| Token | Hex | oklch |
|-------|-----|-------|
| gold-100 | `#FBFBF0` | `oklch(0.985 0.014 106.7)` |
| gold-500 | `#D4AF37` | `oklch(0.767 0.139 91.1)` |
| gold-900 | `#4A3B09` | `oklch(0.359 0.067 91.0)` |

Other confirmed facts from the reference:
- `--radius: 0.75rem` (this project currently ships `0.625rem`).
- `--ring` is mapped to **brand-500**, not brand-600.
- The `.dark` class performs a **full surface inversion** (50↔950, 100↔900, 200↔800,
  300↔700, 400↔600, 500 fixed), then re-derives `--background`/`--foreground`/
  `--card`/`--border` from the swapped scale. Brand and accent scales are NOT inverted.
- Fonts: `--font-sans: "Plus Jakarta Sans"` (already correctly wired in this project),
  `--font-display: "Outfit"` (NOT currently loaded anywhere in this project).
- Component patterns: `rounded-lg`, `shadow-sm` on primary actions, subtle
  `hover:bg-surface-50` hover states. No new pattern beyond applying the tokens above.
</confirmed_reference_tokens>

<grep_findings>
<!-- Pre-resolved so the executor does not re-explore. -->

**How `--accent` is actually consumed today:** grep across `src/` finds
`--accent` / `--accent-foreground` **defined only in `src/app/globals.css`** and read
only through shadcn primitives' `bg-accent` / `text-accent-foreground` utilities
(button ghost/outline hover, etc.). **No application page references it.**
`src/app/not-found.tsx:75` mentions "accent" in a prose comment only — not a token use.

→ **Decision (implement this, do not re-litigate):** leave `--accent` /
`--accent-foreground` as their shadcn-conventional *neutral hover* role (slate-100 /
slate-900), and introduce the brand gold as a **new, separate** `--gold-accent` token
pair. Repointing `--accent` to gold would turn every ghost-button hover in the app
gold, which is not what the reference does — the reference's cards and buttons hover
to `surface-50`, and gold appears only as a deliberate brand highlight.

**Heading inventory** (`<h1>` / `<h2>` across `src/app`, line numbers as of planning):

| File | Line | Element |
|------|------|---------|
| `src/app/page.tsx` | 22 | `h1` (marketing root) |
| `src/app/not-found.tsx` | 61 | `h1` |
| `src/app/login/page.tsx` | 29 | `h1` |
| `src/app/signup/page.tsx` | 37 | `h1` |
| `src/app/suspended/page.tsx` | 47 | `h1` |
| `src/app/onboarding/plan/page.tsx` | 118 | `h1` |
| `src/app/onboarding/plan/plan-picker.tsx` | 149 | `h2` |
| `src/app/onboarding/create-store/page.tsx` | 69 | `h1` |
| `src/app/(dashboard)/dashboard/page.tsx` | 65, 82 | `h1`, `h2` |
| `src/app/(dashboard)/dashboard/plan/page.tsx` | 57, 113 | `h1` ×2 |
| `src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx` | 134 | `h2` |
| `src/app/s/[slug]/page.tsx` | 58 | `h1` — **STOREFRONT, out of scope, do not touch** |

Two corrections to the task brief, already resolved:
1. `src/app/store-not-found/page.tsx` contains **no headings** — it is a three-line
   route that calls `notFound()`, which renders `src/app/not-found.tsx`. Apply
   `font-heading` in `not-found.tsx` instead; `store-not-found/page.tsx` needs no edit.
2. `src/app/onboarding/create-store/create-store-form.tsx` contains no heading-role
   element; only `create-store/page.tsx` does.

**Already correct, needs no edit:** `src/components/ui/card.tsx:41` — `CardTitle`
already carries `font-heading`. It picks up Outfit automatically the moment
`--font-heading` stops aliasing `--font-sans`. Do not edit `card.tsx`.

**Not headings — leave in Plus Jakarta Sans:** `plan-picker.tsx:179` and
`plan-switch-form.tsx:153` are `text-2xl` `<span>` **price numerals**, not headings.
</grep_findings>

<tasks>

<task type="auto">
  <name>Task 1: Register the merchant-platform design reference</name>
  <files>.planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md</files>
  <action>
Create the design-reference doc. Match the register/analysis voice and depth of the
existing files in that directory — read the first ~40 lines of
`.planning/design-references/SHOPIFY-THEME-STORE-RESEARCH.md` for tone: prose-first,
explicit about how the material was obtained, explicit about what is verified vs.
claimed, and it ends by drawing implications for EINORT's own phases. Aim for 60-120
lines. Do not write it as a bare token dump.

Cover, at minimum:

1. **What this is and where it came from.** Private GitHub repo
   `https://github.com/njeirheinard21-ai/Einort-Commerce-Front-End`, supplied by the
   project owner as the dedicated design for the **merchant platform** surfaces.
   Generated by Google AI Studio as a Vite + React prototype. State plainly and
   prominently: **visual/token reference only — its code is not to be ported.**
   Note it was cloned to a scratchpad for inspection, so a future reader knows to
   re-clone rather than expect a local copy.

2. **Confirmed token values.** Reproduce the three scales and the ancillary facts from
   this plan's `<confirmed_reference_tokens>` block — hex *and* the oklch equivalents,
   since this project's `globals.css` is oklch-only. Include the radius (`0.75rem`),
   the `--ring` → brand-500 mapping, the dark-mode full-inversion mechanic, and the
   two-font split (Plus Jakarta Sans body / Outfit display).

3. **File inventory / coverage.** The repo covers three distinct surfaces, which is
   why it matters well beyond this retrofit:
   - *Merchant dashboard* (`src/pages/`): Dashboard, Orders, Products, Inventory,
     Customers, Payments, Delivery, Analytics, Domains, Storefront, Settings, Auth.
   - *Platform Super Admin* (`src/pages/admin/`, 20 pages): AdminOverview, Merchants,
     MerchantDetail, Stores, Orders, Products, Customers, Payments, Subscriptions,
     Usage, Domains, Themes, FeatureFlags, Fraud, Health, Errors, AuditLogs,
     Notifications, Support, Analytics, Settings.
   - *Storefront* (`src/pages/storefront/`): StoreLayout, StoreHome, ProductDetails,
     Cart, Checkout.
   - *Shell + primitives*: `components/layout/{AppShell,Header,Sidebar}`,
     `components/admin/layout/AdminShell`, `components/ui/{Button,Card,Input,Badge,
     Table,Tabs,Skeleton,PageSkeleton,EmptyState,SearchModal,Motion}`,
     `components/ThemeProvider`.
   - Tokens live in `src/index.css`.

4. **Canonical-source flag.** State explicitly that this repo is the canonical design
   source for (a) this retrofit and (b) all future `ui-phase` / `discuss-phase` work in
   **Phases 3-6** covering merchant dashboard sections and the platform admin surface.
   Call out that the sidebar+header `AppShell` is deliberately deferred to Phase 3+
   (this retrofit is tokens+typography only) so a later planner reads that as a known
   open item rather than an oversight.

5. **Do-not-conflate cross-reference.** A separate, *earlier* visual reference exists
   for the **flagship storefront template** — a zinc-monochrome / editorial DTC
   aesthetic, a different design system for a different surface. Phase 1's
   `01-UI-SPEC.md` mistakenly applied that storefront palette to the entire merchant
   platform (see the leading comment block in `src/app/globals.css`, which this plan
   corrects). Record which reference governs which surface so no future reader repeats
   the mix-up. Also note the reference repo carries its *own* storefront pages, which
   means the storefront surface now has two candidate sources and that tension must be
   resolved deliberately during Phase 4-5 planning, not silently.

Do not modify any other file in this task.
  </action>
  <verify>
    <automated>test -f .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md &amp;&amp; test $(grep -vc '^$' .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md) -ge 50 &amp;&amp; grep -q 'njeirheinard21-ai/Einort-Commerce-Front-End' .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md &amp;&amp; grep -q 'D4AF37' .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md &amp;&amp; grep -qi 'zinc' .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md &amp;&amp; grep -qi 'admin' .planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md</automated>
  </verify>
  <done>The doc exists, is ≥50 non-blank lines, names the repo URL, carries the gold hex, cross-references the zinc storefront reference, and inventories the admin surface.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit globals.css to the blue/gold/slate token set</name>
  <files>src/app/globals.css</files>
  <action>
Replace the `:root` and `.dark` token values with the map below. **Keep the file
uniformly oklch — do not introduce any hex value.** Do not add, remove, or rename any
token other than the two new `--gold-accent*` entries. Do not touch the `@layer base`
block. Do not touch the `--radius-*` calc chain in `@theme inline` (it derives from
`--radius`, which does change).

**2a. Update the leading comment block** (currently lines 52-56, "Zinc monochrome,
editorial (01-UI-SPEC.md 'Color')"). Replace it with a comment that records: the
palette is blue (brand) / gold (accent) / slate (surface), sourced from
`.planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md`; that it
supersedes `01-UI-SPEC.md`'s zinc palette, which was mistakenly sourced from the
storefront/DTC flagship reference — a different surface; and that `.dark` is now a
real, derived inversion of the surface scale (per the reference's own `.dark` swap)
rather than leftover `shadcn init` scaffolding, even though light remains the only
appearance the product ships today.

**2b. `:root`** — set exactly these values (comment each with its scale position, e.g.
`/* slate-50 */`, so the next reader can trace it back):

    --background: oklch(0.984 0.003 247.9);          /* slate-50  */
    --foreground: oklch(0.129 0.041 264.7);          /* slate-950 */
    --card: oklch(1 0 0);                            /* white — see note below */
    --card-foreground: oklch(0.129 0.041 264.7);     /* slate-950 */
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.129 0.041 264.7);
    --primary: oklch(0.546 0.215 262.9);             /* brand-600 */
    --primary-foreground: oklch(1 0 0);
    --secondary: oklch(0.968 0.007 247.9);           /* slate-100 */
    --secondary-foreground: oklch(0.208 0.040 265.8);/* slate-900 */
    --muted: oklch(0.968 0.007 247.9);               /* slate-100 */
    --muted-foreground: oklch(0.554 0.041 257.4);    /* slate-500 */
    --accent: oklch(0.968 0.007 247.9);              /* slate-100 — neutral hover */
    --accent-foreground: oklch(0.208 0.040 265.8);   /* slate-900 */
    --gold-accent: oklch(0.767 0.139 91.1);          /* #D4AF37 */
    --gold-accent-foreground: oklch(0.359 0.067 91.0);/* gold-900 */
    --border: oklch(0.929 0.013 255.5);              /* slate-200 */
    --input: oklch(0.929 0.013 255.5);               /* slate-200 */
    --ring: oklch(0.623 0.188 259.8);                /* brand-500 */
    --radius: 0.75rem;
    --chart-1: oklch(0.546 0.215 262.9);             /* brand-600 */
    --chart-2: oklch(0.714 0.143 254.6);             /* brand-400 */
    --chart-3: oklch(0.767 0.139 91.1);              /* gold-500 */
    --chart-4: oklch(0.711 0.035 256.8);             /* slate-400 */
    --chart-5: oklch(0.446 0.037 257.3);             /* slate-600 */
    --sidebar: oklch(1 0 0);
    --sidebar-foreground: oklch(0.129 0.041 264.7);
    --sidebar-primary: oklch(0.546 0.215 262.9);
    --sidebar-primary-foreground: oklch(1 0 0);
    --sidebar-accent: oklch(0.968 0.007 247.9);
    --sidebar-accent-foreground: oklch(0.208 0.040 265.8);
    --sidebar-border: oklch(0.929 0.013 255.5);
    --sidebar-ring: oklch(0.623 0.188 259.8);

Leave `--destructive: oklch(0.577 0.245 27.325)` untouched. Leave `--success` and its
entire justification comment block untouched — it is an unrelated, already-justified
addition (D-02 slug-availability signal) and is not part of this palette.

**Card note (deliberate, documented deviation — add it as a short inline comment):**
the reference sets both `--background` and `--card` to `surface-50`, relying on
border+shadow for card separation. This project's Phase 1-2 surfaces are all
*centered cards on an otherwise empty page*, where identical values would collapse the
card into the page. Keeping `--card` white against a slate-50 `--background` preserves
the reference's visual intent (cards read as elevated) with better contrast for this
layout. This is the only intentional departure from the reference's literal values.

**2c. `.dark`** — the reference's full surface inversion, re-derived:

    --background: oklch(0.129 0.041 264.7);          /* slate-950 */
    --foreground: oklch(0.984 0.003 247.9);          /* slate-50  */
    --card: oklch(0.208 0.040 265.8);                /* slate-900 — elevated */
    --card-foreground: oklch(0.984 0.003 247.9);
    --popover: oklch(0.208 0.040 265.8);
    --popover-foreground: oklch(0.984 0.003 247.9);
    --primary: oklch(0.623 0.188 259.8);             /* brand-500 — lifts on dark */
    --primary-foreground: oklch(1 0 0);
    --secondary: oklch(0.279 0.037 260.0);           /* slate-800 */
    --secondary-foreground: oklch(0.984 0.003 247.9);
    --muted: oklch(0.279 0.037 260.0);               /* slate-800 */
    --muted-foreground: oklch(0.711 0.035 256.8);    /* slate-400 */
    --accent: oklch(0.279 0.037 260.0);              /* slate-800 — neutral hover */
    --accent-foreground: oklch(0.984 0.003 247.9);
    --gold-accent: oklch(0.767 0.139 91.1);          /* gold does not invert */
    --gold-accent-foreground: oklch(0.985 0.014 106.7);/* gold-100 */
    --border: oklch(0.279 0.037 260.0);              /* slate-800 */
    --input: oklch(0.279 0.037 260.0);               /* slate-800 */
    --ring: oklch(0.623 0.188 259.8);                /* brand-500 */
    --chart-1: oklch(0.714 0.143 254.6);             /* brand-400 */
    --chart-2: oklch(0.809 0.096 251.8);             /* brand-300 */
    --chart-3: oklch(0.767 0.139 91.1);              /* gold-500 */
    --chart-4: oklch(0.711 0.035 256.8);             /* slate-400 */
    --chart-5: oklch(0.869 0.020 252.9);             /* slate-300 */
    --sidebar: oklch(0.208 0.040 265.8);
    --sidebar-foreground: oklch(0.984 0.003 247.9);
    --sidebar-primary: oklch(0.623 0.188 259.8);
    --sidebar-primary-foreground: oklch(1 0 0);
    --sidebar-accent: oklch(0.279 0.037 260.0);
    --sidebar-accent-foreground: oklch(0.984 0.003 247.9);
    --sidebar-border: oklch(0.279 0.037 260.0);
    --sidebar-ring: oklch(0.623 0.188 259.8);

Leave `.dark`'s `--destructive: oklch(0.704 0.191 22.216)` untouched. Do not add a
`--success` override to `.dark` — it correctly inherits from `:root` today.

Note the `--border` / `--input` change in `.dark`: the old values were alpha-channel
(`oklch(1 0 0 / 10%)`). Replace them with the opaque slate-800 above, matching the
reference's `--border: var(--surface-200)` under inversion.

**2d. `@theme inline`** — add two entries beside the existing `--color-accent*` lines
so the gold is reachable as `bg-gold-accent` / `text-gold-accent-foreground`:

    --color-gold-accent: var(--gold-accent);
    --color-gold-accent-foreground: var(--gold-accent-foreground);

Do **not** touch the `--font-heading` line in this task — Task 3 owns it.
  </action>
  <verify>
    <automated>grep -v '^\s*[/*]' src/app/globals.css | grep -Ec '285\.(8|9)|286\.(0|3)' | grep -qx 0 &amp;&amp; grep -q -- '--radius: 0.75rem' src/app/globals.css &amp;&amp; grep -q -- '--gold-accent: oklch(0.767 0.139 91.1)' src/app/globals.css &amp;&amp; grep -q -- '--color-gold-accent: var(--gold-accent)' src/app/globals.css &amp;&amp; grep -q -- '--primary: oklch(0.546 0.215 262.9)' src/app/globals.css &amp;&amp; grep -q -- '--success: oklch(0.596 0.145 163.225)' src/app/globals.css &amp;&amp; ! grep -qE '#[0-9a-fA-F]{6}' src/app/globals.css &amp;&amp; npm run lint</automated>
  </verify>
  <done>No zinc oklch hues remain outside comments; radius is 0.75rem; both gold tokens are defined and exposed via @theme inline; primary is brand-600; --success is byte-identical to before; no hex literal exists anywhere in the file; lint passes.</done>
</task>

<task type="auto">
  <name>Task 3: Load Outfit and apply font-heading to heading-role elements</name>
  <files>src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/app/not-found.tsx, src/app/login/page.tsx, src/app/signup/page.tsx, src/app/suspended/page.tsx, src/app/onboarding/plan/page.tsx, src/app/onboarding/plan/plan-picker.tsx, src/app/onboarding/create-store/page.tsx, src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/dashboard/plan/page.tsx, src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx</files>
  <action>
**3a. `src/app/layout.tsx`** — add a second `next/font/google` import alongside the
existing `Plus_Jakarta_Sans`:

    import { Outfit, Plus_Jakarta_Sans } from "next/font/google";

    const outfit = Outfit({
      variable: "--font-heading",
      subsets: ["latin"],
      weight: ["300", "400", "500", "600", "700"],
      display: "swap",
    });

Add a doc comment above it in the same voice as the existing `plusJakartaSans` block:
Outfit is the **display face** from the merchant-platform design reference
(`--font-display` there), used for heading-role elements only; Plus Jakarta Sans
remains the body face for everything else. Note that the existing
`plusJakartaSans` comment's "exactly two weights" claim is scoped to the *body* face
and is not contradicted by Outfit's five — update that comment's wording if it reads
as a whole-platform claim rather than a Plus-Jakarta-Sans-specific one.

Apply the variable on the `<html>` element beside the existing one:

    className={`${plusJakartaSans.variable} ${outfit.variable} h-full antialiased`}

Change nothing else in this file — `metadata`, the `template` suffix, and the `<body>`
element are untouched.

**3b. `src/app/globals.css`** — in the `@theme inline` block, replace
`--font-heading: var(--font-sans);` with `--font-heading: var(--font-heading);`.
This mirrors the existing `--font-sans: var(--font-sans);` line one row above: the
value is supplied by next/font on `<html>`, and `@theme inline` merely re-exports it
as Tailwind's `font-heading` utility. Add a trailing comment naming Outfit.

**3c. Apply `font-heading`** to each heading-role element in the table below. The edit
is purely additive — insert `font-heading` at the **start** of the existing
`className` string and change nothing else about the element, its children, or its
surrounding markup. Line numbers are from planning time; match on the element and its
class string, not the line number.

| File | Element |
|------|---------|
| `src/app/page.tsx` | the `h1` (`text-4xl ...`) |
| `src/app/not-found.tsx` | the `h1` (`text-4xl ...`) |
| `src/app/login/page.tsx` | the `h1` |
| `src/app/signup/page.tsx` | the `h1` |
| `src/app/suspended/page.tsx` | the `h1` |
| `src/app/onboarding/plan/page.tsx` | the `h1` |
| `src/app/onboarding/plan/plan-picker.tsx` | the `h2` (`text-lg ...`) |
| `src/app/onboarding/create-store/page.tsx` | the `h1` |
| `src/app/(dashboard)/dashboard/page.tsx` | the `h1` **and** the `h2` (`text-base ...`) |
| `src/app/(dashboard)/dashboard/plan/page.tsx` | **both** `h1` elements |
| `src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx` | the `h2` (`text-lg ...`) |

Example of the expected shape of each edit:

`className="text-2xl leading-tight font-semibold tracking-tight text-foreground"`
becomes
`className="font-heading text-2xl leading-tight font-semibold tracking-tight text-foreground"`

**Do NOT apply `font-heading` to any of the following** — this is the boundary that
keeps the change a *display*-font change rather than a whole-app font swap:
- Body copy, `<p>`, labels, helper text, form field text, or button labels anywhere.
- `src/app/s/[slug]/page.tsx` — storefront surface, governed by the zinc/DTC
  storefront reference, explicitly out of scope for this retrofit.
- `plan-picker.tsx` and `plan-switch-form.tsx`'s `text-2xl` `<span>` **price
  numerals** — these are data, not headings.
- `src/components/ui/card.tsx` — `CardTitle` already carries `font-heading` and
  inherits Outfit automatically once 3b lands. Editing it is a no-op regression risk.
- `src/app/store-not-found/page.tsx` — contains no markup; it only calls `notFound()`.
  </action>
  <verify>
    <automated>grep -q 'Outfit' src/app/layout.tsx &amp;&amp; grep -q -- '--font-heading' src/app/layout.tsx &amp;&amp; grep -q 'outfit.variable' src/app/layout.tsx &amp;&amp; grep -q -- '--font-heading: var(--font-heading)' src/app/globals.css &amp;&amp; ! grep -q -- '--font-heading: var(--font-sans)' src/app/globals.css &amp;&amp; test $(grep -rl 'font-heading' src/app | wc -l) -eq 12 &amp;&amp; ! grep -q 'font-heading' 'src/app/s/[slug]/page.tsx' &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Outfit is loaded as `--font-heading` and applied on `<html>`; `@theme inline` re-exports it instead of aliasing `--font-sans`; exactly 12 files under `src/app` reference `font-heading` (globals.css + layout-adjacent pages listed above); the storefront route is untouched; lint and typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 4: Full regression gate</name>
  <files>(no files modified — verification only)</files>
  <action>
Run the full gate in order and fix only what breaks. This is a pure token/typography
swap: **no test should change**. The suite is expected at **250 passing, 250 total**,
byte-identical to before this plan. None of these tests assert on color or font — the
suite's job here is to confirm no accidental JSX or logic breakage crept in while
touching 11 heading elements.

    npm run lint
    npm run typecheck
    npx next build
    npx dotenv -e .env.test -- npx vitest run

If a test fails, the correct response is to fix the JSX edit that broke it — **not** to
update the test. A test change in this plan means Task 3 damaged something.

If `npx next build` warns about font fetching, confirm it is a network warning and not
a resolution failure — `Outfit` is a real Google Fonts family and `next/font/google`
self-hosts it at build time (no runtime third-party request is introduced).

Do not commit yet; Task 5 gates the commit.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npx next build &amp;&amp; npx dotenv -e .env.test -- npx vitest run</automated>
  </verify>
  <done>Lint clean, typecheck clean, production build succeeds, and the vitest suite reports 250 passed / 250 total with zero test files added, removed, or modified by this plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual confirmation of the re-skin</name>
  <action>Pause for the developer to visually confirm the palette and typography swap in a browser, per the steps below. Do not commit until approved.</action>
  <what-built>
The merchant platform is re-skinned to the user's actual dedicated design: blue-600
primary actions, slate surface chrome, a gold accent token available for future use,
`0.75rem` radius, and Outfit as the heading face over Plus Jakarta Sans body copy.
The design source is registered at
`.planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md`.
Page structure, routes, and layouts are unchanged by design.
  </what-built>
  <how-to-verify>
1. **Check for an existing dev server before starting one.** Use the Browser pane
   tools first (e.g. `tabs_context`) to see whether a server is already serving on
   **port 3001**. Only run `npm run dev` if nothing is already up — do not start a
   duplicate.

2. Navigate to and eyeball each of these:
   - `/signup` — heading in Outfit; the primary submit button is **blue**, not
     near-black zinc; the card sits on a faintly cool slate page, not pure white.
   - `/login` — same treatment; focus a text input and confirm the focus ring is
     **blue** (brand-500), not grey.
   - `/onboarding/plan` — plan card headings in Outfit; price numerals still in Plus
     Jakarta Sans (they should read visually distinct from the headings); the selected
     / primary action is blue.
   - `/dashboard` — requires a session, so complete a fresh signup first. Confirm the
     `h1` and the section `h2` are Outfit and the surrounding chrome is slate.

3. Confirm the negative: body copy, form labels, helper text, and button labels are
   still Plus Jakarta Sans. If *everything* switched to Outfit, `font-heading` was
   applied too broadly or `--font-sans` was disturbed.

4. Confirm the corner radius reads slightly softer than before (0.75rem vs 0.625rem)
   on cards and buttons.

5. Confirm no zinc/near-black primary button survives anywhere you visit.
  </how-to-verify>
  <resume-signal>Type "approved" to commit, or describe what looks wrong (name the page and the element).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → Google Fonts | `next/font/google` fetches the Outfit family at build time and self-hosts the result. No runtime third-party request, no new client-side network egress, no user data crosses this boundary. |

No application trust boundary changes. This plan modifies presentation tokens,
one font declaration, and `className` strings on heading elements. It adds no route,
no server action, no input parsing, no auth check, and no data access path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-GU4-01 | Tampering | `src/app/layout.tsx` font fetch | accept | `next/font/google` resolves and self-hosts at build time from Google's registry; the fetched asset is a font file with no execution surface. Same trust posture as the already-shipped Plus Jakarta Sans dependency — no new risk introduced. |
| T-GU4-02 | Denial of Service | production build | mitigate | A typo'd font family name fails `next build` loudly rather than degrading at runtime; Task 4's `npx next build` gate catches it before commit. |
| T-GU4-03 | Information Disclosure | heading `className` edits across 11 files | mitigate | Purely additive class-string edits. Task 4 runs the full 250-test suite unchanged; any accidental JSX or conditional-render damage that could expose or hide gated content (e.g. the suspended / entitlement surfaces) surfaces as a test failure. Task 3's verify additionally asserts the storefront route was not touched. |
| T-GU4-SC | Tampering | npm/pip/cargo installs | n/a | **No package installs in this plan.** `Outfit` ships via the already-installed `next/font/google`; no `package.json` change. Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npm run lint` — clean
- `npm run typecheck` — clean
- `npx next build` — succeeds, Outfit resolves
- `npx dotenv -e .env.test -- npx vitest run` — 250/250, no test file added/changed/removed
- `git diff --stat` — touches only the 14 paths in `files_modified`; `package.json`
  and `package-lock.json` are **not** in the diff
- No hex color literal in `src/app/globals.css`
- No zinc oklch hue (`285.x` / `286.x`) outside comments in `src/app/globals.css`
- `src/app/s/[slug]/page.tsx` and `src/components/ui/card.tsx` are unmodified
</verification>

<success_criteria>
- `.planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md` exists,
  names the repo URL, records the blue/gold/slate token values in both hex and oklch,
  inventories the merchant / admin / storefront page coverage, flags itself canonical
  for Phases 3-6, and cross-references the zinc/DTC storefront flagship reference as a
  distinct source for a distinct surface.
- `src/app/globals.css` carries the full blue/gold/slate oklch token set in both
  `:root` and `.dark`, `--radius: 0.75rem`, a dedicated `--gold-accent` pair exposed
  through `@theme inline`, an unchanged `--success`, an unchanged `--destructive`, and
  a corrected leading comment. `--accent` remains shadcn's neutral hover slot.
- Outfit is loaded via `next/font/google` as `--font-heading`, applied on `<html>`, and
  re-exported by `@theme inline`; `CardTitle` inherits it with no edit to `card.tsx`.
- `font-heading` appears on exactly the 11 heading-role elements enumerated in Task 3
  and nowhere else; the storefront route is untouched.
- The full gate is green with the test suite unchanged at 250/250.
- Human visual confirmation on `/signup`, `/login`, `/onboarding/plan`, `/dashboard`.
- No page structure, route, layout, or component logic changed anywhere.
</success_criteria>

<output>
Create `.planning/quick/260823-gu4-retrofit-merchant-platform-ui-tokens-blu/260823-gu4-SUMMARY.md` when done.
</output>
