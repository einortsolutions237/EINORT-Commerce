---
phase: quick-260907-a2v
quick: 260907-a2v
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QT-01]

files_modified:
  - src/components/dashboard-header-controls.tsx
  - src/lib/strings/index.ts

must_haves:
  truths:
    - "A merchant viewing any dashboard page sees no Super Admin Panel button in the header."
    - "The header still renders the theme toggle and the decorative notification bell, unchanged."
    - "No link to /admin exists anywhere in the rendered app, so no merchant can reach a 404 by clicking header chrome."
    - "The removal is a real deletion — no commented-out JSX, no `false &&` guard, no `hidden` class."
  artifacts:
    - path: "src/components/dashboard-header-controls.tsx"
      provides: "Header control cluster reduced to ThemeToggle + bell"
      absent: "ShieldCheck, next/link import, strings.dashboard.header.superAdminPanel"
    - path: "src/lib/strings/index.ts"
      provides: "dashboard.header namespace without the superAdminPanel entry"
      absent: "superAdminPanel"
  key_links:
    - from: "src/components/dashboard-header-controls.tsx"
      to: "src/lib/strings/index.ts"
      via: "strings.dashboard.header.notificationsLabel only"
      pattern: "strings\\.dashboard\\.header\\.notificationsLabel"
---

<objective>
Remove the Super Admin Panel header button from the merchant dashboard entirely.

Purpose: quick task 260906-egn shipped it as a deliberate stub linking to `/admin` — a route that does not exist — rendered unconditionally because this session shape carries no role concept. In practice every merchant sees a button that always 404s and implies an admin capability they do not have. Since there is still no way to distinguish a platform owner from a regular merchant, the honest state is "not rendered at all" until Phase 6 builds real admin auth and role-based visibility.

Output: a header control cluster containing only the theme toggle and the decorative bell, with the button's icon import, its `next/link` import, and its now-orphaned copy string deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/components/dashboard-header-controls.tsx
@src/lib/strings/index.ts
</context>

<preflight_findings>
Verified before planning — the executor does NOT need to re-derive any of this:

**Every reference to the button, repo-wide** (`grep -rn "superAdminPanel|Super Admin|ShieldCheck|/admin" src/ tests/`):

| Location | What it is | Disposition |
|---|---|---|
| `src/components/dashboard-header-controls.tsx:2` | `ShieldCheck` in the lucide-react import | delete the specifier (keep `Bell`) |
| `src/components/dashboard-header-controls.tsx:1` | `import Link from "next/link"` | delete — `Link` is used ONLY by this button (line 57) |
| `src/components/dashboard-header-controls.tsx:53-61` | the `<Button render={<Link href="/admin" />}>` block | delete |
| `src/components/dashboard-header-controls.tsx:10-13, 24-34` | module header prose describing the stub | rewrite so the comment matches the file |
| `src/lib/strings/index.ts:552-558` | `superAdminPanel: "Super Admin Panel"` + its doc comment | delete both |
| `src/lib/strings/index.ts:533-536` | `header` namespace doc comment naming the stub | rewrite |
| `src/server/auth/auth.ts:74`, `prisma/schema.prisma` `platformRole` | the *data model* Super Admin marker | LEAVE ALONE — Phase 6 reads it; unrelated to header chrome |
| `src/app/(dashboard)/dashboard/storefront-editor/section-list.tsx`, `src/app/s/[slug]/sections/trust-bar-*.tsx` | `ShieldCheck`/`ShieldCheckIcon` for the trust-bar section | LEAVE ALONE — different surface, different icon usage |

**Nothing tests this button.** `tests/unit/dashboard-nav.test.ts` reads only `app-sidebar.tsx` (its prose/reachability scanners are scoped to the sidebar module); it contains zero references to `superAdminPanel`, `/admin`, or `dashboard-header-controls.tsx`. No test needs adding, changing, or deleting.

**Nothing else in the header changes.** `strings.dashboard.header.notificationsLabel` and the `themeToggle*` keys stay; `Button` stays (the bell uses it); `ThemeToggle` stays.
</preflight_findings>

<tasks>

<task type="auto">
  <name>Task 1: Delete the Super Admin Panel button and its orphaned string</name>
  <files>src/components/dashboard-header-controls.tsx, src/lib/strings/index.ts</files>
  <action>
Two files, deletion only. Do not touch `app-sidebar.tsx`, `dashboard-topbar-search.tsx`, `theme-toggle.tsx`, the `(dashboard)/layout.tsx` header row, or the Overview page — all are unrelated and already verified working.

**`src/components/dashboard-header-controls.tsx`:**

1. Delete the entire `<Button ... render={<Link href="/admin" />}>…</Button>` element (the third child of the flex row), including its `<ShieldCheck aria-hidden="true" />` child and its `{strings.dashboard.header.superAdminPanel}` label. The component returns the same `<div className="flex items-center gap-1">` wrapper with exactly two children left: `<ThemeToggle />` and the bell `<Button>`.
2. Delete `import Link from "next/link"` outright — `Link` had exactly one use site and lint runs `--max-warnings=0`, so leaving it fails the gate.
3. Narrow the lucide-react import to `import { Bell } from "lucide-react"`.
4. Keep `Button`, `strings`, and `ThemeToggle` imports — the bell still needs all three (`strings.dashboard.header.notificationsLabel`).
5. Rewrite the module header comment so it describes the file as it now is: a cluster of the theme toggle and a decorative bell. Keep the existing "THE BELL IS DECORATIVE" section verbatim — it is still true and still load-bearing. Delete the whole "SUPER ADMIN PANEL IS A HEADER BUTTON" section. Replace it with a short note, in this codebase's established why-comment style, recording that a Super Admin entry point was deliberately removed here (quick task 260907-a2v) because `/admin` does not exist and this session shape carries no role check, so an unconditionally-rendered link showed every merchant a 404 for a capability they do not have — and that Phase 6 is where it returns, gated on a real platform-role check, not un-commented from here. Keep the closing `min-h-11` / 44px touch-target paragraph.

**`src/lib/strings/index.ts`:**

6. Delete the `superAdminPanel: "Super Admin Panel",` entry and the `/** … */` doc comment directly above it from the `dashboard.header` namespace. Leave `themeToggleLabel`, `themeLight`, `themeSystem`, `themeDark`, and `notificationsLabel` untouched.
7. Update the `header` namespace doc comment above it (which currently enumerates "the 3-way theme toggle, a decorative notification bell, and the Super Admin Panel stub") to drop the stub from that list. Keep the "None of this reuses `nav` — a header control is not a rail destination" sentence.

**Removal discipline (this is the point of the task):** actually delete. No commented-out JSX, no `{false && …}`, no `hidden`/`sr-only`/`display:none`, no unreferenced `superAdminPanel` key left behind "for Phase 6". Phase 6 will write its own gated entry point; a dormant one here would be the exact misleading artifact this task exists to remove.
  </action>
  <verify>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; grep -rn "superAdminPanel" src/ tests/ ; test $(grep -rl "superAdminPanel" src/ tests/ 2>/dev/null | wc -l) -eq 0</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; test $(grep -v '^\s*[*/]' src/components/dashboard-header-controls.tsx | grep -c -e 'ShieldCheck' -e 'next/link' -e '/admin') -eq 0</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; grep -q 'strings.dashboard.header.notificationsLabel' src/components/dashboard-header-controls.tsx &amp;&amp; grep -q '&lt;ThemeToggle /&gt;' src/components/dashboard-header-controls.tsx</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; npm run lint</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; npm run typecheck</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; npm run test:unit</automated>
    <automated>cd "D:/Maxs/Claude/einort-commerce" &amp;&amp; npm run build</automated>
  </verify>
  <done>
`src/components/dashboard-header-controls.tsx` renders exactly two controls (theme toggle, bell), imports only `Bell` from lucide-react, and has no `next/link` import and no `/admin` reference in code. `superAdminPanel` appears nowhere under `src/` or `tests/`. The comment headers in both files describe what the files now contain. All four gates — `lint` (`--max-warnings=0`), `typecheck`, `test:unit`, `build` — pass clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant session → dashboard chrome | An authenticated merchant renders the dashboard header; nothing here reads untrusted input, and no new one is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a2v-01 | Information disclosure | `dashboard-header-controls.tsx` | mitigate | The removed link advertised the existence and path of an unbuilt platform-admin surface to every merchant. Deleting it removes that hint. This task strictly reduces the client's knowledge of platform internals; it adds none. |
| T-a2v-02 | Elevation of privilege | `/admin` route | accept | No `/admin` route exists, so removing a link to it changes no authorization posture. The real gate is Phase 6's `User.platformRole` check — which this task deliberately does not stub, prejudge, or half-build. |
| T-a2v-SC | Tampering | package installs | accept | No package-manager install of any kind in this plan (`package.json` / `package-lock.json` are not in `files_modified`). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build` — all clean. Lint is the real detector for a missed import: `--max-warnings=0` fails on an unused `Link` or `ShieldCheck`.
2. `grep -rn "superAdminPanel" src/ tests/` returns nothing.
3. `grep -rn "/admin" src/components/` returns nothing (the `src/server/db/admin.ts` and `src/server/admin/**` matches elsewhere are the database access-tier, not this button).
4. The header's remaining controls are untouched: `<ThemeToggle />` present, bell `<Button>` present with `strings.dashboard.header.notificationsLabel` as its `aria-label`.
</verification>

<success_criteria>
- Every merchant loading any `/dashboard/*` page sees a header with a theme toggle and a bell, and no Super Admin Panel button.
- No dead code, comment-out, or conditional-false remnant of the button in either file.
- Sidebar nav grouping, Cmd/Ctrl+K search modal, theme toggle, and the Overview page are byte-unchanged.
- All four gates pass.
</success_criteria>

<output>
Create `.planning/quick/260907-a2v-hide-the-super-admin-panel-header-button/260907-a2v-SUMMARY.md` when done.
</output>
