---
phase: quick/260831-vd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/dashboard/plan/page.tsx
autonomous: true
requirements: [QUICK-260831-vd2]

must_haves:
  truths:
    - "A merchant whose trial has expired lands on /dashboard/plan and still sees the unchanged 'Your trial has ended.' heading, body and 'Contact us to subscribe' WhatsApp link — same strings, same link, same behavior as today."
    - "Below that content, the same merchant sees all three plan tiers rendered read-only: tier name, price, and '/month' suffix for Starter, Business and Professional."
    - "The tier the merchant is currently on carries a ring plus a Check icon and a visually-hidden 'Selected' label — colour is never the only signal (WCAG 1.4.1)."
    - "Prices on the expired branch read exactly as they do on the non-expired branch — same 5,000 XAF shape, from the same module-level priceFormatter, never a second formatter."
    - "There is no Switch button, no click target, no subscribe/upgrade CTA and no 'use client' anywhere in the expired branch — the WhatsApp contact link remains the only functional CTA in that state."
    - "No trial-day line ({days} left / one day left) appears in the expired branch."
    - "The non-expired branch, PlanSwitchForm, switchPlan, merchantAction and all entitlement/trial logic are byte-for-byte unchanged."
  artifacts:
    - path: "src/app/(dashboard)/dashboard/plan/page.tsx"
      provides: "Read-only three-tier display inside the expired-trial early return, plus a hoisted shared tier/name/price array both branches read"
      contains: "lg:grid-cols-3"
  key_links:
    - from: "src/app/(dashboard)/dashboard/plan/page.tsx (expired branch)"
      to: "src/lib/strings.ts (plan.selectedLabel, plan.priceSuffix, plan[tier].name)"
      via: "existing string keys only — no new key is added"
      pattern: "strings\\.plan\\.selectedLabel"
    - from: "src/app/(dashboard)/dashboard/plan/page.tsx (expired branch)"
      to: "src/app/(dashboard)/dashboard/plan/page.tsx (non-expired branch)"
      via: "one shared, hoisted tier/name/price array consumed by both — one formatter, one source of tier data"
      pattern: "PLAN_TIERS\\.map"
---

<objective>
On the `trial.state === "expired"` branch of `/dashboard/plan`, add a read-only display of the
three plan tiers below the existing "Your trial has ended." content.

Purpose: today an expired-trial merchant sees a dead end — a heading, a sentence and a WhatsApp
link — with no reminder of what the tiers even are. Showing the tiers read-only gives the
merchant the information they need to have the contact conversation, without inventing an
in-app switch path that Phase 2 deliberately refused.

Output: one modified file, `src/app/(dashboard)/dashboard/plan/page.tsx` — a hoisted shared
tier/name/price array plus a static card grid inside the existing early return. No new strings,
no new component, no new route, no server-side behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/app/(dashboard)/dashboard/plan/page.tsx
@src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx

<pre_verified_facts>
The orchestrator and planner already read every relevant file. Do NOT re-derive these; do NOT
go exploring for a subscribe route or a payment page — there is none.

**1. Every string this task needs already exists in `src/lib/strings.ts`. Add NO new key.**
- `strings.plan.starter.name` / `.business.name` / `.professional.name` — "Starter" / "Business" /
  "Professional", addressed as `strings.plan[tier].name`.
- `strings.plan.priceSuffix` — `"/month"`, documented there as "A separate element beside the
  formatted price, never inside it."
- `strings.plan.selectedLabel` — `"Selected"`, documented there as "Visually hidden, beside the
  `check` icon on the chosen card."
- `strings.plan.dashboard.expiredHeading` / `.expiredBody` / `.expiredCta` and
  `strings.trial.contactUrl` / `.contactUrlLabel` — already rendered by the branch; unchanged.

The planner checked the whole `plan:` namespace (`src/lib/strings.ts` lines 236–361). Nothing is
missing. If the executor believes a new key is needed, that is a signal the design drifted —
stop and re-read this section rather than adding one.

**2. The price formatter is already module-level and already correct.**
`page.tsx` declares, above the component:
`const priceFormatter = new Intl.NumberFormat("en-US", { maximumFractionDigits: 0 });`
and formats as `` `${priceFormatter.format(PLANS[tier].monthlyPriceXaf)} XAF` ``.

Its doc comment (lines 40–62) is load-bearing and explains why this deliberately is NOT
CLAUDE.md's `fr-CM` / `style: "currency"` convention (quick task `260831-urm`). **Do not touch
that comment, do not add a second formatter, do not reintroduce `fr-CM` or `style: "currency"`
anywhere in this file.** The new read-only cards must consume the SAME formatter output.

**3. The current-tier visual language already exists in `plan-switch-form.tsx`. Copy it, do not
reuse the component.** The exact classes to match:
- grid: `grid items-stretch gap-4 lg:grid-cols-3`
- card: `flex flex-col gap-4 rounded-lg border border-border bg-muted p-6`, plus
  `ring-2 ring-primary` when the tier is the merchant's current tier (via `cn` from `@/lib/utils`)
- header row: `flex items-start justify-between gap-2`, `h2` with
  `font-heading text-lg leading-snug font-semibold text-foreground`
- current-tier marker: `<Check aria-hidden="true" className="size-5 text-primary" />` followed by
  `<span className="sr-only">{strings.plan.selectedLabel}</span>`
- price row: `<p className="flex flex-wrap items-baseline gap-1">` containing a
  `text-2xl leading-tight font-semibold tracking-tight text-foreground` span (the price) and a
  `text-base leading-normal text-muted-foreground` span (`strings.plan.priceSuffix`)

These are the tokens already in the file — no palette literal, no hex, no arbitrary value. That
keeps `tests/unit/surface-token-isolation.test.ts` green by construction.

**4. `PLANS` carries `import "server-only"`.** `page.tsx` is a Server Component and already
imports it, so reading it in the expired branch is free and safe. This is also why the new
display MUST stay server-rendered.
</pre_verified_facts>

<decisions_already_made>
Five questions the executor might otherwise stop and second-guess. All settled — implement as
stated, and record decisions 1 and 2 as in-file comments.

**1. NO redundant sub-heading above the card grid.**
`strings.plan.dashboard.heading` is `"Your plan"`. Rendering it directly beneath
`"Your trial has ended."` reads oddly — two headings stacked, the second weaker than the first.
The `expiredHeading` already anchors the page. The grid follows the existing text block with
vertical spacing only. Write a short comment at the grid recording that the omission is
deliberate, so a later reader does not "fix" it by adding one.

**2. Hoist a shared tier/name/price array above the `if (expired)` early return.**
Structure (this is the planner's call — implement exactly this shape):

- Above the `if (ctx.trial.state === "expired")` branch, build one `PLAN_TIERS.map(...)` array of
  `{ tier, name, price }` — the three fields BOTH branches need. It is pure, synchronous,
  three iterations; hoisting it costs the expired branch nothing.
- The expired branch renders directly from that array.
- The non-expired branch derives its `PlanSwitchCard[]` from the same array by adding
  `memberLimit: PLANS[tier].limits.members` per entry.

`memberCount` (the `await platformDb.member.count(...)`) and the `memberLimit` field stay in the
non-expired branch ONLY. A read-only display has no limits to enforce and must not issue a
database query the expired state does not need. Do **not** hoist the `await` above the branch.

Do not change the exported `PlanSwitchCard` interface in `plan-switch-form.tsx`, and do not
change `PlanSwitchForm`'s props. The derived array must still satisfy the existing type.

**3. Spacing: wrap, do not respace.**
The existing expired-branch container is `mx-auto flex w-full max-w-3xl flex-col gap-4`. Change
the container's gap to `gap-6` (matching the non-expired branch's outer container exactly) and
move the existing `h1` / `p` / `a` into an inner `<div className="flex flex-col gap-4">` so their
current mutual spacing is preserved byte-for-byte. The grid becomes the container's second child.
This mirrors the non-expired branch, which is an outer `gap-6` column whose first child is a
grouped header block. The `h1`, `p` and `a` elements themselves — their classes, their strings,
the `ExternalLink` icon, the `sr-only` label, the `target`/`rel` attributes — are copied over
unchanged. Keep `max-w-3xl`; the non-expired branch renders the same three cards at the same
width.

**4. Zero interactivity. This is the whole point.**
No `"use client"` in `page.tsx`. No `Button`. No `onClick`. No `useState`. No `switchPlan` import.
No `<a>`/`<Link>` wrapping a card. No "Subscribe" / "Upgrade" / "Choose" CTA of any kind.
Do not import, reuse, extend, wrap or parameterize `PlanSwitchForm` — it stays exactly as it is,
used only by the non-expired branch. The only interactive element in the expired branch remains
the existing WhatsApp `Contact us to subscribe` link.

Rationale to cite in the code comment, quoted rather than paraphrased —
`02-CONTEXT.md` § Addendum resolves OQ-3 as: *"No. D-06's plan-switch capability applies only
during an active trial. Once expired, the only self-service action is the D-10 'contact us'
placeholder — there is no in-app plan-switch path out of read-only in this phase, consistent
with the real subscribe flow being deferred."* `switchPlan`'s `merchantAction({ mode: "write" })`
refuses the write server-side regardless, so this branch is a UX courtesy, never the control —
the file's existing header comment already says exactly that; keep it and extend it to mention
that the branch now also *shows* the tiers read-only.

**5. NO trial-day line in the expired branch.**
`strings.plan.dashboard.trialDaysLeft` and `strings.trial.oneDayLeft` stay where they are, in the
non-expired branch's `trialLine`. The trial is over; the number is meaningless in this state.
Do not move the `trialLine` computation above the branch.
</decisions_already_made>

<explicit_non_goals>
Confirmed with the user. Building any of these is a task failure, not initiative.

- **No redirect to a subscription-payment page.** No such page exists and none may be created —
  no placeholder route, no `/dashboard/subscribe`, no `TODO` stub. That flow needs Phase 6
  infrastructure (a platform-owned receiving number + subscription-claim flow) that does not
  exist yet, and is deferred on purpose.
- **No change** to `switchPlan`, `merchantAction`, `resolveEntitlements`, `PLANS`, `PLAN_TIERS`,
  or any entitlements/trial module.
- **No change** to `plan-switch-form.tsx`, to the non-expired branch's rendered output, to
  `/onboarding/plan`, or to any other page.
- **No new file.** This is one file changed. `src/lib/strings.ts` is expected to remain
  unmodified (see pre-verified fact 1).
</explicit_non_goals>

<environment_caveat>
This project's worktrees have repeatedly shipped with gitignored build artifacts missing, which
makes the gates emit false failures. Quick task `260831-urm` hit and fixed this. If `npm run
typecheck` reports a wall of errors about the Prisma client or Next's `PageProps`/`LayoutProps`
globals, or `npm run test:unit` fails to resolve `server-only`, restore from the main checkout at
`D:\Maxs\Claude\einort-commerce` before concluding anything is broken:
- `src/generated/` (Prisma client)
- `.next/types` (Next 16 route-type globals)
- `node_modules/server-only` (the Vitest alias target)

All three are gitignored; restoring them modifies nothing tracked. Report it as an environment
repair in the summary, not as a code failure.
</environment_caveat>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the three plan tiers read-only on the expired-trial branch of /dashboard/plan</name>

  <files>src/app/(dashboard)/dashboard/plan/page.tsx</files>

  <action>
Edit `src/app/(dashboard)/dashboard/plan/page.tsx` only.

**Imports.** Add `Check` to the existing `lucide-react` import (alongside `ExternalLink`) and add
`import { cn } from "@/lib/utils";` in the existing `@/` import group, matching the file's current
import ordering. Add nothing else. Do not import `Button`, `switchPlan`, or `PlanSwitchForm` into
any new position.

**Hoist the shared tier data.** Immediately after `const ctx = await requireMerchantContext();`
and BEFORE `if (ctx.trial.state === "expired")`, build one array from `PLAN_TIERS` of
`{ tier, name: strings.plan[tier].name, price: <priceFormatter output followed by a literal " XAF"> }`.
Give it a doc comment explaining that both branches read it so a price and a tier name can never
diverge between the in-trial switcher and the expired-trial read-only display, and that the
`await` member count deliberately stays below the branch because the read-only display enforces
no limits.

**Non-expired branch.** Replace the existing `cards: PlanSwitchCard[]` construction so it derives
from the hoisted array — spread each entry and add `memberLimit: PLANS[entry.tier].limits.members`.
Keep the `memberCount` await, the `PlanSwitchCard` type annotation, the `trialLine` computation,
and the entire returned JSX exactly as they are. Preserve the existing doc comment above the
`memberCount` await, trimming only whatever sentence the hoist makes untrue.

**Expired branch.** Keep the `h1`, `p` and `a` elements exactly as they are today — same classes,
same `strings` keys, same `target`/`rel`, same `ExternalLink`, same `sr-only` label. Move them
verbatim into an inner `<div className="flex flex-col gap-4">`, and change the outer container's
`gap-4` to `gap-6` so the container matches the non-expired branch's outer column.

As the container's second child, render the read-only tier grid from the hoisted array:
- grid wrapper: `grid items-stretch gap-4 lg:grid-cols-3`
- one card per entry, keyed by `tier`, classed with `cn` as
  `"flex flex-col gap-4 rounded-lg border border-border bg-muted p-6"` plus
  `ring-2 ring-primary` when `entry.tier === ctx.plan.tier`
- header row `flex items-start justify-between gap-2` containing an `h2`
  (`font-heading text-lg leading-snug font-semibold text-foreground`) with the tier name, and —
  only when the entry is the current tier — a `<Check aria-hidden="true" className="size-5
  text-primary" />` followed by `<span className="sr-only">{strings.plan.selectedLabel}</span>`
- price row `<p className="flex flex-wrap items-baseline gap-1">` with the price in a
  `text-2xl leading-tight font-semibold tracking-tight text-foreground` span and
  `{strings.plan.priceSuffix}` in a `text-base leading-normal text-muted-foreground` span

Nothing else goes in the card. No button, no bullets, no tagline, no member limit, no trial-day
line, no click handler.

**Comments (follow the file's existing rigor — explain WHY, warn against the obvious wrong fix).**
Two additions, both inside `/** */` blocks or `{/* */}` JSX comments matching the local style:
1. At the grid: state that the display is read-only ON PURPOSE, quote `02-CONTEXT.md`'s OQ-3
   resolution (the wording is in `<decisions_already_made>` item 4), note that `switchPlan`'s
   `merchantAction({ mode: "write" })` refuses the write server-side regardless, and note that a
   subscribe/payment redirect is deferred to Phase 6 (platform receiving number +
   subscription-claim flow) and must not be added here. Cite quick task `260831-vd2`.
2. At the same place or just above it: record that the omission of a
   `strings.plan.dashboard.heading` sub-heading is deliberate — the `expiredHeading` above already
   anchors the page and a second, weaker heading directly beneath it reads oddly.

Extend the file's existing "A FUNCTIONAL POST-EXPIRY SWITCHER IS A CONTRACT VIOLATION" header
block by one sentence noting the branch now also renders the three tiers read-only, so the header
still describes what the branch does. Do not rewrite or shorten the rest of that header, and do
not touch the `priceFormatter` doc comment at all.

Add no string to `src/lib/strings.ts`. Add no palette literal, no hex value, no Tailwind
arbitrary value, no `"use client"`.
  </action>

  <verify>
    <automated>
bash -c '
set -e
cd "$(git rev-parse --show-toplevel 2>/dev/null || echo .)"
F="src/app/(dashboard)/dashboard/plan/page.tsx"
FAIL=0

# Strip // comments and JSDoc *-led lines before any negative count: the required
# rationale comments name the very tokens being counted (grep -c counts comments).
CODE=$(sed -e "s://.*::" "$F" | grep -v "^[[:space:]]*\*")

# --- Positive: the read-only grid exists, with the matching visual language. ---
grep -qF "lg:grid-cols-3" "$F" || { echo "FAIL: no three-column tier grid in $F"; FAIL=1; }
grep -qF "ring-2 ring-primary" "$F" || { echo "FAIL: no current-tier ring in $F"; FAIL=1; }
grep -qF "strings.plan.selectedLabel" "$F" || { echo "FAIL: no sr-only Selected label in $F"; FAIL=1; }
grep -qF "strings.plan.priceSuffix" "$F" || { echo "FAIL: no /month price suffix in $F"; FAIL=1; }
grep -qF "260831-vd2" "$F" || { echo "FAIL: no 260831-vd2 rationale comment in $F"; FAIL=1; }

# --- Positive: exactly one shared tier array, read by both branches. ---
N=$(printf "%s\n" "$CODE" | grep -c "PLAN_TIERS.map" || true)
[ "$N" = "1" ] || { echo "FAIL: expected 1 PLAN_TIERS.map (shared, hoisted), found $N"; FAIL=1; }

# --- Positive: one formatter only, still the 260831-urm decimal shape. ---
N=$(printf "%s\n" "$CODE" | grep -c "new Intl.NumberFormat" || true)
[ "$N" = "1" ] || { echo "FAIL: expected exactly 1 Intl.NumberFormat, found $N"; FAIL=1; }
grep -qF "new Intl.NumberFormat(\"en-US\", {" "$F" || { echo "FAIL: 260831-urm decimal formatter changed"; FAIL=1; }
N=$(printf "%s\n" "$CODE" | grep -c "fr-CM" || true)
[ "$N" = "0" ] || { echo "FAIL: $N live fr-CM occurrence(s) reintroduced in $F"; FAIL=1; }
N=$(printf "%s\n" "$CODE" | grep -c "style: \"currency\"" || true)
[ "$N" = "0" ] || { echo "FAIL: $N live style:\"currency\" occurrence(s) reintroduced in $F"; FAIL=1; }

# --- Negative: zero interactivity anywhere in the page. ---
for TOKEN in "use client" "onClick" "useState" "switchPlan" "<Button"; do
  N=$(printf "%s\n" "$CODE" | grep -cF "$TOKEN" || true)
  [ "$N" = "0" ] || { echo "FAIL: interactive token \"$TOKEN\" found $N time(s) in $F"; FAIL=1; }
done

# --- Negative: PlanSwitchForm still rendered exactly once (non-expired branch only). ---
N=$(printf "%s\n" "$CODE" | grep -c "<PlanSwitchForm" || true)
[ "$N" = "1" ] || { echo "FAIL: expected 1 <PlanSwitchForm render, found $N"; FAIL=1; }

# --- Negative: trial-day copy still referenced exactly once each (trialLine only). ---
for TOKEN in "trialDaysLeft" "oneDayLeft"; do
  N=$(printf "%s\n" "$CODE" | grep -cF "$TOKEN" || true)
  [ "$N" = "1" ] || { echo "FAIL: $TOKEN referenced $N time(s), expected 1 (non-expired trialLine only)"; FAIL=1; }
done

# --- Negative: the member-count query stays below the branch (read-only display queries nothing). ---
N=$(printf "%s\n" "$CODE" | grep -c "platformDb.member.count" || true)
[ "$N" = "1" ] || { echo "FAIL: expected 1 platformDb.member.count, found $N"; FAIL=1; }

# --- Scope: exactly one tracked file modified; the switcher island untouched. ---
git diff --quiet -- "src/app/(dashboard)/dashboard/plan/plan-switch-form.tsx" || { echo "FAIL: plan-switch-form.tsx was modified"; FAIL=1; }
git diff --quiet -- "src/lib/strings.ts" || { echo "FAIL: strings.ts was modified — no new key should be needed"; FAIL=1; }
git diff --quiet -- "src/app/onboarding/plan/page.tsx" || { echo "FAIL: onboarding plan page was modified"; FAIL=1; }
CHANGED=$(git status --porcelain -- src | awk "{ \$1=\"\"; print }" | sed "s/^ //" | tr -d "\"" | sort)
[ "$CHANGED" = "src/app/(dashboard)/dashboard/plan/page.tsx" ] || { echo "FAIL: expected exactly 1 changed source file, got: $CHANGED"; FAIL=1; }

[ "$FAIL" = "0" ] && echo "PASS: gates green"
exit $FAIL
'
    </automated>
    <automated>npm run lint</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run test:unit</automated>
  </verify>

  <done>
`/dashboard/plan`'s expired-trial branch renders the unchanged "Your trial has ended." heading,
body and WhatsApp contact link, followed by a static three-card grid showing Starter / Business /
Professional with `5,000 XAF` / `12,500 XAF` / `25,000 XAF` and the `/month` suffix, with the
merchant's current tier carrying `ring-2 ring-primary`, a `Check` icon and an `sr-only`
"Selected" label. No button, no click handler, no `"use client"`, no trial-day line, no new
string key. `PLAN_TIERS.map` appears exactly once (hoisted, shared by both branches); the
`platformDb.member.count` await and the `memberLimit` field stay in the non-expired branch. The
gate script above, `npm run lint` (`--max-warnings=0`), `npm run typecheck` and
`npm run test:unit` are all green, and `git status` shows exactly one modified source file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session cookie → `requireMerchantContext()` | The only channel that establishes tenant identity for this page; unchanged by this task. |
| server render → merchant browser | Read-only tier names and prices sourced from the server-only `PLANS` registry cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vd2-01 | Elevation of Privilege | expired-trial branch of `/dashboard/plan` | mitigate | The branch renders zero interactive controls and imports no action. `switchPlan`'s `merchantAction({ mode: "write" })` remains the server-side control and is untouched — the display can never become a write path. Asserted by the gate's zero-count checks on `switchPlan`, `onClick`, `<Button` and `"use client"`. |
| T-vd2-02 | Information Disclosure | `PLANS` registry read in a Server Component | accept | Plan tier names and monthly prices are public marketing data, already rendered unauthenticated on `/onboarding/plan`. No limit, member count, tenant id or trial timestamp crosses to the client. |
| T-vd2-03 | Tampering | client-supplied plan tier | mitigate | Nothing on this branch accepts input. `ctx.plan.tier` is derived server-side by `resolveEntitlements` from the organization row and is used only to decide which card gets a ring. |
| T-vd2-SC | Tampering | package installs | mitigate | No package is installed by this task — `lucide-react` and `@/lib/utils` are already direct dependencies used by the sibling file in the same directory. Any install attempt is out of scope and a task failure. |
</threat_model>

<verification>
- Gate script above passes (grid present, current-tier ring + `sr-only` label present, exactly one
  `PLAN_TIERS.map` and one `Intl.NumberFormat`, zero interactive tokens, `<PlanSwitchForm>`
  rendered once, trial-day copy referenced once each, `platformDb.member.count` still once,
  exactly one changed source file).
- `npm run lint` — zero warnings (`--max-warnings=0` is a hard gate).
- `npm run typecheck` — clean. In particular `PlanSwitchCard[]` must still typecheck after the
  derive-from-hoisted-array change, without editing `plan-switch-form.tsx`.
- `npm run test:unit` — green, specifically `tests/unit/surface-token-isolation.test.ts` and
  `tests/unit/dashboard-nav.test.ts`.
- `git status` shows exactly one modified tracked file.
- `npx next build` is a BONUS, not a gate — the known worktree/Turbopack `node_modules` junction
  issue makes it unreliable here. If it fails for that reason, note it and rely on the gates
  above. Do not spend context chasing it.
</verification>

<success_criteria>
- An expired-trial merchant sees all three tiers with names and prices below the unchanged
  "Your trial has ended." block, and can tell at a glance which tier they are on — by ring AND by
  the screen-reader "Selected" label, never by colour alone.
- Prices read identically on both branches of the page and on `/onboarding/plan`.
- The WhatsApp `Contact us to subscribe` link is still the only functional CTA in the expired
  state; there is no subscribe route, no upgrade button and no click target on any card.
- The Phase 2 OQ-3 contract and the Phase 6 deferral are both recorded in-file, quoted, and
  traceable to quick task `260831-vd2`, so a later reader does not "helpfully" wire a switcher.
- `src/lib/strings.ts`, `plan-switch-form.tsx`, `switchPlan`, `merchantAction` and all
  entitlement/trial logic are unmodified.
</success_criteria>

<output>
Create `.planning/quick/260831-vd2-show-a-read-only-display-of-the-3-plan-t/260831-vd2-SUMMARY.md` when done.
</output>
