---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 06
type: execute
wave: 3
depends_on: ["05.1-05"]
files_modified:
  - src/components/theming/template-thumbnail.tsx
  - src/components/theming/template-picker.tsx
  - tests/unit/template-picker-contract.test.ts
autonomous: true
requirements: [TMPL-06]
user_setup: []

must_haves:
  truths:
    - "Opening Change template in the editor's 320px rail shows one readable card per row, not three cards crammed into 288px."
    - "The onboarding branding page shows three template cards per row at roughly 309px each, each with a full-bleed 16:10 preview frame."
    - "Every template still renders, grouped under a named segment heading that prints its own count."
    - "In onboarding, the merchant's chosen industry group is hoisted first and titled Recommended for you."
    - "A template with no generated preview shows the CSS-wireframe thumbnail inside the same frame — no broken image, no skeleton, no error copy."
    - "Locked cards stay visible and dimmed with a neutral Requires tier chip; the current template card stays visible and inert."
    - "Arrow keys traverse every rendered tile in one radio group, across segment-group boundaries."
  artifacts:
    - path: "src/components/theming/template-picker.tsx"
      provides: "Container-query grid, segment-grouped partition, full-bleed 16:10 image frame with D-05 fallback branch"
      contains: "@container"
    - path: "src/components/theming/template-thumbnail.tsx"
      provides: "Optional className prop merged onto the frame div via cn(), default unchanged"
      contains: "className"
    - path: "tests/unit/template-picker-contract.test.ts"
      provides: "Source-scan contract: container queries present, viewport grid breakpoints absent, no priority, fallback branch present"
  key_links:
    - from: "src/components/theming/template-picker.tsx"
      to: "next/image"
      via: "fill + sizes + object-cover, lazy by default, never priority"
      pattern: "sizes="
    - from: "src/components/theming/template-picker.tsx"
      to: "src/components/theming/template-thumbnail.tsx"
      via: "previewUrl === null branch renders TemplateThumbnail with a neutralising className"
      pattern: "TemplateThumbnail"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a ... I want to ... so that ...` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.1` to convert):**
>
> The template picker (onboarding and the storefront editor's "Change template" action) shows a real rendered screenshot per template in a large, polished grid — not the placeholder CSS-wireframe thumbnails Phase 5 shipped — closing the gap against the owner-supplied Shopify Discover-Themes reference before Phase 5's Wave 6 builds its 50-template contact sheet on top of it.

**MVP-mode note:** this completes the first user-visible vertical slice. When it lands, a merchant on either surface gets the full redesigned grid — correct column count, larger cards, segment headings, the 16:10 preview frame — rendering the wireframe fallback inside it. Plan 05.1-08 then fills those frames with real screenshots. The slice is deliberately shippable before the screenshots exist, which is exactly what D-05's fallback buys.

<objective>
Rebuild the shared picker's grid engine, card anatomy and segment grouping to `05.1-UI-SPEC.md`, and add the `previewUrl`-vs-wireframe branch D-05 specifies.

Purpose: the current grid sets `grid grid-cols-2 gap-4 sm:grid-cols-3` — viewport breakpoints — inside an editor rail that is a fixed 320px column, so every desktop merchant opening "Change template" gets three cards inside 288px, roughly 85px each. That is a measurable bug, not a taste question, and D-04's "down to 1-2 if needed" is unreachable with viewport breakpoints at all.
Output: one redesigned client component, one optional prop on the wireframe thumbnail, and the source-scan contract test that pins the grid engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-PATTERNS.md
@src/components/theming/template-picker.tsx
@src/components/theming/template-thumbnail.tsx
@tests/unit/surface-token-isolation.test.ts
</context>

<interfaces>
`TemplateTile` after plan 05.1-05 — the picker consumes exactly this, and both RSCs already supply it in `INDUSTRY_SEGMENTS` order:

- `readonly key: string`
- `readonly name: string`
- `readonly segment: string`
- `readonly segmentTag: string`
- `readonly segmentLabel: string`
- `readonly minTier: PlanTier`
- `readonly sections: readonly TemplateSectionRef[]`
- `readonly primaryAccent: string`
- `readonly locked: boolean`
- `readonly previewUrl: string | null`

`TemplatePicker` props after plan 05.1-05: `tiles`, `selectedKey`, `onChange`, `currentKey?`, `sortBySegment?`, `error?`. There is no `showAllToggle`.

Strings available after plan 05.1-05:
- `strings.branding.templateGroupRecommended` = `"Recommended for you · {segment} ({n})"`
- `strings.branding.templateGroupHeading` = `"{segment} ({n})"`
- unchanged: `strings.branding.templateLockedChip` (`"Requires {tier}"`), `strings.branding.templateLockedTooltip`, `strings.editor.templateCurrentBadge`, `strings.editor.templateRetainedCaption`

`TemplateThumbnail` today (`src/components/theming/template-thumbnail.tsx:286-300`) takes `{ sections, primaryAccent }` and hardcodes its frame as `aspect-[3/4] rounded-md border border-border bg-background overflow-hidden min-w-[140px]`. `cn` is already imported at line 3.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the wireframe thumbnail's frame overridable and correct its header</name>
  <files>src/components/theming/template-thumbnail.tsx</files>
  <read_first>
    - `src/components/theming/template-thumbnail.tsx` in full — the header at lines 14-22 (the "RENDERS FROM THE SKELETON. NEVER FROM A STORED IMAGE." block that TMPL-06 overrides), `AccentBlock` at lines 40-48 (the one sanctioned `style` attribute in the tree), and the component signature and frame at lines 286-318
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md` § D-05 Fallback and decision U-11
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-PATTERNS.md` § `src/components/theming/template-thumbnail.tsx`
  </read_first>
  <behavior>
    - Test 1 (source assertion): calling `TemplateThumbnail` with no `className` leaves the frame's class string byte-identical to today's — every existing call site is unchanged in behaviour.
    - Test 2 (source assertion): the frame div's className is produced by `cn(...)` with the default string first and `className` last, so a caller can override `aspect-*`, `min-w-*`, `rounded-*` and `border-*`.
    - Test 3 (source assertion): the file still contains exactly one `style={{ backgroundColor:` occurrence — `AccentBlock` is untouched.
    - Test 4 (source assertion): the header no longer asserts unqualified that this component never renders from a stored image; it records the TMPL-06 override and the component's new D-05 fallback role.
    These are asserted from `tests/unit/template-picker-contract.test.ts` in Task 2 (there is no DOM in either Vitest project), so write them there rather than inventing a component-render harness.
  </behavior>
  <action>
    Two surgical changes to `src/components/theming/template-thumbnail.tsx`. Nothing else in the file moves.

    (a) Add one optional prop. The signature becomes `{ sections, primaryAccent, className }` with `readonly className?: string` in the props type, and the frame `<div>`'s className becomes
    `cn("aspect-[3/4] rounded-md border border-border bg-background overflow-hidden min-w-[140px]", className)`
    — default string first, caller's override last, so `tailwind-merge` resolves conflicts in the caller's favour. `cn` is already imported at line 3; add no new import. With `className` omitted the output is byte-identical to today, so all existing call sites are untouched.

    Document the prop inline: it exists for exactly one caller — the picker's D-05 fallback, which drops this portrait wireframe into a landscape `aspect-[16/10]` card frame and must neutralise the hardcoded aspect, the min-width, and the double border/radius the card already supplies. Cite `05.1-UI-SPEC.md` U-11.

    (b) Rewrite the header block at lines 14-22. It currently reads "RENDERS FROM THE SKELETON. NEVER FROM A STORED IMAGE." and argues against stored images on two grounds: a regeneration obligation on every skeleton change, and real image bytes on the low-end Android this market runs on. TMPL-06 overrides that decision. Leaving the block as-is ships a comment arguing against the code beside it.

    The rewritten block must:
    - Record that TMPL-06 / phase 05.1 overrode the original decision, and that this component is now the D-05 FALLBACK rather than the primary rendering path.
    - State that both original concerns were real and were addressed rather than dismissed: D-02 turns the regeneration obligation into one documented command (`npm run templates:previews`), and the byte concern is answered by an 800px lossy WebP, lazy-loaded, with a correct `sizes` attribute and never `priority`.
    - State that this component is still the thing that renders when a preview is missing, so it must keep working and must not be deleted.
    - Keep the ALL-CAPS lead-in style the file and codebase use.

    (c) Do NOT touch `AccentBlock` — its single `style={{ backgroundColor: primaryAccent }}` is the one sanctioned inline colour in the whole component tree (`surface-token-isolation.test.ts` allows it by name). Do NOT touch `accentSectionIndex`, the section block components, or `aria-hidden="true"` on the frame.
  </action>
  <acceptance_criteria>
    - `grep -c "className" src/components/theming/template-thumbnail.tsx` returns at least `3` (the prop type, the destructure, the `cn` call).
    - `grep -c 'cn("aspect-\[3/4\] rounded-md border border-border bg-background overflow-hidden min-w-\[140px\]", className)' src/components/theming/template-thumbnail.tsx` returns `1`.
    - `grep -c "style={{ backgroundColor:" src/components/theming/template-thumbnail.tsx` returns `1`.
    - `grep -c "TMPL-06" src/components/theming/template-thumbnail.tsx` returns at least `1`.
    - `grep -c "NEVER FROM A STORED IMAGE" src/components/theming/template-thumbnail.tsx` returns `0` — the superseded claim is gone rather than left contradicting the shipped behaviour.
    - `npx vitest run tests/unit/surface-token-isolation.test.ts` exits 0.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'style={{ backgroundColor:' src/components/theming/template-thumbnail.tsx)" -eq 1 &amp;&amp; test "$(grep -c 'NEVER FROM A STORED IMAGE' src/components/theming/template-thumbnail.tsx)" -eq 0 &amp;&amp; grep -q "TMPL-06" src/components/theming/template-thumbnail.tsx &amp;&amp; npx vitest run tests/unit/surface-token-isolation.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`TemplateThumbnail` accepts an optional `className` merged via `cn()` with today's classes as the default, `AccentBlock` is untouched, and the header records the TMPL-06 override instead of arguing against it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign the picker grid, card and segment grouping, with its source-scan contract test</name>
  <files>src/components/theming/template-picker.tsx, tests/unit/template-picker-contract.test.ts</files>
  <read_first>
    - `src/components/theming/template-picker.tsx` in full, as it stands after plan 05.1-05
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md` in full — § Grid Engine, § Segment Grouping, § Card Anatomy, § Badges, § D-05 Fallback, § Interaction & States, § Accessibility, § Spacing Scale (including the exceptions table), § Color (the five accent uses, no additions)
    - `tests/unit/surface-token-isolation.test.ts` in full — the walker, the comment-blanking `codeLinesIn`, the `report()` formatter, the `fixture()` controls, and the anti-vacuity assertion inside each ban
    - `src/app/s/[slug]/sections/product-grid-section.tsx` lines 292-325 — the in-repo `<Image fill sizes object-cover>` idiom to copy structurally (but NOT its `--motion-*` custom properties, which are storefront tokens)
    - `src/app/(dashboard)/dashboard/claims/claim-card.tsx` lines 181-221 — the established "decide the fallback from data, never from a broken-image glyph" posture
  </read_first>
  <behavior>
    Write `tests/unit/template-picker-contract.test.ts` FIRST, to this contract, then redesign the component until it is green. There is no DOM in either Vitest project (`environment: "node"`), so every assertion is a source scan over comment-blanked lines — the established pattern.
    - Test 1: `template-picker.tsx` contains `@container`.
    - Test 2: it contains `@lg:grid-cols-` and `@4xl:grid-cols-`.
    - Test 3: it contains NO `sm:grid-cols-`, `md:grid-cols-`, or `lg:grid-cols-` (the bug this plan exists to fix). Comment lines are blanked first, so the component's own header may say "no `sm:grid-cols-*` here" without failing its own scan — this is load-bearing.
    - Test 4: it contains `aspect-[16/10]`.
    - Test 5: it contains no `priority` token on any line (50 tiles with `priority` cancels lazy loading).
    - Test 6: it contains both branches of the D-05 fallback — a reference to `previewUrl` and a reference to `TemplateThumbnail`.
    - Test 7: it contains no `onError` (the fallback is decided before render, from manifest state).
    - Test 8: it contains no `gap-5` and no `gap-4` on a grid container — the closed spacing scale has no 20px step and U-02 sets `gap-6`.
    - Test 9: it contains `role="group"` and `aria-labelledby` (per-segment group semantics, U-12) and contains no `<h3` (group headings are spans, not heading elements).
    - Test 10: it still contains `disabled=`, `aria-disabled`, and the inert-current-card early return — the Phase 5 T-05-32 / T-05-35 controls survive the redesign.
    - Test 11 (anti-vacuity): the scanned file list is non-empty and the file's line count is above a floor, with a failure message naming the constant to update if the component moved.
    - Test 12 (positive control): the viewport-breakpoint offender predicate returns true for the synthetic line `fixture("src/components/theming/template-picker.tsx", "className=\"grid grid-cols-2 gap-4 sm:grid-cols-3\"")` — the exact string this plan deletes.
    - Test 13 (negative control): the same predicate returns false for `fixture("src/components/theming/template-picker.tsx", "className=\"grid grid-cols-1 gap-6 @lg:grid-cols-2 @4xl:grid-cols-3\"")`.
  </behavior>
  <action>
    Redesign `src/components/theming/template-picker.tsx`. The surrounding shape is already correct; change only what is called out. Copy nothing from `05.1-RESEARCH.md`'s example grid where the UI-SPEC contradicts it — the UI-SPEC wins (`@lg`/`@4xl` not `@sm`/`@2xl`; `gap-6` not `gap-5`).

    (a) GRID ENGINE (U-01, U-02). Delete `className="grid grid-cols-2 gap-4 sm:grid-cols-3"` from the `<RadioGroup>`. Keep ONE `<RadioGroup>` wrapping every group — never one per group, which would fragment arrow-key navigation across the set. Each segment group renders as a `<div className="@container">` wrapper containing `<div className="grid grid-cols-1 gap-6 @lg:grid-cols-2 @4xl:grid-cols-3">`.
    The `onValueChange` handler, including its `if (currentKey !== undefined && key === currentKey) return;` inert-current-card guard, must stay BYTE-IDENTICAL.

    (b) SEGMENT GROUPING (U-07, U-12). Partition `tiles` into groups by `tile.segment`, preserving first-appearance order — both RSCs already hand tiles in `INDUSTRY_SEGMENTS` order (plan 05.1-05), which is why the component needs no registry import. When `sortBySegment` is defined and matches a group's segment, hoist that group to the front. Grouping is a PARTITION, not a filter: assert to yourself that the sum of group lengths equals `tiles.length`, and state that invariant in the header.
    - Heading for the hoisted group (onboarding only): `strings.branding.templateGroupRecommended.replace("{segment}", label).replace("{n}", String(count))`.
    - Heading for every other group: `strings.branding.templateGroupHeading` with the same two replacements.
    - Label source is `tiles[0].segmentLabel` within the group — never `strings.branding.segments[tile.segment]`, which would need a narrowing the `"use client"` boundary forbids.
    - Heading element is a `<span>` (or `<div>`), never `<h3>`. Style: `text-sm leading-normal font-semibold text-foreground pb-2 border-b border-border`, full grid width.
    - Each group wrapper carries `role="group"` and `aria-labelledby` pointing at its own heading's id.
    - Spacing: heading to grid `16px` (`mt-4` on the grid or `mb-4` on the heading); group to group `32px` (`gap-8` on the groups container).
    - A zero-length group is never rendered.

    (c) CARD ANATOMY (U-03, U-04). The card is
    `relative flex flex-col overflow-hidden rounded-lg border bg-card transition-colors duration-200` plus `group` (omit `group`, or omit the image's scale class, for locked and current cards so the lift never suggests an unavailable action), merged through `cn()` with:
    - unselected: `border-border hover:border-primary/40 hover:bg-accent`
    - selected: `border-primary ring-2 ring-primary`
    - locked: `opacity-60`
    Drop the shipped `p-2` — the image is full-bleed to the card edges. Keep the `has-[:focus-visible]:outline-2 has-[:focus-visible]:outline-offset-2 has-[:focus-visible]:outline-ring` ring on the card and the absolutely-positioned invisible `RadioGroupItem` (that is what makes the whole card one target), including its `disabled`, `aria-disabled`, `aria-labelledby` and `cursor-not-allowed` wiring, unchanged.

    (d) IMAGE FRAME AND D-05 BRANCH. Frame: `relative aspect-[16/10] w-full overflow-hidden border-b border-border bg-muted`. Inside it:
    - `tile.previewUrl !== null` renders `next/image` with `src={tile.previewUrl}`, `alt=""` (decorative — the `<Label>` is the accessible name via `aria-labelledby`; a duplicated alt would double-announce every card in a 50-item group), `fill`, `sizes="(min-width: 1024px) 340px, (min-width: 640px) 50vw, 92vw"`, and `className="object-cover object-top transition-transform duration-200 group-hover:scale-[1.02] motion-reduce:transform-none motion-reduce:transition-none"`. NEVER `priority`. Do not borrow `--motion-*` custom properties from `product-grid-section.tsx` — those are storefront tokens and using one here is the D-12 cross-surface leak `surface-token-isolation.test.ts` ban #3 catches. The `motion-reduce:` guards are mandatory because `globals.css`'s `prefers-reduced-motion` block is scoped to `[data-surface="storefront"]` and does not cover this component.
    - `tile.previewUrl === null` renders `<TemplateThumbnail sections={tile.sections} primaryAccent={tile.primaryAccent} className="h-full w-full aspect-auto min-w-0 rounded-none border-0" />`. No "Preview unavailable" copy, no error icon, no `<Skeleton>` — D-05 rejects all three; the fallback is silent by design.
    - The `bg-muted` frame IS the loading placeholder (U-14). `aspect-[16/10]` already reserves exact layout space, so there is no shift to mask.

    (e) BADGES. Keep both overlays and their neutral treatment verbatim, changing only the current-badge inset from `top-1 left-1` to `top-2 left-2`. The lock chip stays neutral (`border-border bg-background text-foreground` + `Lock` icon `size-3`, gap `1`); accent is NOT for it.

    (f) TEXT BLOCK. Replace the `mt-2 px-1` / `mt-1 px-1` stack with a single `<div className="flex flex-col gap-2 p-4">` containing the `<Label id={tileLabelId} className="text-sm leading-normal font-semibold text-foreground">` and the `<p className="text-sm leading-normal text-muted-foreground">` segment tag, plus the retained-above-tier caption when applicable. Keep the `Label`/`aria-labelledby` wiring exactly as it is.

    (g) HEADER. The four existing ALL-CAPS invariants (ONE COMPONENT TWO SURFACES; the zero-textual-reference-to-`server-only` rule; SORT NEVER FILTER; THE CURRENT-TEMPLATE CARD IS INERT) survive verbatim. APPEND a fifth: the preview `.webp` contains that template's own storefront palette AS PIXELS, which is content, not a token leak — no CSS custom property crosses the surface boundary, no class is written, and the source scanners see an `<Image src>` and nothing else. State it so a future reader does not "fix" it. Also record the grouping partition invariant and the `@container` reason (the editor rail's width is decoupled from the viewport's).

    (h) FORBIDDEN, each enforced by an existing auto-scanning test: no literal colour or Tailwind palette utility (`surface-token-isolation` bans 1 and 2); no `--brand-accent*` utility (ban 3); no `data-surface="storefront"` (ban 4); no inline user-facing prose (`dashboard-nav.test.ts`); no value import from a `server-only` module (`theming-marker-boundary.test.ts`). No `dark:opacity-*` / `dark:invert` / `dark:brightness-*` on the preview image (U-13 — a dimmed preview misrepresents the template being chosen).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/template-picker-contract.test.ts` exits 0, including its positive and negative control fixtures.
    - `grep -c "@container" src/components/theming/template-picker.tsx` returns at least `1`.
    - `grep -c "@lg:grid-cols-" src/components/theming/template-picker.tsx` and `grep -c "@4xl:grid-cols-" src/components/theming/template-picker.tsx` each return at least `1`.
    - `grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -Ec "(^|[^@])(sm|md|lg):grid-cols-"` returns `0`.
    - `grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -c "priority"` returns `0`.
    - `grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -c "onError"` returns `0`.
    - `grep -c "aspect-\[16/10\]" src/components/theming/template-picker.tsx` returns at least `1`.
    - `grep -c "motion-reduce:" src/components/theming/template-picker.tsx` returns at least `1`.
    - `grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -c "<h3"` returns `0`.
    - `grep -c 'role="group"' src/components/theming/template-picker.tsx` returns at least `1`.
    - `npx vitest run tests/unit/surface-token-isolation.test.ts`, `tests/unit/theming-marker-boundary.test.ts` and `tests/unit/dashboard-nav.test.ts` all exit 0.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` and `npm run build` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/template-picker-contract.test.ts &amp;&amp; test "$(grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -Ec '(^|[^@])(sm|md|lg):grid-cols-')" -eq 0 &amp;&amp; test "$(grep -v '^\s*[*/]' src/components/theming/template-picker.tsx | grep -c priority)" -eq 0 &amp;&amp; grep -q "@container" src/components/theming/template-picker.tsx &amp;&amp; npx vitest run tests/unit/surface-token-isolation.test.ts &amp;&amp; npx vitest run tests/unit/theming-marker-boundary.test.ts &amp;&amp; npx vitest run tests/unit/dashboard-nav.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The picker renders segment-grouped, container-query-sized cards with a full-bleed 16:10 preview frame and a pre-render D-05 fallback; the viewport-breakpoint bug is deleted and pinned by a contract test that has been observed rejecting the exact string that caused it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant plan tier to selectable template set | Locked and current-template affordances render client-side; the real control is server-side |
| dashboard surface to storefront token scope | The storefront's zinc palette and `--brand-accent*` family must not be referenced from a dashboard component (D-12) |
| remote R2 host to `next/image` | An image URL from a non-allowlisted host is a thrown error, not a load failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-22 | Elevation of privilege | tier gate bypass via re-selection | accept (control unchanged) | `assertTemplateAccess` in `switchTemplate` remains the boundary (T-05-32). The redesign preserves `disabled` / `aria-disabled` on locked cards and the byte-identical inert-current-card early return (T-05-35); both asserted by the contract test |
| T-05.1-23 | Tampering | SORT-NEVER-FILTER (Phase 5 D-05) | mitigate | Grouping is a partition; the header states the sum-of-group-lengths invariant and no code path removes a tile |
| T-05.1-24 | Information disclosure | D-12 surface-token boundary | mitigate | No literal colour, no palette utility, no `--brand-accent*`, no `data-surface="storefront"`, no borrowed `--motion-*` property. `tests/unit/surface-token-isolation.test.ts` bans 1-4 auto-scan `src/components/**` and are asserted green |
| T-05.1-25 | Information disclosure | `server-only` boundary in a `"use client"` file | mitigate | The picker imports no registry, defaults or manifest value; `tests/unit/theming-marker-boundary.test.ts` asserted green |
| T-05.1-26 | Denial of service (self-inflicted) | 50 preview images on a Douala mobile connection | mitigate | `next/image` lazy loading left on (no `priority`, asserted by grep and by the contract test), correct `sizes`, 800px lossy WebP source |
| T-05.1-27 | Spoofing | broken-image glyph read as a platform fault | mitigate | D-05's fallback is decided from manifest state in the RSC before render, never from `onError`; `onError` absence asserted by grep |
</threat_model>

<verification>
- `npx vitest run tests/unit/template-picker-contract.test.ts` exits 0
- `tests/unit/surface-token-isolation.test.ts`, `tests/unit/theming-marker-boundary.test.ts`, `tests/unit/dashboard-nav.test.ts` all green
- `npm run test:unit`, `npm run lint` (`--max-warnings=0`), `npm run typecheck`, `npm run build` all exit 0
- Zero viewport `grid-cols` breakpoints and zero `priority` tokens survive in the picker
</verification>

<success_criteria>
One shared component renders one card per row in the editor's 288px rail and three per row on the widened onboarding page, every tile grouped under a named, counted segment heading, with a full-bleed 16:10 frame that silently falls back to the wireframe when a preview is missing — and a contract test that has been observed rejecting the viewport-breakpoint string this plan deletes.
</success_criteria>

<output>
Create `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-06-SUMMARY.md` when done.
</output>
