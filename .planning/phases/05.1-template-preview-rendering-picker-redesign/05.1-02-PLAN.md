---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/images/pipeline.ts
  - src/server/images/r2.ts
  - src/server/theming/preview-manifest.ts
  - tests/unit/image-pipeline.test.ts
  - tests/unit/r2-key.test.ts
  - tests/unit/template-preview-manifest.test.ts
autonomous: true
requirements: [TMPL-06]
user_setup: []

must_haves:
  truths:
    - "Every IMAGE_PRESETS row states its WebP encode mode as its own `lossless` field rather than inheriting it from `enhance`."
    - "The product, claim and logo presets produce byte-identical output to before this change."
    - "A template preview run through the pipeline comes back as one 800px-long-edge lossy WebP labelled `card`, with its source aspect preserved and no photographic finishing applied."
    - "Composing an R2 key for a template preview refuses any value that is not a member of TEMPLATE_KEYS, by throwing."
    - "A template preview key never begins with `tenants/` and survives publicUrlFor without tripping its `/original` refusal."
    - "The preview manifest exists, is typed against TemplateKey, and is safe to import before any preview has been generated."
  artifacts:
    - path: "src/server/images/pipeline.ts"
      provides: "`lossless` field on all four IMAGE_PRESETS rows + the templatePreview row; processImage branches on spec.lossless"
      contains: "templatePreview"
    - path: "src/server/images/r2.ts"
      provides: "templatePreviewPrefixFor(templateKey) — throw-don't-sanitise key derivation"
      exports: ["templatePreviewPrefixFor"]
    - path: "src/server/theming/preview-manifest.ts"
      provides: "Generated TEMPLATE_PREVIEWS manifest (partial by design, D-05 fallback signal) + TemplatePreview interface"
      exports: ["TEMPLATE_PREVIEWS", "TemplatePreview"]
    - path: "tests/unit/template-preview-manifest.test.ts"
      provides: "Manifest drift guard with positive/negative fixture controls"
  key_links:
    - from: "src/server/images/r2.ts"
      to: "src/server/theming/registry.ts"
      via: "isTemplateKey() narrowing inside templatePreviewPrefixFor"
      pattern: "isTemplateKey"
    - from: "src/server/images/pipeline.ts processImage"
      to: "IMAGE_PRESETS row"
      via: "spec.lossless decides the webp encode mode, never the preset's name"
      pattern: "spec\\.lossless"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a … I want to … so that …` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.1` to convert):**
>
> The template picker (onboarding and the storefront editor's "Change template" action) shows a real rendered screenshot per template in a large, polished grid — not the placeholder CSS-wireframe thumbnails Phase 5 shipped — closing the gap against the owner-supplied Shopify Discover-Themes reference before Phase 5's Wave 6 builds its 50-template contact sheet on top of it.

**MVP-mode note (interface-first exception, taken deliberately):** this plan defines the three contracts every later plan consumes — the preset row, the R2 key helper, and the manifest module. None of them independently delivers a user-visible increment, and attempting to slice them vertically would mean writing the same three signatures three times. Phase 5's own planner took the identical exception for 05-01/05-03/05-08. Recorded explicitly per the orchestrator's instruction.

<objective>
Add the three storage-side contracts the preview pipeline needs: an explicit per-row `lossless` flag plus a `templatePreview` preset in `IMAGE_PRESETS`, a `templatePreviewPrefixFor` key helper in `r2.ts`, and the generated `preview-manifest.ts` module with its drift guard.

Purpose: `05.1-RESEARCH.md` Patterns 2 and 3 settled these. Every later plan imports at least one of them — the screenshot script (05.1-07) uses all three, and both RSC call sites (05.1-05) use two. Landing them first means no later plan has to guess a signature.
Output: a fourth `IMAGE_PRESETS` row, one new pure function in `r2.ts`, one new generated module, one new test file, and two extended test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-PATTERNS.md
@src/server/images/pipeline.ts
@src/server/images/r2.ts
@src/server/theming/registry.ts
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase 2026-09-07. No exploration required. -->

From `src/server/images/pipeline.ts` (current, pre-change):
- `export const IMAGE_PRESETS = { product: {...}, claim: {...}, logo: {...} } as const;`
  - `product: { sizes: [400,800,1600], labels: ["thumb","card","detail"], fit: "cover", ratio: 1, format: "webp", enhance: true }`
  - `claim:   { sizes: [1200], labels: ["full"], fit: "inside", ratio: null, format: "webp", enhance: true }`
  - `logo:    { sizes: [128,512], labels: ["small","large"], fit: "contain", ratio: 1, format: "webp", background: {r:0,g:0,b:0,alpha:0}, enhance: false }`
- `export type ImagePresetName = keyof typeof IMAGE_PRESETS;`
- `export interface DerivedImage { label: string; width: number; height: number; contentType: "image/webp"; body: Buffer; }`
- `export async function processImage(input: Buffer, preset: ImagePresetName): Promise<DerivedImage[]>`
- Module constants: `LIMIT_INPUT_PIXELS = 50_000_000`, `SATURATION_BOOST = 1.06`, `WEBP_QUALITY = 82`
- The encode branch currently reads: `spec.enhance ? pipeline.webp({ quality: WEBP_QUALITY }) : pipeline.webp({ lossless: true })`

From `src/server/images/r2.ts`:
- `export type UploadKind = "products" | "claims" | "logos";`
- `export function objectKeyFor(tenantId: string, kind: UploadKind, uploadId: string): string` → `tenants/{tenantId}/{kind}/{uploadId}/original`, throws on a bad id
- `export function derivativePrefixFor(originalKey: string): string`
- `export async function putObject(key: string, body: Buffer, contentType: string): Promise<void>`
- `export function publicUrlFor(key: string): string` — throws when `key.endsWith("/original")`

From `src/server/theming/registry.ts`:
- `export const TEMPLATE_KEYS = [...] as const;` (50 entries)
- `export type TemplateKey = (typeof TEMPLATE_KEYS)[number];`
- `export function isTemplateKey(value: unknown): value is TemplateKey`
- `registry.ts` does NOT import `r2.ts`, so importing the narrower into `r2.ts` creates no cycle. Both modules carry `server-only`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decouple the WebP encode mode into a per-row `lossless` field and add the templatePreview preset</name>
  <files>src/server/images/pipeline.ts, tests/unit/image-pipeline.test.ts</files>
  <read_first>
    - `src/server/images/pipeline.ts` in full — especially the header at lines 3-52 (the "row is the whole specification" rule that licenses this change), the `logo` row's ALL-CAPS header at lines 71-97, and the encode branch around lines 202-220
    - `tests/unit/image-pipeline.test.ts` in full — the fixture-generation recipe in its header (lines 35-63) is the discipline any new fixture must follow
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md` § Architecture Patterns → Pattern 2 (supplies the exact prose for the new row's header)
  </read_first>
  <behavior>
    - Test 1: `processImage(fixture, "product")` still returns 3 derivatives labelled `thumb`/`card`/`detail` at 400/800/1600 square — unchanged from before this task.
    - Test 2: `processImage(fixture, "logo")` still returns 2 derivatives labelled `small`/`large` and its bytes are still a lossless encode (assert via `sharp(body).metadata()` reporting `format: "webp"`, and via the byte length matching the pre-change baseline behaviour for a flat input).
    - Test 3: `processImage(landscapeFixture, "templatePreview")` returns exactly ONE derivative, `label === "card"`.
    - Test 4: that derivative's long edge is exactly 800 and its aspect ratio equals the input's aspect ratio within 1px (proving `fit: "inside"` + `ratio: null` preserved the 16:10 crop rather than squaring it).
    - Test 5: that derivative is a LOSSY webp — assert it is materially smaller than the same input encoded losslessly at the same size (compute both in the test with `sharp` so the assertion is a measured comparison, not a magic number).
    - Test 6: no photographic finishing was applied to `templatePreview` — assert the preset row's `enhance` is `false` and that `IMAGE_PRESETS.templatePreview.lossless` is `false` (the two axes are independent; this is the row that proves it).
    - Test 7: every key of `IMAGE_PRESETS` has a boolean `lossless` field (a structural guard so a future row cannot omit it).
  </behavior>
  <action>
    (a) Add an explicit `lossless: boolean` field to EVERY row of `IMAGE_PRESETS`, with these exact values — chosen so no existing row changes behaviour:
    `product: lossless: false`, `claim: lossless: false`, `logo: lossless: true`.

    (b) Change the encode branch in `processImage` to read the new field instead of reusing `enhance`:
    replace the `spec.enhance ? pipeline.webp({ quality: WEBP_QUALITY }) : pipeline.webp({ lossless: true })`
    ternary with one that branches on `spec.lossless` — lossless when `spec.lossless` is true, `{ quality: WEBP_QUALITY }` otherwise.
    Do NOT touch the separate `if (spec.enhance)` block that applies `.normalise()` / `.modulate({ saturation: SATURATION_BOOST })` / `.sharpen()`; that block keeps reading `enhance`. The whole point of this task is that the two axes stop being one flag.

    (c) Update the module header (currently around lines 45-52) which states that `enhance` "also selects the WebP mode". That sentence becomes false. Rewrite it to describe the two independent axes and cite `TMPL-06 / 05.1` as the reason for the split, keeping the file's existing "the row is the whole specification, so `processImage` reads this flag and NEVER branches on the preset's name" rule verbatim. Also update the `.webp(...)` line of the pipeline-walkthrough comment block (around lines 149-154) which currently says "lossy for an enhanced row and lossless for an unenhanced one".

    (d) Add a fourth row, `templatePreview`, placed after `logo`:
    `sizes: [800]`, `labels: ["card"]`, `fit: "inside"`, `ratio: null`, `format: "webp"`, `enhance: false`, `lossless: false`. No `background` key.

    Give it the codebase-standard multi-paragraph `/** … */` header citing `TMPL-06` and `05.1 D-01`, making these three arguments explicitly:
    - `enhance: false` for the same reason the `logo` row sets it — the input is already flat, correctly exposed UI output whose colours ARE the template's own declared accent; `.normalise()` would shift them and `.sharpen()` would halo the type.
    - `lossless: false` is NOT inconsistent with that. A 2x-DPR screenshot is dense antialiased text, which lossless WebP encodes badly. This is the one place the two axes genuinely diverge, which is why they are now two fields.
    - `fit: "inside"` + `ratio: null` means 800 is the LONG edge and the 16:10 aspect produced by the script's Playwright `clip` is preserved (800×500). The crop decision (D-01) lives in the script's `clip`, never here — Sharp only downscales and re-encodes.

    (e) Do NOT touch `.rotate()` (unconditional and first — the header at lines 134-160 forbids adding a skip branch even though a Playwright PNG carries no EXIF orientation tag). Do NOT touch `LIMIT_INPUT_PIXELS` (50 MP; a 2560×1600 PNG is 4.1 MP).

    (f) Extend `tests/unit/image-pipeline.test.ts` with a `describe("the templatePreview preset (TMPL-06)")` block covering the behaviours above. Any new fixture MUST be generated with Sharp inside the test (or committed with its regeneration recipe in the header, matching the existing `sample-product.jpg` discipline at lines 35-63) — never a downloaded photo, so it carries no licence question.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/image-pipeline.test.ts` exits 0 with the new `templatePreview` describe block present and passing.
    - `grep -v '^\s*[*/]' src/server/images/pipeline.ts | grep -c 'lossless:'` returns `4` (one per preset row).
    - `grep -v '^\s*[*/]' src/server/images/pipeline.ts | grep -c 'spec\.lossless'` returns at least `1`.
    - `grep -v '^\s*[*/]' src/server/images/pipeline.ts | grep -c 'spec\.enhance'` returns exactly `1` (only the finishing-chain branch remains).
    - `node -e "..."` or a test assertion confirms `IMAGE_PRESETS.templatePreview` equals `{sizes:[800],labels:["card"],fit:"inside",ratio:null,format:"webp",enhance:false,lossless:false}`.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/image-pipeline.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`IMAGE_PRESETS` has four rows each carrying an explicit `lossless` flag, `processImage` branches on `spec.lossless` for the encode and `spec.enhance` only for the finishing chain, the three pre-existing rows are behaviourally unchanged, and the new preset is covered by measured assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add templatePreviewPrefixFor to r2.ts</name>
  <files>src/server/images/r2.ts, tests/unit/r2-key.test.ts</files>
  <read_first>
    - `src/server/images/r2.ts` lines 86-140 (`UploadKind`, `UPLOAD_ID_PATTERN`, `TENANT_ID_PATTERN`, `objectKeyFor`, `derivativePrefixFor`) and lines 236-251 (`publicUrlFor`) — the new function sits directly beneath `derivativePrefixFor` and copies its throw-don't-sanitise posture
    - `tests/unit/r2-key.test.ts` in full — especially the `describe("the logos namespace (ONB-03)")` block around lines 163-207, which is the exhaustive shape the new describe block mirrors
    - `src/server/theming/registry.ts` around line 1165 (`isTemplateKey`) and line 431 (`TEMPLATE_KEYS`)
  </read_first>
  <behavior>
    - Test 1: `templatePreviewPrefixFor("flagship-fashion")` returns exactly `"templates/flagship-fashion"`.
    - Test 2: every member of `TEMPLATE_KEYS` produces `templates/{key}` — iterate the whole tuple, do not spot-check.
    - Test 3: `templatePreviewPrefixFor("not-a-template")` throws.
    - Test 4: `templatePreviewPrefixFor("")`, `templatePreviewPrefixFor("../tenants/x")` and `templatePreviewPrefixFor("flagship-fashion/../..")` all throw (the guard says no to traversal-shaped input, not just to unknown names).
    - Test 5: no key produced for any `TEMPLATE_KEYS` member starts with `tenants/` — asserted across the whole tuple, so the two namespaces provably cannot collide.
    - Test 6: `publicUrlFor(`${templatePreviewPrefixFor(key)}/card.webp`)` returns a URL and does NOT throw — the `/original` refusal is not tripped. Mirrors the existing "does not accept the original key itself" test at line 193.
  </behavior>
  <action>
    Add one exported pure function `templatePreviewPrefixFor(templateKey: string): string` to `src/server/images/r2.ts`, placed **directly beneath `derivativePrefixFor`** — that file's own comment states the two halves of the key layout are kept adjacent "so the two halves of the layout cannot drift apart in two different files", and this is a third half of the same layout.

    Implementation contract:
    - Import `isTemplateKey` from `@/server/theming/registry`. Both modules carry `server-only`; `registry.ts` does not import `r2.ts`, so there is no cycle.
    - `if (!isTemplateKey(templateKey)) throw new Error("Invalid template key for preview object key");`
    - `return \`templates/${templateKey}\`;`
    - Throw, never sanitise — the `objectKeyFor` posture. Do not add a normalise/slugify/"close enough" branch.

    Header comment (codebase-standard multi-paragraph `/** … */`, citing `TMPL-06`), stating:
    - `templates/` is a SIBLING namespace to `tenants/`, never nested inside one: a preview is platform content, identical for every merchant, and putting it under a tenant prefix would imply an ownership that does not exist.
    - Throws rather than sanitising, exactly like `objectKeyFor`. The input is always a member of the closed `TEMPLATE_KEYS` tuple and never client data, so this is defence in depth.
    - The derivative basename comes from the `templatePreview` preset's own `labels` entry, so the full key is `templates/{key}/card.webp`. Renaming that label orphans stored objects rather than moving them.

    Do NOT modify `publicUrlFor` — it refuses only keys ending `/original`, and `templates/{key}/card.webp` passes unchanged.
    Do NOT modify `objectKeyFor`, `derivativePrefixFor`, `presignUpload`, `putObject`, or `getObjectBuffer`.

    Extend `tests/unit/r2-key.test.ts` with a sibling `describe("the templates namespace (TMPL-06)")` block covering the behaviours above, placed after the existing `describe("the logos namespace (ONB-03)")` block.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/r2-key.test.ts` exits 0 and its output includes the string `the templates namespace (TMPL-06)`.
    - `grep -c "export function templatePreviewPrefixFor" src/server/images/r2.ts` returns `1`.
    - `grep -v '^\s*[*/]' src/server/images/r2.ts | grep -c "isTemplateKey"` returns at least `2` (the import and the guard call).
    - The test file asserts across the full `TEMPLATE_KEYS` tuple (not a hardcoded subset) — `grep -c "TEMPLATE_KEYS" tests/unit/r2-key.test.ts` returns at least `1`.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/r2-key.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`templatePreviewPrefixFor` exists beside `derivativePrefixFor`, refuses every non-`TemplateKey` input by throwing, provably never collides with the `tenants/` namespace, and round-trips through `publicUrlFor`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the generated preview manifest module and its drift guard</name>
  <files>src/server/theming/preview-manifest.ts, tests/unit/template-preview-manifest.test.ts</files>
  <read_first>
    - `src/server/images/pipeline.ts` lines 54-100 — the `as const` registry idiom this module mirrors in shape (though the manifest is `Partial`, not `as const`)
    - `tests/unit/theming-registry.test.ts` lines 1-75 — the header, the "IT MUST NOT PASS VACUOUSLY" rule, and the note explaining why a unit test may import `src/server/**` at all (`server-only` is aliased to an empty module by `vitest.config.ts`)
    - `tests/unit/surface-token-isolation.test.ts` lines 198-229 — the `fixture()` positive/negative-control idiom this test copies
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md` § Code Examples → "The generated manifest's shape"
    - `src/server/theming/registry.ts` lines 431-484 (`TEMPLATE_KEYS`, `TemplateKey`)
  </read_first>
  <behavior>
    - Test 1: `validatePreviewManifest(TEMPLATE_PREVIEWS)` reports no errors for the real, committed manifest.
    - Test 2 (positive control): a synthetic manifest containing a key that is not in `TEMPLATE_KEYS` is reported as invalid, naming the offending key.
    - Test 3 (positive control): a synthetic manifest whose keys appear out of `TEMPLATE_KEYS` order is reported as invalid, naming the first out-of-order key.
    - Test 4 (positive control): a synthetic entry missing `width`/`height`/`bytes`/`generatedAt`, or with a non-numeric `bytes`, is reported as invalid.
    - Test 5 (negative control): a synthetic manifest containing two real `TemplateKey`s in `TEMPLATE_KEYS` order with well-formed entries is reported as valid — proving the predicate can say yes as well as no.
    - Test 6 (anti-vacuity): the test asserts `TEMPLATE_KEYS.length === 50` and that the fixture controls above actually executed the same predicate the real scan uses — a guard nobody has watched say "no" is not a guard.
    - Test 7: an empty manifest is explicitly declared VALID at this point in the phase, with an inline comment stating that plan `05.1-08` replaces this case with an all-50-present completeness assertion once generation has run. This is why the file's completeness check is deliberately absent here rather than merely forgotten.
  </behavior>
  <action>
    (a) Create `src/server/theming/preview-manifest.ts` as a GENERATED file whose first two lines are exactly:
    `// GENERATED by scripts/generate-template-previews.ts — DO NOT HAND-EDIT.`
    `// Regenerate with: npm run templates:previews`

    It exports:
    - `export interface TemplatePreview { readonly width: number; readonly height: number; readonly bytes: number; readonly generatedAt: string; }` (`generatedAt` is ISO 8601)
    - `export const TEMPLATE_PREVIEWS: Readonly<Partial<Record<TemplateKey, TemplatePreview>>> = {};`

    It imports `TemplateKey` from `./registry` as a **type-only** import.

    Below the banner, a codebase-standard `/** … */` header citing `TMPL-06` and `05.1 D-05`, stating:
    - PARTIAL BY DESIGN. A missing key IS the signal that the picker falls back to `<TemplateThumbnail>`. This is the fact `D-05` branches on, and it is precisely what a hand-authored `previewImageKey` field on `TemplateDefinition` structurally could not express.
    - Why this is not a field on `TEMPLATES`: the registry records design decisions a human chose; this file records generation state a script produced. `registry.ts` is a 1262-line hand-edited file and a script must not rewrite it. Same posture as `src/generated/prisma/**`.
    - Keys are emitted in `TEMPLATE_KEYS` order so a regeneration diff is stable.
    - It ships EMPTY until `npm run templates:previews` has been run (plan 05.1-08); an empty manifest means every tile renders the wireframe fallback, which is correct, not broken.

    The file MUST lint clean under `--max-warnings=0` (two-space indent, double quotes, trailing commas). Do not add an `eslint-disable`.

    (b) Create `tests/unit/template-preview-manifest.test.ts`. Export a pure predicate from the test file itself — e.g. `function previewManifestErrors(manifest: Record<string, unknown>): string[]` — so that the real scan and the fixture controls provably run the same code path. Assertions per the behaviour list above.

    Header comment mirroring `theming-registry.test.ts`: state that it runs in the `unit` project (no database, no network), that `server-only` is aliased to an empty module by `vitest.config.ts` which is what permits importing `src/server/**` here, and carry forward the "IT MUST NOT PASS VACUOUSLY" rule verbatim in spirit — a set difference against an empty introspected shape reports "no drift" with total confidence and zero coverage.

    Do NOT add an "all 50 templates present" assertion in this task. The manifest is empty until 05.1-08 runs generation, and a red `npm run test:unit` between waves would break this phase's per-task sampling gate. 05.1-08 adds that assertion in the same commit that populates the manifest. Say so in an inline comment so the omission reads as sequencing, not oversight.

    (c) Do NOT add a `previewImageKey` field to `TemplateDefinition` and do not modify `src/server/theming/registry.ts` in any way.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/template-preview-manifest.test.ts` exits 0.
    - `head -2 src/server/theming/preview-manifest.ts` prints the two-line GENERATED banner, with `npm run templates:previews` on the second line.
    - `node -e "..."` / a test assertion confirms `TEMPLATE_PREVIEWS` is an object (empty at this point) and that `Object.keys(TEMPLATE_PREVIEWS).every(k => TEMPLATE_KEYS.includes(k))` holds.
    - `git diff --quiet -- src/server/theming/registry.ts` exits 0 (the registry was not touched).
    - The test file contains at least one assertion that the predicate REJECTS a bad fixture and at least one that it ACCEPTS a good fixture — `grep -c "fixture" tests/unit/template-preview-manifest.test.ts` returns at least `4`.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/template-preview-manifest.test.ts &amp;&amp; git diff --quiet -- src/server/theming/registry.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`preview-manifest.ts` exists, carries its GENERATED banner, exports an empty-but-typed `TEMPLATE_PREVIEWS`, and its drift guard has been observed both accepting a valid fixture and rejecting four distinct invalid ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template key → R2 object key | A key string composes a storage path; a bad value could reach or shadow tenant-owned objects |
| generated file → committed source | A script writes into `src/`, a tree humans otherwise own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04 | Tampering | `templatePreviewPrefixFor` | mitigate | `isTemplateKey()` narrowing then `throw`; never sanitise. Asserted against traversal-shaped inputs and across the full `TEMPLATE_KEYS` tuple (ASVS V5) |
| T-05.1-05 | Tampering | R2 `templates/` namespace | mitigate | `objectKeyFor` hard-prefixes every tenant key with `tenants/`; a test asserts no preview key ever starts with `tenants/`, so the two namespaces provably cannot collide (ASVS V12) |
| T-05.1-06 | Information disclosure | `publicUrlFor` | accept | Unchanged. Previews are deliberately public, identical for every merchant, and contain no tenant data. The `/original` refusal is asserted still to hold for the new key shape |
| T-05.1-07 | Tampering | `src/server/theming/registry.ts` | mitigate | The manifest is a separate generated file precisely so no script ever rewrites the hand-edited registry; asserted by `git diff --quiet` on that path |
</threat_model>

<verification>
- `npm run test:unit` exits 0
- `npm run lint` exits 0 (`--max-warnings=0`)
- `npm run typecheck` exits 0
- `git diff --quiet -- src/server/theming/registry.ts` — the registry is untouched
- `prisma/schema.prisma` and `prisma/migrations/**` are untouched (no migration is needed this phase)
</verification>

<success_criteria>
The pipeline states its encode mode per row, a fourth preset describes a template preview, `templatePreviewPrefixFor` refuses everything that is not a real template key, and an empty-but-typed manifest is importable by both RSC call sites and by the generation script.
</success_criteria>

<output>
Create `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-02-SUMMARY.md` when done.
</output>
