---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 09
type: execute
wave: 5
depends_on: ["05.1-08"]
files_modified:
  - src/server/theming/preview-manifest.ts
  - tests/unit/template-preview-manifest.test.ts
  - .planning/phases/05-template-segment-expansion/05-22-PLAN.md
autonomous: false
requirements: [TMPL-06]
user_setup: []

must_haves:
  truths:
    - "All 50 templates have a real rendered preview image stored in R2 and recorded in the generated manifest."
    - "The manifest drift guard now fails the build if any template is missing a preview, not just if a present one is malformed."
    - "A merchant on the onboarding branding step sees 50 real screenshot previews in a segment-grouped 3-up grid."
    - "A merchant opening Change template in the editor sees the same 50 real previews, one readable card per row in the 320px rail."
    - "Phase 5's Wave 6 plan (05-22) references this phase's real-screenshot preview mechanism, not the superseded CSS-wireframe thumbnails."
    - "A human has reviewed all 50 previews and confirmed they are distinct and non-broken."
  artifacts:
    - path: "src/server/theming/preview-manifest.ts"
      provides: "All 50 TEMPLATE_KEYS entries, in registry order, behind the GENERATED banner"
      min_lines: 60
    - path: "tests/unit/template-preview-manifest.test.ts"
      provides: "Completeness assertion — every TEMPLATE_KEYS member is present in the manifest"
      contains: "TEMPLATE_KEYS"
    - path: ".planning/phases/05-template-segment-expansion/05-22-PLAN.md"
      provides: "Wave 6 contact sheet reconciled to consume the real-screenshot preview mechanism"
  key_links:
    - from: "src/server/theming/preview-manifest.ts"
      to: "src/app/onboarding/branding/page.tsx"
      via: "every key present yields a non-null previewUrl, so every tile renders next/image"
      pattern: "TEMPLATE_PREVIEWS"
    - from: ".planning/phases/05-template-segment-expansion/05-22-PLAN.md"
      to: "src/components/theming/template-picker.tsx"
      via: "the contact sheet exercises the redesigned, real-screenshot component"
      pattern: "preview"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a ... I want to ... so that ...` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.1` to convert):**
>
> The template picker (onboarding and the storefront editor's "Change template" action) shows a real rendered screenshot per template in a large, polished grid — not the placeholder CSS-wireframe thumbnails Phase 5 shipped — closing the gap against the owner-supplied Shopify Discover-Themes reference before Phase 5's Wave 6 builds its 50-template contact sheet on top of it.

**MVP-mode note:** this completes the second and final slice. After Task 1 a merchant on either surface can, for the first time, tell fifty templates apart by looking at what they actually render. That is the capability TMPL-06 names.

<objective>
Generate previews for all fifty templates, tighten the manifest drift guard from "valid if present" to "must be complete", reconcile Phase 5's Wave 6 plan to the new mechanism, and close the phase with a human review of the full set.

Purpose: this is the plan that turns TMPL-06 from mostly-built into true, and it discharges `05.1-RESEARCH.md` Open Question 6 — the sequencing rationale for inserting this decimal phase at all was that 05-22's 50-thumbnail contact sheet should exercise the redesigned, real-screenshot component. That claim has to be checked, not assumed.
Output: a complete manifest, a strengthened drift guard, a reconciled 05-22, and an approved visual review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-VALIDATION.md
@.planning/phases/05-template-segment-expansion/05-22-PLAN.md
@src/server/theming/preview-manifest.ts
</context>

<interfaces>
Available and calibrated after plan 05.1-08:

- `npm run templates:previews` with no `--only` regenerates every key in `TEMPLATE_KEYS`, in order, merging into the manifest.
- The crop overlap constant in `scripts/generate-template-previews.ts` is calibrated and human-approved.
- A per-template failure logs, sets `process.exitCode = 1`, and continues — so a partial run is diagnosable from the transcript rather than aborting at template 12.
- `tests/unit/template-preview-manifest.test.ts` (plan 05.1-02) already validates key membership, ordering and entry shape via a named predicate, and carries positive and negative fixture controls. It deliberately does NOT yet assert completeness; this plan adds that in the same commit that makes it true.
- The redesigned picker (05.1-06) renders `next/image` for every tile whose key is present.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate all fifty previews and make the drift guard demand completeness</name>
  <files>src/server/theming/preview-manifest.ts, tests/unit/template-preview-manifest.test.ts</files>
  <read_first>
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-08-SUMMARY.md` — the calibration measurements and the approved crop constant
    - `tests/unit/template-preview-manifest.test.ts` in full — the named predicate and the inline comment marking where the completeness assertion belongs
    - `src/server/theming/preview-manifest.ts` — the banner and header that must survive the regeneration byte-for-byte
    - `README.md`'s regeneration section — follow the documented prerequisites
    - `src/server/theming/registry.ts` lines 431-484 — `TEMPLATE_KEYS`, the 50-member tuple the manifest must match exactly
  </read_first>
  <behavior>
    - Test 1: every member of `TEMPLATE_KEYS` is a key of `TEMPLATE_PREVIEWS` — a set difference in BOTH directions, reported with the missing key names, not a length comparison.
    - Test 2 (anti-vacuity): `TEMPLATE_KEYS.length` is asserted to be 50 first, so a set difference against an empty or truncated tuple cannot report "no drift" with zero coverage.
    - Test 3: `Object.keys(TEMPLATE_PREVIEWS).length` is greater than zero, asserted separately, with a failure message naming `npm run templates:previews` as the remediation.
    - Test 4 (positive control): the completeness predicate reports a synthetic manifest missing one real `TemplateKey` as incomplete, naming that key.
    - Test 5: every entry's `Math.max(width, height)` equals 800 and every `bytes` is greater than zero — the manifest cannot record a zero-byte preview and call it generated.
    - Test 6: every `generatedAt` parses as a valid date.
    - All pre-existing assertions from plan 05.1-02 (key membership, ordering, entry shape, its own fixture controls) continue to pass unchanged.
  </behavior>
  <action>
    (a) PREREQUISITES, per README: scratch store configured and empty of orders, `npm run dev` running, R2 credentials present.

    (b) Run the full generation: `npm run templates:previews` (no `--only`). Expect fifty capture-process-upload iterations. Watch the transcript — the per-template log line prints each key and its resulting byte size, so an outlier is visible.

    (c) If any template failed, do not paper over it. The script continues past a failure by design and sets a non-zero exit code; re-run just the failures with `--only=` after fixing the cause, and record what failed and why in the SUMMARY. A template that cannot be captured is a real finding about that template, not a script defect to be worked around.

    (d) Review the byte budget across all fifty. Record total and per-file sizes in the SUMMARY and compare against RESEARCH assumption A4 (25-50 KB each, roughly 1.3-2.5 MB total if every tile loaded, which lazy loading prevents). If materially over budget, say so with numbers — the mitigation lever is the preset's `sizes`/quality, and changing it is a `pipeline.ts` edit, not a silent acceptance.

    (e) Confirm the regenerated manifest kept its two-line GENERATED banner and full header, that entries are in `TEMPLATE_KEYS` order, and that it lints clean with no `eslint-disable`.

    (f) STRENGTHEN THE DRIFT GUARD in `tests/unit/template-preview-manifest.test.ts`, in this same commit — the assertion and the state that makes it true land together, which is why plan 05.1-02 deliberately deferred it. Add the completeness assertions from the behaviour list above, including the positive control fixture. Replace 05.1-02's inline "completeness is added by 05.1-09" comment with one stating that completeness is now enforced and that a missing preview is a build failure, not a silent fallback.

    Note the deliberate tension and record it in that comment: D-05's fallback still exists in the component for a partial-generation state, but the committed manifest is now required to be complete. The fallback is a runtime safety net; the test is a build-time contract. Both are correct, and neither makes the other redundant.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/template-preview-manifest.test.ts` exits 0 with the completeness assertions present and passing.
    - The manifest contains exactly 50 entries, one per `TEMPLATE_KEYS` member, in `TEMPLATE_KEYS` order.
    - Every entry has `Math.max(width, height) === 800`, `bytes > 0`, and a parseable ISO-8601 `generatedAt`.
    - `head -2 src/server/theming/preview-manifest.ts` still prints the two-line GENERATED banner.
    - `grep -c "eslint-disable" src/server/theming/preview-manifest.ts` returns `0`.
    - The test contains a control fixture proving the completeness predicate rejects a manifest missing one key — `grep -c "fixture" tests/unit/template-preview-manifest.test.ts` is greater than it was before this task.
    - `git diff --quiet -- src/server/theming/registry.ts src/server/theming/defaults.ts prisma/schema.prisma` exits 0.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` and `npm run build` all exit 0.
    - `npm run test:full` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/template-preview-manifest.test.ts &amp;&amp; head -2 src/server/theming/preview-manifest.ts | grep -q "GENERATED by scripts/generate-template-previews.ts" &amp;&amp; test "$(grep -c 'eslint-disable' src/server/theming/preview-manifest.ts)" -eq 0 &amp;&amp; git diff --quiet -- src/server/theming/registry.ts src/server/theming/defaults.ts prisma/schema.prisma &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>All fifty templates have real previews in R2 and in the manifest, the drift guard now fails the build on a missing preview and has been observed rejecting an incomplete fixture, and the byte budget is a recorded number.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Phase 5's Wave 6 contact sheet plan with the new preview mechanism</name>
  <files>.planning/phases/05-template-segment-expansion/05-22-PLAN.md</files>
  <read_first>
    - `.planning/phases/05-template-segment-expansion/05-22-PLAN.md` in full — every reference it makes to the thumbnail or picker component
    - `.planning/ROADMAP.md` around line 263 — the Wave 6 blocking note stating the contact sheet must use 05.1's redesigned real-screenshot thumbnails
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md` Open Question 6
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-CONTEXT.md` § Integration Points
    - `src/components/theming/template-picker.tsx` and `src/components/theming/template-thumbnail.tsx` as they now stand
  </read_first>
  <action>
    `05-22-PLAN.md` is written but not executed. Its 50-template contact sheet exists to run Phase 4's D-14 design-distinctiveness stranger test, and the entire reason phase 05.1 was inserted before it is that the contact sheet should exercise the real-screenshot previews rather than the CSS-wireframe placeholders. That claim must be checked against the actual plan text, not assumed.

    (a) Read `05-22-PLAN.md` end to end and enumerate, in the SUMMARY, every place it references the thumbnail or picker: file paths in `files_modified`, `<read_first>` entries, `<action>` prose, `must_haves` artifacts and key_links, and any verification command.

    (b) For each reference, decide and record whether it is now correct, stale, or ambiguous:
    - `TemplateThumbnail` is no longer the primary rendering path — it is the D-05 fallback. Any instruction to build a contact sheet "from `TemplateThumbnail`" is now stale and would produce exactly the wireframe sheet this phase exists to replace.
    - `TemplatePicker`'s prop contract changed: `showAllToggle` is gone; `TemplateTile` gained `previewUrl` and `segmentLabel`. Any snippet or prop list in 05-22 that predates those changes is stale.
    - The grid is now container-query driven, so a contact-sheet harness that relies on viewport breakpoints to force a column count will not behave as written.

    (c) Edit `05-22-PLAN.md` to reference the current mechanism. Keep the plan's own objective, its two blocking human-verify checkpoints, and its D-14 stranger-test structure intact — this is a reconciliation, not a rewrite, and 05-22 belongs to Phase 5. Add a short, dated note in the plan recording that it was reconciled by phase 05.1 and what changed, so a reader of Phase 5's history sees why its text differs from when it was written.

    (d) If reconciliation turns out to require more than an edit to references — for example if 05-22 assumes a component API that no longer exists in a way that changes its task structure — STOP and report it to the orchestrator rather than restructuring another phase's plan unilaterally. Record the finding in the SUMMARY.

    (e) Update `.planning/ROADMAP.md`'s Wave 6 blocking note (around line 263) only if it is now factually stale. If it still reads correctly, leave it and say so.
  </action>
  <acceptance_criteria>
    - The SUMMARY enumerates every thumbnail/picker reference found in `05-22-PLAN.md` with a correct/stale/ambiguous verdict for each.
    - `grep -c "showAllToggle" .planning/phases/05-template-segment-expansion/05-22-PLAN.md` returns `0`.
    - `05-22-PLAN.md` contains a dated reconciliation note naming phase `05.1`.
    - If `05-22-PLAN.md` references building the sheet from a thumbnail component, it now names the real-preview mechanism (`TEMPLATE_PREVIEWS` / `previewUrl` / the redesigned `TemplatePicker`) rather than only `TemplateThumbnail`.
    - No file under `src/` was modified by this task — `git diff --name-only` for this task lists only paths under `.planning/`.
    - `npm run test:unit`, `npm run lint` and `npm run typecheck` still exit 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'showAllToggle' .planning/phases/05-template-segment-expansion/05-22-PLAN.md)" -eq 0 &amp;&amp; grep -q "05.1" .planning/phases/05-template-segment-expansion/05-22-PLAN.md &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Every thumbnail and picker reference in 05-22 has been audited and reconciled to the shipped mechanism, with a dated note recording why, and any structural incompatibility has been escalated rather than silently resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full 50-preview review and phase gate</name>
  <read_first>
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-VALIDATION.md` § Manual-Only Verifications
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md` § Grid Engine, § Card Anatomy, § Segment Grouping, § Interaction & States
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-CONTEXT.md` § Specific Ideas — the user's own framing of the Shopify Discover-Themes target
    - The Task 1 SUMMARY byte-budget figures
  </read_first>
  <what-built>
    All fifty templates now have real rendered preview images in R2 and in the generated manifest, and the
    redesigned picker renders them on both surfaces.

    Everything automatable has been done and is green: the manifest drift guard now fails the build if any
    template lacks a preview and has been observed rejecting an incomplete fixture; the picker contract test pins
    the container-query grid and the absence of viewport breakpoints, `priority` and `onError`; the D-06
    containment test proves the placeholder photography never reaches `src/`; the surface-token, marker-boundary
    and prose-literal scans are all green; `lint`, `typecheck`, `build`, `test:unit` and `test:full` all pass.
    Byte sizes were measured rather than assumed.

    What remains is the one verification `05.1-VALIDATION.md` marks manual-only: whether fifty preview images are
    visually distinct, non-broken, and good enough to close the gap against the Shopify reference the user named.
    Image quality and distinctiveness are not machine-assertable.
  </what-built>
  <action>
    Start `npm run dev` and present the finished result in both real surfaces:
    1. `http://localhost:3001/onboarding/branding` — the full 50-card, segment-grouped grid on the widened page.
    2. `http://localhost:3001/dashboard/storefront-editor`, "Change template" — the same cards in the 320px rail.
    Also generate a plain contact sheet of all fifty 800px WebPs into a scratch path OUTSIDE the repo (do not
    commit it; Phase 5's 05-22 owns the committed contact sheet) so distinctiveness can be judged at a glance.

    Do not close the phase until the developer responds. Record any rejected templates and the reason.
  </action>
  <how-to-verify>
    1. Scan the contact sheet. Are the fifty visually distinct from one another, or do clusters look interchangeable?
       Note any cluster that does — that is a genuine finding, and it feeds Phase 5's Wave 6 D-14 stranger test
       rather than being a defect of this phase.
    2. Confirm no preview is broken, blank, half-rendered, or captured mid-animation.
    3. Confirm every preview shows hero plus roughly the next section (D-01), consistently across the set.
    4. On the onboarding page: three cards per row at desktop width, each roughly 309px; segment headings present
       with counts; your chosen industry's group hoisted first and titled "Recommended for you".
    5. Confirm every template is still visible — locked ones dimmed with a neutral "Requires {tier}" chip, none
       hidden. Count the cards: fifty.
    6. In the editor rail: one readable card per row, roughly 288px. Confirm the current template shows its
       "Current" badge and that clicking it does nothing.
    7. Select a different template in the editor and confirm the destructive confirm dialog still appears
       unchanged, then cancel.
    8. Toggle the dashboard to dark mode and confirm previews are framed, not dimmed or inverted (U-13).
    9. Narrow the browser to a phone width on the onboarding page and confirm the grid collapses to one column
       with cards at full width.
    10. Tab into the grid and confirm arrow keys move across tiles including across segment-group boundaries, and
        that the focus ring is clearly visible.
    11. Compare against the Shopify "Discover themes" reference you supplied. Does this now read as a theme store?
  </how-to-verify>
  <acceptance_criteria>
    - The developer has responded with an explicit approval or a specific list of problems.
    - The manifest contains 50 entries and the completeness assertion is green.
    - No contact sheet or scratch artifact was committed to the repository — `git status --porcelain` is clean of untracked image files.
    - Any per-template regeneration requested by the developer has been performed with `--only=` and re-verified.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/template-preview-manifest.test.ts &amp;&amp; npx vitest run tests/unit/template-picker-contract.test.ts &amp;&amp; npx vitest run tests/unit/preview-photo-isolation.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <resume-signal>Type "approved" to close the phase, or list the templates to regenerate and what is wrong with each (e.g. "beauty-luxe hero is cut off, grocery-fresh product row is empty").</resume-signal>
  <done>A human has reviewed all fifty previews in both live surfaces and approved them, or named specific templates that were then regenerated and re-reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated manifest to build gate | A committed generated file now carries a build-failing completeness contract |
| this phase to Phase 5's planning artifacts | Editing another phase's unexecuted plan |
| preview imagery to every merchant | Fifty images become the platform's first impression of its own templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-38 | Tampering | silent preview regression after a template redesign | mitigate | The drift guard now fails the build on a missing key, and D-02's documented `npm run templates:previews` is the recorded remediation. A future template change that forgets regeneration is caught by CI, not by a merchant |
| T-05.1-39 | Repudiation | undocumented edits to Phase 5's 05-22 plan | mitigate | Task 2 requires an enumerated audit in the SUMMARY plus a dated reconciliation note inside 05-22 itself, and escalates rather than restructuring another phase's plan unilaterally |
| T-05.1-40 | Denial of service (self-inflicted) | fifty preview images on a Douala mobile connection | mitigate | Measured total byte budget recorded; lazy loading retained (no `priority`, asserted by the picker contract test); correct `sizes` attribute |
| T-05.1-41 | Spoofing | a preview misrepresenting what a merchant will get | mitigate | Every capture comes from the merchant's real published storefront rendered from that template's own `TEMPLATE_DEFAULTS`, plus a blocking human review of all fifty |
| T-05.1-42 | Information disclosure | scratch-store content leaking into a public asset | accept | The scratch store contains only the eight seeded placeholder products and no real merchant data; it is a throwaway store the guard requires to have zero orders |
</threat_model>

<verification>
- `npx vitest run tests/unit/template-preview-manifest.test.ts` — 50 entries, completeness enforced
- `npx vitest run tests/unit/template-picker-contract.test.ts` — grid engine and no-`priority` contract green
- `npx vitest run tests/unit/preview-photo-isolation.test.ts` — D-06 containment green
- `tests/unit/surface-token-isolation.test.ts`, `tests/unit/theming-marker-boundary.test.ts`, `tests/unit/dashboard-nav.test.ts`, `tests/unit/r2-key.test.ts`, `tests/unit/image-pipeline.test.ts`, `tests/unit/preview-target-guard.test.ts` all green
- `npm run test:unit`, `npm run test:full`, `npm run lint`, `npm run typecheck`, `npm run build` all exit 0
- `src/server/theming/registry.ts`, `src/server/theming/defaults.ts` and `prisma/schema.prisma` unchanged across the whole phase
- Human approval recorded for the full 50-preview review
</verification>

<success_criteria>
Fifty real rendered previews exist in R2 and in a build-enforced complete manifest; both picker surfaces render them at theme-store card sizes with correct column counts; Phase 5's Wave 6 plan consumes the new mechanism; and a human has confirmed the set is distinct and non-broken.
</success_criteria>

<output>
Create `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-09-SUMMARY.md` when done. Record the total and per-file byte budget, any template that needed regeneration, the full 05-22 reference audit, and the human approval.
</output>
