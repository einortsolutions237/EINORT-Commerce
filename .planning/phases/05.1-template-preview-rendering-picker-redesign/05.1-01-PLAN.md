---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [TMPL-06]
user_setup: []

must_haves:
  truths:
    - "The developer explicitly approved the playwright install after reviewing registry evidence, before any install command ran."
    - "playwright appears under devDependencies only — never under dependencies."
    - "package.json's postinstall hook still contains exactly one command (scripts/prisma-generate.mjs) — no browser download was appended to it."
    - "A Chromium binary is available to Playwright on this machine."
  artifacts:
    - path: "package.json"
      provides: "playwright@1.63.0 declared as a devDependency"
      contains: "\"playwright\""
    - path: "package-lock.json"
      provides: "Locked resolution for playwright + playwright-core"
      contains: "playwright-core"
  key_links:
    - from: "package.json devDependencies"
      to: "playwright-core"
      via: "single transitive dependency, no install scripts"
      pattern: "playwright"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a … I want to … so that …` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.1` to convert):**
>
> The template picker (onboarding and the storefront editor's "Change template" action) shows a real rendered screenshot per template in a large, polished grid — not the placeholder CSS-wireframe thumbnails Phase 5 shipped — closing the gap against the owner-supplied Shopify Discover-Themes reference before Phase 5's Wave 6 builds its 50-template contact sheet on top of it.

**MVP-mode note (interface-first exception, taken deliberately):** this plan ships no user-visible increment. It is the supply-chain gate that D-03 makes mandatory before any other work in this phase can begin. Phase 5's own planner took the identical exception for 05-01/05-03/05-08. Recorded here so a reviewer does not read it as a horizontal-layer slip.

<objective>
Run the blocking package-legitimacy gate for `playwright` (the phase's one new dependency, tagged `[ASSUMED]` because `slopcheck` is unavailable), then install it as a devDependency and fetch its Chromium binary.

Purpose: D-03 requires this gate. `05.1-RESEARCH.md` § Package Legitimacy Audit already assembled the evidence packet; this plan presents it to a human and acts on the verdict. Every other plan in this phase that touches the screenshot script depends on the outcome.
Output: `playwright@1.63.0` in `devDependencies`, a locked `package-lock.json`, and a Chromium binary in the user browser cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md
@.planning/quick/260906-egn-rebuild-the-merchant-dashboard-shell-and/260906-egn-PLAN.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — playwright@1.63.0</name>
  <files>package.json (read only — this task changes nothing)</files>
  <read_first>
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md` § Package Legitimacy Audit (the evidence packet, already formatted)
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-CONTEXT.md` D-03
    - `.planning/quick/260906-egn-rebuild-the-merchant-dashboard-shell-and/260906-egn-PLAN.md` Task 1 (the `next-themes` precedent this task copies verbatim in shape)
    - `package.json` (confirm `playwright` is genuinely absent before proposing an install)
  </read_first>
  <what-built>
    Nothing yet. This gate runs BEFORE any install.

    `05.1-RESEARCH.md`'s Package Legitimacy Audit could not run `slopcheck` (`pip install slopcheck`
    produced no executable; `command -v slopcheck` → not found), so per the Package Legitimacy Gate
    protocol `playwright` is tagged **[ASSUMED]** and its install must be human-verified. This
    checkpoint is NOT auto-approvable regardless of any `auto_advance` setting, and the project is
    in `mode: yolo` — that does not waive this gate.

    Evidence already gathered and verified against the raw npm registry packument (not just `npm view`):
    - Package `playwright`, version `1.63.0`, install target **devDependencies**
    - Repository: `git+https://github.com/microsoft/playwright.git` (Microsoft org)
    - Weekly downloads: 81,838,822 (vs `puppeteer`'s 10,385,233)
    - Dependencies: exactly one — `playwright-core@1.63.0`, which itself has zero dependencies
    - Install scripts: **none** — no `scripts` field at all in the published packument for either
      `playwright@1.63.0` or `playwright-core@1.63.0`. No `preinstall`, no `install`, no `postinstall`.
    - `engines`: `node >=20` — satisfied by local `v24.16.0`
    - Browser binary is fetched by a separate explicit `npx playwright install chromium`, never by
      `npm install` — which is exactly why `puppeteer@25.10.0` was rejected (it ships
      `postinstall: node install.mjs`, a network-fetching hook that would fire on every Vercel build)
  </what-built>
  <action>
    Do NOT install anything in this task. Present the evidence above to the developer, wait for an
    explicit approval token, and take no further action in this task either way.

    Do not paraphrase, soften, or shorten the evidence packet — the developer is being asked to make
    a supply-chain trust judgment and needs the exact figures.

    If the developer reports that a newer `playwright` version has shipped since 2026-09-07, record
    the version they approve; Task 2 installs the approved version, not blindly `1.63.0`.
  </action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/playwright
    2. Confirm the linked repository is `github.com/microsoft/playwright` (Microsoft org) and that it
       is not a recently-created lookalike (check the repo's star count and first-release date).
    3. Confirm the weekly download count is in the tens of millions (expected ~82M).
    4. Confirm no `postinstall`/`preinstall`/`install` script is listed on the package page.
    5. Confirm the latest published version, and note it if it is newer than `1.63.0`.
    6. Confirm the package's only dependency is `playwright-core` at the same version.
  </how-to-verify>
  <acceptance_criteria>
    - The developer has responded with an explicit `approved` or `rejected` token; no install command
      has been run at any point during this task.
    - `git diff --name-only` shows zero changed files for this task.
    - If approved, the approved version string is recorded in the plan's SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet -- package.json package-lock.json &amp;&amp; echo "GATE-CLEAN: no install ran during the gate"</automated>
  </verify>
  <resume-signal>Type "approved" (optionally with a different version, e.g. `approved 1.64.0`) to allow the install, or "rejected" to abort this phase — there is no fallback implementation. `05.1-RESEARCH.md` rejected `puppeteer` on supply-chain grounds (network-fetching postinstall) and rejected the `renderToStaticMarkup` harness on correctness grounds (six section components import `next/image`, which 404s outside a Next server). A rejection means the phase cannot ship real screenshots and must be re-scoped with the user.</resume-signal>
  <done>Developer has explicitly approved or rejected the install; nothing has been installed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Install playwright as a devDependency and fetch Chromium</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `package.json` (the current `scripts` block — the existing `postinstall` hook must survive untouched)
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-RESEARCH.md` § Architecture Patterns → Anti-Patterns (the two playwright-specific anti-patterns)
  </read_first>
  <action>
    Only run this task if Task 1 returned `approved`.

    (a) Install exactly the approved version as a **devDependency**:
    `npm install --save-dev playwright@1.63.0` (substitute the version the developer approved, if different).

    It MUST land in `devDependencies`, never `dependencies`. Nothing under `src/**` imports it; it is
    consumed only by `scripts/generate-template-previews.ts` (built in plan 05.1-07). Vercel installs
    devDependencies during `next build`, so a `dependencies` placement would ship a headless-browser
    driver into the production runtime for no reason (ASVS V14).

    (b) Fetch the browser binary as a separate, explicit one-time command:
    `npx playwright install chromium`

    (c) DO NOT append `playwright install` to the existing `postinstall` script. `package.json`'s
    `postinstall` is currently `node scripts/prisma-generate.mjs` and must stay exactly that single
    command — appending the browser download would fire a ~150MB fetch on every Vercel build.
    The browser install stays a documented manual command (documented in plan 05.1-07's README task).

    (d) Do not add any `@playwright/test` package. This project's test runner is Vitest 4.1.10 with
    two configured projects; a second runner for one script is unjustified.

    (e) Run `npm run lint` and `npm run typecheck` to confirm the install did not disturb either gate.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); if(p.dependencies.playwright) throw new Error('playwright in dependencies'); if(!p.devDependencies.playwright) throw new Error('playwright missing from devDependencies'); console.log('OK', p.devDependencies.playwright)"` prints `OK` plus the approved version.
    - `node -e "const p=require('./package.json'); if(p.scripts.postinstall !== 'node scripts/prisma-generate.mjs') throw new Error('postinstall was modified: '+p.scripts.postinstall); console.log('postinstall intact')"` prints `postinstall intact`.
    - `node -e "const p=require('./package.json'); if(p.devDependencies['@playwright/test']||p.dependencies['@playwright/test']) throw new Error('@playwright/test must not be installed'); console.log('no test runner added')"` prints `no test runner added`.
    - `node -e "require('playwright'); console.log('playwright resolves')"` prints `playwright resolves`.
    - `npx playwright install --dry-run chromium` reports the chromium browser as already present (or `node -e "const {chromium}=require('playwright'); chromium.launch().then(b=>b.close()).then(()=>console.log('chromium launches'))"` prints `chromium launches`).
    - `npm run lint` and `npm run typecheck` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies.playwright) throw new Error('dep placement'); if(!p.devDependencies.playwright) throw new Error('missing devDep'); if(p.scripts.postinstall!=='node scripts/prisma-generate.mjs') throw new Error('postinstall touched'); console.log('OK')" &amp;&amp; node -e "const {chromium}=require('playwright'); chromium.launch().then(b=>b.close()).then(()=>console.log('chromium launches'))" &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`playwright` is in devDependencies at the approved version, Chromium launches headlessly, `postinstall` is untouched, lint and typecheck are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Untrusted third-party code enters the build machine and (if misplaced) the deploy runtime |
| npm install → Vercel build | Anything in `dependencies`, and any `postinstall` hook, executes on every production build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-SC | Tampering | `npm install playwright` | mitigate | Blocking `checkpoint:human-verify` (Task 1) presenting the raw-packument evidence packet; `[ASSUMED]` because slopcheck is unavailable; not auto-approvable under `auto_advance` or `mode: yolo` |
| T-05.1-01 | Elevation of Privilege | install-time code execution | mitigate | `playwright@1.63.0` verified to publish **no** `scripts` field at all; `puppeteer` rejected specifically for its `postinstall: node install.mjs` network fetch |
| T-05.1-02 | Tampering | `package.json` `postinstall` hook | mitigate | Task 2 explicitly forbids appending the browser download; asserted by a `node -e` equality check on the exact existing command string |
| T-05.1-03 | Information disclosure / attack surface | production runtime bundle | mitigate | `devDependencies` placement asserted programmatically (ASVS V14); nothing under `src/**` imports `playwright` |
</threat_model>

<verification>
- `npm run lint` exits 0
- `npm run typecheck` exits 0
- `npm run test:unit` exits 0 (no test surface changed, but the gate must stay green)
- `playwright` is a devDependency; `@playwright/test` is absent; `postinstall` is unchanged
</verification>

<success_criteria>
A human explicitly approved the dependency after reviewing registry evidence, `playwright` is installed as a devDependency at the approved version, Chromium launches headlessly, and no install hook was added to `package.json`.
</success_criteria>

<output>
Create `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-01-SUMMARY.md` when done. Record the exact approved version string.
</output>
