---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 09
status: complete
tasks_completed: [1, 2, 3]
requirements-completed: [TMPL-06]
completed: 2026-09-08
---

# Plan 05.1-09 Summary: Full 50-Template Generation & Wave 6 Reconciliation

## Status

**All 3 tasks complete. This is the final plan of Phase 05.1 — the phase is now complete.**

## Task 3: Full 50-preview review and phase gate (checkpoint outcome)

The orchestrator pre-verified the live result before presenting it: started a dev server against this exact branch (port 3333), confirmed via direct DOM/JS inspection that all 50 `<img>` elements on the onboarding branding page resolve to real R2 URLs (`realR2Count: 50`, `broken: 0`), confirmed segment-grouped headings render with correct counts (e.g. `Fashion & apparel (8)`), and confirmed dark mode applies no dimming/inversion filter to any preview image (U-13). The full automated gate (664/664 unit tests, lint/typecheck/build, 1004/1005 on the isolation suite with the one failure independently confirmed as a pre-existing unrelated flake — see Task 1-2 verification below) was already green going into the checkpoint.

The developer reviewed the live result themselves at the same URL (both `/onboarding/branding` and the storefront editor's "Change template" panel) and replied **"approved"** — an unqualified approval, no templates flagged for regeneration.

**Acceptance criteria confirmed:**
- The developer responded with explicit approval.
- The manifest contains 50 entries and the completeness assertion is green (confirmed in Task 1).
- No contact-sheet or scratch artifact was committed to the repository — this plan generated no such artifact (05-22 owns that, separately).
- No per-template regeneration was requested, so none was needed.

**Phase 05.1 (Template Preview Rendering & Picker Redesign) is complete.** TMPL-06 is genuinely true now: the template picker on both surfaces shows a real rendered screenshot per template, not a placeholder wireframe, in a grid layout the user confirmed reads as theme-store quality.

## Task 1: Generate all fifty previews, enforce manifest completeness

Ran `npm run templates:previews` with no `--only` flag against the calibrated crop constants from plan `05.1-08`. All 50 `TEMPLATE_KEYS` now have a real, generated entry in `src/server/theming/preview-manifest.ts`, in registry order, GENERATED banner intact, zero `eslint-disable`.

**Byte budget** (measured, not assumed — see `05.1-RESEARCH.md` assumption A4):

| Metric | Value |
|---|---|
| Total | 845,548 bytes (~826 KB) |
| Average per file | ~16,911 bytes (~16.5 KB) |
| Min | 10,862 bytes (`flagship-fashion`) |
| Max | 20,988 bytes |
| RESEARCH prediction | 25-50 KB/image, ~1.3-2.5 MB total |

The actual set comes in well under prediction — no mitigation needed on `pipeline.ts`'s preset.

**Drift guard strengthened** (`tests/unit/template-preview-manifest.test.ts`): the completeness assertions from the plan's `<behavior>` block were added in the same commit as the generation — a missing key now fails the build, not a silent D-05 fallback. A positive-control fixture proves the completeness predicate actually rejects a synthetic manifest missing one key. `TEMPLATE_KEYS.length === 50` is asserted first (anti-vacuity), every entry's `Math.max(width, height) === 800`, every `bytes > 0`, every `generatedAt` parses as a valid date. All pre-existing 05.1-02 assertions (key membership, ordering, entry shape, its own fixture controls) continue to pass unchanged.

No template failed generation — all 50 succeeded on the first full run.

## Task 2: Reconcile Phase 5's Wave 6 (05-22) plan

Audited every thumbnail/picker reference in `05-22-PLAN.md` against the actual shipped 05.1 component contract, per the plan's own instruction to check the claim rather than assume it.

**Finding:** the original sequencing assumption — that Phase 05.1 needed to land before 05-22 because the contact sheet would "consume 05.1's redesigned real-screenshot thumbnails" — does not hold. `05-22-PLAN.md`'s own `scripts/contact-sheet.mjs` was already designed, before Phase 05.1 existed, to render each template's full real default home page directly from `templateDefaultDocument()`/the section components. This is independent of both:
- `TemplateThumbnail` (the CSS-wireframe component, now demoted to the D-05 fallback role by this phase), and
- `TemplatePicker`/`TEMPLATE_PREVIEWS` (05.1's card-sized, cropped hero-plus-one-section R2 screenshots — the wrong shape for a full-home-page contact sheet regardless of which phase built them).

**Verdict per reference, enumerated:**

| Reference in 05-22-PLAN.md | Verdict |
|---|---|
| `key_links: scripts/contact-sheet.mjs → src/server/theming/defaults.ts via "renders every template's real default document, not the picker wireframe"` | **Stale wording** — "the picker wireframe" is ambiguous post-05.1 (the picker itself is no longer wireframe-primary; only its fallback state is). Corrected to name `TemplateThumbnail` and `TemplatePicker`/`TEMPLATE_PREVIEWS` explicitly, and to state why neither is the mechanism. |
| Task 1's generation approach | **Correct, unchanged** — already renders full pages via `templateDefaultDocument()`, not through either picker mechanism. |
| Task 2's and Task 3's checkpoint structure | **Correct, unchanged** — no dependency on picker internals. |
| D-14 stranger-test protocol | **Correct, unchanged.** |

No task text in `05-22-PLAN.md` required a structural change — this was a documentation correction, not a plan restructuring. Per the plan's own instruction, no unilateral restructuring was attempted; this finding is reported, not silently absorbed.

**`ROADMAP.md`'s Wave 6 blocking note** (around line 265) was corrected to match: the prior wording overstated a literal mechanism-coupling dependency. The corrected note explains the real reason Phase 05.1 must still land first — so no reviewer-facing surface in the product still shows CSS-wireframe placeholders as primary UI when 05-22's human stranger tests run — and links to `05-22-PLAN.md`'s own reconciliation note for the full audit.

A dated reconciliation note (2026-09-08, citing plan `05.1-09` Task 2) was added directly inside `05-22-PLAN.md`, above its `<objective>`, so a future reader of Phase 5's history sees why the text differs from when it was originally written.

## Verification — all passed

- `npx vitest run tests/unit/template-preview-manifest.test.ts` — 15/15 (includes the new completeness assertions and positive-control fixture)
- `npm run lint` (`--max-warnings=0`) — clean
- `npm run typecheck` — clean
- `npm run test:unit` — 664/664 passing, 40 files
- `npm run build` — clean, all routes generated
- `npm run test:full` — 1004/1005 passing. The 1 failure (`tests/isolation/claims.test.ts > ORD-03 tenant isolation (T-03-66) > refuses another tenant's claim id...`, a 30s timeout) is a **confirmed pre-existing, unrelated flake** — independently re-verified by running the same single test in isolation against both this worktree's branch and clean, unmodified `master`; it fails identically on both with the same timeout, and this phase's diff touches only `src/server/theming/preview-manifest.ts` and `tests/unit/template-preview-manifest.test.ts` (theming/preview generation), nothing in the claims/signup/order path. Consistent with this project's own documented history of Neon-serverless-connection-timeout flakes under the full isolation suite's ~47-minute runtime.
- `git diff --quiet -- src/server/theming/registry.ts src/server/theming/defaults.ts prisma/schema.prisma` — clean, no drift

## Commits

- `f736ead` — feat(05.1-09): generate all 50 template previews, enforce completeness
- `8457e36` — docs(05.1-09): reconcile Phase 5's Wave 6 plan with the shipped mechanism

## Next Phase Readiness

Phase 05.1 is fully complete — all 9 plans across 5 waves merged and gated, TMPL-06 satisfied, Task 3's full-set human review approved. Phase 5's Wave 6 (`05-22`) is now unblocked (it was waiting on Phase 05.1 completing, per `ROADMAP.md`'s corrected blocking note) and can proceed once its own turn comes up. Phase 05.2 and the in-flight Phase 05.3 (Storefront Editor Page Split) are unaffected — no shared files with this plan.
