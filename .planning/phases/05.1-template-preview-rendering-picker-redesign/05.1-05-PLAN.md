---
phase: 05.1-template-preview-rendering-picker-redesign
plan: 05
type: execute
wave: 2
depends_on: ["05.1-02"]
files_modified:
  - src/components/theming/template-picker.tsx
  - src/lib/strings/index.ts
  - src/app/onboarding/branding/page.tsx
  - src/app/onboarding/branding/loading.tsx
  - src/app/onboarding/branding/branding-form.tsx
  - "src/app/(dashboard)/dashboard/storefront-editor/page.tsx"
  - "src/app/(dashboard)/dashboard/storefront-editor/change-template-panel.tsx"
autonomous: true
requirements: [TMPL-06]
user_setup: []

must_haves:
  truths:
    - "Both picker surfaces hand the component a finished preview URL string or null — the client island never resolves an R2 URL itself."
    - "The Show all N templates toggle and its two strings no longer exist anywhere in the codebase."
    - "Both surfaces order their tiles so segments appear in INDUSTRY_SEGMENTS order, with template order preserved inside each segment."
    - "The onboarding branding page and its loading skeleton are the same width, so the skeleton-to-content transition does not jump."
    - "Every tile still renders on both surfaces — no card was removed by any change in this plan."
  artifacts:
    - path: "src/components/theming/template-picker.tsx"
      provides: "TemplateTile extended with previewUrl and segmentLabel; showAllToggle prop, showAll state and sortedBySegment helper deleted"
      contains: "previewUrl"
    - path: "src/app/onboarding/branding/page.tsx"
      provides: "RSC flatten supplying previewUrl + segmentLabel, segment-ordered, at max-w-5xl"
      contains: "templatePreviewPrefixFor"
    - path: "src/app/(dashboard)/dashboard/storefront-editor/page.tsx"
      provides: "Editor RSC flatten supplying previewUrl + segmentLabel, segment-ordered"
      contains: "TEMPLATE_PREVIEWS"
    - path: "src/lib/strings/index.ts"
      provides: "templateGroupRecommended + templateGroupHeading added; templateShowAll + templateShowRecommended deleted"
      contains: "templateGroupRecommended"
  key_links:
    - from: "src/app/onboarding/branding/page.tsx"
      to: "src/server/theming/preview-manifest.ts"
      via: "TEMPLATE_PREVIEWS lookup deciding previewUrl vs null before render (D-05)"
      pattern: "TEMPLATE_PREVIEWS\\["
    - from: "src/app/(dashboard)/dashboard/storefront-editor/page.tsx"
      to: "src/server/images/r2.ts"
      via: "publicUrlFor(templatePreviewPrefixFor(key) + '/card.webp')"
      pattern: "publicUrlFor"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a ... I want to ... so that ...` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.1` to convert):**
>
> The template picker (onboarding and the storefront editor's "Change template" action) shows a real rendered screenshot per template in a large, polished grid — not the placeholder CSS-wireframe thumbnails Phase 5 shipped — closing the gap against the owner-supplied Shopify Discover-Themes reference before Phase 5's Wave 6 builds its 50-template contact sheet on top of it.

**MVP-mode note:** this is the first half of the user-visible slice. After Task 2 a merchant sees a materially wider onboarding branding page; after plan 05.1-06 the same merchant sees the redesigned cards inside it. The data contract and the visual redesign are split across two plans for one hard reason stated in Task 1: the `TemplateTile` type change ripples to both call sites in a single atomic edit or `npm run typecheck` is red between commits, and this project's sampling gate runs typecheck on every task commit.

<objective>
Extend the shared `TemplateTile` contract with the two fields the redesigned card needs (`previewUrl`, `segmentLabel`), delete the superseded `Show all {N} templates` affordance end-to-end, order both surfaces' tiles by segment, and widen the onboarding container.

Purpose: `05.1-UI-SPEC.md` § Segment Grouping and U-08/U-09 settled all four of these. They are grouped here because they are the changes that cross the RSC/island boundary — plan 05.1-06 then redesigns the component's internals without touching a single call site.
Output: one extended type, two updated RSC flattens, two updated call sites, four string edits, and one page-width change applied to both the page and its skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md
@.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-PATTERNS.md
@src/components/theming/template-picker.tsx
@src/app/onboarding/branding/page.tsx
@src/app/(dashboard)/dashboard/storefront-editor/page.tsx
</context>

<interfaces>
Available after plan 05.1-02 (do not re-derive these; import them):

- `src/server/theming/preview-manifest.ts` — `export const TEMPLATE_PREVIEWS: Readonly<Partial<Record<TemplateKey, TemplatePreview>>>` and `export interface TemplatePreview { readonly width: number; readonly height: number; readonly bytes: number; readonly generatedAt: string; }`. Partial by design: a missing key IS D-05's fallback signal.
- `src/server/images/r2.ts` — `export function templatePreviewPrefixFor(templateKey: string): string` returning `templates/{key}`, and the pre-existing `export function publicUrlFor(key: string): string`. Both `server-only`.

Existing, unchanged:

- `src/server/theming/registry.ts` — `TEMPLATE_KEYS`, `TEMPLATES`, `INDUSTRY_SEGMENTS`, `isTemplateKey`, `TemplateKey`.
- `src/server/theming/defaults.ts` — `templateDefaultTokens(key)`.
- `src/lib/strings/index.ts` — `strings.branding.segments` is already keyed by segment id (`fashion-apparel` to `"Fashion & apparel"` etc., around line 1501). Reuse it; do not re-author segment labels.

Current `TemplateTile` (from `src/components/theming/template-picker.tsx`, lines 70-78) — `key` is a bare `string` on purpose, because the registry's key type lives in a `server-only` module this `"use client"` file must not reference even type-only:

- `readonly key: string`
- `readonly name: string`
- `readonly segment: string`
- `readonly segmentTag: string`
- `readonly minTier: PlanTier`
- `readonly sections: readonly TemplateSectionRef[]`
- `readonly primaryAccent: string`
- `readonly locked: boolean`

Current `TemplatePicker` props (lines 100-119): `tiles`, `selectedKey`, `onChange`, `currentKey?`, `sortBySegment?`, `showAllToggle`, `error?`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the tile contract, delete the show-all toggle, and segment-order both surfaces</name>
  <files>src/components/theming/template-picker.tsx, src/lib/strings/index.ts, src/app/onboarding/branding/page.tsx, src/app/onboarding/branding/branding-form.tsx, src/app/(dashboard)/dashboard/storefront-editor/page.tsx, src/app/(dashboard)/dashboard/storefront-editor/change-template-panel.tsx</files>
  <read_first>
    - `src/components/theming/template-picker.tsx` in full — the header's four ALL-CAPS invariants (lines 21-60), the `TemplateTile` type (62-78), `sortedBySegment` (80-98), the props destructure (100-119), the `showAll` state and `visibleTiles` derivation (120-125), and the toggle button (233-246)
    - `src/app/onboarding/branding/page.tsx` lines 30-50 (`templateCopy`, including the `flagship-fashion` special case that must survive) and lines 160-205 (the flatten and the comment at 196-201 explaining why the RSC resolves URLs)
    - `src/app/(dashboard)/dashboard/storefront-editor/page.tsx` lines 146-192 (`editorTemplateTiles`, including its header comment at 146-164 about `primaryAccent` being the template's own accent)
    - `src/app/onboarding/branding/branding-form.tsx` lines 710-730 (the `<TemplatePicker>` call site)
    - `src/app/(dashboard)/dashboard/storefront-editor/change-template-panel.tsx` lines 200-220 (the `<TemplatePicker>` call site)
    - `src/lib/strings/index.ts` lines 1495-1552 (the `branding` namespace: the `segments` record and the `template*` keys)
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md` § Segment Grouping and § Copywriting Contract
  </read_first>
  <action>
    One atomic edit across six files. It is atomic because a `TemplateTile` field added in one file and supplied in another leaves `npm run typecheck` red in between, and this project's sampling gate runs typecheck on every task commit.

    (a) `src/components/theming/template-picker.tsx` — TYPE AND PROP SURFACE ONLY. Do not touch the card markup, the grid classes, the badges, the tooltip wiring, or the `RadioGroupItem` in this task; plan 05.1-06 owns all of that.
    - Add two fields to `TemplateTile`: `readonly previewUrl: string | null;` and `readonly segmentLabel: string;`.
      Document `previewUrl` inline: null is D-05's fallback signal, decided in the RSC from manifest state BEFORE render — never via an `onError` handler, which flashes a broken image and cannot catch the un-allowlisted-`next/image`-host case at all.
      Document `segmentLabel` inline: it is the human label for `tile.segment`, flattened by the RSC because `tile.segment` is typed `string` and narrowing it here to index `strings.branding.segments` would pull registry types across the `server-only` boundary that `tests/unit/theming-marker-boundary.test.ts` enforces. `segmentTag` stays as a separate field — the two feed different slots.
    - Delete the `showAllToggle` prop from the props type and the destructure, delete the `const [showAll, setShowAll] = useState(false)` line, delete the `visibleTiles` derivation, delete the `sortedBySegment` helper function entirely, and delete the toggle `<button>` block at the end of the component. Remove the now-unused `useState` import if nothing else needs it.
    - `sortBySegment` STAYS as a prop. U-07 repurposes it from "sort" to "which group is hoisted first and titled Recommended for you". In this task it becomes temporarily unused by the render (the component maps `tiles` directly); mark it with a short `// consumed by the grouping introduced in 05.1-06` comment rather than deleting it, and if lint objects to an unused destructured value, keep it in the props type and read it into a `void` statement or leave the destructure in place — do NOT delete the prop and re-add it next plan.
    - The four ALL-CAPS header invariants must survive verbatim. Update only the SORT, NEVER FILTER paragraph to record that the show-all toggle was removed because explicit segment headings prove the same thing better, and that the rendered set is unchanged.

    (b) `src/lib/strings/index.ts`, `branding` namespace (around lines 1538-1550):
    - DELETE `templateShowAll` and `templateShowRecommended` and their doc comments. Real deletion, not commenting out.
    - ADD, with the same one-line doc-comment style:
      `templateGroupRecommended: "Recommended for you · {segment} ({n})"`
      `templateGroupHeading: "{segment} ({n})"`
      `{segment}` and `{n}` are `.replace()` placeholders, matching the existing `templateLockedChip` / `{tier}` convention already used at `template-picker.tsx:183-186`.
    - Leave `segments`, `templateCardTitle`, `templateHelper`, `templateLockedChip`, `templateLockedTooltip` and `templateRequired` exactly as they are. The helper copy's "See every template" phrase stays honest: every card still renders.

    (c) `src/app/onboarding/branding/page.tsx` — extend the existing `TEMPLATE_KEYS.map` flatten (lines 167-180) with the two new fields and add segment ordering:
    - `previewUrl: TEMPLATE_PREVIEWS[key] ? publicUrlFor(`${templatePreviewPrefixFor(key)}/card.webp`) : null`
    - `segmentLabel: strings.branding.segments[definition.segment]`
    - After the map, sort the array so segments appear in `INDUSTRY_SEGMENTS` order, preserving `TEMPLATE_KEYS` order inside each segment. Use a stable comparator on `INDUSTRY_SEGMENTS.indexOf(segment)` with an explicit original-index tiebreaker, the same legibility posture the deleted `sortedBySegment` used. This is what lets the client component derive group order from first appearance without importing the registry.
    - Import `TEMPLATE_PREVIEWS` from `@/server/theming/preview-manifest`, and `publicUrlFor` + `templatePreviewPrefixFor` from `@/server/images/r2`.
    - `templateCopy` (lines 33-45) is unchanged — the `flagship-fashion` special case still applies.
    - Extend the existing comment at lines 196-201 (the "R2_PUBLIC_BASE_URL is not a NEXT_PUBLIC_ variable" note) to say the same reasoning now covers the preview URL, and that the null-vs-string decision is D-05's fallback made here rather than in the island.

    (d) `src/app/(dashboard)/dashboard/storefront-editor/page.tsx` — same two additions inside `editorTemplateTiles`'s returned literal (lines 180-188), plus the same `INDUSTRY_SEGMENTS`-order sort applied to the returned array. Keep `segmentTag: strings.branding.segments[template.segment]` as it is; `segmentLabel` duplicating it on this surface is intentional (the picker consumes them in different slots). Amend the function's header comment (146-164) to record that the preview `.webp` is generated from the same `TEMPLATE_DEFAULTS` the `primaryAccent` line already promises, so the "generic preview of the template's own design, not this merchant's brand" promise survives the redesign.

    (e) `src/app/onboarding/branding/branding-form.tsx` (around line 723) and `src/app/(dashboard)/dashboard/storefront-editor/change-template-panel.tsx` (around line 210): delete the `showAllToggle` prop from both `<TemplatePicker>` call sites. KEEP `sortBySegment={industry === "" ? undefined : industry}` on the onboarding site; the editor site continues to pass none. Change nothing else at either call site.

    (f) Do NOT add `previewImageKey` to `TemplateDefinition`. Do NOT modify `src/server/theming/registry.ts`, `src/server/theming/defaults.ts`, or `prisma/schema.prisma` — no migration is needed this phase.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (both call sites supply the new required fields; neither passes the deleted prop).
    - `grep -rn "showAllToggle\|templateShowAll\|templateShowRecommended\|sortedBySegment" src/ | wc -l` returns `0`.
    - `grep -c "templateGroupRecommended" src/lib/strings/index.ts` and `grep -c "templateGroupHeading" src/lib/strings/index.ts` each return `1`.
    - `grep -c "previewUrl" src/components/theming/template-picker.tsx` returns at least `1`.
    - `grep -c "segmentLabel" src/components/theming/template-picker.tsx` returns at least `1`.
    - `grep -c "TEMPLATE_PREVIEWS" src/app/onboarding/branding/page.tsx` returns at least `1`, and the same for `src/app/(dashboard)/dashboard/storefront-editor/page.tsx`.
    - `grep -c "INDUSTRY_SEGMENTS" src/app/onboarding/branding/page.tsx` returns at least `1`, and the same for the editor page.
    - `npx vitest run tests/unit/theming-marker-boundary.test.ts` exits 0 — the `"use client"` picker still imports no value from a `server-only` module.
    - `npx vitest run tests/unit/dashboard-nav.test.ts` exits 0 — no inline user-facing prose was introduced.
    - `git diff --quiet -- src/server/theming/registry.ts src/server/theming/defaults.ts prisma/schema.prisma` exits 0.
    - `npm run test:unit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; test "$(grep -rl -e showAllToggle -e templateShowAll -e templateShowRecommended -e sortedBySegment src/ | wc -l)" -eq 0 &amp;&amp; git diff --quiet -- src/server/theming/registry.ts src/server/theming/defaults.ts prisma/schema.prisma &amp;&amp; npx vitest run tests/unit/theming-marker-boundary.test.ts &amp;&amp; npx vitest run tests/unit/dashboard-nav.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint</automated>
  </verify>
  <done>`TemplateTile` carries `previewUrl` and `segmentLabel`, both RSCs supply them in `INDUSTRY_SEGMENTS` order, the show-all toggle is gone from the component, both call sites and the string registry, and the server-only boundary test is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the onboarding branding container and its loading skeleton together</name>
  <files>src/app/onboarding/branding/page.tsx, src/app/onboarding/branding/loading.tsx</files>
  <read_first>
    - `src/app/onboarding/branding/page.tsx` lines 183-190 — the `max-w-2xl` wrapper and its existing comment ("Wider than create-store's max-w-md: the industry tile grid needs it")
    - `src/app/onboarding/branding/loading.tsx` in full — line 17's hardcoded `max-w-2xl` and the header explaining the skeleton mirrors the real page's shape
    - `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-UI-SPEC.md` § Onboarding Container Widening and decision U-09 (user-confirmed 2026-09-07)
    - `src/app/onboarding/branding/branding-form.tsx` — the industry tile grid's own classes, to confirm they are NOT changed
  </read_first>
  <action>
    Change `max-w-2xl` to `max-w-5xl` in exactly two places, in one commit:
    - `src/app/onboarding/branding/page.tsx` line 185's wrapper `<div className="w-full max-w-2xl">`
    - `src/app/onboarding/branding/loading.tsx` line 17's `<div className="w-full max-w-2xl">`

    Both files or neither. `05.1-PATTERNS.md` note 3 is explicit: split across tasks this ships a visible skeleton-to-content width jump.

    Update `page.tsx`'s existing comment above the wrapper to state the new reason: at `max-w-5xl` (1024px, roughly 976px of content) the template grid lands 3-up at approximately 309px per card, inside the 300-340px band taken from the Shopify reference. At `max-w-2xl` a 3-up grid yields 205px cards — smaller than the wireframe tiles they replace, which would not close the gap TMPL-06 names. Cite `05.1-UI-SPEC.md` U-09.

    Add a matching one-line comment in `loading.tsx` noting that this width is paired with `page.tsx` and must change with it.

    Explicitly DO NOT change:
    - The industry tile grid's `grid-cols-2 sm:grid-cols-3`. Six tiles across four columns would orphan a two-tile row; UI-SPEC U-09 rejects a fourth column by name.
    - The page's outer `px-4 sm:px-8` padding.
    - Any skeleton block height in `loading.tsx`.
    - Anything under `src/app/(dashboard)/` — this widening is scoped to the onboarding branding page.
  </action>
  <acceptance_criteria>
    - `grep -c "max-w-5xl" src/app/onboarding/branding/page.tsx` returns `1` and `grep -c "max-w-5xl" src/app/onboarding/branding/loading.tsx` returns `1`.
    - `grep -c "max-w-2xl" src/app/onboarding/branding/page.tsx` returns `0` and `grep -c "max-w-2xl" src/app/onboarding/branding/loading.tsx` returns `0`.
    - `grep -c "grid-cols-2 sm:grid-cols-3" src/app/onboarding/branding/branding-form.tsx` is unchanged from before this task (the industry grid was not touched) — `git diff --quiet -- src/app/onboarding/branding/branding-form.tsx` exits 0.
    - `grep -c "U-09" src/app/onboarding/branding/page.tsx` returns at least `1`.
    - `npx vitest run tests/unit/surface-token-isolation.test.ts` exits 0.
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` all exit 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'max-w-5xl' src/app/onboarding/branding/page.tsx)" -eq 1 &amp;&amp; test "$(grep -c 'max-w-5xl' src/app/onboarding/branding/loading.tsx)" -eq 1 &amp;&amp; test "$(grep -c 'max-w-2xl' src/app/onboarding/branding/page.tsx)" -eq 0 &amp;&amp; test "$(grep -c 'max-w-2xl' src/app/onboarding/branding/loading.tsx)" -eq 0 &amp;&amp; git diff --quiet -- src/app/onboarding/branding/branding-form.tsx &amp;&amp; npx vitest run tests/unit/surface-token-isolation.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The branding page and its Suspense skeleton are both `max-w-5xl`, the industry tile grid is untouched, and the reason is recorded in the page's own comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC (server) to `"use client"` island | `R2_PUBLIC_BASE_URL` is not a `NEXT_PUBLIC_` variable and `r2.ts` carries `server-only`; the island must receive a finished string, never the means to build one |
| merchant plan tier to renderable template set | Tier gating is a server-side control; the picker's locked/inert treatment is a courtesy on top of it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-18 | Information disclosure | `server-only` boundary | mitigate | `previewUrl` and `segmentLabel` are both resolved in the RSC and handed down as plain strings; asserted by `tests/unit/theming-marker-boundary.test.ts` staying green |
| T-05.1-19 | Elevation of privilege | tier gate bypass by re-selection | accept (control unchanged) | `assertTemplateAccess` / `switchTemplate` on the server remains the boundary (T-05-32). This plan does not touch `locked`, `disabled`, `aria-disabled`, or the inert-current-card guard (T-05-35) |
| T-05.1-20 | Tampering | SORT-NEVER-FILTER (Phase 5 D-05) | mitigate | Deleting the show-all toggle removes a control, never a card. The segment sort is a stable reorder with an explicit index tiebreaker; `tiles.length` is preserved. Plan 05.1-06's contract test asserts the partition invariant |
| T-05.1-21 | Information disclosure | inline user-facing prose | mitigate | Both new headings live in `src/lib/strings/index.ts`; `tests/unit/dashboard-nav.test.ts` scans `.tsx` for prose-shaped literals and is asserted green |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0 (`--max-warnings=0`)
- `npm run test:unit` exits 0
- `npm run build` exits 0
- `tests/unit/theming-marker-boundary.test.ts`, `tests/unit/dashboard-nav.test.ts` and `tests/unit/surface-token-isolation.test.ts` are all green
- `src/server/theming/registry.ts`, `src/server/theming/defaults.ts` and `prisma/schema.prisma` are unchanged
</verification>

<success_criteria>
Both surfaces flatten a finished `previewUrl` and `segmentLabel` in `INDUSTRY_SEGMENTS` order, the superseded show-all affordance is gone from component, call sites and strings alike, and the onboarding page and its skeleton share one width.
</success_criteria>

<output>
Create `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-05-SUMMARY.md` when done.
</output>
