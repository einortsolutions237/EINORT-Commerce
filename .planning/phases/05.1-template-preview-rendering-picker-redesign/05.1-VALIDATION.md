---
phase: 05.1
slug: template-preview-rendering-picker-redesign
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-09-07
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.10, two projects (`unit`, `isolation`). `environment: "node"` for both — there is no DOM; component behavior is verified by source-scanning contract tests (the established pattern: `dashboard-nav.test.ts`, `surface-token-isolation.test.ts`, `theming-marker-boundary.test.ts`). |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm run test:unit` (→ `vitest run tests/unit --reporter=dot`) |
| **Full suite command** | `npm run test:full` (→ `dotenv -e .env.test -- vitest run`, needs the dedicated Neon test branch) |
| **Additional gates** | `npm run lint` (`--max-warnings=0`), `npm run typecheck` (`tsc --noEmit`) |
| **Estimated runtime** | ~5s (unit) / several minutes (full, subject to this project's documented Neon connectivity variance) |

---

## Sampling Rate

- **After every task commit:** `npm run test:unit && npm run lint && npm run typecheck`
- **After every plan wave:** `npm run test:full`
- **Phase gate:** full suite green + the human review of all 50 generated previews, then `/gsd:verify-work`
- **Max feedback latency:** ~5s per task (unit)

---

## Per-Task Verification Map

| Requirement | Behavior | Test Type | Automated Command | File Exists |
|---|---|---|---|---|
| TMPL-06 | Every entry in the generated preview manifest is a real `TemplateKey`; all 50 present once generation completes; key order matches `TEMPLATE_KEYS` | unit (drift guard, mirrors `theming-registry.test.ts`) | `npx vitest run tests/unit/template-preview-manifest.test.ts` | ❌ Wave 0 |
| TMPL-06 | `templatePreviewPrefixFor` throws on a non-`TemplateKey`; never emits a key under `tenants/`; round-trips through `publicUrlFor` without tripping the `/original` refusal | unit (mirrors `r2-key.test.ts`) | `npx vitest run tests/unit/r2-key.test.ts` | ✅ exists — extend |
| TMPL-06 | `IMAGE_PRESETS.templatePreview` produces an 800px-long-edge WebP, aspect preserved, lossy encode | unit | `npx vitest run tests/unit/image-pipeline.test.ts` | ✅ exists — extend |
| TMPL-06 | Picker renders `next/image` when a preview URL exists and `TemplateThumbnail` when it doesn't (D-05 fallback); grid uses container queries, not viewport breakpoints; no `priority` on tiles | unit (source-scan contract) | `npx vitest run tests/unit/template-picker-contract.test.ts` | ❌ Wave 0 |
| TMPL-06 | No literal colour, Tailwind palette utility, `data-surface="storefront"`, or `brand-accent` utility introduced by the redesigned card | unit (auto-scanning) | `npx vitest run tests/unit/surface-token-isolation.test.ts` | ✅ exists — no change |
| TMPL-06 | The `"use client"` picker imports no value from a `server-only` registry/defaults/manifest module | unit (auto-scanning) | `npx vitest run tests/unit/theming-marker-boundary.test.ts` | ✅ exists — no change |
| TMPL-06 | No inline user-facing prose introduced in the redesigned picker | unit (auto-scanning) | `npx vitest run tests/unit/dashboard-nav.test.ts` | ✅ exists — no change |
| TMPL-06 | The screenshot script's scratch-tenant target guard refuses an unsafe target | unit (mirrors `seed-guard.test.ts`) | `npx vitest run tests/unit/seed-guard.test.ts` | ✅ exists — extend or sibling |
| D-06 | Placeholder preview photography is referenced only from the screenshot-generation script's own module — never from `TEMPLATE_DEFAULTS` or any file reachable by a real merchant request | unit (source-scanning boundary test, mirrors `surface-token-isolation.test.ts`'s method) | `npx vitest run tests/unit/preview-photo-isolation.test.ts` | ❌ Wave 0 |
| TMPL-06 | 50 generated previews are visually distinct and non-broken | **manual-only** — image quality is not machine-assertable; this is exactly what Phase 5's Wave 6 (05-22) contact-sheet stranger test exists for | `checkpoint:human-verify` reviewing the generated contact sheet | ❌ Wave 0 (checkpoint task) |

*Status: all ⬜ pending until execution begins.*

---

## Wave 0 Requirements

- [ ] `tests/unit/template-preview-manifest.test.ts` — TMPL-06, manifest ⇄ `TEMPLATE_KEYS` drift guard
- [ ] `tests/unit/template-picker-contract.test.ts` — TMPL-06, fallback branch + container-query grid + no-`priority` source-scan
- [ ] `tests/unit/preview-photo-isolation.test.ts` — D-06, placeholder-photo boundary enforcement
- [ ] Extend `tests/unit/r2-key.test.ts`, `tests/unit/image-pipeline.test.ts`, `tests/unit/seed-guard.test.ts`
- [ ] Framework install: **none** — Vitest 4.1.10 and both projects already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 50 generated previews are visually distinct, non-broken, and match the cropped hero+section framing (D-01) | TMPL-06 | Image quality/distinctiveness is not machine-assertable | `checkpoint:human-verify` reviewing the generated contact sheet before the phase gate closes; folds naturally into Phase 5's own Wave 6 (05-22) contact-sheet review rather than duplicating it |
| Package legitimacy of the new headless-browser dependency (`playwright`, tagged `[ASSUMED]` — slopcheck unavailable) | D-03 | Supply-chain trust judgment requires a human to independently confirm registry/repo/download-volume evidence, per this project's established `next-themes` precedent | `checkpoint:human-verify` before `npm install playwright` runs, evidence packet in `05.1-RESEARCH.md`'s Package Legitimacy Audit section |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (planner will confirm at plan-write time)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (planner will confirm at plan-write time)
- [x] Wave 0 covers all MISSING references (3 new files + 3 extensions, listed above)
- [x] No watch-mode flags
- [x] Feedback latency < 5s (unit) at task granularity
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-07
