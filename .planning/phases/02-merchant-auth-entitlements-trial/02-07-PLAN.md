---
phase: 02-merchant-auth-entitlements-trial
plan: 07
type: execute
wave: 6
depends_on: ["02-04", "02-05", "02-06"]
files_modified:
  - .planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md
autonomous: false
requirements: [TEN-04, SUB-01, SUB-02, ONB-05]

must_haves:
  truths:
    - "The whole phase passes one green gate: full suite, lint, typecheck and a production build"
    - "The manual-only behaviours the validation contract names are actually walked by a human before the phase is called done"
    - "The plan-selection screen reads like a real pricing page, and the read-only dashboard reads as intentional rather than broken"
  artifacts:
    - path: ".planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md"
      provides: "Signed-off validation contract with nyquist_compliant and wave_0_complete resolved"
      contains: "nyquist_compliant"
  key_links:
    - from: "02-VALIDATION.md per-task verification map"
      to: "the test files this phase created"
      via: "every row's automated command resolves to an existing, passing spec"
      pattern: "tests/(unit|isolation)/"
---

## Phase Goal

**As a** merchant, **I want to** sign in to a dashboard that knows which store is mine from my session alone and shows my plan and trial state, **so that** I can run my store knowing my subscription limits are enforced on the server, not just displayed in the UI.

<objective>
Run the phase gate and walk the two behaviours that cannot be automated.

Purpose: 02-VALIDATION.md's sign-off requires the full suite green with zero skipped plus a human walkthrough of the plan screen and the read-only dashboard. Those two are judgment calls about whether a merchant would understand their own state, which is precisely the thing CONTEXT's `<specifics>` section says must never be left ambiguous.
Output: a green gate, an updated validation contract, and a recorded human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md
@.planning/phases/02-merchant-auth-entitlements-trial/02-CONTEXT.md
@.planning/phases/02-merchant-auth-entitlements-trial/02-05-SUMMARY.md
@.planning/phases/02-merchant-auth-entitlements-trial/02-06-SUMMARY.md

Targeted reads:
- `.planning/phases/02-merchant-auth-entitlements-trial/02-UI-SPEC.md` lines 197-256 (the tier copy the walkthrough checks against) and lines 258-283 (banner states)
- `README.md` — the documented local run and `*.localhost` verification instructions Phase 1 wrote
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the phase gate and sign off the validation contract</name>
  <files>.planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md` in full — every row of the per-task verification map and the Wave 0 checklist that this task resolves
    - `package.json` scripts — the exact gate commands (`test:unit`, `test:full`, `lint`, `typecheck`)
    - `.planning/phases/01-multi-tenant-foundations-domain-resolution/01-07-SUMMARY.md` — the Windows `next start` smoke-check procedure Phase 1 established
  </read_first>
  <action>Run the four gates in order and capture their output: `npm run test:full`, `npm run lint`, `npm run typecheck`, `npx next build`. Then run the production smoke check the same way Phase 1 documented it — start the built app and confirm the apex serves `/login`, `/signup` and `/dashboard`, and that a storefront subdomain's `/dashboard` still 404s (the route group is apex-only because the proxy rewrites subdomain paths into `/s/{slug}/…`, where no dashboard route file exists). If any gate fails, fix the cause in the owning file rather than weakening the assertion, and record the fix in the summary.

Then update `.planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md`: tick every Wave 0 checkbox whose file now exists and passes, replace each `❌ W0` marker in the per-task verification map with the file that satisfies it, set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, and tick the Validation Sign-Off boxes that the gate proves. Leave the two manual-only rows unticked — Task 2 owns them. Do not tick anything the run did not actually demonstrate.</action>
  <verify>
    <automated>npm run test:full &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:full` reports 0 failures and 0 skipped, with a total at or above the inherited 186 plus this phase's new specs.
    - `npm run lint` exits 0 at `--max-warnings=0`; `npm run typecheck` exits 0; `npx next build` completes.
    - `grep -c "❌ W0" .planning/phases/02-merchant-auth-entitlements-trial/02-VALIDATION.md` is 0.
    - The validation frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`.
    - Every automated command listed in the validation map resolves to a file that exists (`ls` each path referenced in the map succeeds).
    - Committed as `docs(02-07): sign off the phase 2 validation contract`.
  </acceptance_criteria>
  <done>The phase passes one green gate and the validation contract reflects reality rather than intent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walkthrough of the plan screen, the trial banner and read-only mode</name>
  <action>Start the development server, then present the numbered walkthrough below to the developer and pause. Do not tick the two manual-only rows in 02-VALIDATION.md and do not write the summary until an explicit verdict comes back. If the verdict names a defect, fix it in the owning file and re-present the affected steps rather than recording the defect as a known issue.</action>
  <what-built>
    The full merchant path this phase adds: signup now routes through a mandatory three-tier plan screen; `/login` exists with a cross-link from `/signup`; `/dashboard` shows the store with a persistent trial countdown that escalates in the final two days; `/dashboard/plan` switches tier during the trial and shows a terminal state after it; `/suspended` handles a suspended organization; and the raw `/api/auth/organization/*` write endpoints refuse renames, deletes and over-limit members.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, then open `http://localhost:3000/signup` and create a new store. Confirm the submit lands on `/onboarding/plan` (same-origin, no full page reload) rather than jumping to the storefront.
    2. On `/onboarding/plan`: confirm three cards in the order Starter → Business → Professional, prices rendered as 5 000 / 12 500 / 25 000 XAF with no decimals, the `Most Popular` badge on Business only, Business **not** pre-selected, no "skip"/"decide later" control anywhere, and the CTA disabled until a card is chosen. Tap a card body (not just a radio) on a narrow window and confirm the whole card is the target. Confirm the feature bullets match `02-UI-SPEC.md` § Tier copy.
    3. Choose a plan and confirm you land on your storefront. Then open `http://localhost:3000/dashboard`: confirm the store name and address are yours, and the trial banner reads `10 days left in your trial.` in neutral styling with a clock icon.
    4. Sign out, then sign back in at `http://localhost:3000/login`. Confirm a wrong password and a made-up email produce the **identical** message, and that a correct sign-in returns you to `/dashboard`.
    5. Force the urgent state: in Prisma Studio (`npx prisma studio`) set your organization's `trialEndsAt` to about 36 hours from now. Reload `/dashboard` and confirm the banner switches to the urgent treatment with a warning icon and reads `1 day left in your trial.` (singular, no "0 days", no hours countdown).
    6. Force expiry: set `trialEndsAt` to yesterday. Reload `/dashboard` and confirm the dashboard **still loads and still shows your data**, the read-only banner explains the state, write controls are visible but disabled rather than hidden, and `Contact us to subscribe` opens `https://wa.me/237686661578` in a new tab. Then open `/dashboard/plan` and confirm it shows the terminal state with no working switcher.
    7. Restore an active trial (clear `trialEndsAt`), open `/dashboard/plan`, switch to another tier, and confirm the confirmation line renders inline (never a modal) and the success message appears on the page rather than as a toast.
    8. Set the organization's `status` to `suspended` in Prisma Studio and reload `/dashboard`; confirm you land on `/suspended` with a way to contact and a way to sign out. Then confirm the anonymous storefront path is unchanged: visit the store's subdomain and confirm it still shows the same `/store-not-found` body as an unknown hostname, disclosing nothing about suspension. Restore `status` to `active`.
    Judgment calls to report on explicitly: does the plan screen read like a real pricing page, and does the read-only dashboard read as an intentional state rather than a broken one?
  </how-to-verify>
  <verify>
    <human-check>Developer confirms steps 1-8, explicitly answering the two judgment calls</human-check>
  </verify>
  <acceptance_criteria>
    - The developer returns an explicit verdict covering all eight steps.
    - The two manual-only rows in 02-VALIDATION.md are ticked only after that verdict, and the Approval line is updated.
    - Any reported defect is fixed and re-verified rather than logged as a known issue.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe what looked wrong (which step, what you saw, what you expected)</resume-signal>
  <done>A human has walked the merchant path end to end and confirmed the plan screen and the read-only dashboard read the way the contract requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification process → shipped behaviour | A gate that passes without exercising the raw endpoints or the expired state would certify a phase whose controls are untested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-38 | Repudiation | A phase gate that tests the UI but never the raw `/api/auth/organization/*` writes | mitigate | `tests/isolation/org-endpoints.test.ts` runs inside `npm run test:full`, and the gate requires 0 skipped so the suite cannot pass by omission |
| T-02-39 | Tampering | Validation contract ticked ahead of reality | mitigate | Every `❌ W0` marker must be replaced by an existing file, asserted by grep, and the two manual rows stay unticked until the human verdict lands |
| T-02-40 | Information Disclosure | Suspension leaking to anonymous storefront visitors after this phase's `/suspended` work | mitigate | Walkthrough step 8 compares the suspended store's public hostname against the unknown-hostname body directly |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed across the phase; the gate would surface any lockfile change |
</threat_model>

<verification>
- `npm run test:full`, `npm run lint`, `npm run typecheck` and `npx next build` all green.
- The human walkthrough returns an explicit approval covering the plan screen and the read-only dashboard.
</verification>

<success_criteria>
- The phase's automated contract is complete, green and honest — no skipped tests, no unresolved Wave 0 markers.
- A human has confirmed the two behaviours that only a human can judge.
</success_criteria>

<output>
Create `.planning/phases/02-merchant-auth-entitlements-trial/02-07-SUMMARY.md` when done.
</output>
