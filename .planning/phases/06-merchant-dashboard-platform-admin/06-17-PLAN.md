---
phase: 06-merchant-dashboard-platform-admin
plan: 17
type: execute
wave: 7
depends_on: ["06-05", "06-10", "06-13", "06-14", "06-16"]
files_modified:
  - tests/unit/phase-06-requirement-coverage.test.ts
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md
autonomous: false
requirements: [DASH-01, DASH-02, ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, SUB-03]

must_haves:
  truths:
    - "Every Phase 6 requirement has a surface that satisfies it, and a test asserts the surfaces exist"
    - "The full test suite, lint, typecheck and build are green together"
    - "The platform owner can actually reach /admin on a real browser after running the bootstrap script, and nobody else can"
    - "A real merchant↔platform round trip works: message, attachment, reply, unread clearing, and a suspension notice landing in the thread"
    - "No project document still claims resend is unwired or that the admin header stub returns"
  artifacts:
    - path: "tests/unit/phase-06-requirement-coverage.test.ts"
      provides: "A source-scanning assertion that every ADM/DASH/SUB requirement has its named surface"
      min_lines: 80
  key_links:
    - from: "tests/unit/phase-06-requirement-coverage.test.ts"
      to: ".planning/REQUIREMENTS.md"
      via: "the eight requirement IDs, each mapped to a file that must exist"
      pattern: "ADM-05"
---

## Phase Goal

ROADMAP `**Goal:**` for Phase 6, verbatim: *A merchant can run their business day-to-day from a dashboard that surfaces what needs attention, and the platform owner can operate and support the pilot fleet of stores from a pilot-scoped Super Admin surface — including a direct messaging channel to every merchant and the ability to verify their subscription payments through it.*

**This plan's slice:** proving the whole goal is true, and leaving no project document contradicting what was built.

<objective>
Close Phase 6: a requirement-coverage guard, the full gate, the two manual verifications 06-VALIDATION.md names as automation-impossible, and the documentation corrections this phase's research uncovered.

Purpose: 06-VALIDATION.md lists two behaviours no automated test can prove — that the bootstrap script set `platformRole` on the intended real account and no other, and that the full two-session human experience of the thread actually works. Both are phase-gate obligations, not nice-to-haves. Separately, three project documents assert things this phase proved false, and STATE.md records that Phases 2, 3, 4 and 5 each left an unclosed gate; leaving a fifth is how the pattern continues.
Output: one contract test, one green full gate, one blocking human checkpoint, and four corrected documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md
@.planning/phases/06-merchant-dashboard-platform-admin/06-RESEARCH.md
@.planning/phases/06-merchant-dashboard-platform-admin/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The requirement-coverage guard and the full gate</name>
  <files>tests/unit/phase-06-requirement-coverage.test.ts</files>
  <read_first>
    - tests/unit/phase-03-requirement-coverage.test.ts (the existing precedent for this kind of test: how it maps requirement IDs to files or exports, and its anti-vacuous structure)
    - tests/unit/single-order-state-writer.test.ts (stripCommentLines and the three-part anti-vacuous shape)
    - .planning/REQUIREMENTS.md (the exact wording of DASH-01, DASH-02, ADM-01..05 and SUB-03)
    - .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md (the per-task verification map, whose TBD ids this plan replaces with the real plan/task numbers)
  </read_first>
  <action>
    Create `tests/unit/phase-06-requirement-coverage.test.ts` in the database-free `unit` project, modeled on `tests/unit/phase-03-requirement-coverage.test.ts`. Declare a table mapping each of the eight requirement IDs to the concrete artifacts that satisfy it, and assert each artifact exists on disk with non-trivial content. Suggested mapping, adjusted to whatever the executed plans actually produced:

    - `DASH-01` → `src/components/dashboard/attention-band.tsx` and the `Products live` card in `src/app/(dashboard)/dashboard/overview-metrics.tsx`
    - `DASH-02` → `LOW_STOCK_THRESHOLD` and `attentionCounts` exported from `src/server/dashboard/queries.ts`
    - `ADM-01` → `src/server/admin/suspend.ts`, `src/app/admin/page.tsx`
    - `ADM-02` → `src/app/admin/claims/page.tsx`, `src/server/admin/claims.ts`
    - `ADM-03` → `src/server/admin/domain.ts`, `src/components/admin/domain-cell.tsx`, `src/app/admin/support/page.tsx`
    - `ADM-04` → an **upper bound**: assert `src/app/admin/` contains exactly the four rail destinations (`page.tsx`, `claims`, `subscriptions`, `support`) plus `merchants/[id]` as a detail route, and nothing else. ADM-04 is a scope requirement, and the only way to test a scope ceiling is to assert nothing was added. Include a failure message explaining that a fifth destination means the deferred ~17-page admin surface started leaking in because the route tree made it cheap.
    - `ADM-05` → `src/server/support/messages.ts`, `src/app/(dashboard)/dashboard/support/page.tsx`, `src/app/admin/support/[tenantId]/page.tsx`, `src/server/support/notify.ts`
    - `SUB-03` → `src/server/subscription/claims.ts`, `src/server/admin/subscription-claims.ts`, `src/app/admin/subscriptions/page.tsx`

    Add the anti-vacuous guard first: assert the mapping table is non-empty and that at least one listed path resolves, so a refactor that moves everything does not silently pass an all-skipped suite.

    Then run the full gate in this task and record the results in the summary: `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build`, and `npm run test:full`. Record the observed `test:full` runtime — STATE.md tracks the isolation suite at 22–27 minutes before this phase, and this phase registered three new tenant-scoped models, so the measurement is input to the already-flagged v2.0 concern about the suite's growth.

    If any gate fails, the fix belongs in the plan that owns the file, not here. Report it rather than patching across plan boundaries.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit &amp;&amp; npm run build &amp;&amp; npm run test:full</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/phase-06-requirement-coverage.test.ts` passes and names all eight requirement IDs: `grep -c "DASH-01\|DASH-02\|ADM-01\|ADM-02\|ADM-03\|ADM-04\|ADM-05\|SUB-03" tests/unit/phase-06-requirement-coverage.test.ts` returns at least 8.
    - The ADM-04 ceiling assertion exists and is an upper bound, not a lower one: `grep -ci "no more than\|exactly\|ceiling\|fifth" tests/unit/phase-06-requirement-coverage.test.ts` returns at least 1.
    - `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build` and `npm run test:full` all exit 0.
    - `grep -ro 'variant="gold"' src/app src/components | wc -l` returns exactly 5.
    - `git diff --stat eslint.config.mjs` shows no change across the whole phase.
    - The summary records the observed `test:full` runtime.
  </acceptance_criteria>
  <done>Every requirement has a surface, the ceiling on admin scope is machine-checked, and every gate is green together.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the documents this phase proved wrong</name>
  <files>CLAUDE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md (the § Key Dependencies entry for `resend` claiming it is "not yet wired into any send call — no other source file under `src/` imports `resend` as of this analysis")
    - .planning/ROADMAP.md (the Phase 6 v2.0 reconciliation note claiming resend is still unwired, plus the Phase 6 entry's `**Plans**: TBD` line)
    - .planning/REQUIREMENTS.md (the eight unchecked requirement boxes and the traceability table rows marked Pending)
    - .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md (the per-task map whose Task IDs are all `TBD` and whose frontmatter says nyquist_compliant: false)
    - src/server/claims/notify.ts (the evidence: a complete, shipped Resend integration predating this phase)
  </read_first>
  <action>
    Four documentation corrections, each backed by evidence produced in this phase.

    **CLAUDE.md § Key Dependencies, the `resend` entry.** The claim that no source file under `src/` imports `resend` is stale — `src/server/claims/notify.ts` has been a complete production integration since Phase 3. Replace the "not yet wired" sentence with an accurate one naming both consumers (`src/server/claims/notify.ts` and `src/server/support/notify.ts`) and the shared degradation posture (both env vars optional; absent keys produce a named `console.warn` and the in-app badge remains the reliable channel). Change nothing else in CLAUDE.md.

    **ROADMAP.md.** Correct the same stale Resend sentence in the Phase 6 v2.0 reconciliation note. Replace Phase 6's `**Plans**: TBD` with the real plan count and the list of the seventeen plan files with one-line objectives, and mark the phase's checkbox and Success Criteria state according to what actually shipped. Do not alter any other phase's entry.

    **REQUIREMENTS.md.** Mark `DASH-01`, `DASH-02`, `ADM-01`, `ADM-02`, `ADM-03`, `ADM-04`, `ADM-05` and `SUB-03` complete in both the checklist and the traceability table — but **only** those whose success criteria the gate and the human checkpoint actually confirmed. If any is partial, say which clause and why, rather than checking the box. Two known scoped-out items must be recorded rather than silently carried as complete: `resolveEntitlements` does not enforce `subscriptionCurrentPeriodEnd` (KD-V2-02, deferred deliberately in plan 06-16), and the low-stock threshold is a constant rather than a merchant setting (R-5, replaced in Phase 9).

    **06-VALIDATION.md.** Replace every `TBD` Task ID with the real plan and task number that satisfied it, set each row's Status to its observed result, tick the Wave 0 checklist, tick the Validation Sign-Off boxes the gate proved, and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter once they are actually true. If a row is not satisfied, leave it red and say so — a validation document that reports what was hoped for is worse than one that reports a gap.

    Additionally, record in the phase summary the two deliberate deviations from 06-UI-SPEC.md that this phase resolved in favour of locked CONTEXT decisions or verified constraints:
    - **D-22 over Assumption A2:** PDF attachments were built (plan 06-13) as a separate non-re-encoding path, against 06-UI-SPEC.md § S's images-only row and its Open Item #1.
    - **D-03 over the `dashboard-header-controls.tsx` comment:** the Super Admin header link did **not** return; the comment was updated in plan 06-04 to record it as a considered deviation.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "not yet wired into any send call" CLAUDE.md` returns 0.
    - `grep -c "support/notify" CLAUDE.md` returns at least 1.
    - `grep -ci "resend is still unwired\|no module under src/ imports it" .planning/ROADMAP.md` returns 0.
    - No `**Plans**: TBD` remains under the Phase 6 entry in `.planning/ROADMAP.md`.
    - `grep -c "TBD-" .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md` returns 0.
    - `grep -c "nyquist_compliant: true" .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md` returns 1, and it is only set if every row is genuinely satisfied.
    - The two known scoped-out items are recorded in REQUIREMENTS.md: `grep -c "KD-V2-02" .planning/REQUIREMENTS.md` returns at least 1 and `grep -ci "low-stock\|LOW_STOCK" .planning/REQUIREMENTS.md` returns at least 1.
    - No other phase's ROADMAP entry changed: `git diff .planning/ROADMAP.md` touches only the Phase 6 section and the Phase 6 line in the top-level list.
  </acceptance_criteria>
  <done>No project document still contradicts what was built, and the validation strategy reports observed results rather than intentions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The two manual-only verifications</name>
  <files>.planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-merchant-dashboard-platform-admin/06-VALIDATION.md (§ Manual-Only Verifications — the two rows this task discharges and their stated reasons for being automation-impossible)
    - scripts/promote-admin.ts (the bootstrap script's argument shape and its `--yes` requirement, before running it against a real account)
    - .planning/phases/06-merchant-dashboard-platform-admin/06-UI-SPEC.md (§ C0, § S and § Responsive strategy — the behaviours the 360px spot-check is measured against)
  </read_first>
  <action>
    Run the two manual verifications below with the developer present, then record each numbered step's observed result in 06-VALIDATION.md's Manual-Only Verifications section and in the plan summary. Do not mark the phase complete until the developer has answered. If a step fails, capture exactly what was observed and route the fix to the plan that owns the file rather than patching here.
  </action>
  <what-built>
    The whole of Phase 6: the Platform Super Admin at `/admin` (merchants list with domain status, order-payment claims ledger, subscription-payments ledger, support inbox and per-merchant threads, suspend and restore), the merchant support thread with image and PDF attachments and an email nudge, the SUB-03 subscription payment submit-and-review flow, and the `/dashboard` attention band with the `Products live` card.

    Everything automatable has already been automated and is green: lint, typecheck, the unit suite, the build, and the full isolation suite including the three new tenant-scoped models. The two checks below are the ones 06-VALIDATION.md records as automation-impossible — one because it depends on the bootstrap script hitting the *real* intended account row, and one because it is a two-session human experience no test exercises.
  </what-built>
  <how-to-verify>
    **Check A — the admin-account bootstrap (06-VALIDATION.md, manual verification 1).**
    1. Run `npm run admin:promote -- your@email.example --yes` against your development database with your own account's email.
    2. Confirm the script prints the resolved user id and email **before** writing, and prints the before and after `platformRole`.
    3. Sign in at `/login` with that account. Confirm you land on `/admin`, not `/dashboard` and not `/onboarding/create-store`.
    4. Confirm the gold `EINORT Platform Admin` strip is visible and legible in **both** light and dark themes, and that the four rail items are present.
    5. In a private window, sign in as an ordinary merchant account and visit `/admin`. Confirm you get the **ordinary site 404** — the same page as `/asdf` — with no admin chrome, no gold strip, and no "you don't have permission" wording.
    6. While signed out entirely, visit `/admin`. Confirm the same 404, and that you are **not** redirected to `/login`.
    7. Confirm no link to `/admin` appears anywhere in the merchant dashboard.

    **Check B — the full merchant↔platform round trip (06-VALIDATION.md, manual verification 2).**
    Use two browser sessions: a merchant in one, the bootstrapped owner in the other.
    1. As the merchant, open `/dashboard/support` and send a message with **an image attachment**. Confirm the bubble appears immediately in its pending state and then settles.
    2. As the merchant, send a second message with **a PDF attachment**. Confirm it renders as a document tile, not a broken image.
    3. As the owner, open `/admin/support`. Confirm the merchant's thread is at the top with an unread count, and that merchants with no messages still appear lower down.
    4. Open the thread. Confirm the image opens in the lightbox and the PDF **downloads** rather than rendering inline in the browser tab. Confirm the merchant's messages appear on the left and yours on the right.
    5. Reply. Confirm the merchant's session shows the reply and that the merchant's `Support` rail badge shows an unread count which **clears when they open the page**, with no "mark as read" control anywhere.
    6. If `RESEND_API_KEY` and `RESEND_FROM_EMAIL` are configured, confirm the email nudge arrives and that it does **not** quote the message body or any transaction reference. If they are not configured, confirm the server log shows the named `DEGRADED` warning instead — that is the correct behaviour, not a failure.
    7. As the owner, suspend that merchant from `/admin` with a written reason. Confirm the reason appears in the merchant's thread as a system message that reads naturally, that the merchant's storefront subdomain stops serving, and that the merchant's next dashboard load lands on `/suspended`.
    8. Restore the merchant from the same control. Confirm the storefront serves again and the restore message appears in the thread.
    9. As the merchant, submit a subscription payment from `/dashboard/plan` with a reference and a receipt. Confirm the submit button is replaced by the claim card, and that the claim appears inline in the thread.
    10. As the owner, confirm that payment from `/admin/subscriptions`. Confirm the dialog names the resulting date, that the same date appears afterwards in the `Covers through` column, and that the merchant's thread shows the confirmation.

    Also spot-check at 360px width: the merchants list, the claims ledger and the subscriptions ledger must stack into cards with **no horizontal scroll**, and both composers must stay pinned to the bottom.
  </how-to-verify>
  <verify>
    <human-check>All 17 numbered steps across Check A and Check B pass, plus the 360px spot-check.</human-check>
    <automated>MISSING — automation-impossible by design; 06-VALIDATION.md § Manual-Only Verifications states why. The automated half of this phase's coverage is Task 1's full gate.</automated>
  </verify>
  <acceptance_criteria>
    - The developer has answered the resume signal.
    - Every numbered step in Check A and Check B has a recorded observed result in 06-VALIDATION.md's Manual-Only Verifications section — pass or fail, never blank.
    - Check A step 5 and step 6 are recorded as producing the *same* page as an ordinary missing route, in the developer's own words.
    - Check B step 4 is recorded as a download, not an inline render.
    - Check B step 6 is recorded as either a received email with no reference quoted, or the named `DEGRADED` warning in the server log.
    - Any failing step is written up with what was observed and which plan owns the file, and is not patched inside this plan.
  </acceptance_criteria>
  <done>Both manual-only verifications from 06-VALIDATION.md are discharged with recorded results and a developer verdict.</done>
  <resume-signal>Type "approved" if every check passes, or describe exactly which numbered step failed and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the bootstrap script → the real production/dev account row | The one write no automated test can verify against the intended target |
| documentation → the next agent's assumptions | A stale document is an instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-88 | Elevation of Privilege | the bootstrap promoting the wrong account | mitigate | Check A step 2 requires the operator to read the resolved id and email before the write, and the script refuses any user holding an organization membership (plan 06-01) |
| T-06-89 | Information Disclosure | the `/admin` 404 being distinguishable in a real browser | mitigate | Check A steps 5 and 6 compare the merchant and anonymous responses against an ordinary missing route, which is the only way to confirm byte-identical rendering end to end |
| T-06-90 | Tampering | a fifth admin destination leaking the deferred admin surface in | mitigate | The ADM-04 ceiling assertion in `tests/unit/phase-06-requirement-coverage.test.ts` fails the build on a fifth page |
| T-06-91 | Information Disclosure | a PDF rendering inline on the app origin | mitigate | Check B step 4 confirms the download behaviour a header misconfiguration would silently break, which no unit test observes |
| T-06-92 | Repudiation | closing the phase without a verification record, as Phases 2–5 each did | mitigate | This plan is the gate; STATE.md records four prior unclosed gates and this one produces a summary, a corrected VALIDATION.md, and a signed human checkpoint |
| T-06-SC | Tampering | npm installs | mitigate | Zero packages installed across the entire phase, matching 06-RESEARCH.md § Package Legitimacy Audit's prediction |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run test:unit && npm run build && npm run test:full` all green in one run.
- `grep -ro 'variant="gold"' src/app src/components | wc -l` returns exactly 5.
- `git diff --stat eslint.config.mjs` shows no change across the phase.
- Both manual verifications in 06-VALIDATION.md signed off with recorded results.
</verification>

<success_criteria>
- All eight Phase 6 requirements have a surface, asserted by a coverage test, with ADM-04's ceiling machine-checked.
- The full suite, lint, typecheck and build are green together, and the isolation runtime is measured and recorded.
- Both manual-only verifications are signed off by the developer.
- CLAUDE.md, ROADMAP.md, REQUIREMENTS.md and 06-VALIDATION.md all describe what actually shipped, including the two deliberate deviations and the two deferred items.
</success_criteria>

<output>
Create `.planning/phases/06-merchant-dashboard-platform-admin/06-17-SUMMARY.md` when done. Record the `test:full` runtime, the two deviations (D-22 over A2, D-03 over the header-stub comment), the two deferrals (KD-V2-02 entitlement enforcement, R-5's low-stock constant), and the human checkpoint's verdict.
</output>
