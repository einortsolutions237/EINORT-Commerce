---
phase: 05.2-marketing-landing-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/strings/marketing.ts
  - src/lib/strings/index.ts
  - src/app/page.tsx
  - tests/unit/landing-page-contract.test.ts
autonomous: false
requirements: [MKTG-01]
user_setup: []

must_haves:
  truths:
    - "A visitor to the apex root page sees a real landing page — top lockup, hero, three differentiator sections (payments, templates, speed, in that order), a how-it-works section and a final CTA — not the 3-element wordmark/tagline/button placeholder."
    - "Every claim on the page is traceable to 05.2-RESEARCH.md's Verified Feature Ledger — no invented merchant counts, logos, testimonials, or unbuilt-feature claims (custom domains, discount codes, bulk import, staff accounts, delivery zones, low-stock alerts, priority support) appear anywhere in the copy."
    - "The hero renders correctly in both manifest states: a single centered copy-only column when TEMPLATE_PREVIEWS has no entry for flagship-fashion (today), and a two-column grid with a real R2-hosted screenshot the moment it does (post 05.1-09) — with no wireframe, no stock photo and no reserved empty box ever shown."
    - "Every visible string on the page is sourced from strings.root.* (spread from marketingCopy) or strings.signup.loginLink — zero inline JSX prose in src/app/page.tsx."
    - "The page uses only semantic design tokens (no literal colour, no Tailwind palette utility, no brand-accent token, no gold variant, no dark: utility, no data-surface=\"storefront\") and stays reachable only via the apex hostname — a storefront subdomain never renders it."
    - "Both primary CTA buttons read \"Create my store\" and link to /signup with identical markup; the top-lockup \"Sign in\" link and the final-CTA \"Already have a store? Sign in\" link both go to /login as plain text, never as a second button."
  artifacts:
    - path: "src/lib/strings/marketing.ts"
      provides: "All landing-page copy (hero, three differentiators, how-it-works, final CTA, metadata), spread into strings.root"
      contains: "export const marketingCopy"
    - path: "src/lib/strings/index.ts"
      provides: "root namespace now sourced from marketingCopy instead of the three-key placeholder object"
      contains: "root: { ...marketingCopy }"
    - path: "src/app/page.tsx"
      provides: "The five-band RSC landing page: top lockup, hero with manifest-conditional image slot, three differentiators, how-it-works, final CTA, plus title.absolute metadata"
      contains: "TEMPLATE_PREVIEWS"
    - path: "tests/unit/landing-page-contract.test.ts"
      provides: "MKTG-01's mechanical honesty guard: band coverage, no inline prose in page.tsx, banned-claims list and voice contract over marketing.ts"
      contains: "describe(\"landing page contract\""
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/strings/marketing.ts"
      via: "strings.root.* references, spread from marketingCopy"
      pattern: "strings\\.root\\."
    - from: "src/app/page.tsx"
      to: "src/server/theming/preview-manifest.ts"
      via: "TEMPLATE_PREVIEWS[HERO_TEMPLATE_KEY] branch decided before render"
      pattern: "TEMPLATE_PREVIEWS\\["
    - from: "src/app/page.tsx"
      to: "src/server/images/r2.ts"
      via: "publicUrlFor(`${templatePreviewPrefixFor(HERO_TEMPLATE_KEY)}/card.webp`) composes the hero src from a compile-time constant only"
      pattern: "templatePreviewPrefixFor"
    - from: "tests/unit/landing-page-contract.test.ts"
      to: "src/lib/strings/marketing.ts"
      via: "banned-claims and voice-contract source scan"
      pattern: "marketing\\.ts"
---

## Phase Goal

> **ROADMAP `**Goal:**` verbatim (NOT in `As a ... I want to ... so that ...` user-story form — flagged to the orchestrator; run `/gsd mvp-phase 05.2` to convert if MVP-mode user-story framing is wanted):**
>
> A prospective merchant landing on einort.com's root page understands, within seconds, what EINORT does and why it fits their business, and is compelled toward signup by honest, specific claims about real Cameroon-first payment methods, real template quality, and genuine speed-to-launch — not the current bare wordmark-plus-button placeholder.

<objective>
Replace the current 3-element placeholder at `src/app/page.tsx` with the real, D-04-shaped marketing landing page: a new `src/lib/strings/marketing.ts` copy module (D-01's Verified Feature Ledger, D-06's no-gold rule, D-03's English-only voice), the rewritten page implementing the top lockup + hero + three differentiators + how-it-works + final CTA, and the new source-scanning contract test that makes MKTG-01's honesty clause and the six existing build-failing guards mechanically enforced going forward.

Purpose: this is the merchant's first-ever contact with EINORT, immediately before onboarding in the same design system — it must be honest, professional, and structurally incapable of drifting into the unbuilt-feature claims already living in `strings.plan.*.features`.
Output: `src/lib/strings/marketing.ts`, the one-line `index.ts` change, the rewritten `src/app/page.tsx`, and `tests/unit/landing-page-contract.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-marketing-landing-page-redesign/05.2-CONTEXT.md
@.planning/phases/05.2-marketing-landing-page-redesign/05.2-RESEARCH.md
@.planning/phases/05.2-marketing-landing-page-redesign/05.2-UI-SPEC.md
@.planning/phases/05.2-marketing-landing-page-redesign/05.2-PATTERNS.md
@src/app/page.tsx
@src/app/login/page.tsx
@src/lib/strings/index.ts
@src/lib/strings/flagship.ts
@src/server/theming/preview-manifest.ts
@src/server/images/r2.ts
@tests/unit/dashboard-nav.test.ts
@tests/unit/surface-token-isolation.test.ts
@tests/unit/preview-photo-isolation.test.ts
</context>

<interfaces>
`strings.root` today (`src/lib/strings/index.ts:42-46`): `{ wordmark: BRAND, tagline: string, cta: string }`, consumed only by `src/app/page.tsx` (grep-confirmed, three hits, all in the file this plan rewrites).

`strings.signup.loginLink` (`src/lib/strings/index.ts:172`) = `"Already have a store? Sign in"` — reuse this verbatim for the final-CTA secondary link rather than declaring a duplicate string in `marketingCopy` (the checker's non-blocking nit on `05.2-UI-SPEC.md`).

`TEMPLATE_PREVIEWS` (`src/server/theming/preview-manifest.ts`): `Readonly<Partial<Record<TemplateKey, TemplatePreview>>>`, ships `{}` until `05.1-09` runs. An absent key is the D-05 signal to render the hero copy-only, not an error.

`templatePreviewPrefixFor(templateKey: string): string` and `publicUrlFor(key: string): string` (`src/server/images/r2.ts:157,273`). `templatePreviewPrefixFor` throws on a non-`TemplateKey` input and returns `templates/{key}`; the derivative basename is `card.webp` (the `templatePreview` preset's sole label, `src/server/images/pipeline.ts:128-137`), so the full derivative key is `` `${templatePreviewPrefixFor(HERO_TEMPLATE_KEY)}/card.webp` ``. `publicUrlFor` throws on any key ending `/original` — never call it with anything but the composed derivative key. `"flagship-fashion"` is a member of `TEMPLATE_KEYS` (`src/server/theming/registry.ts:432`) — TMPL-01's frozen portfolio anchor, per UI-SPEC U-13.

`Button` (`src/components/ui/button.tsx`): `variant` defaults to `"default"` (`bg-primary text-primary-foreground`); accepts Base UI's `render` prop for the button-as-link idiom already used on the current placeholder page (`<Button render={<Link href="/signup" />} className="mt-8 min-h-11 px-6 text-sm font-semibold">`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the marketing copy module and wire it into strings.root</name>
  <files>src/lib/strings/marketing.ts, src/lib/strings/index.ts</files>
  <read_first>
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-CONTEXT.md` — D-01 (no fabrication), D-03 (English-only voice), D-06 (no gold)
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-RESEARCH.md` § Verified Feature Ledger (the allow-list — `strings.plan.*.features` is NOT a source), § Candidate Copy, § Validation Architecture → Phase Requirements → Test Map (the exact banned-phrase list)
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-UI-SPEC.md` § Copywriting Contract (the finalized, checker-approved copy table — authoritative over RESEARCH.md's draft) and § Decisions Made Under "Claude's Discretion" U-02/U-12 (top-lockup "Sign in" wording, never `text-primary`)
    - `src/lib/strings/flagship.ts` in full (lines 1-26 already read this session) — the header-comment precedent for an extracted strings module, and why *this* module's justification (file-size self-containment) differs from flagship's (TS7022 circularity)
    - `src/lib/strings/index.ts` lines 1-46, 160-206 — `BRAND`, the current `root` object, the `flagshipCopy` spread precedent (`import` + `flagship: { ...flagshipCopy }`), and `strings.signup.loginLink` / `strings.login.signupLink`'s exact values
  </read_first>
  <behavior>
    This module is a `const` copy catalogue, not business logic — there is no `expect(fn(input)).toBe(output)` to write against it. The enforcement is content-shaped instead: every string must avoid the banned-claims list and the voice contract, and Task 2's `landing-page-contract.test.ts` is where that becomes an automated assertion. Verify this task's output with the grep-based acceptance criteria below before moving to Task 2; do not wait for the vitest contract test to exist to catch a violation here.
  </behavior>
  <action>
    Create `src/lib/strings/marketing.ts` exporting `marketingCopy` as a single `as const` object. Open with a header comment (matching the codebase's substantial-header convention) stating: this module exists for file-size self-containment (`index.ts` is already 1867 lines; this is not flagship.ts's TS7022 circularity reason), every claim below is traceable to `05.2-RESEARCH.md` § Verified Feature Ledger, `strings.plan.*.features` is explicitly NOT a source for this file, and `strings.signup.loginLink` is reused rather than duplicated for the final-CTA secondary link (name the checker's nit).

    Populate `marketingCopy` with exactly these top-level keys and values, taken verbatim from `05.2-UI-SPEC.md` § Copywriting Contract (the approved, checker-signed-off table — not RESEARCH.md's draft, where the two differ in wording):

    Keep the three legacy keys so nothing else that might read `strings.root.*` breaks: `wordmark` = the literal string `"EINORT"` (do not import `BRAND` back from `./index` — that reintroduces the exact circularity flagship.ts's header warns about; add a comment noting it must stay byte-identical to `BRAND` in `index.ts`), `tagline` = `"Create your online store in minutes."`, `cta` = `"Create my store"`.

    Add `signIn` = `"Sign in"` (the short top-lockup label — distinct from the full-sentence final-CTA link, which is not duplicated here; see below).

    Add `metaTitle` = `"EINORT — Create your online store in Cameroon"` and `metaDescription` = `"Sell online in Cameroon with Mobile Money, Orange Money, cash on delivery and WhatsApp orders. Pick a template, add your products, and publish your storefront in minutes."`.

    Add a `hero` object with `heading` = `"Your store online, taking orders today."`, `body` = `"Pick your industry, add your logo and your products, and publish a storefront at your own yourshop.einort.com address. Your customers pay by MTN Mobile Money, Orange Money, cash on delivery, or send their order straight through WhatsApp."`, `ctaNote` = `"Free for 10 days. No card required."`.

    Add a `payments` object (differentiator 1, placed first per Finding 1) with `heading` = `"Get paid the way Cameroon actually pays."`, `body` = `"Every EINORT store takes MTN Mobile Money and Orange Money transfers, cash on delivery, and WhatsApp orders from the day it opens. Your customer sees your receiving number and the exact amount, sends the money, and submits their transaction reference — with a screenshot if they have one. You confirm or reject it from your dashboard in one tap."`, `constraint` = `"No order is ever marked paid on the customer's word alone, and the same transaction reference can't be used twice. There is no card gateway and no automatic settlement — the money arrives in your own wallet, exactly as it does today."`.

    Add a `templates` object (differentiator 2) with `heading` = `"Fifty templates. Six industries. Not one of them generic."`, `body` = `"Fashion, electronics, beauty, grocery, furniture and general retail each get their own layout structure — a different section order, a different way of showing products, a different type treatment. Add your logo and your brand colors and the whole storefront takes them, right down to the buttons."`, `constraint` = `"Starter reaches 10 templates, Business 25, Professional all 50. Every plan gets the full 10-day trial first."`.

    Add a `speed` object (differentiator 3) with `heading` = `"From sign-up to a live address, in one sitting."`, `body` = `"Name your business, choose your industry, upload your logo, pick your colors and a template. EINORT publishes your store at yourshop.einort.com straight away — a real address you can share the same afternoon. Photos taken on a phone are resized and cleaned up automatically, so one bad picture doesn't wreck the page."`, `constraint` = `"No developer, no hosting to set up, no domain to buy."`.

    Add a `howItWorks` object with `heading` = `"Three steps, no developer."` and a `steps` tuple of exactly three `{ title, body }` entries: step 1 `title` = `"Create your store"`, `body` = `"Email, password, and the address you want. Your .einort.com address is live the moment you finish."`; step 2 `title` = `"Make it yours"`, `body` = `"Pick your industry and a template, add your logo and colors, and list your first products."`; step 3 `title` = `"Start selling"`, `body` = `"Share your address. Take orders by Mobile Money, cash on delivery or WhatsApp, and confirm each payment from your dashboard."`.

    Add a `finalCta` object with `heading` = `"Ten days. No card. Your own storefront."` and `body` = `"Every plan starts with a full 10-day trial of everything it includes. Set your store up, show it to a customer, and decide after."`. Do NOT add a `signInLink` field here — `page.tsx` will import and render `strings.signup.loginLink` directly for that spot, per the interface note above.

    Voice contract, applied to every string above: no exclamation marks, no "Oops"/"Whoops", no emoji, no superlative with no referent, no invented count. Every numeric claim printed on this page (10-day trial, 10/25/50 templates per tier) must match `src/server/entitlements/plans.ts`'s actual `PLANS` values exactly; do not print the 5,000/12,500/25,000 XAF monthly prices — they are out of D-04's scope for this page.

    Then, in `src/lib/strings/index.ts`: add `import { marketingCopy } from "./marketing";` alongside the existing `flagshipCopy` import (same alphabetical grouping), and replace the current `root: { wordmark: BRAND, tagline: ..., cta: ... }` object (lines 42-46) with `root: { ...marketingCopy }` — the exact spread idiom already used for `flagship: { ...flagshipCopy }`.
  </action>
  <acceptance_criteria>
    - `grep -c "export const marketingCopy" src/lib/strings/marketing.ts` returns `1`.
    - `grep -c "root: { ...marketingCopy }" src/lib/strings/index.ts` returns `1`.
    - `grep -iE "custom domain|discount code|bulk import|staff account|delivery zone|low-stock|priority support|testimonial|trusted by|millions|thousands of" src/lib/strings/marketing.ts` matches nothing (empty output).
    - `grep -c "!" src/lib/strings/marketing.ts` returns `0`.
    - `grep -icE "oops|whoops" src/lib/strings/marketing.ts` returns `0`.
    - `grep -c "signInLink" src/lib/strings/marketing.ts` returns `0` (final-CTA link is not duplicated here).
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -iE 'custom domain|discount code|bulk import|staff account|delivery zone|low-stock|priority support|testimonial|trusted by|millions|thousands of' src/lib/strings/marketing.ts)" && test "$(grep -c '!' src/lib/strings/marketing.ts)" -eq 0 && grep -q "export const marketingCopy" src/lib/strings/marketing.ts && grep -q "root: { ...marketingCopy }" src/lib/strings/index.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <done>`src/lib/strings/marketing.ts` exists, exports `marketingCopy` with every D-04 band's copy sourced only from the Verified Feature Ledger, and `strings.root` resolves from it; the module compiles and lints clean and contains none of the banned unbuilt-feature phrases or voice-contract violations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the landing page and add the honesty contract test</name>
  <files>src/app/page.tsx, tests/unit/landing-page-contract.test.ts</files>
  <read_first>
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-UI-SPEC.md` in full — § Page Structure (bands 0-6, exact classes/gaps/widths), § Color (accent reserved-for list, § Honest-Constraint Treatment), § Typography (the one scoped H1 exception), § Dark Mode (none), § Responsive Behavior
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-RESEARCH.md` § Pattern 3 (hero image slot, option d), § Pattern 4 (`next/image` — `priority` deprecated), § Pattern 5 (`title: { absolute }`), § Pattern 6 (proxy — verify, don't change), § Pitfalls 1-9, § Security Domain, § Validation Architecture → Phase Requirements → Test Map
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-PATTERNS.md` — the `src/app/page.tsx` and `tests/unit/landing-page-contract.test.ts` sections in full, including the exact import list, metadata pattern, CTA button pattern, cross-link pattern, hero image slot pattern, and icon badge pattern
    - `src/app/page.tsx` (current, to be replaced) and `src/app/login/page.tsx` (logo import + metadata + cross-link idioms)
    - `tests/unit/dashboard-nav.test.ts` in full — `stripComments`, `QUOTED_PROSE`, `looksLikeProse`, and the "IT MUST NOT PASS VACUOUSLY" convention
    - `tests/unit/surface-token-isolation.test.ts` and `tests/unit/preview-photo-isolation.test.ts` in full — the ban-regex and banned-token idioms to compose from
  </read_first>
  <behavior>
    Write `tests/unit/landing-page-contract.test.ts` to this contract, observe it fail against the current placeholder `page.tsx`, then rewrite the page until it is green. There is no DOM in either Vitest project — every assertion is a source scan, matching the three analog tests.
    - Test 1 (anti-vacuity): `src/app/page.tsx` and `src/lib/strings/marketing.ts` both exist and are non-empty after comment-stripping.
    - Test 2 (band coverage): `page.tsx`'s comment-stripped source references every one of `strings.root.hero`, `strings.root.payments`, `strings.root.templates`, `strings.root.speed`, `strings.root.howItWorks`, `strings.root.finalCta` (a literal substring match on each dotted path is sufficient — do not require a specific JSX shape).
    - Test 3 (no inline prose): reuse `stripComments` + `QUOTED_PROSE` + `looksLikeProse` from `dashboard-nav.test.ts`, scoped to `src/app/page.tsx` only, exempting lines matching the same `NON_COPY_LINE` pattern (`import`/`className`/`href`/`aria-*`).
    - Test 4 (banned claims): scan `src/lib/strings/marketing.ts` for the phrase list `custom domain`, `discount code`, `bulk import`, `staff account`, `delivery zone`, `low-stock`, `priority support`, `testimonial`, `trusted by`, `millions`, `thousands of`, and a digit-followed-by-`merchant`/`business`/`store` regex, case-insensitive.
    - Test 5 (voice contract): scan `marketing.ts` for `!`, and case-insensitively for `oops`/`whoops`.
    - Test 6 (positive control): the banned-claims predicate fires on a synthetic fixture line containing `"custom domain"` — proves the matcher discriminates rather than passing vacuously.
    - Test 7 (negative control): the same predicate does not fire on a synthetic fixture line containing `"Mobile Money"`.
    Include the "IT MUST NOT PASS VACUOUSLY" header-comment convention naming this file's purpose and citing MKTG-01/D-01.
  </behavior>
  <action>
    Rewrite `src/app/page.tsx` as a Server Component implementing all seven bands from `05.2-UI-SPEC.md` § Page Structure, in this exact order: (0) top lockup, (1) hero, (2) payments differentiator, (3) templates differentiator, (4) speed differentiator, (5) how it works, (6) final CTA.

    Imports: `Link` from `next/link`; `Image` from `next/image`; `Wallet`, `LayoutGrid`, `Zap` and the `LucideIcon` type from `lucide-react`; `einortLogo` from `@/assets/brand/einort-logo.png`; `Button` from `@/components/ui/button`; `strings` from `@/lib/strings`; `TEMPLATE_PREVIEWS` from `@/server/theming/preview-manifest`; `publicUrlFor` and `templatePreviewPrefixFor` from `@/server/images/r2`; `type Metadata` from `next`.

    Metadata export: `title: { absolute: strings.root.metaTitle }`, `description: strings.root.metaDescription` — the `absolute` form is mandatory, a bare string would render "EINORT · EINORT" through the layout's `%s · EINORT` template.

    Hero image slot (module scope, above the component): declare `const HERO_TEMPLATE_KEY = "flagship-fashion";` with a comment citing D-02/D-05 and TMPL-01. Inside the component (or at module scope, since this is pure and side-effect-free), compute `const heroPreview = TEMPLATE_PREVIEWS[HERO_TEMPLATE_KEY];` and `const heroSrc = heroPreview ? publicUrlFor(\`${templatePreviewPrefixFor(HERO_TEMPLATE_KEY)}/card.webp\`) : null;`. Never call `templatePreviewPrefixFor` with anything but this literal constant.

    Band 0 (top lockup): non-sticky, inline in this file, `py-6` (exception — not a scale token), `max-w-5xl mx-auto flex items-center justify-between px-4 sm:px-8`. Left: `<Image src={einortLogo} alt="" className="h-8 w-auto" loading="eager" />` beside `<span className="text-sm font-semibold text-foreground">{strings.root.wordmark}</span>` — no `priority` (deprecated in Next 16). Right: `<Link href="/login" className="text-foreground underline underline-offset-3">{strings.root.signIn}</Link>` — plain text, not a button, never `text-primary`.

    Band 1 (hero), `bg-background py-16 px-4 sm:px-8`: branch on `heroSrc` BEFORE rendering any JSX (never `onError`). When `heroSrc` is `null`: a single `max-w-2xl mx-auto text-center` column with the H1 (`font-heading text-4xl sm:text-5xl lg:text-6xl leading-[1.1] font-semibold tracking-tight text-foreground`, text `strings.root.hero.heading`), the subheadline (`mt-4 text-base leading-normal text-muted-foreground`, text `strings.root.hero.body`), the CTA button (`Button` default variant, `render={<Link href="/signup" />}`, `className="mt-6 min-h-11 px-6 text-sm font-semibold"`, label `strings.root.cta`), and the CTA microcopy directly beneath (`mt-2 text-sm leading-normal text-muted-foreground`, text `strings.root.hero.ctaNote`). When `heroSrc` is non-null: the same text column (without the outer centering/max-width) sits in a `max-w-5xl mx-auto grid gap-8 lg:grid-cols-2 lg:gap-12 lg:items-center` grid, text column first in DOM order, and the second column renders `<Image src={heroSrc} alt="" width={1280} height={800} sizes="(min-width: 1024px) 480px, 100vw" loading="eager" fetchPriority="high" className="rounded-lg border border-border" />` — never `priority`.

    Bands 2-4 (differentiators): one repeated shape, three instances, in the payments → templates → speed order. Alternate `bg-muted/40 border-t border-border` (payments, speed) and `bg-background border-t border-border` (templates) per `05.2-UI-SPEC.md` § Page Structure's table. Each band: `py-16 px-4 sm:px-8`, inner `max-w-3xl mx-auto`. A `size-12 rounded-full bg-primary/10` icon badge (`Wallet`/`LayoutGrid`/`Zap`, `size-6 text-primary`), then the H2 (`mt-2 font-heading text-2xl leading-tight font-semibold tracking-tight text-foreground`, e.g. `strings.root.payments.heading`), then the body paragraph (`mt-4 text-base leading-normal text-muted-foreground`, e.g. `strings.root.payments.body`), then the honest-constraint line (`mt-3 text-base leading-normal text-foreground` — full ink, NOT muted, NOT bordered, NOT italic, NOT prefixed with "Note:", e.g. `strings.root.payments.constraint`). Either write the three bands out directly or factor a small, non-exported helper function defined inside this same file (not a new component file — Pattern 2 explicitly warns against a `src/components/marketing/` tree of single-use files) that takes `{ icon: LucideIcon; heading: string; body: string; constraint: string }` and returns the shared JSX, then call it three times with an inline array of the three differentiators' icon + `strings.root.*` values.

    Band 5 (how it works), `bg-background border-t border-border py-16 px-4 sm:px-8`, inner `max-w-5xl mx-auto`: H2 (`strings.root.howItWorks.heading`), then `grid grid-cols-1 gap-12 sm:grid-cols-3 sm:gap-8`, each of the three `strings.root.howItWorks.steps` entries rendered as a centered column with a `size-12 rounded-full bg-primary/10` badge containing the numeral (`1`/`2`/`3`) at `font-heading text-2xl leading-none font-semibold text-primary`, then the step title (`text-foreground` label role), then the step body (`text-muted-foreground`).

    Band 6 (final CTA), `bg-muted/40 border-t border-border py-16 px-4 sm:px-8`, inner `max-w-2xl mx-auto text-center`: H2 (`strings.root.finalCta.heading`), body (`strings.root.finalCta.body`), the identical CTA `Button` markup from the hero (same label `strings.root.cta`, same classes), then beneath it a plain text link rendering `strings.signup.loginLink` (imported via the whole `strings` object already in scope — do NOT add a duplicate string to `marketing.ts`), `text-foreground underline underline-offset-3`, `href="/login"`.

    Do not touch `src/proxy.ts` or `src/app/layout.tsx` — this phase changes exactly one route.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/landing-page-contract.test.ts` exits 0, including its positive and negative control fixtures.
    - `grep -c "title: { absolute:" src/app/page.tsx` returns `1`.
    - `grep -c "TEMPLATE_PREVIEWS\[" src/app/page.tsx` returns at least `1`.
    - `grep -c "flagship-fashion" src/app/page.tsx` returns at least `1`.
    - `grep -v '^\s*[*/]' src/app/page.tsx | grep -c "priority"` returns `0` (no bare `priority` prop anywhere in live code).
    - `grep -c 'fetchPriority="high"' src/app/page.tsx` returns `1`.
    - `grep -c 'variant="gold"' src/app/page.tsx` returns `0`.
    - `grep -c "brand-accent" src/app/page.tsx` returns `0`.
    - `grep -Ec "(zinc|slate|blue|amber|emerald|red|green|yellow|indigo|gray)-[0-9]{2,3}" src/app/page.tsx` returns `0`.
    - `grep -c 'data-surface="storefront"' src/app/page.tsx` returns `0`.
    - `grep -c "dark:" src/app/page.tsx` returns `0`.
    - `npx vitest run tests/unit/surface-token-isolation.test.ts` exits 0.
    - `npx vitest run tests/unit/dashboard-nav.test.ts` exits 0.
    - `npx vitest run tests/unit/preview-photo-isolation.test.ts` exits 0.
    - `npx vitest run tests/unit/proxy.test.ts` exits 0 (apex-only reachability unaffected — no proxy code was changed).
    - `npm run test:unit`, `npm run lint`, `npm run typecheck`, `npm run build` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/landing-page-contract.test.ts &amp;&amp; test "$(grep -v '^\s*[*/]' src/app/page.tsx | grep -c priority)" -eq 0 &amp;&amp; grep -q 'fetchPriority="high"' src/app/page.tsx &amp;&amp; test "$(grep -c 'variant=\"gold\"' src/app/page.tsx)" -eq 0 &amp;&amp; test "$(grep -c 'brand-accent' src/app/page.tsx)" -eq 0 &amp;&amp; test "$(grep -c 'data-surface=\"storefront\"' src/app/page.tsx)" -eq 0 &amp;&amp; npx vitest run tests/unit/surface-token-isolation.test.ts &amp;&amp; npx vitest run tests/unit/dashboard-nav.test.ts &amp;&amp; npx vitest run tests/unit/preview-photo-isolation.test.ts &amp;&amp; npx vitest run tests/unit/proxy.test.ts &amp;&amp; npm run test:unit &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>`src/app/page.tsx` renders all seven bands from real, verified-feature-ledger-sourced copy, the hero correctly branches on `TEMPLATE_PREVIEWS` state with no placeholder ever shipped, every one of the six existing build-failing guards plus the new `landing-page-contract.test.ts` passes, `npm run build` succeeds, and the apex-only reachability of `/` is confirmed unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-device visual review of the landing page</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/05.2-marketing-landing-page-redesign/05.2-UI-SPEC.md` § Honest-Constraint Treatment and § Checker Sign-Off — the acceptance reference for step 2 below, which is the one design detail the UI-SPEC checker flagged for a real-device pass rather than approving outright from static review.
  </read_first>
  <what-built>The full landing page redesign: top lockup, hero (copy-only today, manifest-conditional image slot for when 05.1-09 ships), three differentiator sections in payments → templates → speed order with honest-constraint lines, how-it-works, and final CTA — all automated tests, lints, typecheck and build passing.</what-built>
  <how-to-verify>
Run `npm run dev` and open `http://localhost:3001/` (or `npm run build && npm run start` then `http://localhost:3000/`), then:

1. Resize to a narrow mobile viewport (≈375px) first — this market is phone-first on 3G/4G Android. Confirm: the hero text and CTA button are reachable without scrolling past a heavy image (there is no image yet, so this should already be true); every section stacks in a single column; the "Create my store" button meets a visible 44px touch target; nothing overflows horizontally.
2. Confirm the honest-constraint line under each differentiator (the "No order is ever marked paid...", "Starter reaches 10 templates...", "No developer, no hosting..." lines) reads as confident, full-ink body text — NOT muted, NOT boxed, NOT italic, NOT prefixed with "Note:" or a warning icon.
3. Confirm zero gold anywhere on the page, and that the whole page reads as an extension of the same blue/slate merchant-platform look as `/login` and `/signup` — not a new visual language.
4. Widen to desktop (≥1024px). Confirm the hero collapses to the single centered column described in step 1 (the manifest is empty today, so there should be no broken image, no gray box, no layout shift) and the how-it-works section shows three columns side by side.
5. Click "Create my store" (goes to `/signup`) and "Sign in" in both the top lockup and the final CTA (goes to `/login`) to confirm the links resolve.

Report any step that does not behave as described, quoting the step number.
  </how-to-verify>
  <verify>
    <human-check>Steps 1-5 above, performed against `npm run dev` (or `next start`) in a browser at both a narrow mobile viewport and a desktop viewport.</human-check>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run test:unit</automated>
  </verify>
  <action>Run the four automated gates first and confirm all exit 0. Then start the app (`npm run dev`, or `npm run build && npm run start` for a production-mode check) and present the five-step walkthrough above to the developer. Do not perform any of the five steps on the developer's behalf and do not mark a step passed from a static code read — these steps exist specifically to check rendered appearance, colour hierarchy and viewport behaviour that no automated assertion in this plan covers. Wait for the resume signal, then transcribe the per-step result into `05.2-01-SUMMARY.md`. If any step fails, record the observed behaviour verbatim and stop rather than closing out the phase.</action>
  <resume-signal>Type "approved" or describe the failing step numbers and what you saw instead</resume-signal>
  <acceptance_criteria>
    - All four automated gates in `<verify><automated>` exit 0 immediately before the human walkthrough.
    - Every one of steps 1-5 is explicitly marked pass or fail in `05.2-01-SUMMARY.md`, with observed behaviour recorded for any failure.
    - No step is marked "not checked".
  </acceptance_criteria>
  <done>A human has viewed the live landing page at both a mobile and a desktop viewport and confirmed it matches the UI-SPEC contract, or has reported specific failing steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → apex `/` | Public, unauthenticated GET; zero input surface — no forms, no query-string-driven rendering, no mutation of any kind |
| `next/image` optimizer → Cloudflare R2 | Hero screenshot fetch, restricted to `next.config.ts`'s `R2_PUBLIC_BASE_URL`-derived `remotePatterns` allowlist |
| Marketing copy → prospective merchant's purchase decision | Content-integrity boundary: every claim must be mechanically traceable to a shipped feature (MKTG-01, D-01) — this is a trust boundary between what the platform says and what it does |
| Storefront subdomain → apex route tree | `src/proxy.ts`'s `classifyHost` must keep `/` unreachable from a merchant subdomain; this phase does not modify the proxy and must not weaken this boundary by accident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Information Disclosure | `next/image` hero `src` composition | mitigate | `heroSrc` is composed server-side only from the compile-time-constant `HERO_TEMPLATE_KEY` via `templatePreviewPrefixFor`, which throws on any non-`TemplateKey` input; no user- or request-derived value ever reaches the composed key or the `<Image src>` |
| T-05.2-02 | Tampering | Design-token isolation (incident `260823-gu4` precedent) | mitigate | Zero literal colour, zero Tailwind palette utility, zero `brand-accent`, zero `data-surface="storefront"` — enforced by `tests/unit/surface-token-isolation.test.ts` bans 1/2/4/6, re-run as part of Task 2's verification |
| T-05.2-03 | Tampering | Gold-accent budget (D-06) | mitigate | Zero `variant="gold"` anywhere on this page — enforced by `tests/unit/dashboard-nav.test.ts`'s exactly-two, sidebar-scoped gold budget |
| T-05.2-04 | Tampering | D-05 placeholder-photo containment | mitigate | Zero `preview-assets` / `PREVIEW_PLACEHOLDER_PHOTOS` reference anywhere under `src/` — enforced by `tests/unit/preview-photo-isolation.test.ts`, re-run unmodified in Task 2 |
| T-05.2-05 | Repudiation (content integrity) | Marketing copy vs. the actual shipped feature set (MKTG-01, D-01) | mitigate | Every claim in `marketing.ts` is traceable to `05.2-RESEARCH.md`'s Verified Feature Ledger; `landing-page-contract.test.ts`'s banned-claims list and voice-contract scan is the mechanical guard against a future edit quietly reintroducing an unbuilt-feature claim; no testimonial, no invented merchant count, no borrowed logo |
| T-05.2-06 | Spoofing / Denial of Service | Public unauthenticated POST surface | accept (avoided by design) | No form, no email capture, no mutation exists on this page (D-04) — there is no endpoint to rate-limit because there is no endpoint |
| T-05.2-07 | Elevation of Privilege | Apex-only reachability of `/` | accept (control unchanged, verified not rebuilt) | `src/proxy.ts`'s `classifyHost` keeps `/` unreachable from a storefront subdomain; `tests/unit/proxy.test.ts` is re-run in Task 2's verification with zero proxy code changes in this phase |
</threat_model>

<verification>
- `npx vitest run tests/unit/landing-page-contract.test.ts` exits 0, including positive/negative control fixtures
- `tests/unit/surface-token-isolation.test.ts`, `tests/unit/dashboard-nav.test.ts`, `tests/unit/preview-photo-isolation.test.ts`, `tests/unit/proxy.test.ts` all green, unmodified
- `npm run test:unit`, `npm run lint` (`--max-warnings=0`), `npm run typecheck`, `npm run build` all exit 0
- Zero literal colours, palette utilities, `brand-accent`, `variant="gold"`, `dark:` utilities, or `data-surface="storefront"` in `src/app/page.tsx`
- Human visual review (Task 3) approved on both mobile and desktop viewports
</verification>

<success_criteria>
The apex root page is a real, five-band-plus-lockup marketing landing page whose every claim is traceable to the Verified Feature Ledger, whose hero is complete and honest in both the empty and populated `TEMPLATE_PREVIEWS` states, which introduces zero new design tokens and zero gold, and which passes every existing build-failing guard plus the new honesty contract test — replacing the 3-element placeholder in full.
</success_criteria>

<output>
Create `.planning/phases/05.2-marketing-landing-page-redesign/05.2-01-SUMMARY.md` when done.
</output>
