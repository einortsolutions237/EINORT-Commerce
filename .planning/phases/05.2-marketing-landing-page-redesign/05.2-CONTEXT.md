# Phase 05.2: Marketing Landing Page Redesign - Context

**Gathered:** 2026-09-07
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace the current bare-bones root page (`src/app/page.tsx` — wordmark, one tagline, one CTA, explicitly documented in its own header comment as "deliberately NOT a marketing site — that is future scope with its own phase") with a real, professional public marketing/landing page for EINORT-Commerce, modeled on the quality bar of Shopify's own marketing site (shopify.com), triggered by the user supplying a screenshot of that exact page and asking for the same standard.

Explicitly NOT in scope: the `/signup` flow, the onboarding flow, or the dashboard — this phase is the root `/` page only, the merchant's very first impression before they ever click through to create a store.

</domain>

<decisions>
## Implementation Decisions

### Trust-signal strategy
- **D-01:** No fabricated or borrowed social proof. Lead with concrete, real differentiators instead of a "powering millions of businesses" claim EINORT cannot honestly make: real Cameroon-first payment methods (Mobile Money, Orange Money, Cash on Delivery), a real browsable template gallery, and a genuine minutes-not-months setup story. No invented merchant counts, no fake logos, no borrowed testimonials.

### Visual asset strategy
- **D-02:** The hero visual is a real screenshot of an actual EINORT storefront template — not a custom illustration or generic stock graphic. **Sequencing dependency:** this depends on Phase 05.1 (Template Preview Rendering & Picker Redesign) actually landing its real-screenshot generation pipeline (currently Wave 1 of 5 complete as of this writing).
- **D-05 (resolves D-02's open mechanism question, 2026-09-07):** Research found D-02's original two fallback options are both dead: `tests/unit/preview-photo-isolation.test.ts` (shipped in Phase 05.1's Wave 1, after this CONTEXT.md was written) fails the build if the placeholder-photo module or its files are referenced anywhere under `src/`, and there is no `public/` directory in this project to serve a static temporary image from either way. **Resolution: a copy-led hero with a manifest-fed image slot.** The hero ships as pure copy/layout now, with an image slot that reads directly from `TEMPLATE_PREVIEWS` (`src/server/theming/preview-manifest.ts`) — when the manifest is empty (today), the slot renders nothing extra; the moment Phase 05.1's real generation run (05.1-09) populates it, the hero picks up a real screenshot automatically. No temporary/fake image ever ships, no tracked "swap it later" follow-up needed, and the page is finished-looking at every point in time.

### Language for the page
- **D-03:** English, matching the rest of the product. The entire product (dashboard, onboarding, storefront) is English-only today (confirmed: no French strings, no locale/language infrastructure anywhere in the codebase) — a French or bilingual landing page would be the one inconsistent surface, and real localization is out of this phase's scope.

### Page structure & length
- **D-04:** A focused, single-scroll page: hero, 3 key differentiator sections (payment methods / template quality / speed-to-launch), a simple "how it works" (pick industry → customize → publish), and one final CTA. Not a full Shopify-length page — sections like testimonials or broad pricing comparisons don't have honest content to fill yet and are deliberately excluded, consistent with D-01.

### Color palette on this page
- **D-06 (2026-09-07):** No gold on this marketing page. `tests/unit/dashboard-nav.test.ts` asserts `variant="gold"` is used exactly twice codebase-wide (the existing plan-tier badges); rather than risk that contract test or dilute gold's meaning onto a new surface, this page stays blue/slate only.

### Claude's Discretion
- Exact copywriting (headlines, body copy, CTA wording) — informed by deep research into what makes SaaS/e-commerce-builder landing pages convert, and by the project's existing brand voice (see canonical refs below).
- Exact visual layout, spacing, and component choices within the existing merchant-platform blue/slate design system (see canonical refs) — this page should extend that system, not invent a new one, since it is the same brand a merchant will see moments later in onboarding.
- Whether/how to show the 3 differentiator sections visually (icons, small illustrations, screenshots) — left to the UI-SPEC step.
- Exact "how it works" step count and framing.
- Whether to include a minimal top lockup with a "Sign in" link for returning merchants (research raised this; D-04 doesn't preclude it and `/login` already exists) — left to the UI-SPEC step.
- Scope of OG image / robots / sitemap metadata for this page: explicitly OUT of scope for this phase (not silently omitted — a future SEO/metadata pass can add these).

</decisions>

<specifics>
## Specific Ideas

The user supplied a screenshot of shopify.com's actual marketing page ("Start an online store for free" hero, email-capture input + "Start free trial" button, "Powering millions of businesses worldwide" + real client logos strip below the fold) as the explicit visual/structural quality target — not to be copied element-for-element (per D-01, the trust-bar/logo-strip portion specifically cannot be honestly replicated), but as the bar for hero clarity, typographic confidence, and overall polish.

The user's own words on intent: "generate write ups and visual that will better represent to Users/client/Merchant to what this platform is all about... display the best visuals, writeups, information and data that will infuse users in choosing this platform" — i.e., this page's job is to make a prospective Cameroonian merchant understand what EINORT does and want to sign up, using real, deep research into effective landing-page copywriting and structure, not a light copy pass.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Design reference (this page must stay inside this system)
- `.planning/design-references/EINORT-COMMERCE-FRONT-END-MERCHANT-PLATFORM.md` — the locked blue/gold/slate token system, `0.75rem` radius, Plus Jakarta Sans + Outfit. This page is the merchant's first-ever contact with the brand, immediately followed by onboarding in the same system — it must extend this reference, not diverge from it.

### Project context (what this page must accurately represent)
- `.planning/PROJECT.md` — the product's actual core value proposition, constraints (no live payment gateway, manual Mobile Money/Orange Money/COD/WhatsApp), and target market (Cameroon/Douala-first).
- `.planning/REQUIREMENTS.md` — the real, shipped feature set this page's copy must not overstate or misrepresent.
- `.planning/phases/05-template-segment-expansion/05-CONTEXT.md`, `05-RESEARCH.md` — the 50-template system (10 Starter/15 Business/25 Professional, 6 segments) this page's "template quality" differentiator section should accurately describe.
- `.planning/phases/05.1-template-preview-rendering-picker-redesign/05.1-CONTEXT.md` — the real-screenshot pipeline this page's hero visual depends on (see D-02's sequencing note).

### Existing code this phase touches
- `src/app/page.tsx` — the current placeholder page, to be replaced.
- `src/lib/strings/index.ts` — the `root` namespace (`strings.root.wordmark`, `.tagline`, `.cta`) currently backing the placeholder; this phase will grow this namespace substantially.

</canonical_refs>

<deferred>
## Deferred Ideas

- **Full bilingual (EN/FR) support across the whole product** — raised during discussion as an alternative to D-03, but explicitly deferred: this phase stays English-only to match the rest of the product; real localization is a separate, larger future initiative if EINORT pursues the Francophone Cameroon market more deliberately.
- **Testimonials, broad pricing comparison, FAQ sections** — excluded from this phase's page structure (D-04) since EINORT has no real customer testimonials yet and detailed pricing lives on its own onboarding/plan screens already; may be added in a future revision once real content exists.

### Reviewed Todos (not folded)
None — no pending todos matched this phase.

</deferred>

---

*Phase: 05.2-marketing-landing-page-redesign*
*Context gathered: 2026-09-07*
