---
phase: 05-template-segment-expansion
plan: 13
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/electronics.ts
  - src/server/theming/templates/electronics.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 9 electronics templates have real, industry-appropriate, Douala-appropriate copy in the project's voice."
    - "Every electronics default document parses against pageDocumentSchema and matches its registry row's sections and order."
    - "Sibling templates sharing a skeleton do not share an accent, so no two read as the same shop recoloured."
  artifacts:
    - path: "src/lib/strings/templates/electronics.ts"
      provides: "Display names, segment tags and ~215 copy strings for the segment's 9 templates"
    - path: "src/server/theming/templates/electronics.ts"
      provides: "9 document builders and 9 token builders"
  key_links:
    - from: "src/server/theming/templates/electronics.ts"
      to: "src/lib/strings/templates/electronics.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the electronics segment: 9 templates drawn from 4 skeletons, split 2 Starter / 3 Business / 4 Professional, with real copy and real accent pairs.

Purpose: TMPL-03 requires at least three additional segments get their own structurally distinct layout, and D-10 extends that to all six because the picker is segment-sorted — a merchant who selects electronics at onboarding must find templates designed for electronics, not a fashion boutique with a different colour.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the electronics copy namespace</name>
  <files>src/lib/strings/templates/electronics.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding 50-row allocation table. Read the 9 rows whose `segment` is `electronics`: their keys, their `minTier`, and their skeletons. A template with no `editorial-split` section needs no `editorialSplit` copy.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the exact nesting and comment register to mirror, including the note that trust-bar icon names are never copy.
    - src/server/theming/schema.ts lines 82-138 — the caps: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/PROJECT.md — the Cameroon/Douala-first market context.
  </read_first>
  <action>
Fill `src/lib/strings/templates/electronics.ts` with a complete copy set for each of this segment's 9 template keys, keyed exactly as the registry names them.

Per template, provide `name`, `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules:

- **All 9 hero voices must be distinct** from each other and from every other segment's. The metric's signature is `hero.eyebrow + hero.heading` and it asserts uniqueness across all 50.
- Write for a Douala electronics business — phones and accessories, computing, home appliances, repair-and-resale. Electronics buyers care about warranty, authenticity, delivery and after-sale support; the trust-bar copy is where that lands, and it is what makes this segment read differently from fashion. Concrete over aspirational.
- `ctaHref` and `viewAllHref` point at `/`. Never the internal `/s/{slug}` rewrite-target prefix — `tests/unit/storefront-link-prefix.test.ts` fails the build on one.
- Trust-bar items carry heading and body copy only; the icon is a schema enum chosen in Task 2.
- Every string inside its cap.
- The two `minTier: "starter"` templates get the strongest copy, not the leftovers — Starter is the tier most merchants land on.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports exactly 9 template keys, each matching a `TEMPLATES` key whose `segment` is `electronics`.
    - Every template's copy groups correspond exactly to the section types its registry row declares.
    - All `hero.eyebrow + hero.heading` pairs are distinct within the module.
    - A tsx script confirms every string is within its schema cap.
    - `grep -c '/s/' src/lib/strings/templates/electronics.ts` returns 0.
    - No exclamation mark and no emoji in any string.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Nine complete, distinct, cap-respecting copy sets exist for electronics, written for the segment's actual buying concerns.</done>
</task>

<task type="auto">
  <name>Task 2: Author the electronics document and token builders</name>
  <files>src/server/theming/templates/electronics.ts</files>
  <read_first>
    - src/server/theming/templates/electronics.ts — the module contract plan 05-08 created: the 9 builder pairs, their names, and their section lists.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 9 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — the literal shape to clone, the `id === type` rule, the null-image rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, and the two-accents-only token shape.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`; foregrounds and the focus ring are derived, so no contrast library is needed.
    - src/lib/strings/templates/electronics.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 9 document builders and 9 token builders in `src/server/theming/templates/electronics.ts`, following the same rules every segment module obeys:

Each `*Document()` returns a fresh literal on every call; its `sections` array matches its registry row's declared types and order exactly; each section's `id` is its own `type` string; every string is a member expression into `strings.templates[<key>]` with zero inline prose; hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`; editorial-split sections set `imageKey: null`; product-grid sections set `itemCount: DEFAULT_ITEM_COUNT` unless the variant argues otherwise (a `showcase` grid renders fewer, larger tiles — document any deviation in a comment); trust-bar sections carry exactly three blocks each with an `icon` from the schema's closed enum.

Each `*Tokens()` returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`, with the two copy fields read from `strings.templates[<key>]`. **Two templates sharing a skeleton must not share a `primaryAccent`** — the metric asserts no two templates share both structure and accent, and siblings are exactly where that collides. Accents are hex values satisfying `hexColorSchema`; do not store derived foregrounds.

Never hoist a builder's literal to module scope and never freeze one.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 9 keys, `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, the document's section types equal the registry row's section types in order.
    - For each key, every section satisfies `id === type` and ids are unique within the document.
    - For each key, two successive `templateDefaultDocument(key)` calls return different object references, and their `sections` arrays are also distinct references.
    - No non-null image key appears anywhere in the file.
    - No two templates sharing a skeleton share a `primaryAccent` — verify with a tsx script grouping by skeleton string.
    - Every settings string is a `strings.templates` member expression; no prose literal appears in the file.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Nine electronics templates build fresh, schema-valid, image-free default documents matching their registry rows, with distinct accents across siblings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| copy strings → schema caps | an over-length string makes a document unparseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-50 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Per-string cap check here; plan 05-20's generalized parse test is the build-level gate |
| T-05-51 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; generalized mutation test in plan 05-20 |
| T-05-52 | Tampering | a template default referencing an image outside the tenant prefix | mitigate | All image keys `null`; `storageKeySchema` untouched |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 9 documents parse, match their rows, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 9 complete copy sets and 9 builder pairs exist for electronics, matching the registry rows exactly.
- All hero voices in the segment are distinct; sibling templates do not share an accent.
- Trust-bar copy reflects the segment's real buying concerns rather than generic reassurance.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-13-SUMMARY.md` when done.
</output>
