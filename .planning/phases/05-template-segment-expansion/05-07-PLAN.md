---
phase: 05-template-segment-expansion
plan: 07
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/s/[slug]/sections/product-grid-section.tsx
  - src/app/s/[slug]/sections/product-grid-dense.tsx
  - src/app/s/[slug]/sections/product-grid-showcase.tsx
  - src/app/s/[slug]/sections/editorial-split-section.tsx
  - src/app/s/[slug]/sections/editorial-split-banner.tsx
autonomous: true
requirements: [TMPL-03]

must_haves:
  truths:
    - "A product grid can render at three genuinely different densities from the same settings and the same catalog data."
    - "An editorial section can render either as an image-beside-text split or as a full-width ink band with no image slot at all."
    - "The editorial CTA is never the merchant's accent, in any variant, because ink-on-ink collides."
  artifacts:
    - path: "src/app/s/[slug]/sections/product-grid-section.tsx"
      provides: "Three-arm variant switch dispatching to grid / dense / showcase"
      contains: "SectionVariant<\"product-grid\">"
    - path: "src/app/s/[slug]/sections/product-grid-dense.tsx"
      provides: "The dense grid: one column step denser at every breakpoint, square tiles, inline price"
    - path: "src/app/s/[slug]/sections/product-grid-showcase.tsx"
      provides: "The showcase grid: one column step wider, generous gaps, price below a rule"
    - path: "src/app/s/[slug]/sections/editorial-split-banner.tsx"
      provides: "The banner editorial: full-width ink band, no image column, ever"
  key_links:
    - from: "src/app/s/[slug]/sections/product-grid-section.tsx"
      to: "src/server/theming/schema.ts"
      via: "SectionVariant<\"product-grid\"> prop type"
      pattern: "SectionVariant"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Add the three remaining rendering variants — `product-grid:dense`, `product-grid:showcase`, and `editorial-split:banner` — completing the twelve-component variant set D-02 requires.

Purpose: the product grid is one of the two mandatory sections in every skeleton, so its three variants carry more of the distinctiveness load than any other section type. `editorial-split:banner` is the second of the two variants `05-RESEARCH.md` Pitfall 4 requires to have no image slot at all, so that some templates cannot fail the empty-band-on-day-one trap by construction.
Output: five files — two switches, three new variant components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-UI-SPEC.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split product-grid into a three-arm switch and build the dense variant</name>
  <files>src/app/s/[slug]/sections/product-grid-section.tsx, src/app/s/[slug]/sections/product-grid-dense.tsx</files>
  <read_first>
    - src/app/s/[slug]/sections/product-grid-section.tsx — read the whole file: its container classes, its header row, its category-chip rendering including the selected-chip accent (accent reserved-for item 4), its "View all" link (item 3), its tile markup with `aspect-[4/5]`, its out-of-stock treatment, its no-image tile treatment, its `itemCount` handling, and its `<Reveal>` plus stagger index cap.
    - src/app/s/[slug]/sections/render-data.ts — the product shape the grid consumes.
    - src/server/theming/schema.ts — the `product-grid` settings shape (`heading`, `viewAllLabel`, `viewAllHref`, `itemCount`), `SECTION_VARIANTS["product-grid"]`, `SectionVariant<"product-grid">`.
    - .planning/phases/05-template-segment-expansion/05-UI-SPEC.md § `product-grid` — variant `dense` (the full property table) and the note at § Spacing Scale that `gap-2` is a genuine scale token, not a one-off constant.
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § Color (accent reserved-for items 3 and 4), § Typography.
  </read_first>
  <action>
In `src/app/s/[slug]/sections/product-grid-section.tsx`:

- Move the current body verbatim into a local `ProductGridGrid` (the flagship's design; keep its name descriptive but do not alter its markup).
- Export a new `ProductGridSection({ settings, data, variant })` returning `: ReactElement`, switching on `variant` with exactly three arms (`"grid"`, `"dense"`, `"showcase"`) and no `default` arm.

Create `src/app/s/[slug]/sections/product-grid-dense.tsx` exporting `ProductGridDense`, marker-free, implementing exactly the 05-UI-SPEC contract:

- Container identical to `grid`'s: `mx-auto max-w-7xl px-6 py-16 md:px-8 md:py-24`. Header row and category chips unchanged, including the accent-filled selected chip.
- Grid: `grid grid-cols-2 gap-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5` — one column step denser than `grid` at every breakpoint, with the gap held at `gap-2` (8px) at every breakpoint. That tighter gap versus `grid`'s 16/24px is the density signal; do not widen it at `md`.
- Tile image: `aspect-square`, not `aspect-[4/5]`. The tighter aspect is part of the density signal and is the one place the site-wide product aspect is deliberately overridden.
- Tile copy: name and price on ONE line — `mt-2 flex items-baseline justify-between gap-2`, name `line-clamp-1` Body 16/400 (single line, not `grid`'s two), price Body 16/600 `tabular-nums`.
- Out-of-stock, no-image tile, and `itemCount` handling: unchanged from `grid`'s contract — reuse the same treatments.
- Motion: `<Reveal>` on the header; tiles stagger at `--motion-stagger` with the index capped at 9.
- Price formatting stays `Intl.NumberFormat("fr-CM", { style: "currency", currency: "XAF" })`, exactly as `grid` does. No currency library.
- Accent appears exactly twice: the "View all" link and the selected category chip. Not on a tile, not on a border.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/surface-token-isolation.test.ts tests/unit/theming-marker-boundary.test.ts tests/unit/storefront-link-prefix.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `ProductGridSection` accepts `variant: SectionVariant<"product-grid">`, returns `: ReactElement`, and has no `default` arm.
    - Deleting the `"showcase"` arm produces a typecheck error (confirm once, then revert).
    - `grep -c 'lg:grid-cols-5' src/app/s/[slug]/sections/product-grid-dense.tsx` returns 1; `grep -c 'aspect-square'` returns 1; `grep -c 'aspect-\[4/5\]'` returns 0.
    - `grep -c 'gap-2' src/app/s/[slug]/sections/product-grid-dense.tsx` returns at least 1 and there is no `md:gap-` or `lg:gap-` override on the grid container.
    - `grep -c 'line-clamp-1' src/app/s/[slug]/sections/product-grid-dense.tsx` returns 1.
    - `grep -c 'fr-CM' src/app/s/[slug]/sections/product-grid-dense.tsx` returns 1 (or the shared formatter helper is imported — confirm which `grid` uses and match it).
    - `tests/unit/storefront-link-prefix.test.ts` passes — no `/s/{slug}` prefix leaks into an internal link.
    - `npm run lint`, `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Product grid dispatches on three variants and `dense` is one column step tighter at every breakpoint with square tiles and an inline price line.</done>
</task>

<task type="auto">
  <name>Task 2: Build the showcase grid variant</name>
  <files>src/app/s/[slug]/sections/product-grid-showcase.tsx</files>
  <read_first>
    - src/app/s/[slug]/sections/product-grid-section.tsx — `ProductGridGrid`'s container, header, chips, tile markup, out-of-stock and no-image treatments, and its currency formatting.
    - src/app/s/[slug]/sections/hero-stack.tsx (from plan 05-05) — its `h-px w-16 bg-border` divider, whose language `showcase`'s rule echoes.
    - .planning/phases/05-template-segment-expansion/05-UI-SPEC.md § `product-grid` — variant `showcase`, § Spacing Scale exceptions (the `gap-8` / `md:gap-12` entry and the note that product tile aspect stays `aspect-[4/5]` site-wide), § Decisions Made Under "Claude's Discretion" (the stagger cap of 3 and why).
  </read_first>
  <action>
Create `src/app/s/[slug]/sections/product-grid-showcase.tsx` exporting `ProductGridShowcase`, marker-free, implementing exactly the 05-UI-SPEC contract:

- Container identical to `grid`'s. Header row and category chips unchanged.
- Grid: `grid grid-cols-1 gap-8 sm:grid-cols-2 md:gap-12` — one column step wider than `grid` at every breakpoint, with generous gaps. `gap-8` and `gap-12` are the existing `xl` and `2xl` scale tokens reused, not new values.
- Tile image: `aspect-[4/5]`, unchanged. `showcase` does not get a new aspect ratio, only more surrounding space.
- Tile copy: `mt-4` name Body 16/400 with the same two-line clamp `grid` uses → `mt-2 h-px w-8 bg-border` (a small rule, deliberately echoing `hero:stack`'s divider so the quiet editorial language is consistent across variants that use it) → `mt-2` price Body 16/600 `tabular-nums`, **below the rule, not beside the name**.
- Out-of-stock, no-image tile, and `itemCount` handling: unchanged from `grid`'s contract.
- Motion: `<Reveal>` on the header; tiles stagger at `--motion-stagger` with the index capped at 3 — proportional to how many tiles fit per row before scrolling.
- Currency formatting and accent budget identical to `dense`: accent only on the "View all" link and the selected chip.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/surface-token-isolation.test.ts tests/unit/theming-marker-boundary.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sm:grid-cols-2' src/app/s/[slug]/sections/product-grid-showcase.tsx` returns 1 and `grep -c 'grid-cols-1'` returns 1; there is no `md:grid-cols-` or `lg:grid-cols-` on the grid container.
    - `grep -c 'gap-8' src/app/s/[slug]/sections/product-grid-showcase.tsx` returns 1 and `grep -c 'md:gap-12'` returns 1.
    - `grep -c 'aspect-\[4/5\]' src/app/s/[slug]/sections/product-grid-showcase.tsx` returns 1.
    - `grep -c 'h-px w-8 bg-border' src/app/s/[slug]/sections/product-grid-showcase.tsx` returns 1, and the price element appears after it in JSX order.
    - The stagger index cap is 3 (`grep -c 'Math.min' src/app/s/[slug]/sections/product-grid-showcase.tsx` returns at least 1 and the bound is 3 — match whatever cap idiom `grid` already uses).
    - `npm run lint`, `npm run typecheck` exit 0; both named tests pass.
  </acceptance_criteria>
  <done>`ProductGridShowcase` renders fewer, larger tiles with generous gaps, the site-wide product aspect, and a rule between name and price.</done>
</task>

<task type="auto">
  <name>Task 3: Split editorial-split into a two-arm switch and build the banner variant</name>
  <files>src/app/s/[slug]/sections/editorial-split-section.tsx, src/app/s/[slug]/sections/editorial-split-banner.tsx</files>
  <read_first>
    - src/app/s/[slug]/sections/editorial-split-section.tsx — read the whole file: its ink inversion (`bg-foreground text-background`), its two-column grid, its image column and the one-column collapse when `imageKey` is null, its heading size step (32→40px/600/1.1), its CTA which is `bg-background text-foreground` and NOT the accent, and its `<Reveal>` usage.
    - src/server/theming/schema.ts line 130 area — `editorial-split`'s `imageKey: storageKeySchema.nullable()`.
    - .planning/phases/05-template-segment-expansion/05-UI-SPEC.md § `editorial-split` — variant `banner`, § Typography (the note that `banner` reuses `split`'s existing size step and does not get its own), § Color (the "Accent is NOT for … the `editorial-split` CTA on the ink band (any variant, including `banner`)" rule), § Image Policy.
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md — the locked ink-on-ink collision rule.
  </read_first>
  <action>
In `src/app/s/[slug]/sections/editorial-split-section.tsx`:

- Move the current body verbatim into a local `EditorialSplitSplit` (keep the name descriptive; do not alter its markup or its null-image collapse).
- Export a new `EditorialSplitSection({ settings, data, variant })` returning `: ReactElement`, switching on `variant` with exactly two arms (`"split"`, `"banner"`) and no `default` arm.

Create `src/app/s/[slug]/sections/editorial-split-banner.tsx` exporting `EditorialSplitBanner`, marker-free, implementing exactly the 05-UI-SPEC contract:

- Band: `bg-foreground text-background`, full-bleed — the same ink inversion `split` uses. **No grid, no image column at all, ever**, regardless of whether `settings.imageKey` is set. This is the second of the two no-image-slot variants Pitfall 4 requires.
- Inner: `mx-auto max-w-3xl px-6 py-16 text-center md:px-8 md:py-24`, reusing the existing three-container band-padding exception unchanged.
- Content: eyebrow (Label uppercase `text-background/70`) → `mt-4` heading (32→40px/600/1.1, `tracking-tight`, the same step `split` uses — not a new size step) → `mt-6` body (`text-background/80 max-w-prose mx-auto`) → `mt-8` CTA.
- CTA: `bg-background text-foreground hover:bg-background/90`, pill, `min-h-12 px-8`. **Not the accent** — identical locked rule to `split`'s CTA, because an accent fill on an ink band is the ink-on-ink collision 04-UI-SPEC forbids.
- Motion: `<Reveal>` on the band; content elements stagger at `--motion-stagger`.

Name in the file header that this variant ignores `imageKey`, and cross-reference 05-UI-SPEC § Decisions Made Under "Claude's Discretion": the editor still renders the image field for the section TYPE, and no variant-conditional field hiding happens this phase.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `EditorialSplitSection` accepts `variant: SectionVariant<"editorial-split">`, returns `: ReactElement`, and has no `default` arm.
    - Deleting the `"banner"` arm produces a typecheck error (confirm once, then revert).
    - `grep -v '^\s*\*\|^\s*//' src/app/s/[slug]/sections/editorial-split-banner.tsx | grep -c 'imageKey'` returns 0.
    - `grep -c 'next/image' src/app/s/[slug]/sections/editorial-split-banner.tsx` returns 0.
    - `grep -c 'brand-accent' src/app/s/[slug]/sections/editorial-split-banner.tsx` returns 0 — the CTA is `bg-background text-foreground`.
    - `grep -c 'max-w-3xl' src/app/s/[slug]/sections/editorial-split-banner.tsx` returns 1 and `grep -c 'grid-cols'` returns 0.
    - `npm run test:unit` passes in full, including `surface-token-isolation` and `theming-marker-boundary`.
    - `npm run lint`, `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Editorial split dispatches on two variants and `banner` is a centred full-width ink band with no image slot and a background-filled CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog rows → grid tiles on a public page | product names, prices and images render unauthenticated |
| merchant-authored editorial copy → ink band markup | copy reaches the DOM under an inverted palette |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | client-influenced price display in a new grid variant | mitigate | Both new variants consume the same server-resolved `StorefrontRenderData` `grid` does; no price is recomputed or re-read client-side, and formatting stays `Intl.NumberFormat("fr-CM", …)` |
| T-05-25 | Denial of Service | a fourth product-grid variant rendering nothing | mitigate | Exhaustive three-arm `switch`, `: ReactElement`, no `default` |
| T-05-26 | Tampering | `banner` rendering an unvalidated image key | mitigate | `banner` has no image path at all; `next/image` is not imported |
| T-05-27 | Information Disclosure | `server-only` reachable from a section component | mitigate | No registry/defaults imports; `tests/unit/theming-marker-boundary.test.ts` enforces it |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint` and `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- The `grid` and `split` variants are byte-identical moves of the Phase 4 bodies.
</verification>

<success_criteria>
- The full twelve-component variant set exists: hero ×3, trust-bar ×2, product-grid ×3, editorial-split ×2, contact ×2.
- `product-grid:dense` and `:showcase` differ from `grid` by a column step and a gap at every breakpoint.
- `editorial-split:banner` has no image slot, no `next/image` import, and a non-accent CTA.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-07-SUMMARY.md` when done.
</output>
