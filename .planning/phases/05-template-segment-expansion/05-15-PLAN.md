---
phase: 05-template-segment-expansion
plan: 15
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/grocery-food.ts
  - src/server/theming/templates/grocery-food.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 8 grocery-food templates have real, industry-appropriate, Douala-appropriate copy in the project's voice."
    - "The segment's single Starter template is strong enough to stand alone, because it is the only one a Starter merchant in this segment can pick."
    - "Every grocery-food default document parses against pageDocumentSchema and matches its registry row's sections and order."
  artifacts:
    - path: "src/lib/strings/templates/grocery-food.ts"
      provides: "Display names, segment tags and ~190 copy strings for the segment's 8 templates"
    - path: "src/server/theming/templates/grocery-food.ts"
      provides: "8 document builders and 8 token builders"
  key_links:
    - from: "src/server/theming/templates/grocery-food.ts"
      to: "src/lib/strings/templates/grocery-food.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the grocery-food segment: 8 templates drawn from 4 skeletons, split 1 Starter / 3 Business / 4 Professional, with real copy and real accent pairs.

Purpose: with 10 Starter slots across 6 segments the split cannot be two everywhere, and this segment is one of the two that gets a single Starter template. That one template is the entire Starter experience for a Douala grocery or food business, so it carries more weight than any other row in this plan — a weak one produces exactly the segment-shaped dead end D-10 and the per-segment Starter-coverage assertion exist to prevent.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the grocery-food copy namespace</name>
  <files>src/lib/strings/templates/grocery-food.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding allocation table. Read the 8 rows whose `segment` is `grocery-food`, noting which single row carries `minTier: "starter"`.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the nesting and comment register to mirror.
    - src/server/theming/schema.ts lines 82-138 — the caps: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/PROJECT.md — the Cameroon/Douala-first market context, and the manual Mobile Money / cash-on-delivery payment reality that shapes what a food seller can honestly promise.
  </read_first>
  <action>
Fill `src/lib/strings/templates/grocery-food.ts` with a complete copy set for each of this segment's 8 template keys, keyed exactly as the registry names them.

Per template, provide `name`, `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules:

- **All 8 hero voices must be distinct** from each other and from every other segment's.
- Write for a Douala grocery or food business — provisions, fresh produce, prepared food, drinks, bulk staples. Buyers in this category care about freshness, same-day or next-day delivery windows, quantity and price clarity; that is the trust-bar's job and it is what makes this segment read differently from beauty or furniture.
- **The one Starter template must be the strongest in the segment.** It is the whole Starter experience for grocery merchants. Write it first and give it the best copy, then work outward.
- Do not promise delivery speed or payment behaviour the platform cannot support — V1 has no live payment gateway; payment is manual Mobile Money / Orange Money transfer, WhatsApp order, or cash on delivery.
- `ctaHref` and `viewAllHref` point at `/`. Never the internal `/s/{slug}` rewrite-target prefix.
- Trust-bar items carry heading and body copy only; the icon is a schema enum chosen in Task 2.
- Every string inside its cap.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports exactly 8 template keys, each matching a `TEMPLATES` key whose `segment` is `grocery-food`.
    - Every template's copy groups correspond exactly to the section types its registry row declares.
    - All `hero.eyebrow + hero.heading` pairs are distinct within the module.
    - A tsx script confirms every string is within its schema cap.
    - `grep -c '/s/' src/lib/strings/templates/grocery-food.ts` returns 0.
    - No exclamation mark and no emoji in any string.
    - The Starter row's copy is complete for every section its skeleton declares — no group omitted.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Eight complete, distinct, cap-respecting copy sets exist for grocery-food, with the single Starter template authored as the segment's strongest.</done>
</task>

<task type="auto">
  <name>Task 2: Author the grocery-food document and token builders</name>
  <files>src/server/theming/templates/grocery-food.ts</files>
  <read_first>
    - src/server/theming/templates/grocery-food.ts — the module contract plan 05-08 created: the 8 builder pairs, their names, and their section lists.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 8 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — the literal shape to clone, the `id === type` rule, the null-image rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, the token shape.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`.
    - src/lib/strings/templates/grocery-food.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 8 document builders and 8 token builders in `src/server/theming/templates/grocery-food.ts`, following the rules every segment module obeys:

Each `*Document()` returns a fresh literal on every call; its `sections` array matches its registry row's declared types and order exactly; each section's `id` is its own `type` string; every string is a member expression into `strings.templates[<key>]` with zero inline prose; hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`; editorial-split sections set `imageKey: null`; product-grid sections set `itemCount: DEFAULT_ITEM_COUNT` unless the variant argues otherwise — a grocery catalogue is typically wider than a fashion one, so a `dense` grid may justify a higher count; document any deviation in a comment; trust-bar sections carry exactly three blocks each with an `icon` from the schema's closed enum.

Each `*Tokens()` returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`. **Two templates sharing a skeleton must not share a `primaryAccent`.** Accents are hex values satisfying `hexColorSchema`; foregrounds and the focus ring are derived, so do not store them and do not add a contrast library.

Never hoist a builder's literal to module scope and never freeze one.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 keys, `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, the document's section types equal the registry row's section types in order.
    - For each key, every section satisfies `id === type` and ids are unique within the document.
    - For each key, two successive `templateDefaultDocument(key)` calls return different object references, and their `sections` arrays are also distinct references.
    - No non-null image key appears anywhere in the file.
    - No two templates sharing a skeleton share a `primaryAccent`.
    - Every settings string is a `strings.templates` member expression; no prose literal appears in the file.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Eight grocery-food templates build fresh, schema-valid, image-free default documents matching their registry rows, with distinct accents across siblings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| template copy → merchant promises to shoppers | copy that promises delivery or payment behaviour the platform cannot support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-56 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Per-string cap check here; plan 05-20's generalized parse test is the build-level gate |
| T-05-57 | Repudiation | default copy promising a payment or delivery capability V1 lacks | mitigate | Copy rules forbid promising gateway payment or guaranteed delivery windows; V1 is manual Mobile Money, WhatsApp order, or cash on delivery |
| T-05-58 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; generalized mutation test in plan 05-20 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 8 documents parse, match their rows, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 8 complete copy sets and 8 builder pairs exist for grocery-food, matching the registry rows exactly.
- The single Starter template is complete and is the segment's strongest.
- All hero voices in the segment are distinct; sibling templates do not share an accent.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-15-SUMMARY.md` when done.
</output>
