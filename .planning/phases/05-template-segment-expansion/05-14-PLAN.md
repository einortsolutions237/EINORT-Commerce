---
phase: 05-template-segment-expansion
plan: 14
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/beauty-cosmetics.ts
  - src/server/theming/templates/beauty-cosmetics.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 8 beauty-cosmetics templates have real, industry-appropriate, Douala-appropriate copy in the project's voice."
    - "Every beauty-cosmetics default document parses against pageDocumentSchema and matches its registry row's sections and order."
    - "Sibling templates sharing a skeleton do not share an accent."
  artifacts:
    - path: "src/lib/strings/templates/beauty-cosmetics.ts"
      provides: "Display names, segment tags and ~190 copy strings for the segment's 8 templates"
    - path: "src/server/theming/templates/beauty-cosmetics.ts"
      provides: "8 document builders and 8 token builders"
  key_links:
    - from: "src/server/theming/templates/beauty-cosmetics.ts"
      to: "src/lib/strings/templates/beauty-cosmetics.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the beauty-cosmetics segment: 8 templates drawn from 4 skeletons, split 2 Starter / 2 Business / 4 Professional, with real copy and real accent pairs.

Purpose: D-10 requires all six segments be covered so the segment-sorted picker is never irrelevant. Beauty and cosmetics is the segment where the no-stock-imagery constraint bites hardest — it is a visually-led category with no photographs available — so its templates lean on typography, colour and the `hero:stack` / `editorial-split:banner` variants that are designed to need no image at all.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the beauty-cosmetics copy namespace</name>
  <files>src/lib/strings/templates/beauty-cosmetics.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding allocation table. Read the 8 rows whose `segment` is `beauty-cosmetics`: their keys, their `minTier`, and their skeletons.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the nesting and comment register to mirror.
    - src/server/theming/schema.ts lines 82-138 — the caps: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/phases/05-template-segment-expansion/05-UI-SPEC.md § Image Policy — why these templates carry no photography and what that means for the copy's job.
  </read_first>
  <action>
Fill `src/lib/strings/templates/beauty-cosmetics.ts` with a complete copy set for each of this segment's 8 template keys, keyed exactly as the registry names them.

Per template, provide `name`, `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules:

- **All 8 hero voices must be distinct** from each other and from every other segment's. The metric's signature is `hero.eyebrow + hero.heading` and it asserts uniqueness across all 50.
- Write for a Douala beauty or cosmetics business — skincare, haircare, fragrance, makeup, salon retail. Buyers in this category care about authenticity, skin type and shade suitability, and delivery discretion; that is what the trust-bar copy should carry and it is what makes this segment read differently from electronics.
- **The copy carries more weight here than in any other segment**, because no template ships a photograph. A hero heading that only works over an image is the wrong heading; write for a typographic page.
- `ctaHref` and `viewAllHref` point at `/`. Never the internal `/s/{slug}` rewrite-target prefix.
- Trust-bar items carry heading and body copy only; the icon is a schema enum chosen in Task 2.
- Every string inside its cap.
- The two `minTier: "starter"` templates get the strongest copy.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports exactly 8 template keys, each matching a `TEMPLATES` key whose `segment` is `beauty-cosmetics`.
    - Every template's copy groups correspond exactly to the section types its registry row declares.
    - All `hero.eyebrow + hero.heading` pairs are distinct within the module.
    - A tsx script confirms every string is within its schema cap.
    - `grep -c '/s/' src/lib/strings/templates/beauty-cosmetics.ts` returns 0.
    - No exclamation mark and no emoji in any string.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Eight complete, distinct, cap-respecting copy sets exist for beauty-cosmetics, written to carry a page that has no photography.</done>
</task>

<task type="auto">
  <name>Task 2: Author the beauty-cosmetics document and token builders</name>
  <files>src/server/theming/templates/beauty-cosmetics.ts</files>
  <read_first>
    - src/server/theming/templates/beauty-cosmetics.ts — the module contract plan 05-08 created: the 8 builder pairs, their names, and their section lists.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 8 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — the literal shape to clone, the `id === type` rule, the null-image rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, the token shape.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`.
    - src/lib/strings/templates/beauty-cosmetics.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 8 document builders and 8 token builders in `src/server/theming/templates/beauty-cosmetics.ts`, following the rules every segment module obeys:

Each `*Document()` returns a fresh literal on every call; its `sections` array matches its registry row's declared types and order exactly; each section's `id` is its own `type` string; every string is a member expression into `strings.templates[<key>]` with zero inline prose; hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`; editorial-split sections set `imageKey: null`; product-grid sections set `itemCount: DEFAULT_ITEM_COUNT` unless the variant argues otherwise; trust-bar sections carry exactly three blocks each with an `icon` from the schema's closed enum.

Each `*Tokens()` returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`. **Two templates sharing a skeleton must not share a `primaryAccent`.** Accents are hex values satisfying `hexColorSchema`; foregrounds and the focus ring are derived by `deriveThemeCssVars`, so do not store them and do not add a contrast library.

Because this segment has no imagery at all, the accent pair is doing a disproportionate share of the distinctiveness work — choose eight genuinely separated pairs across the segment, not eight shades of one hue.

Never hoist a builder's literal to module scope and never freeze one.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 keys, `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, the document's section types equal the registry row's section types in order.
    - For each key, every section satisfies `id === type` and ids are unique within the document.
    - For each key, two successive `templateDefaultDocument(key)` calls return different object references, and their `sections` arrays are also distinct references.
    - No non-null image key appears anywhere in the file.
    - No two templates sharing a skeleton share a `primaryAccent`, and the 8 `primaryAccent` values across the segment are all distinct.
    - Every settings string is a `strings.templates` member expression; no prose literal appears in the file.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Eight beauty-cosmetics templates build fresh, schema-valid, image-free default documents matching their registry rows, with eight genuinely separated accent pairs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| copy strings → schema caps | an over-length string makes a document unparseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-53 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Per-string cap check here; plan 05-20's generalized parse test is the build-level gate |
| T-05-54 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; generalized mutation test in plan 05-20 |
| T-05-55 | Tampering | pressure to source stock photography for a visual category | mitigate | All image keys `null` and asserted; `storageKeySchema` untouched. RESEARCH Finding 3 makes this a technical constraint, not a preference |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 8 documents parse, match their rows, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 8 complete copy sets and 8 builder pairs exist for beauty-cosmetics, matching the registry rows exactly.
- All hero voices in the segment are distinct; all 8 primary accents are distinct.
- Every template reads as finished with no photograph.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-14-SUMMARY.md` when done.
</output>
