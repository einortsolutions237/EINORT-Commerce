---
phase: 05-template-segment-expansion
plan: 16
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/furniture-home.ts
  - src/server/theming/templates/furniture-home.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 8 furniture-home templates have real, industry-appropriate, Douala-appropriate copy in the project's voice."
    - "Every furniture-home default document parses against pageDocumentSchema and matches its registry row's sections and order."
    - "Sibling templates sharing a skeleton do not share an accent."
  artifacts:
    - path: "src/lib/strings/templates/furniture-home.ts"
      provides: "Display names, segment tags and ~190 copy strings for the segment's 8 templates"
    - path: "src/server/theming/templates/furniture-home.ts"
      provides: "8 document builders and 8 token builders"
  key_links:
    - from: "src/server/theming/templates/furniture-home.ts"
      to: "src/lib/strings/templates/furniture-home.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the furniture-home segment: 8 templates drawn from 4 skeletons, split 2 Starter / 2 Business / 4 Professional, with real copy and real accent pairs.

Purpose: furniture and homeware is the segment where the `product-grid:showcase` variant earns its place — few, large, considered items rather than a dense catalogue — so this segment is where the widest grid variant does most of its distinctiveness work. D-10 requires the segment be covered so the picker is never irrelevant for a home-goods merchant.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the furniture-home copy namespace</name>
  <files>src/lib/strings/templates/furniture-home.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding allocation table. Read the 8 rows whose `segment` is `furniture-home`: their keys, their `minTier`, and their skeletons.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the nesting and comment register to mirror.
    - src/server/theming/schema.ts lines 82-138 — the caps: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/PROJECT.md — the Cameroon/Douala-first market context.
  </read_first>
  <action>
Fill `src/lib/strings/templates/furniture-home.ts` with a complete copy set for each of this segment's 8 template keys, keyed exactly as the registry names them.

Per template, provide `name`, `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules:

- **All 8 hero voices must be distinct** from each other and from every other segment's.
- Write for a Douala furniture or homeware business — made-to-order pieces, imported furniture, soft furnishings, kitchen and décor. Buyers here care about dimensions and materials, delivery and assembly logistics for bulky items, and lead time on made-to-order work. That is what the trust-bar copy should carry and it is what makes this segment read differently from grocery.
- The `productGrid.heading` and `editorialSplit` copy should suit fewer, larger, more considered items where the skeleton uses the `showcase` grid variant — read each row's variant before writing its grid heading.
- `ctaHref` and `viewAllHref` point at `/`. Never the internal `/s/{slug}` rewrite-target prefix.
- Trust-bar items carry heading and body copy only; the icon is a schema enum chosen in Task 2.
- Every string inside its cap.
- The two `minTier: "starter"` templates get the strongest copy.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports exactly 8 template keys, each matching a `TEMPLATES` key whose `segment` is `furniture-home`.
    - Every template's copy groups correspond exactly to the section types its registry row declares.
    - All `hero.eyebrow + hero.heading` pairs are distinct within the module.
    - A tsx script confirms every string is within its schema cap.
    - `grep -c '/s/' src/lib/strings/templates/furniture-home.ts` returns 0.
    - No exclamation mark and no emoji in any string.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Eight complete, distinct, cap-respecting copy sets exist for furniture-home, written for the segment's real logistics and lead-time concerns.</done>
</task>

<task type="auto">
  <name>Task 2: Author the furniture-home document and token builders</name>
  <files>src/server/theming/templates/furniture-home.ts</files>
  <read_first>
    - src/server/theming/templates/furniture-home.ts — the module contract plan 05-08 created: the 8 builder pairs, their names, and their section lists.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 8 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — the literal shape to clone, the `id === type` rule, the null-image rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, the token shape.
    - src/app/s/[slug]/sections/product-grid-showcase.tsx — the showcase variant's tile density, so `itemCount` can be chosen deliberately for rows that use it.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`.
    - src/lib/strings/templates/furniture-home.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 8 document builders and 8 token builders in `src/server/theming/templates/furniture-home.ts`, following the rules every segment module obeys:

Each `*Document()` returns a fresh literal on every call; its `sections` array matches its registry row's declared types and order exactly; each section's `id` is its own `type` string; every string is a member expression into `strings.templates[<key>]` with zero inline prose; hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`; editorial-split sections set `imageKey: null`; trust-bar sections carry exactly three blocks each with an `icon` from the schema's closed enum.

For product-grid sections, choose `itemCount` deliberately per variant: `DEFAULT_ITEM_COUNT` for `grid`, a higher count for `dense`, and a lower count for `showcase` — a showcase grid renders two large tiles per row, so eight of them is a long scroll before the next section. Record the chosen count and its reason in a comment on each showcase-bearing builder.

Each `*Tokens()` returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`. **Two templates sharing a skeleton must not share a `primaryAccent`.** Accents are hex values satisfying `hexColorSchema`; foregrounds and the focus ring are derived, so do not store them and do not add a contrast library.

Never hoist a builder's literal to module scope and never freeze one.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 keys, `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, the document's section types equal the registry row's section types in order.
    - For each key, every section satisfies `id === type` and ids are unique within the document.
    - For each key, two successive `templateDefaultDocument(key)` calls return different object references, and their `sections` arrays are also distinct references.
    - Every builder whose row uses `product-grid:showcase` sets an `itemCount` lower than `DEFAULT_ITEM_COUNT`, with a comment stating why.
    - No non-null image key appears anywhere in the file.
    - No two templates sharing a skeleton share a `primaryAccent`.
    - Every settings string is a `strings.templates` member expression; no prose literal appears in the file.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Eight furniture-home templates build fresh, schema-valid, image-free default documents matching their registry rows, with item counts chosen per grid variant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| copy strings → schema caps | an over-length string makes a document unparseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-59 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Per-string cap check here; plan 05-20's generalized parse test is the build-level gate |
| T-05-60 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; generalized mutation test in plan 05-20 |
| T-05-61 | Tampering | a template default referencing an image outside the tenant prefix | mitigate | All image keys `null`; `storageKeySchema` untouched |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 8 documents parse, match their rows, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 8 complete copy sets and 8 builder pairs exist for furniture-home, matching the registry rows exactly.
- Showcase-bearing templates carry a deliberately reduced `itemCount`.
- All hero voices in the segment are distinct; sibling templates do not share an accent.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-16-SUMMARY.md` when done.
</output>
