---
phase: 05-template-segment-expansion
plan: 17
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/general-retail.ts
  - src/server/theming/templates/general-retail.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 9 general-retail templates have real copy that reads as a specific shop, not as a template with the specifics removed."
    - "The segment's single Starter template is strong enough to stand alone, because it is the only one a Starter merchant in this segment can pick."
    - "Every general-retail default document parses against pageDocumentSchema and matches its registry row's sections and order."
  artifacts:
    - path: "src/lib/strings/templates/general-retail.ts"
      provides: "Display names, segment tags and ~215 copy strings for the segment's 9 templates"
    - path: "src/server/theming/templates/general-retail.ts"
      provides: "9 document builders and 9 token builders across 5 skeletons"
  key_links:
    - from: "src/server/theming/templates/general-retail.ts"
      to: "src/lib/strings/templates/general-retail.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the general-retail segment: 9 templates drawn from 5 skeletons, split 1 Starter / 3 Business / 5 Professional, with real copy and real accent pairs.

Purpose: this is the segment most at risk of failing TMPL-05, because "general retail" invites generic copy and genericness is this phase's explicit failure condition. It carries the largest skeleton count (5) precisely to compensate — structure has to do more of the distinctiveness work here than in any other segment. Like grocery-food it gets a single Starter template, which must therefore be excellent.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the general-retail copy namespace</name>
  <files>src/lib/strings/templates/general-retail.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding allocation table. Read the 9 rows whose `segment` is `general-retail`, noting the single row with `minTier: "starter"` and the 5 distinct skeletons.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the nesting and comment register to mirror.
    - src/server/theming/schema.ts lines 82-138 — the caps: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/phases/05-template-segment-expansion/05-RESEARCH.md § The Distinctiveness Gate at N=50 — Layer 1 rule 3 (all 50 hero voices distinct) and Layer 2's "shops that look like the same shop" question, which this segment is most likely to fail.
  </read_first>
  <action>
Fill `src/lib/strings/templates/general-retail.ts` with a complete copy set for each of this segment's 9 template keys, keyed exactly as the registry names them.

Per template, provide `name`, `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules — this segment needs more discipline than the others:

- **All 9 hero voices must be distinct** from each other and from every other segment's 41.
- "General retail" is a registry category, not a voice. Do NOT write nine variations of "everything you need in one place." Give each of the nine a specific implied shop — a stationery and school-supplies seller, a hardware and tools shop, a gift and party supplier, a sports and outdoor seller, a books-and-media shop, a pet supplies seller, a bulk-and-wholesale trader, a mixed neighbourhood store, a seasonal-and-occasions shop, or similar — and write that shop's copy. A merchant reading the picker should be able to tell which one is nearest to their own business.
- **The one Starter template must be the strongest in the segment.** Write it first.
- `ctaHref` and `viewAllHref` point at `/`. Never the internal `/s/{slug}` rewrite-target prefix.
- Trust-bar items carry heading and body copy only; the icon is a schema enum chosen in Task 2.
- Every string inside its cap.
- Do not promise a payment or delivery capability V1 lacks — payment is manual Mobile Money / Orange Money transfer, WhatsApp order, or cash on delivery.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports exactly 9 template keys, each matching a `TEMPLATES` key whose `segment` is `general-retail`.
    - Every template's copy groups correspond exactly to the section types its registry row declares.
    - All `hero.eyebrow + hero.heading` pairs are distinct within the module.
    - No two templates in the module share a `productGrid.heading`.
    - A tsx script confirms every string is within its schema cap.
    - `grep -c '/s/' src/lib/strings/templates/general-retail.ts` returns 0.
    - No exclamation mark and no emoji in any string.
    - The summary names, per template key, the specific implied shop its copy was written for — so the distinctiveness reviewer can check the claim.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Nine complete, distinct, cap-respecting copy sets exist for general-retail, each written for a specific implied shop rather than for the category label.</done>
</task>

<task type="auto">
  <name>Task 2: Author the general-retail document and token builders</name>
  <files>src/server/theming/templates/general-retail.ts</files>
  <read_first>
    - src/server/theming/templates/general-retail.ts — the module contract plan 05-08 created: the 9 builder pairs, their names, and their section lists across 5 skeletons.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 9 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — the literal shape to clone, the `id === type` rule, the null-image rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, the token shape.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`.
    - src/lib/strings/templates/general-retail.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 9 document builders and 9 token builders in `src/server/theming/templates/general-retail.ts`, following the rules every segment module obeys:

Each `*Document()` returns a fresh literal on every call; its `sections` array matches its registry row's declared types and order exactly; each section's `id` is its own `type` string; every string is a member expression into `strings.templates[<key>]` with zero inline prose; hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`; editorial-split sections set `imageKey: null`; product-grid sections set `itemCount` per their variant (`DEFAULT_ITEM_COUNT` for `grid`, higher for `dense`, lower for `showcase`, with a comment on any deviation); trust-bar sections carry exactly three blocks each with an `icon` from the schema's closed enum chosen to fit that template's implied shop.

Each `*Tokens()` returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`. This segment has 5 skeletons across 9 templates, so four skeletons carry two siblings and one carries one. **No two siblings may share a `primaryAccent`**, and because this is the segment most at risk of reading generic, aim for nine accents that are clearly separated across the whole segment rather than merely distinct within each sibling pair. Accents are hex values satisfying `hexColorSchema`; foregrounds and the focus ring are derived, so do not store them and do not add a contrast library.

Never hoist a builder's literal to module scope and never freeze one.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 9 keys, `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, the document's section types equal the registry row's section types in order.
    - For each key, every section satisfies `id === type` and ids are unique within the document.
    - For each key, two successive `templateDefaultDocument(key)` calls return different object references, and their `sections` arrays are also distinct references.
    - The 9 documents draw on exactly 5 distinct skeleton strings, matching the registry.
    - No non-null image key appears anywhere in the file.
    - The 9 `primaryAccent` values are all distinct, and no two siblings share one.
    - Every settings string is a `strings.templates` member expression; no prose literal appears in the file.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Nine general-retail templates build fresh, schema-valid, image-free default documents across five skeletons, with nine clearly separated accents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| generic category copy → the distinctiveness gate | this is the segment most likely to produce "shops that look like the same shop" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-62 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Per-string cap check here; plan 05-20's generalized parse test is the build-level gate |
| T-05-63 | Repudiation | nine templates reading as the same generic shop | mitigate | Each written for a specific implied shop, named in the summary; distinct hero voices and distinct grid headings asserted; 5 skeletons rather than 4 |
| T-05-64 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; generalized mutation test in plan 05-20 |
| T-05-65 | Repudiation | default copy promising a payment or delivery capability V1 lacks | mitigate | Copy rules forbid promising gateway payment or guaranteed delivery windows |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 9 documents parse, match their rows, span 5 skeletons, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 9 complete copy sets and 9 builder pairs exist for general-retail, matching the registry rows exactly.
- Each template is written for a specific implied shop, documented in the summary.
- The single Starter template is complete and is the segment's strongest.
- The 9 primary accents are all distinct.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-17-SUMMARY.md` when done.
</output>
