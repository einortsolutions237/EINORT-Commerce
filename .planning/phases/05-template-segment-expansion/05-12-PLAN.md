---
phase: 05-template-segment-expansion
plan: 12
type: execute
wave: 3
depends_on: ["05-03", "05-08"]
files_modified:
  - src/lib/strings/templates/fashion-apparel.ts
  - src/server/theming/templates/fashion-apparel.ts
autonomous: true
requirements: [TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All 8 fashion-apparel templates have real, industry-appropriate, Douala-appropriate copy in the project's voice."
    - "Every fashion-apparel default document parses against pageDocumentSchema and matches its registry row's sections and order."
    - "flagship-fashion's copy and default document are byte-identical to Phase 4."
  artifacts:
    - path: "src/lib/strings/templates/fashion-apparel.ts"
      provides: "Display names, segment tags and ~170 copy strings for the segment's 8 templates"
    - path: "src/server/theming/templates/fashion-apparel.ts"
      provides: "7 document builders and 7 token builders (flagship-fashion keeps its own in defaults.ts)"
  key_links:
    - from: "src/server/theming/templates/fashion-apparel.ts"
      to: "src/lib/strings/templates/fashion-apparel.ts"
      via: "every settings string is a member expression into strings.templates"
      pattern: "strings.templates"
---

## Phase Goal

Merchants outside the fashion segment get their own structurally distinct storefront, and the template library reaches 50 visually distinct variations (10 Starter / 15 Business / 25 Professional) that a stranger would not mistake for one another.

> Planner note: the ROADMAP `**Goal:**` line for Phase 5 is not in `As a … I want to … so that …` form. It is reproduced verbatim rather than invented. Run `/gsd mvp-phase 5` if the user-story framing is wanted.

<objective>
Author the fashion-apparel segment: 8 templates drawn from 4 skeletons, split 2 Starter / 2 Business / 4 Professional, with real copy and real accent pairs.

Purpose: copy is the dominant cost of TMPL-04 and one of the three axes the distinctiveness metric measures (structure, accent, voice). `05-RESEARCH.md` § Copy volume puts this segment at roughly 170-190 strings. `flagship-fashion` is the one template in this segment that must NOT be authored — it keeps its key, its skeleton and its default document byte-for-byte, because `tests/setup/seed-two-tenants.ts` depends on fixture byte-identity and `theming-registry.test.ts` pins its section order, ids and content.
Output: one copy module and one builder module, both complete for this segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-template-segment-expansion/05-CONTEXT.md
@.planning/phases/05-template-segment-expansion/05-RESEARCH.md
@.planning/phases/05-template-segment-expansion/05-PATTERNS.md
@.planning/phases/05-template-segment-expansion/05-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the fashion-apparel copy namespace</name>
  <files>src/lib/strings/templates/fashion-apparel.ts</files>
  <read_first>
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — the binding 50-row allocation table. Read the 8 rows whose `segment` is `fashion-apparel`: their keys, their `minTier`, and their skeletons (which sections, in what order, in which variants). A template with no `contact` section needs no `contact` copy.
    - src/lib/strings/index.ts lines 1-33 — the voice contract: direct, second person, no exclamation marks, no "Oops", no emoji, English.
    - src/lib/strings/index.ts, the `flagship` namespace (around line 1322) — the exact nesting and comment register to mirror, including the note that trust-bar icon names are a schema enum on the settings row and never copy.
    - src/server/theming/schema.ts lines 82-138 — the character caps every string must satisfy: hero/editorial `heading` 120, `body` 280, `ctaLabel` 30; trust `heading` 48, trust `body` 140; grid `heading` 80.
    - .planning/PROJECT.md — the Cameroon/Douala-first market context these strings must read as written for.
  </read_first>
  <action>
Fill `src/lib/strings/templates/fashion-apparel.ts` with a complete copy set for each of this segment's 8 template keys, keyed exactly as the registry names them.

Per template, provide: `name` (the display name shown on picker cards — Label role, so keep it short and concrete), `segmentTag`, `announcement`, `footerTagline`, and one copy group per section the template's skeleton actually declares: `hero.{eyebrow,heading,body,ctaLabel,ctaHref}`, `trustBar.item{One,Two,Three}.{heading,body}`, `productGrid.{heading,viewAllLabel,viewAllHref}`, `editorialSplit.{eyebrow,heading,body,ctaLabel,ctaHref}`, `contact.{heading,body,ctaLabel}`.

Content rules:

- **All 8 hero voices must be distinct.** The distinctiveness metric's rule 3 signature is `hero.eyebrow + hero.heading` and it asserts all 50 are unique. Two templates whose hero reads the same are two templates that were not really authored.
- Write for a Douala fashion or apparel business selling online — boutiques, tailors, resale, streetwear, traditional wear. Concrete over aspirational. No stock-marketing register.
- `ctaHref` and `viewAllHref` point at `/`, because the product grid lives on the storefront root and this phase adds no new routes. Do not use the internal `/s/{slug}` rewrite-target prefix; `tests/unit/storefront-link-prefix.test.ts` fails the build on one.
- Trust-bar items carry heading and body copy. The icon is NOT copy — it is a closed `z.enum` value on the settings row, chosen in Task 2.
- Every string must be inside its cap. The caps are enforced by the generalized default-document parse test in plan 05-20, so an over-length string is a red unit test, not a runtime surprise.
- The two `minTier: "starter"` templates in this segment are the ones most merchants will see. Give them the strongest copy, not the leftovers.

Do NOT add a `flagship-fashion` entry here. It reads its copy from `strings.flagship`, unchanged; adding a duplicate would create two sources of truth for a fixture the isolation suite pins.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - The module exports an object with exactly 7 template keys (this segment's 8 minus `flagship-fashion`), each matching a key in `TEMPLATES` whose `segment` is `fashion-apparel`.
    - Every template's copy groups correspond exactly to the section types its registry row declares — no group for a section the skeleton omits, and no missing group for one it includes.
    - All `hero.eyebrow + hero.heading` pairs in this module are distinct from each other and from `strings.flagship.hero`'s.
    - Every string is within its schema cap — verify with a tsx script that walks the module and compares lengths against the caps in `schema.ts`.
    - `grep -c '/s/' src/lib/strings/templates/fashion-apparel.ts` returns 0.
    - No exclamation mark and no emoji: `grep -c '!' src/lib/strings/templates/fashion-apparel.ts` returns 0 outside code syntax.
    - `npm run test:unit` passes; `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Seven complete, distinct, cap-respecting copy sets exist for the segment's non-flagship templates, in the project's voice and for its market.</done>
</task>

<task type="auto">
  <name>Task 2: Author the fashion-apparel document and token builders</name>
  <files>src/server/theming/templates/fashion-apparel.ts</files>
  <read_first>
    - src/server/theming/templates/fashion-apparel.ts — the module contract plan 05-08 created: the 7 builder pairs, their names, and their section lists.
    - .planning/phases/05-template-segment-expansion/05-08-SUMMARY.md — this segment's 8 rows with their skeletons and tiers.
    - src/server/theming/defaults.ts lines 89-224 — `flagshipDefaultDocument()` and `flagshipDefaultTokens()`: the exact literal shape to clone, the `id === type` rule, the `backgroundImageKey: null` rationale, `DEFAULT_OVERLAY_OPACITY`, `DEFAULT_ITEM_COUNT`, and the two-accents-only token shape.
    - src/lib/theme-defaults.ts — `deriveThemeCssVars` / `accentForeground`, which derive foregrounds and the focus ring from the two accents, so an unreadable pair cannot be persisted. No contrast library is needed.
    - src/lib/strings/templates/fashion-apparel.ts — Task 1's copy, the only source of strings for this module.
    - src/server/theming/schema.ts — the trust-bar icon `z.enum` values.
  </read_first>
  <action>
Fill the 7 document builders and 7 token builders in `src/server/theming/templates/fashion-apparel.ts`.

Each `*Document()`:

- Returns a fresh literal on every call. Never hoist to a module constant, never freeze one. The fresh-object rule is pinned by a mutation test and its violation is silent, cross-tenant corruption.
- Its `sections` array matches its registry row's declared types and order exactly. The drift test in plan 05-20 compares the two and fails on any disagreement.
- Each section's `id` is its own `type` string.
- Every string is a member expression into `strings.templates[<key>]`. Zero inline prose in this file.
- Hero sections set `backgroundImageKey: null` and `overlayOpacity: DEFAULT_OVERLAY_OPACITY`. Editorial-split sections set `imageKey: null`. This is mandatory for all 50, not a default.
- Product-grid sections set `itemCount: DEFAULT_ITEM_COUNT` unless the skeleton's variant argues for a different count — `showcase` renders fewer, larger tiles, so a smaller count is legitimate; record the reasoning in a comment if you deviate.
- Trust-bar sections carry exactly three blocks, each with an `icon` from the schema's closed enum chosen to fit the item's meaning.

Each `*Tokens()`:

- Returns `{ primaryAccent, secondaryAccent, announcementText, footerTagline }`, with the two copy fields read from `strings.templates[<key>]` and the two accents chosen per template.
- **Accent pairs must be distinct across siblings.** Two templates sharing a skeleton must not share a `primaryAccent` — the distinctiveness metric's rule 1 asserts no two templates share both structure and accent, and this segment's siblings are exactly where that collides.
- Accents are hex values satisfying `hexColorSchema`. Foregrounds and the focus ring are derived by `deriveThemeCssVars`; do not store them and do not add a contrast library.

Do not add a builder for `flagship-fashion` and do not edit `flagshipDefaultDocument()` or `flagshipDefaultTokens()`.
  </action>
  <verify>
    <automated>cd D:/Maxs/Claude/einort-commerce && npm run typecheck && npm run lint && npx vitest run tests/unit/theming-registry.test.ts tests/unit/contrast.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - A tsx script asserts, for each of this segment's 7 authored keys: `pageDocumentSchema.safeParse(templateDefaultDocument(key)).success === true`.
    - For each key, `templateDefaultDocument(key).sections.map(s => s.type)` equals `TEMPLATES[key].sections.map(r => r.type)`.
    - For each key, every section satisfies `id === type`, and the ids are unique within the document.
    - For each key, `templateDefaultDocument(key) !== templateDefaultDocument(key)` and their `sections` arrays are also distinct references.
    - Every hero sets `backgroundImageKey: null` and every editorial-split sets `imageKey: null`; `grep -c 'backgroundImageKey: null' src/server/theming/templates/fashion-apparel.ts` matches the number of hero-bearing templates and there is no non-null image key anywhere in the file.
    - No two templates sharing a skeleton share a `primaryAccent` — verify with a tsx script grouping by skeleton string.
    - `grep -c '"' src/server/theming/templates/fashion-apparel.ts` shows no prose literals: every settings string is a `strings.templates` member expression.
    - `tests/unit/theming-registry.test.ts` still passes, including the flagship mutation test.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Seven fashion-apparel templates build fresh, schema-valid, image-free default documents that match their registry rows, with distinct accent pairs across siblings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template default document → live storefront render | a default that fails to parse degrades a real merchant's store |
| copy strings → schema caps | an over-length string makes a document unparseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-46 | Denial of Service | a copy string exceeding its Zod cap | mitigate | Caps checked per string in this plan's criteria; plan 05-20's generalized parse test is the build-level gate |
| T-05-47 | Tampering | a builder hoisted to a shared literal | mitigate | Fresh-object assertion per key; the generalized mutation test in plan 05-20 covers all 50 |
| T-05-48 | Tampering | a template default referencing an image outside the tenant prefix | mitigate | All image keys are `null`; `storageKeySchema` is untouched |
| T-05-49 | Repudiation | the flagship's fixture byte-identity broken by an "improvement" | mitigate | No `flagship-fashion` entry in either file; `tests/setup/seed-two-tenants.ts` and `theming-registry.test.ts` remain green |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase — `05-RESEARCH.md` § Package Legitimacy Audit records an empty table |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck` exit 0.
- `npm run test:unit` passes in full.
- A tsx script confirms all 7 documents parse, match their rows, are fresh per call, and carry no image keys.
</verification>

<success_criteria>
- 7 complete copy sets and 7 builder pairs exist for fashion-apparel, matching the registry rows exactly.
- All hero voices in the segment are distinct; sibling templates do not share an accent.
- `flagship-fashion` is untouched in both files.
</success_criteria>

<output>
Create `.planning/phases/05-template-segment-expansion/05-12-SUMMARY.md` when done.
</output>
