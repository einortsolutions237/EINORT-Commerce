---
phase: 07-trial-template-tier-business-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/entitlements.test.ts
  - tests/isolation/trial.test.ts
  - src/server/entitlements/resolve.ts
  - src/server/entitlements/plans.ts
  - src/server/theming/access.ts
  - src/lib/strings/index.ts
  - src/lib/strings/marketing.ts
autonomous: true
requirements: [ONB-05]

must_haves:
  truths:
    - "A merchant who signs up today has a full-feature trial that runs 30 days from signup, enforced server-side on every write gate"
    - "Every existing Organization row with a null trialEndsAt is on 30 days from its own createdAt, with no migration, backfill or data touch"
    - "Every trial figure a merchant reads on /, /signup, /onboarding/plan and /dashboard/plan says 30 days, not 10"
    - "A later edit that reintroduces TRIAL_DAYS = 10 fails the unit suite rather than shipping quietly"
    - "No source comment in src/ or tests/ still asserts a 10-day trial as fact"
  artifacts:
    - path: "src/server/entitlements/resolve.ts"
      provides: "TRIAL_DAYS = 30, the single enforced trial-length rule"
      contains: "export const TRIAL_DAYS = 30"
    - path: "tests/unit/entitlements.test.ts"
      provides: "The pinning assertion plus TRIAL_DAYS-relative boundary probes"
      contains: "expect(TRIAL_DAYS).toBe(30)"
    - path: "src/lib/strings/index.ts"
      provides: "Signup and plan-surface trial copy stating 30 days"
      contains: "Start my 30-day trial"
    - path: "src/lib/strings/marketing.ts"
      provides: "Landing-page trial copy stating 30 days"
      contains: "Thirty days. No card."
  key_links:
    - from: "tests/unit/entitlements.test.ts"
      to: "src/server/entitlements/resolve.ts"
      via: "import of TRIAL_DAYS, with boundary instants expressed relative to it"
      pattern: "TRIAL_DAYS \\+ 1"
    - from: "src/server/entitlements/resolve.ts"
      to: "Organization.createdAt"
      via: "derive-never-stamp trial expiry"
      pattern: "org\\.trialEndsAt \\?\\? new Date\\(org\\.createdAt"
---

<objective>
Make the 30-day trial (ONB-05) true both as an enforced server-side rule and as every number a merchant reads.

Purpose: ONB-05 was updated in place for milestone v2.0 from 10 days to 30. Today `TRIAL_DAYS = 10` is enforced by `resolveEntitlements` and stated in nine copy strings. Until both move, the product enforces one promise and prints another.

Output: `TRIAL_DAYS = 30`, every trial-day copy string and load-bearing source comment updated, and the entitlement test suite re-anchored so it pins 30 and never breaks again on a future trial-length change.
</objective>

## Phase Goal

**ROADMAP goal, verbatim:** The two Master Spec V3 commercial rules — a 30-day trial and a 15/17/18 template tier split — are true everywhere the product enforces or states them, with no surface still quoting the old numbers.

> MVP_MODE is enabled for this phase, but ROADMAP.md's `**Goal:**` line for Phase 7 is not written in `As a … I want to … so that …` form. The planner does not invent a user story where the roadmap does not supply one — run `/gsd mvp-phase 7` if the user-story framing is wanted. Task decomposition below still follows vertical-slice rules: Task 1 is the failing end-to-end assertion, Task 2 makes one complete merchant-observable rule true (enforced **and** stated) in a single move rather than shipping a constant now and its copy later.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-trial-template-tier-business-rules/07-CONTEXT.md
@.planning/phases/07-trial-template-tier-business-rules/07-RESEARCH.md
@.planning/phases/07-trial-template-tier-business-rules/07-PATTERNS.md
@.planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md
@CLAUDE.md

<verified_state>
Every file:line below was re-verified at HEAD on 2026-09-13 at plan time (Phase 6 had not landed, so 07-RESEARCH.md's inventory is still exact).

**The one functional site**

| File:line | Current | New |
|-----------|---------|-----|
| `src/server/entitlements/resolve.ts:34` | `export const TRIAL_DAYS = 10;` | `= 30` |

**The nine copy / comment sites that state "10 days"** (all confirmed present at these exact lines)

| File:line | Current text | Owner |
|-----------|--------------|-------|
| `src/lib/strings/index.ts:111` | `subline: "Free for 10 days. No card required.",` | this plan |
| `src/lib/strings/index.ts:267` | `"Free for 10 days. No card required. You can change your plan any time during your trial.",` | this plan |
| `src/lib/strings/index.ts:277` | `cta: "Start my 10-day trial",` | this plan |
| `src/lib/strings/marketing.ts:29` | `* this page (10-day trial, 10/25/50 templates per tier) matches` | **trial half only** — the `10/25/50` half belongs to plan 07-02 |
| `src/lib/strings/marketing.ts:50` | `ctaNote: "Free for 10 days. No card required.",` | this plan |
| `src/lib/strings/marketing.ts:66` | `"Starter reaches 10 templates, Business 25, Professional all 50. Every plan gets the full 10-day trial first.",` | **trial half only** — the counts half belongs to plan 07-02 |
| `src/lib/strings/marketing.ts:95` | `heading: "Ten days. No card. Your own storefront.",` | this plan |
| `src/lib/strings/marketing.ts:96` | `body: "Every plan starts with a full 10-day trial of everything it includes. …"` | this plan |
| `src/server/entitlements/plans.ts:123` | `* 10-day trial regardless of tier, and this registry is pure tier data that` | this plan |
| `src/server/entitlements/resolve.ts:23` | `* count from the server. ONB-05 reads "10 days from signup", so` | this plan |
| `src/server/entitlements/resolve.ts:33` | `/** ONB-05: every plan gets the same 10-day full-feature trial. */` | this plan |
| `src/server/entitlements/resolve.ts:97` | `* 10 days has the same editor a Business merchant has, because the trial is` | this plan |
| `src/server/theming/access.ts:37` | `* - D-15 elevates the EDITOR during the 10-day trial because losing it at` | this plan |

**DO NOT TOUCH — confirmed false positives**

| Site | Why it must not change |
|------|------------------------|
| `src/lib/strings/index.ts:5` | The "30-day solo" there is the *project timeline*, not the trial |
| `strings.trial.daysLeft`, `strings.trial.oneDayLeft`, `strings.plan.dashboard.trialDaysLeft` | Interpolate a server-computed integer; already correct at any trial length |
| `tests/unit/overview-buckets.test.ts:70,72` | "Ten days before/after the window" is the analytics bucket window, unrelated to the trial |
| `src/app/**` (any `.tsx`) | Zero inline trial literals exist; a `.tsx` in this plan's diff means a CLAUDE.md copy-centralization violation |
| `.planning/PROJECT.md` L52, `.planning/ROADMAP.md` L27/L141 | Deliberate historical record of what Phase 2 shipped |
| `prisma/schema.prisma:132` | Names the constant (`createdAt + TRIAL_DAYS`), not its value — already correct |
| `tests/isolation/read-only.test.ts:201`, `tests/isolation/storefront-editor.test.ts:257` | Force expiry with an explicit non-null `trialEndsAt`; the `??` short-circuits before `TRIAL_DAYS` |
</verified_state>

<interfaces>
The contract this plan depends on, already in the repo — no exploration needed.

From `src/server/entitlements/resolve.ts`:
- `export const TRIAL_DAYS: number` — the rule constant (currently `10`)
- `export const TRIAL_URGENT_DAYS: number` — sibling constant, the doc-comment house style to imitate
- `resolveEntitlements(org, now) => MerchantContext` — pure, no clock read, no I/O
- Line 135 derives expiry as `org.trialEndsAt ?? new Date(org.createdAt.getTime() + TRIAL_DAYS * DAY_MS)`

From `tests/unit/entitlements.test.ts`:
- `T0` — the fixture epoch; `at(ms)` — an instant offset from `T0`
- `DERIVED_END = new Date(T0.getTime() + TRIAL_DAYS * DAY_MS)` (L52-53) — already trial-length-agnostic
- `atDaysLeft(days) => new Date(DERIVED_END.getTime() - days * DAY_MS)` (L303-305) — **the register every failing site must be rewritten into**
- Six top-level `describe` blocks: `registry`, `member limit`, `trial active`, `trial boundary`, `daysLeft`, `urgency` — **do not rename any of them**; `02-VALIDATION.md` and `07-VALIDATION.md` target them with `-t` filters
</interfaces>

<accepted_consequence>
D-04 says the 30-day trial applies retroactively to every existing `Organization` row. This is true of the null-`trialEndsAt` population only — a row carrying a non-null `trialEndsAt` (a support-granted override) short-circuits the `??` and is not extended. This is **accepted, not overlooked**: it is exactly what D-05 requires (no backfill), it can never shorten anyone's trial, and no production code path writes that column today (the only two writers in the repo are test fixtures). Record this in the SUMMARY. Do not add a migration.
</accepted_consequence>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-anchor the trial test suites to TRIAL_DAYS and pin 30 (RED)</name>

  <files>tests/unit/entitlements.test.ts, tests/isolation/trial.test.ts</files>

  <read_first>
    - tests/unit/entitlements.test.ts (whole file — the boundary instants are spread across five describe blocks and two module-level consts)
    - tests/isolation/trial.test.ts lines 1-30 and 125-205
    - .planning/phases/07-trial-template-tier-business-rules/07-RESEARCH.md § 3a and § 3b (the authoritative site list)
    - .planning/phases/07-trial-template-tier-business-rules/07-PATTERNS.md § tests/unit/entitlements.test.ts (the atDaysLeft register to rewrite into)
  </read_first>

  <behavior>
    - `expect(TRIAL_DAYS).toBe(30)` — the one deliberately-hard literal, the assertion whose entire job is to pin the number
    - `daysLeft` on day 1 of the window is `TRIAL_DAYS - 1`; on day 9 it is `TRIAL_DAYS - 9`
    - An instant one day past the derived end resolves to `state === "expired"` and `isUrgentTrial === false`
    - A Starter org one day past the derived end is refused the editor with `EditorLockedError`
    - A stored non-null `trialEndsAt` strictly later than the derived end still wins over the `createdAt` derivation (the assertion must stay able to detect a resolver that ignores `trialEndsAt`)
    - End-to-end (isolation): a signup-created org has `trialEndsAt` NULL and `ctx.trial.daysLeft === TRIAL_DAYS`
  </behavior>

  <action>
    Rewrite every trial-relative instant in these two files so it is expressed against `TRIAL_DAYS` / `DERIVED_END` instead of an absolute day number. Do this task **before** touching `src/`, so the suite goes red for the right reason.

    In `tests/unit/entitlements.test.ts`, applying 07-RESEARCH § 3a's site table exactly:
    - L271: change `expect(TRIAL_DAYS).toBe(10)` to `.toBe(30)`. Leave it a hard literal — making it relative would make it tautological, the same argument `tests/unit/product-limit.test.ts`'s `EXPECTED_CAP` header makes.
    - L275-279: the two `daysLeft` expectations currently expecting `9` and `1` become `TRIAL_DAYS - 1` and `TRIAL_DAYS - 9`, keeping their existing `at(1 * DAY_MS)` / `at(9 * DAY_MS)` observation instants.
    - L326: `at(11 * DAY_MS)` becomes `at((TRIAL_DAYS + 1) * DAY_MS)`. Do not renumber `11` to `31`.
    - L363-365: `DURING_TRIAL` stays `at(2 * DAY_MS)`; `AFTER_TRIAL` becomes `at((TRIAL_DAYS + 1) * DAY_MS)`. The doc comment `/** Inside the derived 10-day window. */` becomes `30-day`.
    - L497: `ctxAt({ planTier: "starter" }, at(11 * DAY_MS))` becomes `at((TRIAL_DAYS + 1) * DAY_MS)`.
    - L245-253 — **the vacuous site, not a red one.** `granted = at(30 * DAY_MS)` becomes exactly `DERIVED_END` at `TRIAL_DAYS = 30`, so the test silently stops being able to detect a resolver that ignores `trialEndsAt`. Change it to `at((TRIAL_DAYS + 20) * DAY_MS)`.
    - L226-232 — the second vacuous site. "is subscribed, and writable, once past the end" observes at `at(30 * DAY_MS)`, which is now *at* the end, not past it, so the test name becomes false. Change to `at((TRIAL_DAYS + 1) * DAY_MS)`.
    - L180 `it("is active on day 9")` at `at(9 * DAY_MS)` was a near-boundary probe on a 10-day window. Re-express as `at((TRIAL_DAYS - 1) * DAY_MS)` and rename the `it` to describe "the day before the boundary" rather than a fixed day number.
    - Comment debt in the same file: L23 `ONB-05 (10-day trial …)`, L29-30 `the day-10 boundary`, L347 `day 2 of their 10-day trial`. Rewrite in the same voice with the new window length. The example day (2) still works and should not change — only the window length does.
    - **Do not rename any of the six top-level `describe` blocks** — `07-VALIDATION.md` targets them with `-t` filters.

    In `tests/isolation/trial.test.ts`, applying 07-RESEARCH § 3b:
    - L193's `createdAt.getTime() + TRIAL_DAYS * DAY_MS` is already correct — leave it untouched.
    - L195: `expect(TRIAL_DAYS).toBe(10)` becomes `.toBe(30)`.
    - L199: `expect(ctx.trial.daysLeft).toBe(10)` becomes `.toBe(TRIAL_DAYS)`; its adjacent comment "still a full 10" becomes "still a full 30".
    - Prose at L7, L17, L131 and the test *name* at L159 (`"derives endsAt as createdAt + 10 days …"`) are text-only edits to the new window length.

    Do not add a new helper, do not restructure either file, and do not weaken any assertion.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/entitlements.test.ts --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/unit/entitlements.test.ts` FAILS, and the failure output names `TRIAL_DAYS` expected `30` received `10` (a RED-for-the-right-reason commit, not a broken-test commit).
    - `grep -nE "at\(1?[019] \* DAY_MS\)" tests/unit/entitlements.test.ts` returns no line that represents a trial boundary; every remaining bare `at(N * DAY_MS)` is either `at(2 * DAY_MS)` (mid-trial, deliberately absolute) or `at(90 * DAY_MS)` (deliberately far past any window).
    - `grep -c "TRIAL_DAYS" tests/unit/entitlements.test.ts` is strictly greater than its pre-task value.
    - `grep -rniE "(10[- ]day|ten[- ]day|10 days|ten days)" tests/unit/entitlements.test.ts tests/isolation/trial.test.ts` returns zero lines.
    - `grep -nE "describe\(\"(registry|member limit|trial active|trial boundary|daysLeft|urgency)\"" tests/unit/entitlements.test.ts` returns all six blocks, unrenamed.
    - `npm run lint && npm run typecheck` both pass.
  </acceptance_criteria>

  <done>Both trial test files express every boundary instant relative to `TRIAL_DAYS`, pin the value at 30, and the unit suite is red solely on the not-yet-changed constant.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the 30-day trial — the constant, its load-bearing comments, and every trial figure a merchant reads (GREEN)</name>

  <files>src/server/entitlements/resolve.ts, src/server/entitlements/plans.ts, src/server/theming/access.ts, src/lib/strings/index.ts, src/lib/strings/marketing.ts</files>

  <read_first>
    - src/server/entitlements/resolve.ts lines 1-60 and 90-140 (module header, both constants, the derive site)
    - src/server/entitlements/plans.ts lines 115-130 (the D-15 comment)
    - src/server/theming/access.ts lines 1-45 (header block)
    - src/lib/strings/index.ts lines 1-10 and 105-120 and 260-285
    - src/lib/strings/marketing.ts lines 20-100
    - The `<verified_state>` table above — it is the exhaustive site list; do not run a find-and-replace on "10"
  </read_first>

  <action>
    Make the 30-day trial true as an enforced rule and as a stated number in one move, so the repo is never self-contradictory between them.

    **The one functional change.** In `src/server/entitlements/resolve.ts`, change `export const TRIAL_DAYS = 10;` (L34) to `= 30`. That is the whole enforcement change — L135 already derives expiry as `org.trialEndsAt ?? new Date(org.createdAt.getTime() + TRIAL_DAYS * DAY_MS)`, so every existing org with a null `trialEndsAt` extends the moment this ships. Per D-05, write **no** migration, **no** `trialEndsAt` backfill, and **no** dual-rate logic. Do not make the constant env-configurable and do not add a second constant.

    **Source comments that assert the old number as fact.** CLAUDE.md treats module headers as load-bearing, so these change in the same commit, rewritten in the same voice and structure rather than deleted:
    - `resolve.ts:33` — the constant's own doc comment, `10-day` to `30-day`.
    - `resolve.ts:23` — `ONB-05 reads "10 days from signup"` becomes `"30 days from signup"`.
    - `resolve.ts` module header around L14-28 — the passage enumerating "day 1, day 9, the day-10 boundary" becomes the equivalent instants on a 30-day window.
    - `resolve.ts:97` — `a Starter merchant on day 2 of their 10 days` becomes `…of their 30 days`. Keep the example day at 2; only the window length moves.
    - `plans.ts:123` — `D-15 grants every merchant full editor capability during the 10-day trial` becomes `30-day`.
    - `access.ts:37` — `D-15 elevates the EDITOR during the 10-day trial` becomes `30-day`. **This file gets comment-only edits in this plan** — `canUseTemplate`, `accessibleTemplateKeys` and `assertTemplateAccess` are pure functions of `minTier` and `PLAN_TIER_RANK` and must not change. Any non-comment diff line in `access.ts` here is a defect.

    **Copy — exactly the sites in the `<verified_state>` table, worked from file:line, never from a search-and-replace.**
    - `src/lib/strings/index.ts:111` (`strings.signup.subline`) to `"Free for 30 days. No card required."`
    - `src/lib/strings/index.ts:267` (`strings.plan.subline`) to `"Free for 30 days. No card required. You can change your plan any time during your trial."`
    - `src/lib/strings/index.ts:277` (`strings.plan.cta`) to `"Start my 30-day trial"`
    - `src/lib/strings/marketing.ts:50` (`strings.root.hero.ctaNote`) to `"Free for 30 days. No card required."`
    - `src/lib/strings/marketing.ts:95` (`strings.root.finalCta.heading`) to `"Thirty days. No card. Your own storefront."`
    - `src/lib/strings/marketing.ts:96` (`strings.root.finalCta.body`) — `"…a full 10-day trial…"` to `"…a full 30-day trial…"`, rest of the sentence unchanged
    - `src/lib/strings/marketing.ts:66` (`strings.root.templates.constraint`) — **change only the trial clause**: the trailing sentence becomes `"Every plan gets the full 30-day trial first."`. Leave `"Starter reaches 10 templates, Business 25, Professional all 50."` exactly as it is — plan 07-02 owns that half and will change it in wave 2.
    - `src/lib/strings/marketing.ts:29` (module header) — **change only the trial half**: `(10-day trial, …)` becomes `(30-day trial, …)`. Leave `10/25/50 templates per tier` for plan 07-02. Keep the "matches `PLANS` exactly" clause — it is the honesty contract this phase re-satisfies, not removes.

    **Do not touch** `strings.trial.*` (they interpolate a server-computed integer), `src/lib/strings/index.ts:5` (that "30-day" is the project timeline), or any `.tsx`. This plan's diff must contain zero `.tsx` files; one means a user-facing literal was inlined, which violates CLAUDE.md and the prose-literal contract tests.

    Do not introduce interpolation that reads `TRIAL_DAYS` into copy — the literals are deliberate so `tests/unit/landing-page-contract.test.ts` can read the claim as source text.
  </action>

  <verify>
    <automated>npm run test:unit && npm run lint && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export const TRIAL_DAYS" src/server/entitlements/resolve.ts` prints `export const TRIAL_DAYS = 30;`.
    - `npm run test:unit` is green, including `npx vitest run tests/unit/entitlements.test.ts -t "daysLeft"` and `-t "editor capability"`.
    - `npm run test:unit` keeps `tests/unit/landing-page-contract.test.ts` and `tests/unit/dashboard-nav.test.ts` green (the prose-literal and banned-phrase contracts).
    - `grep -rniE "(10[- ]day|ten[- ]day|10 days|ten days)" src/ tests/ | grep -v "overview-buckets.test.ts"` returns zero lines.
    - `grep -n "30-day solo" src/lib/strings/index.ts` still returns line 5, unchanged.
    - `grep -rn "trialEndsAt" prisma/migrations/ 2>/dev/null` returns nothing new — no migration was added.
    - `git diff --name-only` for this task lists exactly the five files in `<files>` and no `.tsx` file.
    - `git diff src/server/theming/access.ts` shows only comment lines changed (every changed line begins with optional whitespace then `*` or `//`).
    - `npm run lint` and `npm run typecheck` both pass.
  </acceptance_criteria>

  <done>`TRIAL_DAYS` is 30, no source comment still claims 10, every merchant-facing trial string says 30 days, and the full unit suite is green with zero `.tsx` files in the diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous/authenticated client → Server Action write gate | `merchantAction({ mode: "write" })` consults `canWrite = subscribed \|\| !expired`, which this plan's constant feeds |
| stored `Organization` row → `resolveEntitlements` | The only computer of trial state; a pure `(row, now)` function with no clock read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | `canWrite` in `src/server/entitlements/resolve.ts` | accept | A 30-day trial grants 20 more days of writes to a non-paying merchant. This is the locked commercial decision (PROJECT.md Key Decisions, 2026-09-13), not a defect. The gate logic itself is unchanged and every other cap (Starter `products: 50`, `members: 1`) still applies throughout the trial. |
| T-07-02 | Tampering | `Organization.trialEndsAt` | mitigate | No backfill and no stamped value (D-05). Expiry stays derived at read time, preserving the invariant `tests/isolation/trial.test.ts:157` guards. A support-granted non-null `trialEndsAt` still wins — documented in `<accepted_consequence>`, population empty in production. |
| T-07-03 | Information Disclosure | `src/lib/strings/marketing.ts` | mitigate | Copy changes are numeric only and route through `strings`. `tests/unit/landing-page-contract.test.ts`'s `BANNED_PHRASES` and voice contract stay green; no new claim, no invented count, no `.tsx` literal. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs zero packages and adds nothing to `package.json`. No install task exists, so the slopcheck gate is vacuous here. |
</threat_model>

<verification>
- `npm run test:unit` green.
- `npm run lint` (`--max-warnings=0`) and `npm run typecheck` green.
- `grep -rniE "(10[- ]day|ten[- ]day|10 days|ten days)" src/ tests/ | grep -v "overview-buckets.test.ts"` returns zero lines.
- Zero `.tsx` files in the plan's diff.
- The isolation half (`npm run test:full -- tests/isolation/trial.test.ts`) is deferred to plan 07-03's phase gate, which probes `TEST_DATABASE_URL` first and reports honestly if the Neon test branch is unavailable.
</verification>

<success_criteria>
- `TRIAL_DAYS === 30` and it is the only trial-length value in the codebase.
- Re-introducing `TRIAL_DAYS = 10` fails `tests/unit/entitlements.test.ts` (ROADMAP success criterion 4, trial half).
- Every trial figure in `src/lib/strings/**` says 30 days; `strings.trial.*` is untouched.
- No migration, backfill, or data touch was performed (D-04/D-05).
- The trial half of `marketing.ts:66` and `marketing.ts:29` is done; the template-count half is deliberately left for plan 07-02.
</success_criteria>

<output>
Create `.planning/phases/07-trial-template-tier-business-rules/07-01-SUMMARY.md` when done. Record in it: the `trialEndsAt`-override consequence as accepted-not-overlooked, and the two `marketing.ts` strings deliberately left half-edited for wave 2.
</output>
