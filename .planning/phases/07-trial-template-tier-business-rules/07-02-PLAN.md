---
phase: 07-trial-template-tier-business-rules
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/unit/template-tier-allocation.test.ts
  - tests/unit/template-distinctiveness.test.ts
  - src/server/theming/registry.ts
  - src/server/entitlements/plans.ts
  - src/server/theming/access.ts
  - src/lib/strings/index.ts
  - src/lib/strings/marketing.ts
  - tests/isolation/template-switch.test.ts
  - tests/isolation/onboarding-template.test.ts
autonomous: true
requirements: [TMPL-04]

must_haves:
  truths:
    - "A Starter merchant can open exactly 15 templates, a Business merchant 32, and a Professional merchant all 50"
    - "No template became more tier-restricted than it was before this phase — every one of the 12 moves is a downward relaxation, so no live merchant store loses access to its own template"
    - "Every segment has at least 2 Starter-accessible templates and at least 2 Professional-only templates, so no tier's value proposition collapses in any industry"
    - "Locked templates still appear visible-but-dimmed in the onboarding picker rather than disappearing"
    - "Every tier-count figure a merchant reads says 15 / 32 / 50"
    - "A later edit that reintroduces the 10/15/25 split, or silently re-tiers any single template, fails the build"
  artifacts:
    - path: "tests/unit/template-tier-allocation.test.ts"
      provides: "The D-03 frozen-table invariant and the D-02 segment-balance floor"
      contains: "EXPECTED_MIN_TIER"
    - path: "src/server/theming/registry.ts"
      provides: "The 15/17/18 minTier population across 50 templates"
      contains: "minTier"
    - path: "src/server/entitlements/plans.ts"
      provides: "Documented cumulative catalog sizes 15 / 32 / null"
      contains: "templates: 32"
    - path: "src/lib/strings/marketing.ts"
      provides: "Landing-page tier-count copy stating 15 / 32 / 50"
      contains: "Starter reaches 15 templates"
  key_links:
    - from: "tests/unit/template-tier-allocation.test.ts"
      to: "src/server/theming/registry.ts"
      via: "TEMPLATE_KEYS-driven it.each over TEMPLATES[key].minTier"
      pattern: "TEMPLATES\\[key\\]\\.minTier"
    - from: "tests/unit/template-distinctiveness.test.ts"
      to: "src/server/entitlements/plans.ts"
      via: "lockstep drift guard between the enforced set size and the documented count"
      pattern: "PLANS\\.(starter|business)\\.limits\\.templates"
    - from: "src/server/theming/access.ts"
      to: "src/server/theming/registry.ts"
      via: "accessibleTemplateKeys filters TEMPLATE_KEYS on PLAN_TIER_RANK >= minTier"
      pattern: "PLAN_TIER_RANK\\[TEMPLATES\\[key\\]\\.minTier\\]"
---

<objective>
Re-tier all 50 templates from 10 Starter / 15 Business / 25 Professional to 15 / 17 / 18 (TMPL-04), and make every figure that states the reachable catalog size — code, comment, copy and test — agree on 15 / 32 / 50.

Purpose: TMPL-04 was updated in place for milestone v2.0. The same 50 templates and the same 6 segments stay; only the tier boundary moves, and it moves downward everywhere so no existing merchant loses access to the template their store already renders.

Output: 12 one-word `minTier` edits, two numeric edits in the plan registry, four copy edits, a new frozen-table invariant test pinning all 50 keys, and two isolation refusal tests restored to actually testing an out-of-tier case.
</objective>

## Phase Goal

**ROADMAP goal, verbatim:** The two Master Spec V3 commercial rules — a 30-day trial and a 15/17/18 template tier split — are true everywhere the product enforces or states them, with no surface still quoting the old numbers.

> See plan 07-01's note on the MVP user-story format discrepancy. Task order here follows vertical-slice rules: Task 1 is the failing invariant, Task 2 makes the merchant capability real (a Starter merchant opens 15 templates), Task 3 makes every stated figure and every tier-refusal fixture match it.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-trial-template-tier-business-rules/07-CONTEXT.md
@.planning/phases/07-trial-template-tier-business-rules/07-RESEARCH.md
@.planning/phases/07-trial-template-tier-business-rules/07-PATTERNS.md
@.planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md
@.planning/phases/07-trial-template-tier-business-rules/07-01-SUMMARY.md
@CLAUDE.md

<the_two_triples>
**The single easiest mistake in this phase. Read this before writing any number.**

| Triple | Value | Means | Used in |
|--------|-------|-------|---------|
| **Per-tier `minTier` population** | **15 / 17 / 18** | How many of the 50 registry entries carry each `minTier` value | `access.ts:13`'s header, the new frozen-table test's totals assertion, REQUIREMENTS.md's TMPL-04 text |
| **Cumulative reachable access** | **15 / 32 / 50** | How many templates each tier can actually open (the sets are strictly nested: `accessibleTemplateKeys` filters on `>=` against a total order) | `plans.ts`'s `limits.templates`, `template-distinctiveness.test.ts`'s triple, **all merchant-facing copy** |

Interchanging them is a silent product lie. 32 is 15 + 17, not 17.
</the_two_triples>

<verified_state>
Re-verified at HEAD on 2026-09-13 at plan time by parsing `registry.ts` directly (50 rows found; totals 10 starter / 15 business / 25 professional, matching 07-RESEARCH.md exactly).

**D-06 / Option D — the 12 moves, and only these 12**

| Move | Keys |
|------|------|
| `business` → `starter` (5) | `fashion-studio`, `electronics-pulse`, `beauty-radiance`, `grocery-pantry`, `retail-bazaar` |
| `professional` → `business` (7) | `fashion-classic`, `fashion-muse`, `electronics-signal`, `beauty-veil`, `beauty-satin`, `grocery-harvest`, `furniture-grain` |

All 12 keys were confirmed present at their stated pre-change tier at plan time. Applying exactly these 12 yields totals `starter: 15, business: 17, professional: 18` — machine-verified at plan time.

**Resulting per-segment mix (starter / business / professional)** — machine-computed at plan time, matches CONTEXT.md D-06 exactly:

| Segment | starter | business | professional |
|---|---|---|---|
| fashion-apparel | 3 | 3 | 2 |
| electronics | 3 | 3 | 3 |
| beauty-cosmetics | 3 | 3 | 2 |
| grocery-food | 2 | 3 | 3 |
| furniture-home | 2 | 3 | 3 |
| general-retail | 2 | 2 | 5 |

**Other verified sites**

| File:line | Current | New |
|-----------|---------|-----|
| `src/server/entitlements/plans.ts:202` | `templates: 10,` | `templates: 15,` |
| `src/server/entitlements/plans.ts:216` | `templates: 25,` | `templates: 32,` |
| `src/server/entitlements/plans.ts:230` | `templates: null,` | unchanged |
| `src/server/entitlements/plans.ts:137-141` | `Business's 25 is Starter's 10 plus 15 more` | `Business's 32 is Starter's 15 plus 17 more` |
| `src/server/theming/access.ts:13` | `TMPL-04's 10/15/25` | `TMPL-04's 15/17/18` (per-tier, not cumulative) |
| `src/lib/strings/index.ts:292` | `"10 templates with your logo, brand colors and basic sections"` | `"15 templates …"` |
| `src/lib/strings/index.ts:310` | `"25 templates reachable (Starter's 10, plus 15 more)"` | `"32 templates reachable (Starter's 15, plus 17 more)"` |
| `src/lib/strings/index.ts:329` | `"All 50 templates reachable"` | unchanged |
| `src/lib/strings/marketing.ts:66` | `"Starter reaches 10 templates, Business 25, Professional all 50. …"` | `"Starter reaches 15 templates, Business 32, Professional all 50. …"` (the trailing trial clause already says 30 after plan 07-01) |
| `src/lib/strings/marketing.ts:29` | `(30-day trial, 10/25/50 templates per tier)` | `(30-day trial, 15/32/50 templates per tier)` |
| `tests/unit/template-distinctiveness.test.ts:330` | test name `"…cumulative counts at 10/25/50"` | `"…cumulative counts at 15/32/50"` |
| `tests/unit/template-distinctiveness.test.ts:347` | `10, 25, 50,` | `15, 32, 50,` |
| `tests/isolation/template-switch.test.ts:373` | comment: `"fashion-classic" is professional-tier` | fixture key swap, see Task 3 |
| `tests/isolation/template-switch.test.ts:532` | comment: `"fashion-studio" is business-tier` | comment-only: `starter-tier` |
| `tests/isolation/onboarding-template.test.ts:404,415` | test name + comment claiming `fashion-classic` is Professional | fixture key swap, see Task 3 |

**Also unchanged and correct:** `strings.root.templates.heading` = `"Fifty templates. Six industries. Not one of them generic."` — the 50 total does not move.
</verified_state>

<expected_min_tier_table>
The complete post-change expectation for all 50 keys, in `TEMPLATE_KEYS` declaration order, computed at plan time from the pre-change registry plus exactly the 12 D-06 moves. This is reference data for Task 1's frozen table. `MOVED` marks the 12.

| key | segment | pre-change | post-change | |
|---|---|---|---|---|
| flagship-fashion | fashion-apparel | starter | starter | |
| fashion-classic | fashion-apparel | professional | business | MOVED |
| fashion-edit | fashion-apparel | starter | starter | |
| fashion-muse | fashion-apparel | professional | business | MOVED |
| fashion-studio | fashion-apparel | business | starter | MOVED |
| fashion-house | fashion-apparel | professional | professional | |
| fashion-runway | fashion-apparel | business | business | |
| fashion-loft | fashion-apparel | professional | professional | |
| electronics-circuit | electronics | starter | starter | |
| electronics-signal | electronics | professional | business | MOVED |
| electronics-grid | electronics | starter | starter | |
| electronics-current | electronics | professional | professional | |
| electronics-pulse | electronics | business | starter | MOVED |
| electronics-volt | electronics | professional | professional | |
| electronics-module | electronics | business | business | |
| electronics-frame | electronics | business | business | |
| electronics-byte | electronics | professional | professional | |
| beauty-glow | beauty-cosmetics | starter | starter | |
| beauty-veil | beauty-cosmetics | professional | business | MOVED |
| beauty-bloom | beauty-cosmetics | starter | starter | |
| beauty-satin | beauty-cosmetics | professional | business | MOVED |
| beauty-radiance | beauty-cosmetics | business | starter | MOVED |
| beauty-luxe | beauty-cosmetics | professional | professional | |
| beauty-aura | beauty-cosmetics | business | business | |
| beauty-muse | beauty-cosmetics | professional | professional | |
| grocery-market | grocery-food | starter | starter | |
| grocery-harvest | grocery-food | professional | business | MOVED |
| grocery-pantry | grocery-food | business | starter | MOVED |
| grocery-fresh | grocery-food | professional | professional | |
| grocery-orchard | grocery-food | business | business | |
| grocery-grove | grocery-food | professional | professional | |
| grocery-cellar | grocery-food | business | business | |
| grocery-larder | grocery-food | professional | professional | |
| furniture-loom | furniture-home | starter | starter | |
| furniture-grain | furniture-home | professional | business | MOVED |
| furniture-oak | furniture-home | starter | starter | |
| furniture-hearth | furniture-home | professional | professional | |
| furniture-timber | furniture-home | business | business | |
| furniture-haven | furniture-home | professional | professional | |
| furniture-nook | furniture-home | business | business | |
| furniture-loft | furniture-home | professional | professional | |
| retail-corner | general-retail | starter | starter | |
| retail-emporium | general-retail | professional | professional | |
| retail-bazaar | general-retail | business | starter | MOVED |
| retail-mercantile | general-retail | professional | professional | |
| retail-general | general-retail | business | business | |
| retail-provisions | general-retail | professional | professional | |
| retail-market | general-retail | business | business | |
| retail-trading | general-retail | professional | professional | |
| retail-district | general-retail | professional | professional | |

`retail-district` stays Professional — this is the key Task 3 swaps into the two refusal tests.
</expected_min_tier_table>

<interfaces>
Contracts the executor needs, already in the repo.

From `src/server/theming/registry.ts`:
- `export const TEMPLATE_KEYS: readonly TemplateKey[]` — declaration order is **load-bearing** (return order of `accessibleTemplateKeys`, the within-segment tiebreaker in `onboarding/branding/page.tsx`, and `template-preview-manifest.test.ts`'s manifest order)
- `export const TEMPLATES: Readonly<Record<TemplateKey, TemplateDefinition>>` — each entry has `key`, `segment`, `minTier: PlanTier`, `sections`
- `export const INDUSTRY_SEGMENTS: readonly IndustrySegment[]` — the six segments

From `src/server/theming/access.ts`:
- `accessibleTemplateKeys(tier: PlanTier): TemplateKey[]` — `TEMPLATE_KEYS.filter(key => PLAN_TIER_RANK[tier] >= PLAN_TIER_RANK[TEMPLATES[key].minTier])`
- `canUseTemplate(tier, key): boolean`, `assertTemplateAccess(tier, key): void`

From `src/server/entitlements/plans.ts`:
- `PLAN_TIER_RANK = { starter: 0, business: 1, professional: 2 }`
- `PLAN_TIERS: readonly PlanTier[]`
- `PLANS: Readonly<Record<PlanTier, PlanDefinition>>`, with `limits.templates: number | null`

Pattern source for the new test file — `tests/unit/product-limit.test.ts` (its `EXPECTED_CAP` independent-expectation table, its "no key left unexpected" completeness case, and its `it.each` driven from the canonical key list) and `tests/unit/template-distinctiveness.test.ts:291-309` (the per-segment loop with a vacuity guard and an argued `expect(value, message)` second argument).
</interfaces>

<notes_for_the_verifier>
1. **ROADMAP success criterion 3's "locked templates stay visible-but-dimmed" is true of `/onboarding/branding` and not of the dashboard themes browser.** `src/app/(dashboard)/dashboard/storefront/page.tsx:98-127` builds tiles from the accessible set plus the merchant's own current key, deliberately. That is a pre-existing Phase 5 / 05.3 design divergence this phase neither introduces nor fixes. Verify criterion 3 against `/onboarding/branding`.
2. **Do not run `npm run templates:previews`.** Previews are keyed by template key, not tier; nothing in this phase invalidates one, and the script re-renders 50 storefronts and re-uploads to R2.
3. **D-06 superseded D-01's open question.** 07-RESEARCH.md § Assumption A1 and § Pitfall 3 asked for a `checkpoint:human-verify` in front of the registry edit because the option was undecided. CONTEXT.md D-06 has since decided it (Option D, exact key lists). The confirmation is not dropped — it moves to plan 07-03's phase-gate checkpoint, where the resulting per-segment table is shown to the user, and Task 1's frozen table is the reviewable artifact in the meantime.
</notes_for_the_verifier>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the D-03 and D-02 invariants before the registry moves (RED)</name>

  <files>tests/unit/template-tier-allocation.test.ts, tests/unit/template-distinctiveness.test.ts</files>

  <read_first>
    - tests/unit/product-limit.test.ts (whole file — the structural twin to copy skeleton and reasoning voice from)
    - tests/unit/template-distinctiveness.test.ts lines 275-395 (rules 5, 6 and 7)
    - .planning/phases/07-trial-template-tier-business-rules/07-PATTERNS.md § NEW tests/unit/template-tier-allocation.test.ts
    - The `<expected_min_tier_table>` and `<the_two_triples>` blocks above
  </read_first>

  <behavior>
    - Every one of the 50 `TEMPLATE_KEYS` appears in `EXPECTED_MIN_TIER` and vice versa — a 51st template, or a removed one, is a red build
    - `TEMPLATES[key].minTier` equals `EXPECTED_MIN_TIER[key]` for all 50 keys
    - The `minTier` population across the 50 entries is exactly 15 starter / 17 business / 18 professional
    - Every `INDUSTRY_SEGMENT` has at least 2 Starter-accessible templates
    - Every `INDUSTRY_SEGMENT` has at least 1 template whose `minTier` is `"professional"`
    - `accessibleTemplateKeys` returns 15 / 32 / 50 for starter / business / professional, and the three sets stay strictly nested
    - `PLANS.starter.limits.templates === 15`, `PLANS.business.limits.templates === 32`, `PLANS.professional.limits.templates === null`
  </behavior>

  <action>
    Write this task **before** editing `src/server/theming/registry.ts`, so the expectation table is authored from the pre-change registry plus the D-06 decision rather than read back out of the code it is supposed to check. Reading the expectation out of the object under test would make the assertion tautological — the same argument `product-limit.test.ts`'s `EXPECTED_CAP` header makes.

    **Create `tests/unit/template-tier-allocation.test.ts`**, modelled structurally on `tests/unit/product-limit.test.ts`:
    - Open with a header comment in this codebase's register: cite `TMPL-04`, `D-03` and `D-02`, state that the table is a snapshot of the *decision* rather than of the code, state that every one of the 12 Phase 7 moves was downward (no key's `minTier` rank increased), and explain the file's relationship to `tests/unit/template-distinctiveness.test.ts` — it **raises** that file's per-segment Starter floor from at-least-1 to at-least-2 and adds a Professional-only floor, rather than duplicating it. `product-limit.test.ts`'s header explains its relationship to `tests/isolation/catalog.test.ts` the same way; match that voice.
    - Declare `const EXPECTED_MIN_TIER: Readonly<Record<TemplateKey, PlanTier>>` with all 50 entries written out literally, in `TEMPLATE_KEYS` declaration order, using the `post-change` column of the `<expected_min_tier_table>` block above.
    - Completeness case: assert `[...TEMPLATE_KEYS].sort()` deep-equals `Object.keys(EXPECTED_MIN_TIER).sort()`, with a comment stating that a 51st template must be added to `EXPECTED_MIN_TIER` before this file will pass — that is the whole point of driving the cases from `TEMPLATE_KEYS`.
    - Per-key case: `it.each(TEMPLATE_KEYS)` asserting `TEMPLATES[key].minTier` equals `EXPECTED_MIN_TIER[key]`.
    - Totals case: count the `minTier` population across all 50 and assert exactly 15 starter / 17 business / 18 professional. Use the **per-tier** triple here, not the cumulative one.
    - D-02 segment-balance case: copy the loop shape from `template-distinctiveness.test.ts:291-309` verbatim, including the vacuity guard on a non-empty `accessibleTemplateKeys("starter")` and the long argued `expect(value, message)` second argument. Assert per `INDUSTRY_SEGMENT`: at least 2 Starter-accessible templates, and at least 1 template with `minTier === "professional"`. Each message must explain what breaks in the product when it fails — a segment with no Professional-only template means a Professional merchant in that industry gets nothing their Business-tier competitor cannot also have, which is the exact outcome D-02 exists to catch. A bare `.toBe(true)` is out of register for this codebase.

    **Edit `tests/unit/template-distinctiveness.test.ts`** (rule 7 block):
    - L347's triple `10, 25, 50,` becomes `15, 32, 50,` — this is the **cumulative** triple.
    - L330's test name `"keeps the tier sets nested and the cumulative counts at 10/25/50"` becomes `"…at 15/32/50"`. This name is a documented `-t` filter target in 07-VALIDATION.md; record the rename in the SUMMARY so the validation map can be updated.
    - **Do not weaken or remove** L353-354's `expect(starter.size).toBe(PLANS.starter.limits.templates)` pair or L355-359's `professional.limits.templates` null assertion. They are the mechanism that turns a forgotten `plans.ts` edit into a red test instead of silent drift.
    - Rules 5 and 6 (L291-326) need no edit — every move is downward, so no segment loses a Starter template and the Starter-accessible distinct-structure count can only rise.

    Use two-space indent and double quotes; no Prettier.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/template-tier-allocation.test.ts tests/unit/template-distinctiveness.test.ts --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `tests/unit/template-tier-allocation.test.ts` exists and FAILS, with failures naming specific template keys whose `minTier` does not yet match (a RED-for-the-right-reason commit).
    - `tests/unit/template-distinctiveness.test.ts` FAILS on the `[15, 32, 50]` triple.
    - `node -e "const s=require('fs').readFileSync('tests/unit/template-tier-allocation.test.ts','utf8');console.log((s.match(/\"[a-z0-9-]+\":\s*\"(starter|business|professional)\"/g)||[]).length)"` prints `50`.
    - `grep -c "EXPECTED_MIN_TIER" tests/unit/template-tier-allocation.test.ts` is at least 4 (declaration, completeness case, per-key case, header mention).
    - `grep -n "TEMPLATE_KEYS" tests/unit/template-tier-allocation.test.ts` shows the per-key cases are driven from `TEMPLATE_KEYS`, not from a hand-typed key list.
    - `grep -n "limits.templates" tests/unit/template-distinctiveness.test.ts` still returns the three lockstep assertions, unweakened.
    - `git diff --stat src/` shows zero changes — no source file was touched in this task.
    - `npm run lint && npm run typecheck` both pass (a failing test still has to compile and lint clean).
  </acceptance_criteria>

  <done>The D-03 frozen table and the D-02 segment floors exist as executable assertions, both red, with no source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Move 12 templates down a tier and bring the documented catalog sizes with them (GREEN)</name>

  <files>src/server/theming/registry.ts, src/server/entitlements/plans.ts, src/server/theming/access.ts</files>

  <read_first>
    - src/server/theming/registry.ts lines 1-60 (the file's own header, including its NO USER-FACING PROSE rule) and the 12 entries named in `<verified_state>`
    - src/server/entitlements/plans.ts lines 130-235 (the `limits.templates` doc comment and all three tier blocks)
    - src/server/theming/access.ts lines 1-45
    - The `<the_two_triples>` block above — `access.ts` takes the per-tier triple, `plans.ts` takes the cumulative one
  </read_first>

  <action>
    Apply exactly the 12 `minTier` value changes from D-06 / Option D listed in `<verified_state>`, plus the two lockstep numeric edits in the plan registry, and nothing else.

    **`src/server/theming/registry.ts` — 12 one-word value edits.** For each of `fashion-studio`, `electronics-pulse`, `beauty-radiance`, `grocery-pantry`, `retail-bazaar`, set `minTier` to `"starter"`. For each of `fashion-classic`, `fashion-muse`, `electronics-signal`, `beauty-veil`, `beauty-satin`, `grocery-harvest`, `furniture-grain`, set `minTier` to `"business"`. Binding constraints:
    - Only the `minTier` line changes per entry. Do not touch `key`, `segment` or `sections`.
    - **Do not touch the `TEMPLATE_KEYS` array.** Its declaration order is load-bearing for `accessibleTemplateKeys`' return order, for the within-segment tiebreaker in `onboarding/branding/page.tsx`, and for `template-preview-manifest.test.ts`'s manifest ordering. A diff line inside that block is a defect. CONTEXT.md left the "reorder or not" question to discretion — the answer is **only the `minTier` field per entry**.
    - Add no trailing `// was "business"` comments. The file's own header scopes it to data; the record of what moved and why lives in Task 1's frozen table, where it is machine-checked, and in the SUMMARY.
    - Change no other entry's `minTier`. The remaining 38 stay exactly as they are.

    **`src/server/entitlements/plans.ts` — the documented catalog size, in lockstep.** Set `PLANS.starter.limits.templates` (L202) to `15` and `PLANS.business.limits.templates` (L216) to `32`. Leave `PLANS.professional.limits.templates` as `null` — "all of them", never a count that could drift from the registry's real size. These are the **cumulative** numbers: 32 is 15 + 17. Then update the doc comment at L137-141 so `Business's 25 is Starter's 10 plus 15 more` reads `Business's 32 is Starter's 15 plus 17 more`, keeping the "not a per-tier increment" clause — it is exactly the misreading `32` invites.

    Do not introduce any read of `limits.templates` as a gate. That comment block warns twice, in all caps, `DO NOT READ THIS VALUE AT A CALL SITE`; the gate stays `accessibleTemplateKeys` / `canUseTemplate`. Gating on the count would make the reachable set depend on declaration order.

    **`src/server/theming/access.ts` — comment only.** L13's `TMPL-04's 10/15/25 split is enforced HERE` becomes `15/17/18`. That line describes the **per-tier `minTier` population**, not the cumulative reachable counts — do not write `15/32/50` there. `canUseTemplate`, `accessibleTemplateKeys`, `assertTemplateAccess` and `PLAN_TIER_RANK` are pure functions of `minTier` and must not change; any non-comment diff line in this file is a defect.
  </action>

  <verify>
    <automated>npm run test:unit && npm run lint && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep -o 'minTier: "[a-z]*"' src/server/theming/registry.ts | sort | uniq -c` reports exactly `15 minTier: "starter"`, `17 minTier: "business"`, `18 minTier: "professional"`.
    - `npx vitest run tests/unit/template-tier-allocation.test.ts` is GREEN — all 50 keys match the frozen table, the totals case passes, and every segment clears the ≥2 Starter / ≥1 Professional-only floor.
    - `npx vitest run tests/unit/template-distinctiveness.test.ts` is GREEN, including the `[15, 32, 50]` triple and the `PLANS.*.limits.templates` lockstep pair.
    - `grep -n "templates:" src/server/entitlements/plans.ts` shows `templates: 15,`, `templates: 32,` and `templates: null,`.
    - `git diff src/server/theming/registry.ts | grep -c "^[-+]"` is exactly `26` (12 removed `minTier` lines, 12 added, and the two `---`/`+++` header lines) — i.e. nothing but `minTier` values moved.
    - `git diff src/server/theming/registry.ts | grep -E "^[-+]" | grep -v "minTier" | grep -vE "^(---|\+\+\+)"` returns nothing — no `TEMPLATE_KEYS` line, no `sections` line, no comment.
    - `git diff src/server/theming/access.ts | grep -E "^[-+]" | grep -vE "^(---|\+\+\+)" | grep -vE "^[-+]\s*(\*|//)"` returns nothing — comment-only diff.
    - `grep -n "10/15/25\|10/25/50" src/server/theming/access.ts src/server/entitlements/plans.ts` returns nothing.
    - `npm run test:unit` green overall, including `tests/unit/template-preview-manifest.test.ts` and `tests/unit/theming-registry.test.ts` (both tier-agnostic — a failure there means declaration order or entry shape was disturbed).
    - `npm run lint` and `npm run typecheck` both pass.
  </acceptance_criteria>

  <done>A Starter merchant reaches 15 templates, a Business merchant 32, a Professional merchant all 50; the enforced set and the documented count agree; and the registry diff contains nothing but 12 `minTier` values.</done>
</task>

<task type="auto">
  <name>Task 3: Make every stated tier figure and every tier-refusal fixture match the new split</name>

  <files>src/lib/strings/index.ts, src/lib/strings/marketing.ts, tests/isolation/template-switch.test.ts, tests/isolation/onboarding-template.test.ts</files>

  <read_first>
    - src/lib/strings/index.ts lines 285-335 (the three plan feature lists)
    - src/lib/strings/marketing.ts lines 20-70 (module header and the templates section)
    - tests/isolation/template-switch.test.ts lines 335-380 and 520-545
    - tests/isolation/onboarding-template.test.ts lines 395-425
    - .planning/phases/07-trial-template-tier-business-rules/07-RESEARCH.md § 3e (the green-but-false trap)
  </read_first>

  <action>
    Two halves, both of which stay green on the test runner if skipped — which is exactly why they are an explicit task rather than something to be caught by `npm run test:unit`.

    **Half one — the four copy sites.** Work from the `<verified_state>` table, not from a search-and-replace on "10". Live traps confirmed in-repo: `aspect-[16/10]` is a preview aspect ratio, `"Up to 10 staff accounts"` is a seat count, `src/lib/strings/index.ts:5`'s "30-day solo" is the project timeline, and `tests/unit/product-limit.test.ts:27`'s `"Business 250"` is a product cap.
    - `src/lib/strings/index.ts:292` (`strings.plan.starter.features[1]`) to `"15 templates with your logo, brand colors and basic sections"`.
    - `src/lib/strings/index.ts:310` (`strings.plan.business.features[1]`) to `"32 templates reachable (Starter's 15, plus 17 more)"` — cumulative reachable total, then the per-tier increment, exactly as the current string is shaped.
    - `src/lib/strings/index.ts:329` (`strings.plan.professional.features[1]`, `"All 50 templates reachable"`) — **unchanged**. The 50 total does not move.
    - `src/lib/strings/marketing.ts:66` (`strings.root.templates.constraint`) — the counts clause becomes `"Starter reaches 15 templates, Business 32, Professional all 50."`. Plan 07-01 already set the trailing trial clause to 30 days; leave that half alone.
    - `src/lib/strings/marketing.ts:29` (module header) — the counts half becomes `15/32/50 templates per tier`. Plan 07-01 already set the trial half. Keep the "matches `PLANS` exactly" clause; this edit is what re-satisfies it.
    - `strings.root.templates.heading` (`"Fifty templates. Six industries. Not one of them generic."`) — unchanged.
    - Zero `.tsx` files in this task's diff. One means a user-facing literal was inlined, violating CLAUDE.md and the prose-literal contract tests.

    **Half two — restore the two refusal tests to actually refusing a Professional key.** Both currently use `fashion-classic` as their Professional-tier example. After Task 2, `fashion-classic` is Business-tier, so both tests keep passing against a Starter fixture while no longer testing the tier their names claim. Swap the fixture key, do not merely edit the comment:
    - `tests/isolation/template-switch.test.ts:373` — replace the `fashion-classic` fixture key with `retail-district` (Professional today, Professional under Option D, referenced by no other test — confirmed at plan time), and update the adjacent comment to say `"retail-district" is professional-tier (registry.ts) — out of reach for a Starter merchant`.
    - `tests/isolation/onboarding-template.test.ts:404` and its L415 comment — same swap to `retail-district`, keeping the test name `"refuses a Professional key from a Starter-tier organization"` now that it is true again.
    - `tests/isolation/template-switch.test.ts:532` — **comment-only** fix: `"fashion-studio" is business-tier` becomes `"fashion-studio" is starter-tier — reachable by this fixture's tier`. The key stays; it is still reachable, and the assertion is still correct.

    This preserves the security property these two files exist to pin: `assertTemplateAccess` refuses an out-of-tier write on the server independent of the picker's `locked` flag. A refusal test that no longer exercises a genuine out-of-tier case has quietly stopped guarding it.
  </action>

  <verify>
    <automated>npm run test:unit && npm run lint && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rnE "10/15/25|10/25/50|Starter reaches 10|Business 25([^0-9]|$)|10, 25, 50" src/ tests/` returns zero lines.
    - `grep -n "Starter reaches 15 templates, Business 32, Professional all 50" src/lib/strings/marketing.ts` returns line 66, and the same line still contains `30-day trial`.
    - `grep -n "15/32/50" src/lib/strings/marketing.ts` returns line 29.
    - `grep -n "All 50 templates reachable" src/lib/strings/index.ts` still returns line 329, unchanged.
    - `grep -c "fashion-classic" tests/isolation/template-switch.test.ts tests/isolation/onboarding-template.test.ts` returns 0 for both files.
    - `grep -n "retail-district" tests/isolation/template-switch.test.ts tests/isolation/onboarding-template.test.ts` returns a hit in each, and each is inside a refusal case.
    - `grep -n "fashion-studio" tests/isolation/template-switch.test.ts` shows the surviving comment now says `starter-tier`, not `business-tier`.
    - `git diff --name-only` for this task lists exactly the four files in `<files>` and no `.tsx` file.
    - `npm run test:unit` green, including `tests/unit/landing-page-contract.test.ts` (`BANNED_PHRASES` and the voice contract) and `tests/unit/template-picker-contract.test.ts` (unchanged — the SORT-NEVER-FILTER locked-tile contract).
    - `npm run lint` and `npm run typecheck` both pass.
  </acceptance_criteria>

  <done>Every merchant-facing tier figure reads 15 / 32 / 50, no comment or test name still asserts the old split, and both server-side refusal tests exercise a genuinely Professional-tier key again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merchant client → template-selection Server Action | `assertTemplateAccess(tier, key)` is the server-side refusal; the picker's `locked` flag is presentation only and must never be the gate |
| registry data → access gate | `accessibleTemplateKeys` / `canUseTemplate` read `minTier`; this plan changes the data they read, never the predicate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation of Privilege | `TEMPLATES[key].minTier` | accept | Privilege relaxation is the intended change and is bounded by construction: all 12 moves are downward by exactly one rank, verified by Task 1's frozen table against the pre-change registry. No plan gains a *capability* — `storefrontEditor`, `discountCodes`, `bulkImport`, `members` and `products` are untouched. |
| T-07-05 | Elevation of Privilege | `assertTemplateAccess` refusal tests | mitigate | Task 3 swaps `fashion-classic` for `retail-district` in both refusal tests so they keep exercising a genuine out-of-tier write. Without the swap the tests stay green while guarding nothing (Phase 5 "client-side-only tier gating" pitfall). |
| T-07-06 | Tampering | `PLANS[tier].limits.templates` | mitigate | The documented count is never read as a gate. `tests/unit/template-distinctiveness.test.ts:353-354` cross-checks it against the enforced set size, so a forgotten `plans.ts` edit is a red test, not silent drift. Task 2's acceptance criteria forbid introducing any call-site read of it. |
| T-07-07 | Tampering | `TEMPLATE_KEYS` declaration order | mitigate | Order is load-bearing for `accessibleTemplateKeys`' return order, the onboarding tiebreaker and the preview manifest. Task 2's diff gate forbids any non-`minTier` line in the registry diff; `tests/unit/template-preview-manifest.test.ts` is the independent check. |
| T-07-08 | Repudiation | which 12 templates moved, and why | mitigate | The 12 moves are the one part of this phase a reviewer cannot re-derive from a constant. Task 1's 50-key frozen table is the durable, machine-checked record; plan 07-03's checkpoint shows the resulting per-segment table to the user. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs zero packages and adds nothing to `package.json`. No install task exists, so the slopcheck gate is vacuous here. |
</threat_model>

<verification>
- `npm run test:unit` green, including the new `tests/unit/template-tier-allocation.test.ts`.
- `npm run lint` (`--max-warnings=0`) and `npm run typecheck` green.
- `grep -o 'minTier: "[a-z]*"' src/server/theming/registry.ts | sort | uniq -c` reports 15 / 17 / 18.
- `grep -rnE "10/15/25|10/25/50|Starter reaches 10|Business 25([^0-9]|$)|10, 25, 50" src/ tests/` returns zero lines.
- Zero `.tsx` files in the plan's diff; comment-only diff in `access.ts`; `minTier`-only diff in `registry.ts`.
- The isolation suite (`template-switch.test.ts`, `onboarding-template.test.ts`) is run at plan 07-03's phase gate, which probes `TEST_DATABASE_URL` first.
</verification>

<success_criteria>
- A Starter merchant can open exactly 15 templates, a Business merchant 32, a Professional merchant all 50 (ROADMAP success criterion 3).
- No template's `minTier` rank increased (D-03), proven by the frozen table rather than assumed.
- Every segment clears at least 2 Starter-accessible and at least 1 Professional-only template (D-02), which is the assertion that would have caught the rejected Option A automatically.
- Reintroducing the 10/15/25 split, or silently re-tiering any single template, fails the build (ROADMAP success criterion 4, template half).
- `npm run templates:previews` was NOT run.
</success_criteria>

<output>
Create `.planning/phases/07-trial-template-tier-business-rules/07-02-SUMMARY.md` when done. Record in it: the 12 moved keys with their old and new tiers, the resulting per-segment table, and the `template-distinctiveness.test.ts` test-name rename (a documented `-t` filter target in 07-VALIDATION.md).
</output>
