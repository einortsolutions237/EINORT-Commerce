---
phase: 07-trial-template-tier-business-rules
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [ONB-05, TMPL-04]

must_haves:
  truths:
    - "The full gate — unit suite, lint, typecheck and production build — is green on the combined phase diff, not just on each plan in isolation"
    - "The isolation suite's four ONB-05/TMPL-04 end-to-end assertions either ran and passed, or the gate states explicitly that TEST_DATABASE_URL was unavailable rather than reporting green off the unit suite alone"
    - "A merchant walking /, /signup, /onboarding/plan and /dashboard/plan sees no surface still saying 10 days"
    - "The user has seen the resulting per-segment tier distribution and confirmed the 12 moved keys match D-06"
  artifacts:
    - path: ".planning/phases/07-trial-template-tier-business-rules/07-03-SUMMARY.md"
      provides: "The phase gate record, including the honest isolation-suite availability statement"
  key_links:
    - from: "the phase gate"
      to: "tests/isolation/trial.test.ts"
      via: "npm run test:full, gated on a TEST_DATABASE_URL probe"
      pattern: "TEST_DATABASE_URL"
---

<objective>
Close Phase 7: run the full gate across the combined diff from plans 07-01 and 07-02, report the isolation suite honestly, and put the two things no automated check can see in front of the user — the cross-page copy walkthrough and the 12-key tier reassignment.

Purpose: two of the four ONB-05 assertions live in `tests/isolation/trial.test.ts`, which needs a Neon test branch that `tests/setup/global-setup.ts` fails closed on. And "no surface still says 10 days" is a cross-page consistency claim with no DOM in the unit project. Both are failure modes where a phase reports green while being wrong.

Output: a gate record, and a user-approved confirmation of the copy sweep and the tier distribution.
</objective>

## Phase Goal

**ROADMAP goal, verbatim:** The two Master Spec V3 commercial rules — a 30-day trial and a 15/17/18 template tier split — are true everywhere the product enforces or states them, with no surface still quoting the old numbers.

> See plan 07-01's note on the MVP user-story format discrepancy.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md
@.planning/phases/07-trial-template-tier-business-rules/07-01-SUMMARY.md
@.planning/phases/07-trial-template-tier-business-rules/07-02-SUMMARY.md
@CLAUDE.md

<gate_reference>
| Gate | Command |
|------|---------|
| Unit suite (no DB) | `npm run test:unit` |
| Full suite (needs `TEST_DATABASE_URL`) | `npm run test:full` |
| Lint (`--max-warnings=0`) | `npm run lint` |
| Types | `npm run typecheck` |
| Production build | `npm run build` |
| Dev server for the walkthrough | `npm run dev` → `http://localhost:3001` |

`.env.test` is gitignored and carries `TEST_DATABASE_URL` only; `vitest.config.ts` maps it into `DATABASE_URL`/`DIRECT_URL` so the isolation suite never touches the development database. `tests/setup/global-setup.ts` **fails closed** — an absent value means the isolation suite cannot run at all, with no fallback.

**Do not run `npm run templates:previews`.** Previews are keyed by template key, not tier; nothing in this phase invalidates one, and it re-renders 50 storefronts and re-uploads to R2.
</gate_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase gate and report the isolation suite honestly</name>

  <files>(no source changes — verification only)</files>

  <read_first>
    - .planning/phases/07-trial-template-tier-business-rules/07-01-SUMMARY.md
    - .planning/phases/07-trial-template-tier-business-rules/07-02-SUMMARY.md
    - .planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md (the per-task verification map to fill in)
    - package.json scripts block
  </read_first>

  <action>
    Run the combined-diff gate. This task changes no source; if any gate fails, stop and report the failure rather than patching around it — a fix belongs in the plan that owns the file.

    First, probe the isolation environment before deciding what to claim. Check whether `.env.test` exists and contains a non-empty `TEST_DATABASE_URL`. Record the answer verbatim in the SUMMARY. If present, run `npm run test:full` and report the real result. If absent, run `npm run test:unit` and state explicitly in the SUMMARY that `tests/isolation/trial.test.ts`, `tests/isolation/template-switch.test.ts` and `tests/isolation/onboarding-template.test.ts` did not run, that two of the four ONB-05 assertions and the TMPL-04 server-side refusal assertions are therefore unverified end to end, and that the phase gate is green on the unit suite only. Do not report a green phase gate off `test:unit` without that statement.

    Then run the remaining gates in order: `npm run test:unit`, `npm run lint`, `npm run typecheck`, `npm run build`.

    Then run the diff-shape gates over the combined phase diff (both plans together):
    - Trial sweep: `grep -rniE "(10[- ]day|ten[- ]day|10 days|ten days)" src/ tests/ | grep -v "overview-buckets.test.ts"` must return zero lines. The `overview-buckets.test.ts` exclusion is the analytics bucket window ("Ten days before the window"), unrelated to the trial and confirmed at plan time.
    - Tier sweep: `grep -rnE "10/15/25|10/25/50|Starter reaches 10|Business 25([^0-9]|$)|10, 25, 50" src/ tests/` must return zero lines. The `Business 25` pattern is bounded so it does not match `tests/unit/product-limit.test.ts`'s `"Business 250"` product cap.
    - Tier population: `grep -o 'minTier: "[a-z]*"' src/server/theming/registry.ts | sort | uniq -c` must report 15 starter / 17 business / 18 professional.
    - Copy centralization: the phase diff must contain zero `.tsx` files — `git diff --name-only HEAD~N | grep -c '\.tsx$'` must be `0`, where `N` spans this phase's commits. One `.tsx` file means a user-facing literal was inlined.
    - No migration: `git status --porcelain prisma/migrations/` and the phase diff must show no new migration file (D-05).

    Finally, update `.planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md`'s per-task verification map: replace the `TBD-01`..`TBD-10` plan/wave placeholders with the real plan and wave IDs from 07-01 and 07-02, mark each row's status, tick the Wave 0 checkboxes (both gaps are closed by `tests/unit/template-tier-allocation.test.ts`), and record the `template-distinctiveness.test.ts` test-name change from `"…counts at 10/25/50"` to `"…counts at 15/32/50"` so the documented `-t` filter target stays accurate.
  </action>

  <verify>
    <automated>npm run test:unit && npm run lint && npm run typecheck && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test:unit`, `npm run lint`, `npm run typecheck` and `npm run build` all exit zero.
    - The SUMMARY states, in one explicit sentence, either that `npm run test:full` ran and passed, or that `TEST_DATABASE_URL` was unavailable and which four assertions are therefore unverified end to end.
    - `grep -rniE "(10[- ]day|ten[- ]day|10 days|ten days)" src/ tests/ | grep -v "overview-buckets.test.ts"` returns zero lines.
    - `grep -rnE "10/15/25|10/25/50|Starter reaches 10|Business 25([^0-9]|$)|10, 25, 50" src/ tests/` returns zero lines.
    - `grep -o 'minTier: "[a-z]*"' src/server/theming/registry.ts | sort | uniq -c` reports exactly 15 / 17 / 18.
    - `grep -n "export const TRIAL_DAYS" src/server/entitlements/resolve.ts` prints `= 30;`.
    - The phase diff contains zero `.tsx` files and zero new files under `prisma/migrations/`.
    - `07-VALIDATION.md` has no `TBD` left in its per-task verification map and both Wave 0 checkboxes are ticked.
  </acceptance_criteria>

  <done>Every automated gate is green on the combined phase diff, the isolation suite's availability is stated rather than assumed, and the validation map is filled in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual copy walkthrough and tier-distribution confirmation</name>

  <files>(no changes — user verification only)</files>

  <read_first>
    - .planning/phases/07-trial-template-tier-business-rules/07-02-SUMMARY.md (the 12 moved keys and the resulting per-segment table)
    - .planning/phases/07-trial-template-tier-business-rules/07-VALIDATION.md § Manual-Only Verifications
  </read_first>

  <action>
    Pause and hand the verification below to the user. Before pausing, start the dev server on `http://localhost:3001` with `npm run dev` so the user does not have to. Paste checklists A, B and C into the prompt verbatim, including the exact expected strings, the 12 moved template keys and the per-segment distribution table, so the user can check every item without opening a source file. Make no source edits in this task. If the user reports a discrepancy, record the specific page and string in the SUMMARY and route the fix to the plan that owns that file — 07-01 for trial copy, 07-02 for tier copy and the registry.
  </action>

  <what-built>
    `TRIAL_DAYS` is now 30 and every trial-day string across signup, the landing page and both plan surfaces says 30 days. All 50 templates were re-tiered to 15 Starter / 17 Business / 18 Professional per D-06 (Option D), the documented catalog sizes moved to 15 / 32 / null in lockstep, and every tier-count string now reads 15 / 32 / 50. Two isolation refusal tests were re-pointed at `retail-district` so they still exercise a genuinely Professional-tier key.
  </what-built>

  <how-to-verify>
    Start the dev server with `npm run dev` and open `http://localhost:3001`.

    **A. The copy sweep — no surface still says 10 days.** This is the one verification with no automated equivalent: there is no DOM in the unit test project, and it is a cross-page consistency claim.
    1. `/` — the hero CTA note should read "Free for 30 days. No card required."; the templates section should read "Starter reaches 15 templates, Business 32, Professional all 50. Every plan gets the full 30-day trial first."; the final CTA heading should read "Thirty days. No card. Your own storefront." and its body "a full 30-day trial".
    2. `/signup` — the subline should read "Free for 30 days. No card required."
    3. `/onboarding/plan` — the subline should read "Free for 30 days…", the primary button "Start my 30-day trial", the Starter card "15 templates with your logo, brand colors and basic sections", the Business card "32 templates reachable (Starter's 15, plus 17 more)", and the Professional card "All 50 templates reachable" (unchanged).
    4. `/dashboard/plan` — same `strings.plan.*` namespace as onboarding, so the same figures; confirm nothing on this page still says 10.
    5. Confirm no page anywhere in that walk still shows "10 days", "ten days", "10 templates" or "Business 25".

    **B. The template picker.** Open `/onboarding/branding`. Templates above the merchant's tier must still appear **visible but dimmed**, not hidden — Phase 5's SORT-NEVER-FILTER rule must survive the re-tiering. *Note: the dashboard themes browser at `/dashboard/storefront` deliberately shows only reachable templates plus the merchant's current one. That is a pre-existing Phase 5 / 05.3 design divergence, not a regression introduced here — do not report it as one.*

    **C. The tier reassignment — the one thing a reviewer cannot re-derive from a constant.** Confirm the 12 moved keys and the resulting distribution match D-06:
    - business → starter (5): `fashion-studio`, `electronics-pulse`, `beauty-radiance`, `grocery-pantry`, `retail-bazaar`
    - professional → business (7): `fashion-classic`, `fashion-muse`, `electronics-signal`, `beauty-veil`, `beauty-satin`, `grocery-harvest`, `furniture-grain`
    - resulting per-segment mix (starter / business / professional): fashion-apparel 3/3/2, electronics 3/3/3, beauty-cosmetics 3/3/2, grocery-food 2/3/3, furniture-home 2/3/3, general-retail 2/2/5
    - every segment therefore has at least 2 Starter-accessible and at least 2 Professional-only templates, which is what D-02's sanity check required and what the rejected Option A failed
  </how-to-verify>

  <acceptance_criteria>
    - The user confirms every page in walkthrough step A shows the 30-day and 15 / 32 / 50 figures and that no surface still shows an old number.
    - The user confirms tier-locked tiles at `/onboarding/branding` are visible-but-dimmed, not filtered out.
    - The user confirms the 12 moved keys and the per-segment distribution match D-06.
    - Any discrepancy the user reports is recorded in the SUMMARY with the specific page and string, and routed back to the plan that owns that file rather than patched here.
  </acceptance_criteria>

  <resume-signal>Type "approved", or describe the specific page and string that is still wrong.</resume-signal>

  <done>The user has walked all four copy surfaces plus the onboarding picker and approved both the copy sweep and the tier distribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase gate → milestone record | A phase reported green on an unrun isolation suite would let an unverified server-side refusal reach the next phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | the phase gate report | mitigate | Task 1 probes `TEST_DATABASE_URL` before claiming anything and must state explicitly which assertions did not run. `tests/setup/global-setup.ts` fails closed, so a missing value cannot silently degrade to the dev branch. |
| T-07-10 | Information Disclosure | merchant-facing copy | mitigate | Task 2's walkthrough is the only check that can see a stale figure rendered on a page; the automated greps cover source text, not composition. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs zero packages. Verification only. |
</threat_model>

<verification>
- All four automated gates exit zero.
- The isolation suite either passed or its unavailability is stated in the SUMMARY.
- Both trial and tier grep sweeps return zero lines.
- The user has approved the manual walkthrough.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (a 30-day trial, and no surface still saying 10) is demonstrated in a browser, not inferred from source.
- ROADMAP success criterion 2 is satisfied by one stated rule: every org with a null `trialEndsAt` is on 30 days from its own `createdAt`, derived at read time, with no migration and no dual-rate logic (D-04 / D-05). The support-override population is documented as an accepted exception in 07-01-SUMMARY.md.
- ROADMAP success criterion 3 is verified against `/onboarding/branding`, with the dashboard themes browser divergence recorded as pre-existing.
- ROADMAP success criterion 4 holds: `npm run test:unit` fails if `TRIAL_DAYS` returns to 10, if the reachable triple returns to 10/25/50, or if any single template is silently re-tiered.
</success_criteria>

<output>
Create `.planning/phases/07-trial-template-tier-business-rules/07-03-SUMMARY.md` when done. It is the phase gate record: gate results, the explicit isolation-suite availability statement, the user's walkthrough approval, and any discrepancy found with the plan it was routed to.
</output>
