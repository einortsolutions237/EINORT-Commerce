---
phase: 05.3-storefront-editor-page-split
plan: 03
subsystem: ui
tags: [nextjs, app-router, rsc, theming, react-server-components, template-picker]

# Dependency graph
requires:
  - phase: 05.3-01
    provides: "themesPageHeading/customizeButton/templateSwitchedToast strings, switchTemplate's revalidatePath argument update, shared infrastructure for the storefront editor page split"
provides:
  - "A dedicated /dashboard/storefront Themes RSC (page.tsx) and loading skeleton"
  - "ThemesBrowser (renamed from ChangeTemplatePanel), page-adapted: no back row, no rail padding, R-3 two-column spotlight, toast-on-success instead of an onSwitched callback"
  - "CurrentTemplateCard widened with an optional sizes prop (default 320px, unchanged for any other caller)"
affects: ["05.3-02", "05.3-04"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RSC self-authorization: requireMerchantContext() as the first statement of every dashboard page, never inherited from (dashboard)/layout.tsx"
    - "Second idempotent ensureStorefrontSeeded entry point on a page other than the combined editor, for pre-Phase-4 (unseeded) tenants"
    - "toast.success with an action button replacing a lifted-state callback, for a page with no parent shell to hand state to"

key-files:
  created:
    - "src/app/(dashboard)/dashboard/storefront/page.tsx"
    - "src/app/(dashboard)/dashboard/storefront/loading.tsx"
    - "src/app/(dashboard)/dashboard/storefront/themes-browser.tsx"
  modified:
    - "src/components/theming/current-template-card.tsx"

key-decisions:
  - "editor-shell.tsx's dangling import of the now-moved change-template-panel.tsx was left untouched — it is explicitly out of this plan's files_modified scope and is 05.3-02's responsibility per 05.3-04-PLAN.md's own framing (\"05.3-02 moved the Editor\"); full-repo typecheck/build is expected green only after 05.3-02 and 05.3-03 are merged, verified by the wave-3 phase gate (05.3-04)."

patterns-established:
  - "Themes page fetches exactly three props (tiles, currentTemplateKey, canEditStorefront) plus a fresh ensureStorefrontSeeded call — no payment settings, no product count, no preview-iframe triple."

requirements-completed: [EDIT-02, EDIT-03]

# Metrics
duration: ~25min (this continuation session; prior session's uncommitted draft work was reviewed, verified against acceptance criteria, and completed/committed rather than redone)
completed: 2026-09-08
---

# Phase 05.3 Plan 03: Themes Page (Storefront Editor Split) Summary

**New `/dashboard/storefront` Themes RSC with a spotlighted current-template card in a width-capped two-column row, a switchable template grid, and a toast-driven "Customize theme" handoff to the Editor — `ChangeTemplatePanel` retired and reborn as `ThemesBrowser`.**

## Performance

- **Duration:** ~25 min (continuation of a prior rate-limited run; no commits existed on this branch at start)
- **Started:** 2026-09-08 (continuation session start)
- **Completed:** 2026-09-08T20:48:43Z
- **Tasks:** 2/2 completed
- **Files modified:** 5 (2 created new, 1 moved+rewritten, 1 modified, plus a deferred-items.md note)

## Accomplishments
- Created the Themes RSC (`storefront/page.tsx`): self-authorizes via `requireMerchantContext()` as its first statement, calls `ensureStorefrontSeeded` as a genuinely new second entry point for pre-Phase-4 tenants, and fetches exactly the three props `ThemesBrowser` needs — no payment settings, no product count, no preview-iframe triple.
- Created the matching `loading.tsx` skeleton, shape-matched to the final two-region layout (two-column spotlight skeleton + 3-up grid of 6 card skeletons).
- Moved and rewrote `change-template-panel.tsx` into `storefront/themes-browser.tsx` as `ThemesBrowser`: dropped `onBack`/`onSwitched`, deleted the back row and rail padding, promoted the panel title to the page's `<h1>`, composed the R-3 two-column spotlight (`CurrentTemplateCard` + name + "Customize theme" button), and replaced the parent-callback pattern with `toast.success` + a `router.push` action.
- Widened `CurrentTemplateCard` with an optional `sizes` prop (default `"320px"`, the existing rail value unchanged for any other caller), used by `ThemesBrowser` as `"(min-width: 768px) 352px, 100vw"`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the Themes RSC and its loading skeleton** - `41e7468` (feat) — also carries the pure `git mv` of `change-template-panel.tsx` -> `themes-browser.tsx`, which was already staged in the index from the prior interrupted run before this session began (see Deviations).
2. **Task 2: Move and rewrite change-template-panel.tsx into themes-browser.tsx; widen current-template-card.tsx** - `b9ad705` (feat) — the actual `ThemesBrowser` rewrite content and the `CurrentTemplateCard` `sizes` prop.

## Files Created/Modified
- `src/app/(dashboard)/dashboard/storefront/page.tsx` - New Themes RSC: `requireMerchantContext`, `ensureStorefrontSeeded`, `editorTemplateTiles` (moved verbatim), renders `ThemesBrowser` with three props
- `src/app/(dashboard)/dashboard/storefront/loading.tsx` - New Suspense skeleton matching the final two-region shape
- `src/app/(dashboard)/dashboard/storefront/themes-browser.tsx` - `ChangeTemplatePanel` renamed `ThemesBrowser`, page-adapted (no back row, no rail padding, R-3 spotlight, toast-on-success)
- `src/components/theming/current-template-card.tsx` - Added optional `sizes?: string` prop (default `"320px"`)

## Decisions Made
- Left `editor-shell.tsx`'s broken import to `./change-template-panel` unfixed — it is out of this plan's scope (not in `files_modified`) and is `05.3-02`'s explicit responsibility per `05.3-04-PLAN.md`. See Deviations and `deferred-items.md`.
- The `git mv` of `change-template-panel.tsx` -> `themes-browser.tsx` ended up bundled into the Task 1 commit rather than Task 2's, because it was already staged in git's index from the prior interrupted executor run before this session started; unstaging and re-splitting it purely to match the plan's task numbering exactly would have required an unnecessary reset. The net diff and file-by-file attribution are correct; only which commit carries the zero-content-change rename differs from a from-scratch execution.

## Deviations from Plan

### Auto-fixed Issues

None — the uncommitted draft work found at session start already matched the plan's Task 1 and Task 2 acceptance criteria; it was verified line-by-line against the plan (not assumed complete) and required no bug fixes.

### Documented, Not Fixed (Rule 4 boundary — explicitly out of scope)

**1. `editor-shell.tsx` imports the now-relocated `./change-template-panel`**
- **Found during:** Full verification (`npm run typecheck`, `npm run build`)
- **Issue:** `src/app/(dashboard)/dashboard/storefront-editor/editor-shell.tsx` (not in this plan's `files_modified`) still imports `ChangeTemplatePanel`/`TemplateSwitchedState` from the relative path this plan's Task 2 moved away, and still wires a `"changeTemplate"` rail target with `onBack`/`onSwitched` props `ThemesBrowser` no longer has. This breaks `npm run typecheck` and `npm run build` for the whole repo when `05.3-03` is evaluated on its own.
- **Why not auto-fixed:** Retiring the "change template" rail entry point (and the rest of `storefront-editor/`) is `05.3-02`'s explicit responsibility, per `05.3-04-PLAN.md`'s own framing ("`05.3-01` built the shared infrastructure, `05.3-02` moved the Editor, `05.3-03` built the Themes page"). `05.3-02` is running in parallel in a separate worktree/branch; editing `editor-shell.tsx` here risks conflicting with or duplicating work already properly scoped and in progress there, and doing it correctly requires context (05.3-02's actual rail-removal design) this plan does not have.
- **Verification of correct attribution:** `npm run build` fails with exactly one Turbopack error, pointing at `editor-shell.tsx:32` (`Module not found: Can't resolve './change-template-panel'`) — no other error. `npm run lint` and the three targeted `vitest` files, plus the full `npm run test:unit` suite (664/664), all pass clean, confirming the failure is isolated to this one cross-plan boundary file.
- **Files NOT modified:** `src/app/(dashboard)/dashboard/storefront-editor/editor-shell.tsx` (left as-is, intentionally)
- **Documented in:** `.planning/phases/05.3-storefront-editor-page-split/deferred-items.md`, for `05.3-04` (the wave-3 phase gate, `depends_on: ["05.3-02", "05.3-03"]`) and the orchestrator's post-merge reconciliation.

**2. Pre-existing, unrelated `tsc` errors for `@/assets/brand/einort-logo.png`**
- **Found during:** `npm run typecheck`
- **Issue:** `src/app/login/page.tsx`, `src/app/signup/page.tsx`, `src/app/page.tsx`, `src/components/app-sidebar.tsx` all fail to resolve `@/assets/brand/einort-logo.png` under `tsc`, even though the asset exists on disk and is tracked in git.
- **Why not auto-fixed:** None of these four files are touched by this plan; this is a pre-existing, unrelated environment/type-declaration issue (`npm run build`'s Turbopack pass and `npm run lint` do not surface it at all). Out of scope per the executor's scope-boundary rule.
- **Documented in:** `.planning/phases/05.3-storefront-editor-page-split/deferred-items.md`

---

**Total deviations:** 0 auto-fixed; 2 documented-and-deferred (both explicitly out of this plan's scope — one a known cross-plan integration boundary with `05.3-02`, one a pre-existing unrelated environment issue).
**Impact on plan:** None on this plan's own deliverables — all of `05.3-03`'s Task 1 and Task 2 acceptance criteria, plus the plan's own scoped verification commands, pass. Repo-wide `typecheck`/`build` green is contingent on `05.3-02` landing, by this phase's own wave-2-parallel/wave-3-gate design.

## Issues Encountered
None beyond the two documented-and-deferred items above.

## Verification Results

- `npm run typecheck` — **fails**, solely on `editor-shell.tsx`'s dangling import (deviation 1) plus the four pre-existing, unrelated logo-asset errors (deviation 2). No error originates in any file this plan modified or created.
- `npm run lint` (`--max-warnings=0`) — **exit 0**, clean.
- `npx vitest run tests/unit/template-picker-contract.test.ts tests/unit/theming-marker-boundary.test.ts tests/unit/surface-token-isolation.test.ts` — **exit 0**, 3 files / 25 tests passed.
- `npm run test:unit` (full suite) — **exit 0**, 40 files / 664 tests passed.
- `npm run build` — **fails**, Turbopack reports exactly one error: `editor-shell.tsx:32` `Module not found: Can't resolve './change-template-panel'` (deviation 1). No other error.
- `test ! -f "src/app/(dashboard)/dashboard/storefront-editor/change-template-panel.tsx"` — **succeeds** (file moved).
- `src/components/theming/template-picker.tsx` byte-unchanged — **confirmed**, `git diff --quiet -- src/components/theming/template-picker.tsx` exits 0.
- All Task 1 acceptance-criteria greps (`editorTemplateTiles` present, `ensureStorefrontSeeded` count 1, forbidden props absent, `requireMerchantContext` count 1, `max-w-5xl` in loading.tsx) — **confirmed**.
- All Task 2 acceptance-criteria greps (`export function ThemesBrowser` count 1, `onBack|onSwitched` count 0, `ChevronLeft|railBack` count 0, `px-4 py-6` count 0, `toast.success` present, `TemplateSwitchedState` retained, R-3 grid class present, `sizes?` on `CurrentTemplateCard`) — **confirmed**.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The Themes half of the split (`/dashboard/storefront`) is functionally complete and independently verified (lint, targeted + full unit suites all green).
- `05.3-02` (the Editor half, in a parallel worktree) must land and retire `editor-shell.tsx`'s `"changeTemplate"` rail target and its import of the now-moved panel before repo-wide `typecheck`/`build` will be green — this is the expected, by-design state of two wave-2-parallel plans prior to merge, not a defect in this plan's work.
- `05.3-04` (wave 3, phase gate) is where the merged, full-repo green build and the dead-route grep sweep should be verified — see `.planning/phases/05.3-storefront-editor-page-split/deferred-items.md` for the specific item to check off there.

---
*Phase: 05.3-storefront-editor-page-split*
*Completed: 2026-09-08*

## Self-Check: PASSED

All created/modified files confirmed present on disk; both task commits (`41e7468`, `b9ad705`) confirmed present in `git log`.
