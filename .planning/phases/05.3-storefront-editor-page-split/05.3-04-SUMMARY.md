---
phase: 05.3-storefront-editor-page-split
plan: 04
subsystem: testing
tags: [grep-gate, vitest, nextjs, phase-gate, storefront-editor]

# Dependency graph
requires:
  - phase: 05.3-storefront-editor-page-split (plan 01)
    provides: "shared infrastructure — strings.editor keys, sidebar href repoint, dual revalidatePath fix"
  - phase: 05.3-storefront-editor-page-split (plan 02)
    provides: "the Editor moved to /dashboard/storefront/editor with a dirty-draft leave guard"
  - phase: 05.3-storefront-editor-page-split (plan 03)
    provides: "the new /dashboard/storefront Themes page"
provides:
  - "Zero-tolerance dead-route grep gate passing across src/ and tests/ (Pitfall 1's binding check)"
  - "D-B zero-new-capability scope-creep verification: no add/remove-section, no undo/redo, no Header/Template/Footer grouping anywhere in the diff; src/lib/editor/reducer.ts and tests/unit/editor-reducer.test.ts confirmed byte-unchanged"
  - "Full automated gate green in documented order: build -> typecheck -> lint -> test:unit -> targeted security regression test"
affects: ["05.3-04 Task 3 (pending — human checkpoint)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Comments that need to reference an intentionally-unrenamed file by name (e.g. tests/isolation/storefront-editor.test.ts, which is NOT part of a route migration) should describe the file's role in prose rather than spell out its exact literal filename, so a route-retirement grep gate targeting the same substring doesn't false-positive on an unrelated, correctly-named file."

key-files:
  created: []
  modified:
    - "src/app/(dashboard)/dashboard/storefront/loading.tsx"
    - "src/app/(dashboard)/dashboard/storefront/page.tsx"
    - "tests/isolation/branding.test.ts"
    - "tests/isolation/onboarding-template.test.ts"
    - "tests/isolation/template-migration-safety.test.ts"
    - "tests/isolation/template-switch.test.ts"

key-decisions:
  - "The dead-route grep gate is deliberately literal and zero-tolerance per the plan's own instruction ('Do not suppress the grep with an exclusion'). Six hits in isolation-test comments referencing tests/isolation/storefront-editor.test.ts by its exact hyphenated filename were rewritten to describe the file's role ('the theming editor isolation suite') instead of naming it literally — the file itself is untouched and was never part of this phase's route migration per 05.3-RESEARCH.md's own test inventory (no-edit), but the literal substring still tripped the gate as specified, so the comments were reworded rather than the gate being weakened."
  - "railBack (strings.editor.railBack) was NOT deleted — settings-panel.tsx:190 is a live .tsx consumer (the Editor rail's back-to-sections affordance), which the plan's Task 1(f) explicitly instructs to leave in place when any .tsx file still references it."
  - "Isolation tests (tests/isolation/template-switch.test.ts, tests/isolation/storefront-editor.test.ts) were not run — TEST_DATABASE_URL is not configured in this worktree. Accepted per 05.3-RESEARCH.md's own Assumption A4: no isolation test needed editing this phase, so this is a documented, non-blocking gap, not a skipped requirement."

requirements-completed: [EDIT-02, EDIT-03]

# Metrics
duration: ~40min (Task 1, includes worktree bootstrap: npm install, .env.local copy, Prisma client generation) + Task 2 checkpoint pending 2026-09-08 to 2026-09-13
completed: 2026-09-13
---

# Phase 05.3 Plan 04: Phase Gate Summary

**This plan is now COMPLETE.** Task 1 (the automated grep/D-B/suite gate) executed 2026-09-08, merged and re-verified clean post-merge 2026-09-13 (build/typecheck/lint/664 unit tests/security test all green). Task 2 (`checkpoint:human-verify`, the six-behavior manual UI walkthrough) was approved by the user 2026-09-13: "All 6 check points good." Phase 05.3 is closed; EDIT-02 and EDIT-03 are marked complete in REQUIREMENTS.md.

Dead-route grep gate returns zero across `src/` and `tests/` (six comment rewrites needed, no functional dead references found), D-B's zero-new-capability constraint verified by grep with the reducer and its test byte-unchanged, and the full automated suite — build, typecheck, lint, 664/664 unit tests, and the tenant-substitution security regression test — all green in the documented order.

## Performance

- **Duration:** ~40 min for Task 1 (includes worktree environment bootstrap: `.env.local` copy from main checkout, `npm install`, Prisma client generation via `postinstall`); Task 2 checkpoint open 2026-09-08 to 2026-09-13, approved on resumption
- **Completed:** 2026-09-13 (both tasks)
- **Tasks:** 2/2 (Task 1 auto, complete; Task 2 checkpoint:human-verify, approved by the user 2026-09-13: "All 6 check points good.")
- **Files modified:** 6

## Accomplishments

- **(a) Dead-route grep gate:** `grep -rln "storefront-editor" src/ tests/ --include=*.ts --include=*.tsx | wc -l` returns `0`. No functional references were missed by `05.3-01`/`05.3-02`/`05.3-03` — every hit found was a stale or coincidentally-matching comment, not a live route string, `revalidatePath` call, or href.
- **(b) Directory retirement:** `src/app/(dashboard)/dashboard/storefront-editor/` does not exist at all (confirmed via `test ! -d`). No action needed — `05.3-03`'s `git mv` of the last remaining file emptied it, and git does not track empty directories.
- **(c) D-B scope-creep check:** zero matches for `add-section|addSection|removeSection|remove-section` in `src/app/(dashboard)/dashboard/storefront/`, zero matches for `undo|redo` in `src/lib/editor/reducer.ts` and `editor-shell.tsx`, zero matches for `Header.*Template.*Footer|header-group|footer-group` in `section-list.tsx`. `src/lib/editor/reducer.ts` is confirmed byte-unchanged (`git diff --quiet` exits 0).
- **(d) EditorState hard constraint:** `tests/unit/editor-reducer.test.ts` is confirmed byte-unchanged and passes (23/23 tests) — the six-key `EditorState` shape assertion holds.
- **(e) Full automated suite, in order:** `npm run build` (clean `.next` rebuild, both `/dashboard/storefront` and `/dashboard/storefront/editor` listed in the route table, no dead route) → `npm run typecheck` (clean) → `npm run lint --max-warnings=0` (clean) → `npm run test:unit` (664/664 across 40 files) → `npx vitest run tests/unit/no-tenant-id-param.test.ts` (3/3). Isolation tests skipped (no `TEST_DATABASE_URL` configured) — documented, non-blocking per A4.
- **(f) railBack orphan check:** `railBack` is still referenced by a real `.tsx` file (`settings-panel.tsx:190`) — left in place, not deleted, per the plan's own conditional instruction.

## Task Commits

1. **Task 1: Dead-route grep gate, D-B scope-creep check, full automated suite** - `c2311c5` (fix)
2. **Task 2: checkpoint:human-verify** - no code changes; approved as-is by the user 2026-09-13 against the running `npm run dev` server

## Files Created/Modified

- `src/app/(dashboard)/dashboard/storefront/loading.tsx` - Updated a stale precedent-citation comment from the retired `dashboard/storefront-editor/loading.tsx` path to the new `dashboard/storefront/editor/loading.tsx` location (Pitfall 8)
- `src/app/(dashboard)/dashboard/storefront/page.tsx` - Reworded the header comment's phase-split description to avoid the literal retired-route substring while keeping the same meaning
- `tests/isolation/branding.test.ts` - Reworded a comment naming `storefront-editor.test.ts` by literal filename to describe it as "the theming editor's isolation suite"
- `tests/isolation/onboarding-template.test.ts` - Same rewording
- `tests/isolation/template-migration-safety.test.ts` - Same rewording
- `tests/isolation/template-switch.test.ts` - Same rewording, five occurrences across the file's header and inline comments

## Decisions Made

- **Reworded comments instead of leaving false positives.** The dead-route grep gate is literal and the plan explicitly forbids suppressing it with an exclusion. Six of the eight initial hits were comments in *other* isolation test files naming `tests/isolation/storefront-editor.test.ts` by its exact filename — a file that is NOT part of this phase's route migration (per `05.3-RESEARCH.md`'s own Focus 5 test inventory, it needs "No edit" and stays named as-is). Rather than treat these as an exempt false-positive class (which the plan does not authorize for part (a) — that leniency is explicitly scoped only to part (c)'s D-B check), every comment was reworded to describe the referenced file's role instead of spelling its literal name, so the gate returns a true zero without weakening its enforcement or renaming an intentionally-stable test file.
- **`railBack` left in place**, not deleted — `settings-panel.tsx:190` is a genuine `.tsx` consumer (the Editor's own settings-panel back-to-sections row, unrelated to the retired `ChangeTemplatePanel`), and the plan's Task 1(f) explicitly says to leave it and note the file when any `.tsx` still references it.
- **Isolation tests not run** — no `TEST_DATABASE_URL` / `.env.test` configured in this worktree. This is the documented, accepted gap per `05.3-RESEARCH.md` Assumption A4 ("no isolation test needed editing this phase"), not a skipped acceptance criterion — the plan's own Task 1(e)(5) explicitly provides this fallback.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree environment bootstrap (no committed change)**
- **Found during:** Pre-Task-1 setup
- **Issue:** This worktree had no `node_modules/`, no `.env.local`, and no generated Prisma client — `npm run build`/`typecheck`/`lint`/`test:unit` were all unrunnable.
- **Fix:** Copied `.env.local` from the main checkout (`D:/Maxs/Claude/einort-commerce/.env.local`) into the worktree root; ran `npm install --no-audit --no-fund` (installs exactly what the existing `package-lock.json` specifies — no new or unverified package names), whose `postinstall` hook (`node scripts/prisma-generate.mjs`) generated `src/generated/prisma/` using the real, now-present `.env.local` connection strings.
- **Files modified:** None tracked — `.env.local` (gitignored, copied not created), `node_modules/` and `src/generated/prisma/` (both gitignored, local-only)
- **Verification:** `npm run build`, `typecheck`, `lint`, `test:unit` all subsequently exit 0
- **Committed in:** N/A (no tracked files changed by this fix)

**2. [Rule 1 - Bug] Six comment hits against the literal dead-route grep gate**
- **Found during:** Task 1(a), the grep gate's first run
- **Issue:** Eight hits total: two stale/misleading comments referencing the retired route directly (`loading.tsx`, `page.tsx`), and six comments across four isolation test files naming an unrelated, intentionally-kept file (`tests/isolation/storefront-editor.test.ts`) by its literal filename, which coincidentally matches the grep pattern.
- **Fix:** All eight rewritten — the two route-referencing ones updated to name the correct new path/phrasing; the six filename-referencing ones reworded to describe the file's role in prose instead of spelling its exact name.
- **Files modified:** listed above (6 files)
- **Verification:** `grep -rln "storefront-editor" src/ tests/ --include=*.ts --include=*.tsx | wc -l` returns `0`; `npm run test:unit` still 664/664 (no test assertions touch the reworded comment text)
- **Committed in:** `c2311c5`

---

**Total deviations:** 2 auto-fixed (1 blocking/environment-only, 1 bug — both necessary to satisfy the plan's own literal acceptance criteria)
**Impact on plan:** No scope creep. Both fixes were required to make the plan's stated, binding acceptance criteria pass exactly as written.

## Issues Encountered

None beyond the deviations documented above.

## User Setup Required

None. The user ran `npm run dev` and walked through all six manual UI checks (sidebar routing, template switch without reload, Editor showing the new template, publish toast, dirty-draft leave-guard dialog, responsive layout) and approved: "All 6 check points good."

## Next Phase Readiness

**This plan is DONE.** Task 1 (grep/D-B/suite gate) and Task 2 (`checkpoint:human-verify`) are both complete. Phase 05.3 (Storefront Editor Page Split) is closed — EDIT-02 and EDIT-03 marked complete in REQUIREMENTS.md, ROADMAP.md ticked, STATE.md updated.

---
*Phase: 05.3-storefront-editor-page-split*
*Completed: 2026-09-13*

## Self-Check: PASSED

All 6 modified files confirmed present on disk with the expected edits. Commit `c2311c5` confirmed present in `git log --oneline`. `grep -rln "storefront-editor" src/ tests/ --include=*.ts --include=*.tsx` re-run and confirmed empty. `src/app/(dashboard)/dashboard/storefront-editor/` confirmed absent via `test ! -d`.
