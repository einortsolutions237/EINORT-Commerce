---
phase: 04-theme-section-block-system-flagship-template
plan: 16
type: execute
wave: 7
depends_on: ["04-10", "04-11", "04-13", "04-15"]
files_modified: []
autonomous: false
requirements: [TMPL-01, TMPL-02, EDIT-02]
must_haves:
  truths:
    - "A stranger shown two seeded demo stores says they are different shops and can name three differences"
    - "The flagship home reads as a genuinely designed storefront, not an obviously templated one"
    - "The preview updates as the merchant types on a real device, with no server round trip in the network tab"
    - "Every automated token, link, registry, isolation and entitlement guard is green at the same time"
  artifacts: []
  key_links: []
---

<objective>
Close the phase's two manual gates: the live-preview device pass, and the Design-Distinctiveness Gate that STATE.md named as this phase's standing risk.

Purpose: `.planning/STATE.md` records it directly — *"design-distinctiveness has no objective completion signal — the side-by-side 'would a stranger think these are the same product' check must be built into this phase's definition of done explicitly."* 04-UI-SPEC.md § Design-Distinctiveness Gate is that check, and it must be an explicit blocking checkpoint rather than an implied review. Six of its seven checks are objectively decidable; the seventh is the requirement, and the other six exist so that failing it is diagnosable rather than mysterious.

Output: a verified phase gate. No source files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-VALIDATION.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate check #6 — token hygiene, and the full automated suite in one pass</name>
  <files>(no files modified — this task verifies)</files>
  <read_first>
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § Design-Distinctiveness Gate (check #6's exact pass condition)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-VALIDATION.md § Per-Task Verification Map (the full list of suites that must be green together)
    - tests/unit/surface-token-isolation.test.ts (all five bans, after plan 04-04 added #5)
  </read_first>
  <action>
Run the whole gate in one pass and record every result verbatim in the summary. Do not "fix and
re-run" silently — if something fails, record what failed, fix it, and record that too, because
the summary is the phase's evidence.

The suite:
- `npm run lint`  (`--max-warnings=0`)
- `npm run typecheck`
- `npm run test:unit`
- `npm run test:full`
- `npm run build`

Then check #6's specific assertions, which the suites cover but which the gate names explicitly
so a partial pass is visible. Run each as its own command and record the count:
- zero literal colours under `src/app/s/**`:
  `grep -rnE "#[0-9a-fA-F]{6}|oklch\(|rgb\(|hsl\(" src/app/s --include=*.tsx | grep -vE "^\S+: *(//|\*|/\*)"`
  must be empty
- zero palette utilities under `src/app/s/**`:
  `grep -rnE "(bg|text|border|ring)-(zinc|slate|gray|neutral|stone)-[0-9]" src/app/s --include=*.tsx`
  must be empty
- no `font-heading`, no `gold-accent`, no `--success` under `src/app/s/**`
- no `data-surface="storefront"` outside `src/app/s/`:
  `grep -rn 'data-surface="storefront"' src --include=*.tsx | grep -v "src/app/s/"` must be empty
- no `brand-accent` utility under `src/app/(dashboard)` or `src/components`:
  `grep -rn "brand-accent" "src/app/(dashboard)" src/components --include=*.tsx` must be empty
- no `font-bold` under `src/app/s/**` (weight 700 is deliberately not loaded)

Also confirm the requirement-coverage claims one last time by running the four guards that would
catch a silent regression from any earlier wave:
- `npx vitest run --project unit tests/unit/theming-registry.test.ts`
- `npx vitest run --project unit tests/unit/entitlements.test.ts`
- `npx vitest run --project isolation tests/isolation/model-registry-drift.test.ts`
- `npx vitest run --project isolation tests/isolation/storefront-editor.test.ts`

If any grep returns a match on a comment line only, that is still a finding worth recording — the
bans exempt comments, but a colour value written into a comment in this tree usually means someone
wanted to write it in code.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:full &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run typecheck` exits 0
    - `npm run test:unit` exits 0
    - `npm run test:full` exits 0
    - `npm run build` exits 0
    - All six check-#6 greps return zero non-comment matches, and each command plus its output is recorded in the summary
    - `tests/unit/theming-registry.test.ts`, `tests/unit/entitlements.test.ts`, `tests/isolation/model-registry-drift.test.ts` and `tests/isolation/storefront-editor.test.ts` all pass individually
  </acceptance_criteria>
  <done>Gate check #6 is green and every automated guard in the phase passes in the same run, with the evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The live-preview device pass (EDIT-02 / D-07)</name>
  <files>(no files modified — this task verifies)</files>
  <read_first>
    - .planning/phases/04-theme-section-block-system-flagship-template/04-VALIDATION.md § Manual-Only Verifications (the first row — why this cannot be automated)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § Preview canvas + iframe chrome, § As-you-type, § Starter, post-trial
  </read_first>
  <action>
Present the walkthrough below to the developer and pause. Do NOT mark this task complete on your
own judgement — no test runner can judge "instant," and steps 5 and 10 exercise failure modes a
desktop-only check would miss. Wait for the resume signal, then record the developer's verbatim
response in the summary, including any step they reported as failing.
  </action>
  <what-built>
    The storefront editor at `/dashboard/storefront-editor`: a 320px rail with a fixed five-section
    list and up/down reorder, a settings panel with six field kinds, a publish bar with
    Save / Publish / Discard, and a cross-origin preview iframe pointing at
    `{slug}.{root}/preview` that re-renders from `postMessage` with no server round trip.
  </what-built>
  <how-to-verify>
    Run `npm run dev` (it binds port 3001) and sign in as a merchant who has completed onboarding.
    A real phone or a throttled mobile emulation is preferable for steps 3 and 5 — no test runner
    can judge "instant."

    1. Open `http://localhost:3001/dashboard/storefront-editor`. Confirm the `Storefront` item is
       visible in the sidebar between `Products` and `Orders`, with **no gold badge**.
    2. Confirm the preview pane shows the flagship-shaped skeleton (one tall block, one short band,
       a 2x2 tile grid) and the line *Loading your storefront preview…*, then fades to the live
       storefront. **A bare spinner is a failure.**
    3. Open devtools -> Network and clear it. Select `Hero`, then type into the heading field.
       Expected: the preview headline changes **as you type, with no visible lag**, and the
       Network tab shows **zero new requests** while typing. A request per keystroke is a failure.
    4. Change the primary accent with the colour picker. Expected: the hero CTA fill changes
       immediately, and its LABEL stays readable (white on a dark accent, ink on a pale one).
    5. Use the viewport toggle to switch to Mobile (390px). Expected: the storefront inside the
       iframe renders its MOBILE layout — a 2-column product grid, a stacked editorial split, a
       40px hero headline. If it still renders the desktop 4-column grid, the breakpoints are
       firing against the window rather than the iframe and this check FAILS.
    6. Move `Products` up one position. Expected: the rail order and the preview order change
       together, the live region announces the new position, and the preview scrolls that section
       into view. Confirm the first row's up button and the last row's down button are
       **disabled**, and that pressing them does nothing rather than erroring.
    7. Confirm there is **no add-section and no remove-section control anywhere** — not even a
       disabled one (D-05).
    8. Edit something, then try to navigate away. Expected: the browser's unsaved-changes prompt.
    9. Click `Save`, then `Publish`. Expected: a `Your storefront is live` toast with a
       `View store` action; open it and confirm the published storefront matches what the preview
       showed.
    10. Break the preview deliberately: stop the dev server's ability to serve the subdomain (or
        set `NEXT_PUBLIC_ROOT_DOMAIN` to a wrong value and reload). Expected: after ~10 seconds,
        a **non-destructive** alert reading *The preview didn't load. Your changes are safe — try
        loading it again.* with a `Reload preview` button. Confirm the draft is still intact.
        Restore the setting afterwards.
    11. Confirm the editor chrome uses **only** the blue/gold/slate dashboard palette. The
        merchant's accent must appear **nowhere** on this screen except inside the iframe and in
        the colour field's small sample chip.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all eleven steps pass, or names the failing step numbers and what they saw instead.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer's verbatim response is recorded in the summary
    - Step 3's "zero new requests while typing" result is recorded explicitly (it is the objective proxy for D-07's no-network-in-the-loop claim)
    - Step 5's mobile-breakpoint result is recorded explicitly (it is the objective proxy for the iframe-viewport argument that forced this architecture)
    - Step 10's timeout result is recorded explicitly, including whether the draft survived
    - No step is marked passing on the executor's own judgement
  </acceptance_criteria>
  <resume-signal>Type "approved" once every step passes, or describe which numbered step failed and what you saw instead.</resume-signal>
  <done>The developer has walked the editor on a real device and confirmed the preview is instant, the mobile toggle produces real mobile breakpoints, and the handshake-timeout path is non-destructive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The Design-Distinctiveness Gate (TMPL-01 — STATE.md's named phase risk)</name>
  <files>(no files modified — this task verifies)</files>
  <read_first>
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § Design-Distinctiveness Gate (all seven checks and their exact pass conditions)
    - .planning/STATE.md § Blockers/Concerns (the sentence this gate exists to close)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-VALIDATION.md § Manual-Only Verifications (the second row)
  </read_first>
  <action>
Present the seven-check gate below to the developer and pause. Do NOT approve any check on your
own judgement — check #1 is the requirement and is not machine-decidable, and 04-UI-SPEC.md states
that a 6/7 pass with #1 failing is NOT a pass. Wait for the resume signal, then record the
pass/fail of each of the seven checks and the stranger's VERBATIM answer to check #1 in the
summary. If #1 fails, also record which of #2-#7 failed, because that set is the diagnosis.
  </action>
  <what-built>
    The fashion/apparel flagship template: five sections (hero, trust bar, product grid, editorial
    split, contact) rendered from a per-tenant published document, with merchant logo, two accent
    colours, the zinc-monochrome editorial base, a CSS-only motion language and a
    `prefers-reduced-motion` floor.
  </what-built>
  <how-to-verify>
    **Setup.** Create TWO demo merchants through the real signup -> plan -> branding flow, with
    deliberately different inputs: different store names, different logos, different industries,
    and clearly different accent pairs. Then use the editor to change each one's hero copy,
    editorial-split copy and section order so they diverge the way two real merchants would.
    Publish both. Screenshot each store's home page at **360px** and at **1440px**.

    Then work through 04-UI-SPEC.md § Design-Distinctiveness Gate's seven checks. Six are
    objective; check #1 is the requirement.

    1. **The stranger test (the one genuinely human check).** Show the two 1440px screenshots
       side by side to someone who has not seen this project and ask, verbatim:
       *"Are these two different shops, or the same shop twice?"*
       **PASS:** they answer "different shops" unprompted AND can name **three** differences
       without help. Anything less is a FAIL — record their exact words.
    2. **Surface separation.** Screenshot the flagship home beside the merchant dashboard.
       **PASS:** zero shared palette, zero shared radius, zero shared heading face. A stranger
       asked *"are these the same product?"* should say "related, but clearly different parts."
    3. **Five rhythms at 360px.** Scroll the home page on a 360px viewport.
       **PASS:** photo hero -> washed band -> white grid -> ink band -> white contact, with **no
       two adjacent sections sharing a background treatment**, and at least four product tiles
       visible.
    4. **Type hierarchy.** Measure the rendered hero headline and the body in devtools.
       **PASS:** 64px at >=768px, 40px at 360px, line-height 1.05, `tracking-tighter`, computed
       font-weight **600 and not 700**, and a >=2.5x size ratio to the 16px body.
    5. **Motion.** Load the page, then set `prefers-reduced-motion: reduce` in devtools and reload.
       **PASS first load:** the hero cascade is visible and sections reveal on scroll.
       **PASS reduced motion:** nothing animates, nothing transitions, no layout shift, and **no
       content is hidden**. A section that never appears is a failure, not a subtlety.
    6. **Token hygiene.** Already verified by Task 1 — confirm it was green in the same run as this
       gate, not in an earlier one.
    7. **Accent extremes.** Re-render the home page with three accents in turn: `#18181B` (ink),
       `#C2410C` (dark orange), `#FDE047` (pale yellow).
       **PASS:** every CTA label is legible at all three; the focus ring is visible at all three
       (tab to a CTA to check); no section's structure changes; and the page never stops reading
       as zinc-monochrome-with-a-tint. Specifically confirm the pale yellow produces an INK label
       and the dark orange a WHITE one, and that the ink accent's focus ring falls back to zinc-400
       rather than becoming invisible.

    **A 6/7 pass with #1 failing is NOT a pass.** The stranger test is the requirement — TMPL-01
    says "genuinely polished, portfolio-quality" — and the other six exist so that failing it is
    diagnosable. If #1 fails, record which of #2-#7 also failed; that is the diagnosis.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms all seven checks pass, reporting the stranger's verbatim answer for check #1 and the three differences they named.</human-check>
  </verify>
  <acceptance_criteria>
    - Two demo stores were created through the real onboarding flow, published, and screenshotted at 360px and 1440px
    - The stranger's verbatim answer to check #1 is recorded, along with the three differences they named unprompted
    - Each of the seven checks is recorded individually as pass or fail
    - The phase is NOT marked complete if check #1 failed, regardless of the other six
    - Check #7's three accent renders are each recorded, including which foreground colour was derived and whether the focus ring stayed visible
  </acceptance_criteria>
  <resume-signal>Type "approved" once all seven checks pass, or report the check numbers that failed with the stranger's verbatim answer for #1.</resume-signal>
  <done>STATE.md's named Phase 4 risk is closed with a recorded, attributable result rather than an implied review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated guard -> human judgement | Six gate checks are decidable by a machine; one is not, and pretending otherwise is how the risk ships |
| Phase gate -> next phase | Phase 5 inherits this template's patterns; a genericness problem here multiplies across ~20 variations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | Design-system leakage across the two surfaces | mitigate | Gate check #6 runs all five `surface-token-isolation` bans plus six explicit greps, and check #2 puts the two surfaces side by side visually — a leak that passes the greps but reads wrong to a human is still caught. |
| T-04-03 | Information disclosure (accessibility) | An unreadable CTA label or an invisible focus ring at an extreme accent | mitigate | Gate check #7 exercises three accents chosen to sit at the extremes of the derivation logic: ink (fails 3:1 vs white, so the ring must fall back to zinc-400), dark orange (white label wins), pale yellow (ink label wins). Check #5 additionally verifies the reduced-motion floor hides no content — a reveal that fails open is an accessibility failure, not a visual one. |
| T-04-38 | Repudiation | A phase closing on a subjective "looks fine" | mitigate | The stranger test is scripted verbatim, requires an unprompted answer plus three named differences, and its result is recorded in the summary. 04-UI-SPEC states that 6/7 with #1 failing is not a pass, and both the task's `<action>` and its acceptance criteria restate that as a blocking condition. |
| T-04-39 | Denial of service | A "preview is instant" claim that only holds on a developer's laptop | mitigate | The device pass step 3 requires an empty Network tab while typing — an objective proxy for "no server round trip" that holds regardless of machine — and steps 5 and 10 exercise the iframe viewport and the handshake-timeout path, which are the two failure modes a desktop-only check would miss. |
| T-04-40 | Repudiation | An executor self-approving a blocking human checkpoint | mitigate | Both checkpoint tasks carry `gate="blocking"`, an explicit `<action>` instruction not to approve on the executor's own judgement, and acceptance criteria requiring the developer's verbatim response in the summary. |
| T-04-SC | Tampering | npm installs | accept | This plan installs nothing and modifies no source file. If the gate fails in a way that tempts a motion library, that is a separate `checkpoint:human-verify` install task requiring the full package legitimacy gate first — it is **not** pre-approved by 04-RESEARCH.md or 04-UI-SPEC.md. |
</threat_model>

<verification>
- `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:full` and `npm run build` all green in one pass
- All six check-#6 greps return zero non-comment matches
- The live-preview device pass approved by the developer
- The Design-Distinctiveness Gate approved by the developer, all seven checks
</verification>

<success_criteria>
- Every automated guard in the phase is green simultaneously, with the evidence recorded
- The preview updates as the merchant types with zero network requests, and the mobile toggle produces real mobile breakpoints
- Two seeded demo stores read as two different shops to someone who has never seen the project
- The flagship reads as zinc-monochrome-with-a-tint at every accent extreme, with legible labels and a visible focus ring
- STATE.md's named Phase 4 risk is closed with a recorded result, not an implied review
</success_criteria>

<output>
Create `.planning/phases/04-theme-section-block-system-flagship-template/04-16-SUMMARY.md` when done, recording the verbatim result of the stranger test and the pass/fail of each of the seven gate checks.
</output>
