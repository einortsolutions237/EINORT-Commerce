---
phase: 04-theme-section-block-system-flagship-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/server/auth/auth.ts
  - src/server/db/tenant-scoped.ts
  - tests/setup/seed-two-tenants.ts
  - tests/isolation/tenant-isolation.test.ts
autonomous: true
requirements: [EDIT-01, ONB-02]
must_haves:
  truths:
    - "The database physically has storefront_theme and storefront_page tables with a tenantId column"
    - "A query against StorefrontTheme or StorefrontPage through scopedDb is tenant-filtered, and the same query through an unregistered path throws"
    - "The two-tenant isolation suite seeds both new models without throwing"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "StorefrontTheme, StorefrontPage models and Organization.industry column"
      contains: "model StorefrontTheme"
    - path: "src/server/db/tenant-scoped.ts"
      provides: "Both new models registered in REGISTERED_MODELS"
      contains: "StorefrontTheme"
    - path: "tests/setup/seed-two-tenants.ts"
      provides: "MODEL_FIXTURES entries for both new models"
      contains: "StorefrontPage"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/server/db/tenant-scoped.ts"
      via: "Prisma.ModelName union membership"
      pattern: "\"StorefrontTheme\""
    - from: "src/server/auth/auth.ts"
      to: "prisma/schema.prisma"
      via: "organization additionalFields industry input:false"
      pattern: "industry"
---

<objective>
Land the Phase 4 data model as one atomic three-part change, push it to the database, and regenerate the Prisma client.

Purpose: `StorefrontTheme` and `StorefrontPage` are the only per-tenant storage for EDIT-01's instance data and D-08's draft/published split. `Organization.industry` is ONB-02/D-02's segment capture. Nothing else in this phase compiles or runs until these exist in the generated client AND in the live database.

Output: two new tenant-scoped Prisma models, one new `Organization` column, both models registered in `TENANT_SCOPED_MODELS`, seed fixtures and isolation probes for both, a committed migration, and a regenerated `src/generated/prisma`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-CONTEXT.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-RESEARCH.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-PATTERNS.md

<interfaces>
<!-- Contracts the executor must match. Do not go looking for these; they are here. -->

`prisma/schema.prisma` `model Organization` already carries: `id`, `name`, `slug @unique`,
`logo String?`, `createdAt`, `metadata`, `status String @default("active")`,
`planTier String?`, `trialEndsAt DateTime?`, `subscriptionStatus String @default("none")`,
`planSelectedAt DateTime?`, `members`, `invitations`, `@@map("organization")`.

`src/server/db/tenant-scoped.ts` line 53:
`const REGISTERED_MODELS: readonly Prisma.ModelName[] = ["StoreSlugHistory", "Category",
"Product", "ProductVariant", "ProductImage", "Order", "OrderItem", "OrderEvent",
"PaymentClaim", "MerchantPaymentSettings"];`
`export const TENANT_SCOPED_MODELS: Set<string> = new Set(REGISTERED_MODELS);`

`tests/isolation/tenant-isolation.test.ts` `ModelProbe` has an optional `singleRowPerTenant?: true`
plus required `newRow: (nonce) => Record<string, unknown>` and `mutation: () => Record<string, unknown>`.
`probeFor(model)` THROWS for any registered model with no probe entry.

`tests/setup/seed-two-tenants.ts` `MODEL_FIXTURES: Record<string, (tenant: TenantFixture) =>
Record<string, unknown>>` — builders never set `tenantId`; the seed loop appends it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StorefrontTheme, StorefrontPage and Organization.industry to the Prisma schema</name>
  <files>prisma/schema.prisma, src/server/auth/auth.ts</files>
  <read_first>
    - prisma/schema.prisma (whole file — you are adding models beside existing ones and must match the `///` doc-comment convention on `planTier` at lines 121-129)
    - src/server/auth/auth.ts (the `organization.additionalFields` block around lines 224-260 — `status`, `planTier`, `trialEndsAt`, `subscriptionStatus`, `planSelectedAt` are the precedent)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-RESEARCH.md § Pattern 1 (the exact model bodies) and § Pitfall 5 (why the logo key does NOT go on Organization.logo)
  </read_first>
  <action>
Add two models and one column, exactly as 04-RESEARCH.md Pattern 1 specifies.

`model StorefrontTheme` — fields: `id String @id @default(cuid())`, `tenantId String @unique`,
`templateKey String @default("flagship-fashion")`, `logoKey String?`, `draftTokens Json`,
`publishedTokens Json`, `publishedAt DateTime?`, `createdAt DateTime @default(now())`,
`updatedAt DateTime @updatedAt`. Attributes: `@@index([tenantId])`, `@@map("storefront_theme")`.

`model StorefrontPage` — fields: `id String @id @default(cuid())`, `tenantId String`,
`pageType String`, `draft Json`, `published Json?`, `publishedAt DateTime?`,
`draftUpdatedAt DateTime @default(now())`, `createdAt DateTime @default(now())`.
Attributes: `@@unique([tenantId, pageType])`, `@@index([tenantId, pageType])`,
`@@map("storefront_page")`.

Neither model declares a relation to `Organization` — every other tenant-scoped model in this
schema keys off a bare `tenantId` string, and `scopedDb` is the mechanism, not a foreign key.

Add `industry String?` to `model Organization`, placed after `planSelectedAt`.

Write `///` triple-slash doc comments in the house style already used on `planTier`
(lines 121-129): name the requirement id, state what NULL means, and state the
`input: false` posture. Specifically record on `StorefrontTheme.templateKey` that it is
DELIBERATELY SEPARATE from `Organization.industry` (D-03 — Phase 5 must not auto-migrate
anyone), and on `StorefrontTheme.logoKey` that it exists instead of `Organization.logo`
because `logo` is a Better Auth CORE field that `input: false` cannot protect
(04-RESEARCH.md Pitfall 5 — verified in
`node_modules/better-auth/dist/plugins/organization/routes/crud-org.mjs`).
On `Organization.industry` record ONB-02/D-02: one of the six `INDUSTRY_SEGMENTS`, NULL means
the branding onboarding step is not done yet, and it is the redirect-ladder gate.

In `src/server/auth/auth.ts`, add `industry` to the `organization.additionalFields` block with
`type: "string"`, `input: false`, `required: false`, and NO `defaultValue` (the column is
nullable and NULL is the meaningful third state, unlike `status`/`subscriptionStatus`).
Copy the doc-comment shape from the `planTier` entry already in that block.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model StorefrontTheme" prisma/schema.prisma` returns 1
    - `grep -c "model StorefrontPage" prisma/schema.prisma` returns 1
    - `grep -c "@@map(\"storefront_theme\")" prisma/schema.prisma` returns 1 and `@@map("storefront_page")` returns 1
    - `grep -c "industry String?" prisma/schema.prisma` returns 1
    - `prisma/schema.prisma` contains `@@unique([tenantId, pageType])` and `tenantId String @unique` on StorefrontTheme
    - `src/server/auth/auth.ts` contains an `industry` key inside `organization.additionalFields` whose object literal includes `input: false`
    - `src/server/auth/auth.ts` `industry` entry does NOT contain `defaultValue`
    - `grep -v '^\s*//\|^\s*\*\|^\s*///' prisma/schema.prisma | grep -c "logo String?"` still returns 1 — `Organization.logo` is left untouched, not repurposed
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both models and the `industry` column exist in `prisma/schema.prisma` with house-style `///` comments; `industry` is declared `input: false` in the Better Auth organization additionalFields.</done>
</task>

<task type="auto">
  <name>Task 2: Register both models in TENANT_SCOPED_MODELS, add seed fixtures and isolation probes</name>
  <files>src/server/db/tenant-scoped.ts, tests/setup/seed-two-tenants.ts, tests/isolation/tenant-isolation.test.ts</files>
  <read_first>
    - src/server/db/tenant-scoped.ts (lines 40-70 — the ORDER IS LOAD-BEARING comment above `REGISTERED_MODELS`, and the `upsert` arm around lines 114-120)
    - tests/setup/seed-two-tenants.ts (the `MODEL_FIXTURES` map around lines 300-370, `FIXTURE_EPOCH`, and the throw-on-missing-builder logic)
    - tests/isolation/tenant-isolation.test.ts (lines 85-300 — the `ModelProbe` interface, `MODEL_PROBES`, `probeFor` and `makeRoomForNewRows`; `MerchantPaymentSettings` at ~line 254 is the `singleRowPerTenant` precedent)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-RESEARCH.md § Pitfall 6
  </read_first>
  <action>
Three edits, one commit. 04-RESEARCH.md Pitfall 6 documents the two guards that clamp this from
both sides: `seed-two-tenants.ts` throws for a registered model with no fixture, and
`model-registry-drift.test.ts` fails for a tenantId-bearing model that is NOT registered.

1. `src/server/db/tenant-scoped.ts` — APPEND `"StorefrontTheme"` then `"StorefrontPage"` to the
   end of `REGISTERED_MODELS`, after `"MerchantPaymentSettings"`. Do not re-sort the array; its
   own comment says the insertion order drives the seed's batched `$transaction` and Postgres
   checks foreign keys immediately. Neither new model has an FK parent, so the end is correct.

2. `tests/setup/seed-two-tenants.ts` — add two `MODEL_FIXTURES` builders. Follow the existing
   builder shape exactly: id derived from the tenant (`${tenant.id}-storefront-theme`,
   `${tenant.id}-storefront-page`), `FIXTURE_EPOCH` for every DateTime, and NEVER set `tenantId`
   (the loop appends it). Set explicit values for `@updatedAt`/`@default(now())` columns —
   the file's own note at ~line 366 says an implicit `@updatedAt` breaks byte-identity between
   runs, which this phase depends on for the `draftUpdatedAt > publishedAt` comparison.
   - StorefrontTheme fixture: `templateKey: "flagship-fashion"`, `logoKey: null`,
     `draftTokens: { primaryAccent: "#18181B", secondaryAccent: "#71717A" }`,
     `publishedTokens` the same object, `publishedAt: FIXTURE_EPOCH`,
     `createdAt: FIXTURE_EPOCH`, `updatedAt: FIXTURE_EPOCH`.
   - StorefrontPage fixture: `pageType: "home"`,
     `draft: { version: 1, sections: [] }`, `published: { version: 1, sections: [] }`,
     `publishedAt: FIXTURE_EPOCH`, `draftUpdatedAt: FIXTURE_EPOCH`, `createdAt: FIXTURE_EPOCH`.
     An empty `sections` array is deliberate here: the fixture proves tenant scoping, not
     document validity, and plan 04-13's own isolation tests seed real documents.

3. `tests/isolation/tenant-isolation.test.ts` — add a `MODEL_PROBES` entry for BOTH models.
   `probeFor()` throws for any registered model without one, so omitting either fails the whole
   suite with a confusing message. `StorefrontTheme` MUST carry `singleRowPerTenant: true`
   because its `tenantId` is `@unique` — the same treatment `MerchantPaymentSettings` already
   has. `StorefrontPage` must NOT carry it (`@@unique([tenantId, pageType])` allows many rows
   per tenant); give its `newRow` a distinct `pageType` derived from the nonce so a second probe
   row does not collide with the fixture's `"home"` row.
   `mutation` for StorefrontTheme: `() => ({ templateKey: "probe-mutated" })`.
   `mutation` for StorefrontPage: `() => ({ pageType: "probe-mutated" })`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StorefrontTheme" src/server/db/tenant-scoped.ts` shows it inside `REGISTERED_MODELS` AFTER `"MerchantPaymentSettings"`
    - `grep -n "StorefrontPage" src/server/db/tenant-scoped.ts` shows it as the last entry of `REGISTERED_MODELS`
    - `tests/setup/seed-two-tenants.ts` `MODEL_FIXTURES` has `StorefrontTheme:` and `StorefrontPage:` keys
    - Neither new fixture builder body contains the token `tenantId`
    - `tests/isolation/tenant-isolation.test.ts` `MODEL_PROBES` has `StorefrontTheme:` with `singleRowPerTenant: true`
    - `tests/isolation/tenant-isolation.test.ts` `MODEL_PROBES` has `StorefrontPage:` WITHOUT `singleRowPerTenant`
    - `npm run typecheck` exits 0 (this will only pass after Task 3 regenerates the client — if it fails here with "Property 'storefrontTheme' does not exist", proceed to Task 3 and re-run)
  </acceptance_criteria>
  <done>Both models are registered, both have seed fixtures with no hardcoded tenantId, and both have isolation probes with the correct `singleRowPerTenant` posture.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the schema to the database and regenerate the Prisma client</name>
  <files>prisma/migrations/, src/generated/prisma/</files>
  <read_first>
    - prisma.config.ts (how DATABASE_URL / DIRECT_URL / TEST_DATABASE_URL are resolved; a TEST_DATABASE_URL in the shell overrides DIRECT_URL)
    - scripts/prisma-generate.mjs (the postinstall generator)
    - prisma/migrations/migration_lock.toml and the three existing migration directory names (this project has a real migration history — do not orphan it)
  </read_first>
  <action>
BLOCKING. Every downstream plan in this phase reads `StorefrontTheme` / `StorefrontPage` through
the generated client. Build and typecheck will NOT catch a missing push, because Prisma types
come from `schema.prisma`, not from the live database — that is exactly the false-positive
verification state this task exists to prevent.

Primary path (matches the three existing migrations in `prisma/migrations/`):
`npx prisma migrate dev --name storefront_theme_page_industry`

If the environment is non-TTY or `migrate dev` cannot run, fall back to
`npx prisma db push`. Use `npx prisma db push --accept-data-loss` ONLY if the push reports data
loss against the local dev branch and you have confirmed the reported loss is limited to the
new/empty tables — never against data that matters.

Then regenerate the client: `node scripts/prisma-generate.mjs` (or `npm install`, which runs it
via the `postinstall` hook). Without this, `Prisma.ModelName` does not include the two new
models and `REGISTERED_MODELS` from Task 2 will not compile.

Apply the same schema to the test branch before running the isolation suite — the suite runs
against `TEST_DATABASE_URL`, a different Neon branch, and `tests/setup/global-setup.ts` fails
closed rather than falling back to dev.

Finally run the full suite. `tests/isolation/model-registry-drift.test.ts` is the proof the push
landed: it reads the generated client's model metadata and fails if a tenantId-bearing model is
not registered.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:full</automated>
  </verify>
  <acceptance_criteria>
    - A new directory exists under `prisma/migrations/` whose `migration.sql` contains `CREATE TABLE "storefront_theme"` and `CREATE TABLE "storefront_page"` and `ALTER TABLE "organization" ADD COLUMN "industry"` (or, on the `db push` fallback path, `npx prisma migrate status` reports the database schema is in sync)
    - `src/generated/prisma` was regenerated after the schema change (the generated `ModelName` union includes `StorefrontTheme` and `StorefrontPage`)
    - `npm run typecheck` exits 0
    - `npx vitest run --project isolation tests/isolation/model-registry-drift.test.ts` passes
    - `npx vitest run --project isolation tests/isolation/tenant-isolation.test.ts` passes, including the two new model probes
    - `npm run test:full` exits 0 — the seed no longer throws for an unfixtured registered model
  </acceptance_criteria>
  <done>The live dev database and the test branch both have `storefront_theme` and `storefront_page` tables and an `organization.industry` column; the generated client knows both models; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Better Auth `/api/auth/organization/*` → Organization row | Any signed-in merchant can POST an organization update body |
| Any `src/server/**` caller → tenant-scoped tables | `scopedDb` is the only sanctioned door; an unregistered model runs unscoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | `Organization.logo` (Better Auth core field) | mitigate | Store the logo key on `StorefrontTheme.logoKey` instead. `baseUpdateOrganizationSchema` includes `logo: z.string().nullish()` as a CORE field, so the `input: false` trick that protects `planTier` does not apply (04-RESEARCH.md Pitfall 5). `Organization.logo` is left unused and unwritten by this phase. |
| T-04-13 | Elevation of privilege | `Organization.industry` via `/organization/update` | mitigate | Declared `input: false` in `src/server/auth/auth.ts` organization `additionalFields`, so no request body can set it — only the server-side `saveBranding` write (plan 04-09) may. |
| T-04-06 | Elevation of privilege | `StorefrontTheme` / `StorefrontPage` reads and writes | mitigate | Both models appended to `REGISTERED_MODELS`, so `scopedDb` stamps and filters `tenantId` on every operation including both halves of an `upsert`; an unregistered model throws rather than running unscoped. Proven by `tests/isolation/tenant-isolation.test.ts` probes added in Task 2. |
| T-04-SC | Tampering | npm installs | accept | This plan installs nothing. 04-RESEARCH.md § Package Legitimacy Audit records a deliberate zero-install posture for the whole phase; the audit table is present and empty by design. |
</threat_model>

<verification>
- `npm run test:full` green (unit + isolation)
- `npm run lint` exits 0 with `--max-warnings=0`
- `npm run typecheck` exits 0
- `npm run build` succeeds
</verification>

<success_criteria>
- `storefront_theme` and `storefront_page` tables exist in both the dev and test databases with a `tenantId` column
- `organization.industry` column exists and is nullable
- `TENANT_SCOPED_MODELS` contains both new model names
- The two-tenant seed runs without throwing, and both isolation probes pass
- `Organization.logo` is untouched
</success_criteria>

<output>
Create `.planning/phases/04-theme-section-block-system-flagship-template/04-01-SUMMARY.md` when done
</output>
