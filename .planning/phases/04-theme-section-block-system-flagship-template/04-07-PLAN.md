---
phase: 04-theme-section-block-system-flagship-template
plan: 07
type: execute
wave: 2
depends_on: ["04-02", "04-04"]
files_modified:
  - src/app/s/[slug]/sections/render-data.ts
  - src/app/s/[slug]/sections/reveal.tsx
  - src/app/s/[slug]/sections/hero-section.tsx
  - src/app/s/[slug]/sections/trust-bar-section.tsx
  - src/app/s/[slug]/sections/editorial-split-section.tsx
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "A shopper on a 360px phone sees a full-bleed hero whose headline, body and CTA cascade in, and the CTA is tinted with the merchant's accent"
    - "A merchant who has uploaded no hero image gets a section that looks deliberate, not broken"
    - "With prefers-reduced-motion set, nothing animates and no content is hidden"
    - "None of these components can be built with a literal colour, a palette utility, or Outfit"
  artifacts:
    - path: "src/app/s/[slug]/sections/render-data.ts"
      provides: "StorefrontRenderData — the client-safe data contract every section receives"
      exports: ["StorefrontRenderData", "StorefrontRenderProduct", "StorefrontRenderCategory"]
    - path: "src/app/s/[slug]/sections/reveal.tsx"
      provides: "IntersectionObserver one-shot scroll reveal, visible-by-default"
      exports: ["Reveal"]
    - path: "src/app/s/[slug]/sections/hero-section.tsx"
      provides: "S1 — full-bleed hero with scrim, cascade, and a no-image mode"
    - path: "src/app/s/[slug]/sections/trust-bar-section.tsx"
      provides: "S2 — 1..4 trust items on a secondary-accent wash band"
    - path: "src/app/s/[slug]/sections/editorial-split-section.tsx"
      provides: "S4 — the page's only inverted ink band"
  key_links:
    - from: "src/app/s/[slug]/sections/hero-section.tsx"
      to: "src/server/theming/schema.ts"
      via: "import type { SectionInstance } — settings props are narrowed from the union"
      pattern: "@/server/theming/schema"
---

<objective>
Build the flagship's motion primitive and three of its five sections, to the exact contracts in 04-UI-SPEC.md § S1, § S2 and § S4.

Purpose: TMPL-01's bar is "would a stranger think this cost money," and the Design-Distinctiveness Gate's checks #3, #4 and #5 are judged on precisely these three sections plus `<Reveal>` — the background-treatment alternation, the 64px display headline at weight 600, and the reduced-motion floor. TMPL-02 makes them the pattern library Phase 5's segment templates inherit, so their conventions matter as much as their pixels.

Output: the client-safe `StorefrontRenderData` contract, `<Reveal>`, and the hero, trust-bar and editorial-split section components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-CONTEXT.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-RESEARCH.md
@.planning/phases/04-theme-section-block-system-flagship-template/04-PATTERNS.md

<interfaces>
<!-- THE LOAD-BEARING CONSTRAINT for this whole directory. -->
Every file under `src/app/s/[slug]/sections/` must be CLIENT-SAFE. `preview-canvas.tsx`
(plan 04-14) is a `"use client"` component that imports `section-renderer.tsx`, which imports
every section. Therefore NO file in this directory may import anything carrying
`import "server-only"` — that means no `@/server/theming/registry`, no
`@/server/theming/defaults`, no `@/server/images/r2`, no `@/server/storefront/queries`.

The only permitted cross-boundary import is `@/server/theming/schema`, which plan 04-02
deliberately built with NO marker line for exactly this reason.

`@/lib/strings`, `@/lib/utils` (`cn`), `@/lib/contrast`, `@/lib/theme-defaults`,
`next/image`, `next/link` and `lucide-react` are all client-safe.

<!-- Settings shapes, from plan 04-02's src/server/theming/schema.ts -->
hero:            eyebrow, heading, body, ctaLabel, ctaHref, backgroundImageKey, overlayOpacity
trust-bar:       blocks[] of { type: "trust-item", icon, heading, body }, 1..4 entries
editorial-split: eyebrow, heading, body, ctaLabel, ctaHref, imageKey

<!-- Storefront tokens available in this scope (globals.css, plan 04-04) -->
Zinc set: --background --foreground --muted --muted-foreground --secondary --border --ring …
Brand:   bg-brand-accent · text-brand-accent-foreground · bg-brand-accent-secondary ·
         text-brand-accent-secondary-foreground · (focus) --brand-accent-ring
Motion:  --motion-ease --motion-quick --motion-reveal --motion-hero --motion-hover --motion-stagger

<!-- tw-animate-css 1.4.0 utilities, verified present in node_modules -->
animate-in · fade-in · slide-in-from-bottom-4 · fill-mode-both · delay-* · animation-duration-[…]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The client-safe render-data contract and the Reveal primitive</name>
  <files>src/app/s/[slug]/sections/render-data.ts, src/app/s/[slug]/sections/reveal.tsx</files>
  <read_first>
    - src/app/s/[slug]/page.tsx (lines 46-110 — the current data bundle: what `listStorefrontProducts` and the category query actually return, field by field. `render-data.ts` redeclares exactly the subset the sections need)
    - src/server/storefront/queries.ts (the return types `StorefrontProductListItem` and the category list item — read them, then REDECLARE the needed fields structurally in `render-data.ts`; do NOT import from this `server-only` module)
    - src/hooks/use-mobile.ts (the repo's only `matchMedia` precedent — the effect/cleanup idiom `reveal.tsx` copies)
    - src/app/(dashboard)/dashboard/products/image-gallery-field.tsx (lines 93-101 — the `imageBaseUrl`-as-a-prop precedent: a client island builds an R2 URL from a base passed in from the server, it never imports `publicUrlFor`)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § Motion Language → `<Reveal>` (every parameter is fixed there)
    - .planning/phases/04-theme-section-block-system-flagship-template/04-PATTERNS.md § No Analog Found → `reveal.tsx`
  </read_first>
  <action>
**`render-data.ts`** — a plain types module, no marker, no `"use client"`, no runtime imports.

This file exists because the sections must be client-safe (see `<interfaces>`) while the data
they render comes from `server-only` queries. It declares the SHAPE structurally rather than
importing it, so nothing under `sections/` ever reaches into `src/server/**` at runtime.

Export:
- `StorefrontRenderProduct` — `{ id, name, slug, priceXaf, imageKey: string | null, inStock: boolean }`
  (match the field names `src/app/s/[slug]/page.tsx` already uses; if a name differs, follow the
  existing page, not this list)
- `StorefrontRenderCategory` — `{ id, name, slug }`
- `StorefrontRenderData` — `{ imageBaseUrl: string; storeName: string;
  products: readonly StorefrontRenderProduct[]; categories: readonly StorefrontRenderCategory[];
  activeCategorySlug: string | null; whatsappHref: string | null }`

`imageBaseUrl` is `R2_PUBLIC_BASE_URL`, passed down from the RSC. `whatsappHref` is a fully-built
`wa.me` deep link or `null` — built SERVER-side (plan 04-10) from the merchant's Phase 3 payment
settings, never assembled from a phone number in a client component.

Write a header stating the client-safety rule in the all-caps warning voice
("EVERYTHING UNDER `sections/` IS CLIENT-SAFE. DO NOT IMPORT A `server-only` MODULE HERE.")
with the reason: `preview-canvas.tsx` is a client component that renders these same components
inside the iframe, and a `server-only` import anywhere in the tree breaks that build.

**`reveal.tsx`** — `"use client"`, ~25 lines, per 04-UI-SPEC's table exactly:
- `IntersectionObserver`, `threshold: 0.1`, `rootMargin: "0px 0px -10% 0px"`
- one-shot: `unobserve()` on first intersection (a section that re-animates on scroll-up is a
  distraction, not a delight)
- reduced motion: if `window.matchMedia("(prefers-reduced-motion: reduce)").matches`, render
  VISIBLE with no animation classes from the first frame
- no-JS / no-IntersectionObserver: render VISIBLE. The default state is visible-and-unanimated
  and the observer ADDS the animation. A reveal that hides content when JS fails is a blank page —
  state this in the header as the load-bearing rule.
- wrapper is a bare `<div>` with no styling of its own, so it cannot change the section's spacing
- accepts an optional `className` merged with `cn(...)` and `children`

The reveal class string, applied only after intersection:
`animate-in fade-in slide-in-from-bottom-4 fill-mode-both ease-[var(--motion-ease)] animation-duration-[var(--motion-reveal)]`

Copy the effect/cleanup idiom from `src/hooks/use-mobile.ts` (add listener, return the remover).
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "@/server/" src/app/s/\[slug\]/sections/render-data.ts src/app/s/\[slug\]/sections/reveal.tsx` returns 0 for both files
    - `render-data.ts` contains no `import` statement at all
    - `reveal.tsx` first line is `"use client";`
    - `reveal.tsx` contains `unobserve` and `prefers-reduced-motion`
    - `reveal.tsx`'s initial render state is visible: the component's default `className` contains no `opacity-0` and no `invisible`
    - `grep -cE "#[0-9a-fA-F]{6}|oklch\(|rgb\(|hsl\(" src/app/s/\[slug\]/sections/reveal.tsx` returns 0
    - `npx vitest run --project unit tests/unit/surface-token-isolation.test.ts` passes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Every later section component has one typed data contract to receive and one motion wrapper to use, and neither reaches into `src/server/**` at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: S1 — the hero section, with its no-image mode</name>
  <files>src/app/s/[slug]/sections/hero-section.tsx</files>
  <read_first>
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § S1 — `hero` (the full property table; every class string below is quoted from it) plus § Typography (Display 40→64px/600/1.05 `tracking-tighter`, and the "weight 700 is deliberately NOT added, never write `font-bold` under `src/app/s/**`" rule) plus § Motion Language → "Hero cascade (on mount, above the fold — no `<Reveal>`)"
    - src/app/s/[slug]/page.tsx (lines 144-182 — the existing `next/image` + token-utility usage, the closest in-repo analog)
    - src/app/s/[slug]/sections/render-data.ts (Task 1)
    - src/server/theming/schema.ts (plan 04-02 — the `hero` settings shape)
    - tests/unit/surface-token-isolation.test.ts (bans 1-3, which scan this file automatically)
  </read_first>
  <action>
Create `src/app/s/[slug]/sections/hero-section.tsx`. NO `"use client"` — it is a plain component
renderable from both the RSC tree and the client preview canvas (it holds no state and no effect).

Props: `{ settings: Extract<SectionInstance, { type: "hero" }>["settings"]; data: StorefrontRenderData }`.
Narrowing from the union rather than redeclaring the shape is the whole point of the
discriminated union — do not hand-write a props interface with the same seven keys.

Frame: `relative isolate flex min-h-[85svh] max-h-[900px] items-center justify-center overflow-hidden`.
`svh`, not `vh` — mobile browser chrome would otherwise crop the CTA.

TWO MODES, and the no-image one is not an afterthought — it is the day-one state for a merchant
who has not uploaded anything, and 04-UI-SPEC requires it to look deliberate:

- IMAGE MODE (`settings.backgroundImageKey !== null`): `next/image` with `fill`,
  `object-cover`, `priority`, `sizes="100vw"`, `alt=""` (decorative — the headline carries the
  meaning). Build the src as `${data.imageBaseUrl}/${settings.backgroundImageKey}/detail.webp`
  using the derivative label that matches the `product` preset's largest size; confirm the label
  against `src/server/images/pipeline.ts`'s `labels` array and use the widest one. Scrim:
  `<div className="absolute inset-0 bg-foreground" style={{ opacity: settings.overlayOpacity }} />`
  — a token utility for the colour and a numeric style for the opacity, so ban #1 sees no colour.
  Text colours come from the `--background` family (`text-background`, `text-background/90`,
  `text-background/80`).
- NO-IMAGE MODE: `bg-secondary` (zinc-100), NO scrim, and every text colour switches to the
  `--foreground` family (`text-foreground`, `text-muted-foreground`).

Content column: `relative z-10 mx-auto flex max-w-3xl flex-col items-center px-6 text-center md:px-8`.

- Eyebrow: Label 14/600, `uppercase tracking-[0.08em]`
- Headline: the page's `<h1>`, `text-[40px] md:text-[64px] font-semibold leading-[1.05] tracking-tighter`,
  `mt-4`. NEVER `font-bold` — weight 700 is not loaded and a synthesised bold is a contract
  violation (04-UI-SPEC § Typography resolves Open Question 5 this way). Display presence comes
  from size + tracking + line-height.
- Body: `mt-6`, Body 16/400/1.6, `max-w-prose`
- CTA: `mt-8`, `rounded-full min-h-12 px-8`, Label 14/600 `uppercase tracking-[0.08em]`,
  `bg-brand-accent text-brand-accent-foreground hover:bg-brand-accent/90`,
  `transition-colors duration-[var(--motion-quick)] ease-[var(--motion-ease)]`. A `next/link` to
  `settings.ctaHref`. The pill radius is deliberate and must NOT be "made consistent" with the
  transactional 0.25rem buttons — marketing CTAs and money buttons are different affordances.

Motion: the on-mount cascade at `--motion-hero`, NOT `<Reveal>`. The hero is already in view and
wrapping it in an observer costs a visible blank frame. Apply
`animate-in fade-in slide-in-from-bottom-4 fill-mode-both ease-[var(--motion-ease)] animation-duration-[var(--motion-hero)]`
with `delay-0` / `delay-200` / `delay-[400ms]` / `delay-[600ms]` on eyebrow / headline / body /
CTA respectively.

A11y: `<section aria-labelledby>` linked to the `<h1>`'s id.

Header: name TMPL-01, S1, D-09 (accent-only), and record the no-image mode as a first-class
state rather than a fallback.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run --project unit tests/unit/surface-token-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "#[0-9a-fA-F]{6}|oklch\(|rgb\(|hsl\(" src/app/s/\[slug\]/sections/hero-section.tsx` returns 0 on non-comment lines
    - `grep -cE "bg-zinc-|text-zinc-|bg-slate-|text-slate-|font-heading|gold-accent" src/app/s/\[slug\]/sections/hero-section.tsx` returns 0
    - `grep -c "font-bold" src/app/s/\[slug\]/sections/hero-section.tsx` returns 0
    - `grep -c "@/server/" src/app/s/\[slug\]/sections/hero-section.tsx` returns 1 and that one import is from `@/server/theming/schema`
    - The file contains `min-h-\[85svh\]` and `max-h-\[900px\]`
    - The file contains `bg-brand-accent` and `text-brand-accent-foreground` exactly on the CTA
    - The file contains both `bg-secondary` (no-image mode) and `bg-foreground` (the scrim)
    - `grep -c "Reveal" src/app/s/\[slug\]/sections/hero-section.tsx` returns 0 — the hero cascades on mount
    - `npx vitest run --project unit tests/unit/surface-token-isolation.test.ts` passes
  </acceptance_criteria>
  <done>The hero renders at 85svh with a 64px display headline at weight 600, an accent-tinted pill CTA, an on-mount cascade, and a no-image mode that reads as a design choice.</done>
</task>

<task type="auto">
  <name>Task 3: S2 trust-bar and S4 editorial-split</name>
  <files>src/app/s/[slug]/sections/trust-bar-section.tsx, src/app/s/[slug]/sections/editorial-split-section.tsx</files>
  <read_first>
    - .planning/phases/04-theme-section-block-system-flagship-template/04-UI-SPEC.md § S2 — `trust-bar` and § S4 — `editorial-split` (both full property tables) plus § Color → "Accent reserved for exactly four things" and "`--brand-accent-secondary` reserved for exactly two things" plus § Typography → the editorial-split heading exception (32→40px, weight 600, line-height 1.1, `tracking-tight` — a fifth SIZE step of the Display role, not a fifth role) plus § Motion Language → the stagger row
    - src/app/s/[slug]/sections/hero-section.tsx (Task 2 — the conventions to match: props narrowed from the union, token-only colours, no `font-bold`)
    - src/app/s/[slug]/sections/reveal.tsx (Task 1)
    - src/app/s/[slug]/sections/render-data.ts (Task 1)
  </read_first>
  <action>
**`trust-bar-section.tsx`** — S2. Props narrowed from the union as in Task 2.

- Band: `border-y border-border bg-brand-accent-secondary/8 py-12 md:py-16`. The 8% wash is one of
  the secondary accent's only two permitted uses; Tailwind v4 emits `color-mix(in oklab, …, transparent)`
  for the `/8`, which is valid for any CSS colour. An 8% wash of any hue on white stays light, so
  zinc-950 text on it always clears 4.5:1 — record that as the reason the wash is safe for an
  arbitrary merchant colour.
- Inner: `mx-auto grid max-w-7xl gap-8 px-6 md:gap-12 md:px-8`
- Columns by block count: 1 -> `grid-cols-1` · 2 -> `sm:grid-cols-2` · 3 -> `sm:grid-cols-3` ·
  4 -> `sm:grid-cols-2 lg:grid-cols-4`. The schema clamps `blocks` to 1..4, so a `default` arm is
  unreachable — handle the four cases explicitly.
- Item: left-aligned. Icon `size-6 text-foreground` `aria-hidden` (the heading text carries the
  meaning) -> `mt-2` heading Label 14/600 -> `mt-1` body Body 16/400/1.6
  `text-muted-foreground max-w-prose`.
- Icons: map the four `z.enum` values to the lucide components `Truck`, `ShieldCheck`, `Clock`,
  `MessageCircle` via an exhaustive `Readonly<Record<…>>` typed against the enum, so a fifth icon
  is a compile error at the table (the `ORDER_TRANSITIONS` idiom).
- Motion: `<Reveal>` on the band; each item carries
  `style={{ animationDelay: \`calc(var(--motion-stagger) * ${index})\` }}`. An inline numeric/`calc`
  style is not a colour, so ban #1 is unaffected.

**`editorial-split-section.tsx`** — S4, the page's ONLY inverted region.

- Band: `bg-foreground text-background`, full-bleed
- Inner: `mx-auto grid max-w-7xl items-center gap-8 px-6 py-16 md:grid-cols-2 md:gap-12 md:px-8 md:py-24`
- Text column: eyebrow Label uppercase `text-background/70` -> `mt-4` heading
  `text-[32px] md:text-[40px] font-semibold leading-[1.1] tracking-tight` -> `mt-6` body
  Body/1.6 `text-background/80 max-w-prose` -> `mt-8` CTA
- **CTA IS NOT THE BRAND ACCENT.** `bg-background text-foreground hover:bg-background/90`, pill,
  `min-h-12 px-8`. This is a LOCKED rule: the default accent is ink (`#18181B`) and an ink button
  on a zinc-950 band is invisible; no foreground derivation can fix a fill-vs-fill collision.
  Write that reason into the file in the all-caps warning voice so nobody later "makes it
  consistent" with the hero CTA.
- Image column: `aspect-[4/3] overflow-hidden rounded`, `object-cover`, `next/image` with
  `sizes="(min-width: 768px) 50vw, 100vw"`, ordered below the text at `<md` and right of it at
  `>=md`. Src built from `data.imageBaseUrl` + `settings.imageKey`, same derivative-label rule as
  the hero.
- **No image:** the section collapses to a single centred `max-w-3xl` text column. Never a
  half-empty grid.
- Motion: `<Reveal>` on the band; text column then image column stagger at `--motion-stagger`.

Both files: no literal colour, no palette utility, no `font-heading`, no `font-bold`, and the
only `@/server/**` import is `@/server/theming/schema`.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run --project unit tests/unit/surface-token-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE "#[0-9a-fA-F]{6}|oklch\(|rgb\(|hsl\(" src/app/s/\[slug\]/sections/trust-bar-section.tsx src/app/s/\[slug\]/sections/editorial-split-section.tsx` returns 0 for both on non-comment lines
    - `grep -rcE "bg-zinc-|text-slate-|font-heading|gold-accent|font-bold" src/app/s/\[slug\]/sections/` returns 0
    - `trust-bar-section.tsx` contains `bg-brand-accent-secondary/8` and `border-y border-border`
    - `trust-bar-section.tsx` maps the four icon enum values through a `Record` typed against the enum, and contains no `default:` arm in that mapping
    - `editorial-split-section.tsx` contains `bg-foreground text-background`
    - `grep -c "bg-brand-accent" src/app/s/\[slug\]/sections/editorial-split-section.tsx` returns 0 — the ink-band CTA is deliberately not the accent
    - `editorial-split-section.tsx` contains an all-caps comment explaining the ink-on-ink collision
    - Both files import `Reveal`; neither the hero nor these two contains `overflow-x`
    - `npx vitest run --project unit tests/unit/surface-token-isolation.test.ts` passes
  </acceptance_criteria>
  <done>The washed trust band and the inverted ink band exist, both revealed on scroll with the shared stagger, and the ink-band CTA is protected from the ink-on-ink collision by a comment a future reader cannot miss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merchant-authored settings -> rendered DOM | Every string in `settings` was typed by a merchant into the editor |
| Merchant-authored storage key -> `next/image` src | An image key from a JSONB column becomes a network fetch |
| Storefront design system -> dashboard design system | These files are also rendered inside the editor's iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering / XSS-adjacent | `settings.overlayOpacity` reaching a `style` attribute | mitigate | The only value these components put in a `style` attribute is a NUMBER — `overlayOpacity`, clamped `0..0.8` by `pageDocumentSchema`, and the stagger `calc()` which contains no merchant input at all. The scrim's COLOUR is the token utility `bg-foreground`, never a merchant value, so there is no colour string on this path to inject through. |
| T-04-15 | Tampering | `settings.backgroundImageKey` / `settings.imageKey` rendered as an image `src` | mitigate | Both are validated by `storageKeySchema` (plan 04-02): an R2 derivative PREFIX matching `^tenants/[A-Za-z0-9_-]+/(products\|logos)/[a-z0-9-]{8,64}$`, never a URL, no `..`, no absolute scheme. The components concatenate a server-supplied `imageBaseUrl` with that validated prefix, and `next.config.ts` `remotePatterns` restricts the host that `next/image` will fetch. |
| T-04-02 | Tampering | Storefront tokens leaking into the dashboard, or palette utilities leaking into the storefront | mitigate | `tests/unit/surface-token-isolation.test.ts` bans #1, #2 and #3 scan this directory automatically. Every acceptance criterion in this plan greps for the same patterns so a violation fails at the task, not at the wave. D-12 forbids weakening any ban. |
| T-04-23 | Denial of service (accessibility) | A reveal that hides content when JavaScript or `IntersectionObserver` is unavailable | mitigate | `<Reveal>` renders VISIBLE by default and the observer only ADDS animation classes; the reduced-motion branch renders visible-and-unanimated from the first frame. Asserted by an acceptance criterion that the default `className` contains no `opacity-0` or `invisible`. |
| T-04-24 | Elevation of privilege (build-time) | A `server-only` module pulled into the client preview bundle through this directory | mitigate | `render-data.ts` redeclares the data shapes structurally instead of importing them, and every file's only `@/server/**` import is `@/server/theming/schema`, which plan 04-02 deliberately built marker-free. Asserted by a per-file grep acceptance criterion. |
| T-04-SC | Tampering | npm installs | accept | This plan installs nothing. `tw-animate-css` 1.4.0 and `lucide-react` are already installed and the required utilities were verified in `node_modules`; 04-RESEARCH.md § Package Legitimacy Audit records the phase's zero-install posture, and a motion library is explicitly NOT pre-approved. |
</threat_model>

<verification>
- `npm run test:unit` green, especially `surface-token-isolation`
- `npm run lint` exits 0 with `--max-warnings=0`
- `npm run typecheck` exits 0
- `npm run build` succeeds
</verification>

<success_criteria>
- `StorefrontRenderData` is declared once, structurally, with zero runtime imports
- `<Reveal>` is visible-by-default, one-shot, and unanimated under reduced motion
- The hero has an image mode and a no-image mode, both deliberate, and cascades on mount without an observer
- The trust bar is the secondary accent's 8% wash; the editorial split is the page's only inverted band and its CTA is not the accent
- Zero literal colours, zero palette utilities, zero `font-heading`, zero `font-bold` anywhere under `sections/`
</success_criteria>

<output>
Create `.planning/phases/04-theme-section-block-system-flagship-template/04-07-SUMMARY.md` when done
</output>
